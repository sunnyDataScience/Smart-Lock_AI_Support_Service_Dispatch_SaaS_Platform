---
id: CR-NNNN
title: "Change Impact Analysis"
status: draft
tier: 4-exploration
owner: HYBRID
created: <YYYY-MM-DD>
target-release: <version-or-sprint>
product-version: null
supersedes: null
superseded-by: null
---


> **⚠️ WORKED EXAMPLE — DELETE BEFORE USE**
> Concrete names below (Customer, Order, Inventory, PurchaseOrder, Stripe, etc.) come
> from a worked e-commerce/ERP example to give AI strong few-shot context. **Replace
> them with your domain's terms** when filling for your project. The structure (sections,
> tables, frontmatter) is what to keep; the example content is what to swap or delete.
> If your domain doesn't fit (CLI / library / ML / embedded), the example is still
> useful as a structural reference — copy the shape, change the words.
# CR-NNNN: <Short Change Title>

> **Tier**: 4-exploration → Change Impact Analysis (per-change ephemeral; archive after implementation)
> **Generated by**: `sunnydata-change-impact-analysis` skill
> **Mandated by**: `.claude/rules/change-governance.md`

---

## 1. Change Statement

**As-is**: <one-sentence description of current behavior>

**To-be**: <one-sentence description of new behavior>

**Driver**: <what prompted this — incident X / customer ask Y / compliance Z / strategic shift W>

## 2. Affected Flow

| Flow ID | Action | Description |
|---|---|---|
| `BF-NNNN` | Modified | Adds branch for partial cancellation |
| `UF-NNNN` | Modified | "Customer cancel order" gets a quantity selector |
| `SF-NNNN` | New | Partial cancellation rule (extracted as reusable SF) |
| `SF-NNNN` | Unchanged | Notification dispatch — only payload changes |

## 3. Affected Spec (FR / NFR)

| Spec ID | Action | Description |
|---|---|---|
| `FR-NNNN` | Modified | Cancel rule: now operates on order_items, not whole order |
| `FR-NNNN` | New | Partial refund calculation rule |
| `NFR-NNNN` | Unchanged | Performance budget for cancel endpoint unchanged |

## 4. Affected API

| API ID | Endpoint | Action | Breaking? | Notes |
|---|---|---|---|---|
| `API-NNNN` | `POST /orders/{id}/cancel` | Schema change | **Yes** | Adds `items[]` field; existing callers need migration |
| `API-NNNN` | `GET /orders/{id}` | Response change | No | Adds `cancelled_qty` per item; old clients ignore extra field |
| `API-NNNN` | `POST /refunds` | New | No | New endpoint, no existing callers |

If breaking: include migration window plan.

## 5. Affected Data

| Entity | Action | Migration |
|---|---|---|
| `order_items` | New column `cancelled_qty INT NOT NULL DEFAULT 0` | One online migration; backfill = default 0 |
| `refunds` | New `partial_refund_id` FK column | Online migration |
| `orders` | New state value `partially_cancelled` | App-level — no DDL |

State machine impact: `order` state machine adds `partially_cancelled` between `paid` and `cancelled`.

## 6. Affected Test

| Test ID | Action | Description |
|---|---|---|
| `TC-NNNN` | Update | Existing cancel happy-path → split into "full cancel" |
| `TC-NNNN` | New | Partial cancel happy-path |
| `TC-NNNN` | New | Partial cancel followed by full cancel of remainder |
| `TC-NNNN` | New | Partial cancel of already-shipped item → rejected |
| `TC-NNNN` | New | Concurrent partial cancel + ship — race resolution |

Coverage delta: +5 TCs; estimated coverage shift from 82% → 84% on `OrderService`.

## 7. Affected Architecture

| Concern | Action | Notes |
|---|---|---|
| Module boundary | Unchanged | Stays inside Order bounded context |
| New ADR? | **Yes** | `ADR-NNNN`: choice between "operate on items" vs "create child orders" |
| External integration | Vendor check | ERP cancel API may not support partial — fallback strategy needed |

## 8. Human Decisions Required

🛑 **CIA blocks code changes until every row here has a recorded decision.**

| # | Question | Options | Owner | Status | Decision |
|---|---|---|---|---|---|
| 1 | Allow partial cancel of already-shipped items? | (a) reject (b) trigger return flow | Product | open | — |
| 2 | If ERP doesn't support partial, fallback approach? | (a) cancel + reorder (b) compensating-tx adapter | Architect | open | — |
| 3 | Refund timing: instant or batch? | (a) sync at cancel (b) nightly batch | Finance | open | — |
| 4 | Customer-visible naming: "partial cancel" or "remove items"? | (a) (b) | UX | open | — |

## 9. Suggested Implementation Order

Once §8 is resolved, implement in this dependency-respecting order:

1. **Decisions** → write `ADR-NNNN` recording the §8 outcomes
2. **Schema** → migrate `order_items.cancelled_qty`, `refunds.partial_refund_id`
3. **Domain** → update `Order` aggregate with partial-cancel invariant (`FR-NNNN`)
4. **SF** → write `SF-NNNN` (partial cancel rule)
5. **API** → update `API-NNNN` openapi schema + handler
6. **Tests** → add `TC-NNNN` × 5 (use `vibecoding-write-tdd` skill)
7. **UI** → update `UF-NNNN` cancel screen with quantity selector
8. **Traceability** → update `TM-0000-traceability-matrix.template.md` with new BF/UF/SF/API/TC links
9. **Docs sync** → re-run `sunnydata-doc-freshness` to verify no tier-2 contract is left stale

## 10. Risks & Rollback

| Risk | Likelihood | Impact | Mitigation |
|---|---|---|---|
| ERP partial-cancel adapter complexity | Medium | High | Spike before committing — 1 week timebox |
| Race between partial-cancel & ship | Medium | High | Pessimistic lock on order at cancel |
| Existing API consumers break | Certain | Medium | Versioned endpoint or feature flag |

**Rollback plan**: feature-flag the new behavior; on incident, flag-off restores old single-cancel path within minutes. Schema additions are nullable/default-zero, so reverse-compatible.

## 11. Out of Scope

- Refund routing across multiple payment methods (separate CR)
- Partial cancellation of subscription orders (separate CR)
- Customer notification template redesign (separate CR)

## 12. Sign-off

| Role | Name | Date | Approved? |
|---|---|---|---|
| Product | | | |
| Architect | | | |
| Engineering Lead | | | |
| QA Lead | | | |
