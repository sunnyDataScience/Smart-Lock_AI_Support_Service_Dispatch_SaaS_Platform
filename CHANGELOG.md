# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

---

## [Unreleased] — 2026-Q2 Tactical Refactor

### Decisions

- **CR-0115 立案：師傅註冊擴充為 KYC 等級（登入/註冊分離 + 敏感 PII + 文件上傳）（branch `docs/cr-0115-technician-registration-kyc`，2026-07-05）**：業主回報師傅註冊「資訊太少」，要求參考業界類似系統擴充。研究（PRO360／ALOA／各州鎖匠執照／水電師傅平台）後業主裁決**最大範圍**：現有 6 欄（姓名/手機/Email/密碼/服務地區/專長品牌）擴為三層 —— Tier 1 非敏感（年資/自我介紹/交通工具/可服務時段/緊急聯絡人/證照）、Tier 2 敏感 PII（身分證字號/生日/地址/撥款銀行帳戶/統編）、Tier 3 文件上傳（身分證正反面/證照/保險證明），並**登入與註冊分離**（`/tech-login` 只留登入、新增獨立 `/tech-register` 多步驟表單）。命中 CIA 五面向（API contract／Domain model／DB schema／External integration／Test plan）+ 敏感 PII 隱私設計 → 產出 CIA `docs/4-exploration/CR-0115-technician-registration-kyc.md`，**🛑 停在 §8 等業主七項設計裁決**（PII 儲存/加密、公開前上傳時機、審核頁誰看全值、必填集、文件中間態、保留政策、雲端落點）後才依 §9 實作。

- **CR-0114 立案：平台方管理系統(platform console)與註冊/審核動線重構（branch `docs/cr-0114-platform-console`，2026-07-05）**：業主口述目標架構＋七項裁決——(1) 師傅審核**移到 platform console**、品牌端唯讀;(2) 品牌申請核准後**純手動開站**(console 只記錄狀態＋產開站指引文字);(3) 品牌選師傅**全部啟用中可見＋標示已授權/未授權**(過濾改標示);(4) 品牌登入頁註冊 tab 改「**品牌員工帳號申請**」(品牌 Admin 審核並指派角色);(5) **權限正典＝UAT 版 7 角色**(子帳號指派 5 員工角色);(6) 授權標示語意＝**鎖品牌授權**(沿用 technician_brand_authorization,零 schema 變更);(7) **自動派工仍只選已授權**。新增第四個 stack:platform-db(5435,`lock_platform` 獨立容器)/platform-api(8003,`API_SURFACE=platform`)/platform-web(3003,`APP_MODE=platform`);身分＝新角色 `platform_admin`(不活化 super_admin 死角色,帳號池與 JWT secret 與品牌隔離)。CR-0113 §8 四問全數回寫,CR-0112 補四 stack 拓撲節。實作分 R1-R5 五輪(見 CR-0114 §9)。

### Added

- **平台維運監控「詳細用量」連結導出到 GCP（CR-0116 A 方案,branch `feat/platform-monitor-usage-link`，2026-07-06）**：業主問「用量能不能一起顯示」。**釐清資料源**:紅綠燈來自 `/health`（便宜、本機可測）,但用量（請求數/CPU/記憶體/instance）住 **GCP Cloud Monitoring**,不在 /health;要在 console 內顯示真實用量須查 GCP Monitoring API（憑證 + project_id + 本機測不到,屬另一輪較大 CIA）。業主選 **A 方案:連過去**。維運監控頁工具列加「詳細用量 ↗」（`target=_blank`）→ 業主自建的 GCP 方案 1 Metrics Scope dashboard;URL 走 build-time env `NEXT_PUBLIC_GCP_MONITORING_URL`（Dockerfile ARG→ENV + compose build arg `PLATFORM_GCP_MONITORING_URL`）,**未設 = 不顯示**（避免指向空頁）。純前端 + config → CIA 豁免。驗證:tsc 0;帶測試 URL 重建 → Playwright 確認連結 href/target 正確 → 回空預設。**已部署本機 docker（platform web）**。

- **平台 console 維運監控:品牌部署 registry + 跨品牌 /health 紅綠燈（CR-0116，branch `feat/platform-ops-monitoring`，2026-07-06）**：因應「一品牌一 GCP 專案」部署下用 GCP Console 一家一家看 container 狀態不可行（>10 視窗上限，2026-07-02 會議 §十 + Action Item 12）。業主選**方案 B（DB registry + UI 管理）**。命中 DB schema / API contract / External integration → **走 CIA gate（`docs/4-exploration/CR-0116-platform-ops-monitoring.md`），§8 七項業主裁決後實作**（淨結果:flat registry 一目標一列、無歷史表、前端 30s 輪詢後端並發 fan-out）。**定位切割**:console 只做**即時紅綠燈**（給非技術者一眼看）；深度指標/告警/歷史走 GCP 原生（Metrics Scope + Uptime Check + Alerting，業主自理）。**Schema**:平台庫加 `monitor_target`（走 `Schema_platform.sql` 冪等 additive）。**後端**:`platform_monitor_service`（registry CRUD + `probe_health` 用 aiohttp 並發探測、單目標逾時 3s、狀態映射 200→up/503→degraded/其他→down、**不落庫**）+ `platform_monitor.py` 5 端點（GET list / GET health / POST / PATCH / DELETE，全 `require_platform_admin`，路由 `health` 字面段先於 `{id}`），mount main.py。**前端**:`/platform` 儀表板改**分頁**（`概覽` ｜ `維運監控`），維運監控分頁＝registry 管理（新增/編輯/刪除 modal）+ 依品牌分組紅綠燈狀態格 + 每 30s 自動輪詢 + 立即檢查。**本機 seed** 三目標（dispatch/tech/platform api 的 /health，docker 服務名）。驗證:`test_platform_monitoring.py` 11 項（CRUD / RBAC 401·403 / 驗證 422 / `_map_status` / probe 只回啟用目標且路由不被 {id} 吃）；tsc 0；重建 platform api+web；**Playwright 端到端**（登入→維運監控分頁 3 目標全綠 200 + 依品牌分組；新增打不通目標→顯示「異常」紅燈、摘要 1 異常；30s 自動輪詢延遲更新）+ **curl 佐證**（壞目標 status=down err=ClientConnectorDNSError 快速失敗）。**已部署本機 docker（platform api+web）**。

- **平台 console 儀表板接真實待審計數 + topbar 加主題切換（branch `feat/platform-dashboard-counts-and-theme`，CR-0114 收尾，2026-07-06）**：業主檢視 `:3003/platform` 儀表板發現兩處「R1 骨架留位沒接上」。**(1) 儀表板計數**：原兩張卡是靜態佔位（文案寫死「下一輪接上」、不抓數、不能點）→ 接上真實 pending 計數，**複用既有 list 端點的 status 過濾（零新 API）**：待審發案方 = `brand-applications(pending)` + `vendors(pending_approval)`、待審師傅 = `technicians(pending_approval)`；卡片改 `Link` 可點進對應審核頁（發案方審核／師傅管理），顯示大數字 + 「品牌申請 N · 廠商帳號 M」分項 subtitle。**(2) 主題切換**：主題系統本就 app-wide（root `layout.tsx` 的 `ThemeProvider` + FOUC script + globals.css `[data-theme]` 變數包含 /platform，平台頁一直隨系統色渲染），只差切換 UI —— 平台 console 頂欄是另外手刻的極簡 topbar，當初沒放 `ThemeToggle`。於登出鈕旁補上 `<ThemeToggle />`（複用品牌後台元件，icon 變體 + popover 三選）。**純前端、零後端／API／DB／測試改動 → CIA 豁免**。驗證：tsc 0；重建 platform-web；Playwright 端到端（登入→儀表板顯示真實計數 0/0/0〔完全重置後無待審〕+ 卡片 href 正確；主題切換 系統→深色 → `data-theme=dark` + `localStorage.theme=dark` 持久 + 頁底 `rgb(15,23,42)`＝深色 `--bg-page`）。**已部署本機 docker（platform web）**。

- **平台 console「品牌申請」＋「廠商審核」合併為「發案方審核」單頁（branch `feat/platform-merge-requestor-review`，CR-0114 收尾，2026-07-06）**：業主回饋「品牌跟廠商審核適合一起，都是發案的一方」（相對於師傅＝接案方）→ 把 `/platform/brand-applications` 與 `/platform/vendors` 兩頁合成單一頁 `/platform/requestors`，console nav 由三項（品牌申請／廠商審核／師傅管理）收為兩項（**發案方審核**／師傅管理）。**純前端整併，零後端／API／測試改動**：兩頁 body 抽為 `web/src/components/platform/BrandApplicationsPanel.tsx` + `VendorsPanel.tsx`（移除各自 `<h1>`，改小字描述以區分「申請意向 vs 已建帳號」），新頁 `requestors/page.tsx` 用**分頁切換**（品牌申請｜廠商帳號）承載兩者——因兩者資料模型與審核動作不同（品牌申請核准填品牌代號＋出開站指引 modal；廠商核准＝啟用登入帳號），用分頁分隔而非硬混成一張清單。刪舊兩頁路由（API 端點 `/api/v1/platform/brand-applications`、`/api/v1/platform/vendors` 不變）。驗證：tsc 0；Playwright（發案方審核頁兩分頁切換、品牌申請核准 prompt/廠商核准動作各自正常）。**已部署本機 docker（platform web）**。

- **廠商審核移植到 platform console（:3003）+ 品牌頁移除（branch `feat/platform-vendor-approvals`，CR-0114 收尾，2026-07-06）**：業主指示把品牌 `:3000/admin/vendor-approvals` 移到 `:3003/platform`（選項＝獨立「廠商審核」頁）。**釐清語意**：`vendors`＝發案方**登入帳號**（role=vendor，tenant_type=requestor，品牌/經銷/鎖店註冊後 pending→核准啟用可登入發案），與平台已有的 `brand_applications`（意向書、不建帳號、核准後人工開站）是**同類對象的不同生命週期階段**，故獨立成頁不併。**後端**：新 `platform_vendor_service`（cross-tenant list 不加 tenant filter + `_resolve_tenant_id` 委派既有 `vendor_service` 狀態機，含冪等 409/audit）+ `platform_vendors.py` 3 端點（GET `/platform/vendors?status=` / POST `:approve` / `:reject`，全 `require_platform_admin`，mount main.py）；品牌 `vendors_v2` **刪 `approveVendorV2`/`rejectVendorV2` 寫端點**（保留 `listVendorsV2` GET 唯讀 + `getVendorSelfV2` 自助，`test_platform_auth` 的 vendor→403 斷言仍成立）。**FK 處理**：`vendors.approved_by` 有 FK → 品牌 `users(id)`，而平台管理員住平台庫（lock_platform）非品牌 user → 平台核准一律存 `approved_by=NULL`（`approved_at` 記時間，「由平台核准」為隱含事實；approver_id 參數保留供未來平台側 audit 表承接，比照 technician_lifecycle_event）。**前端**：新 `/platform/vendors`（狀態篩選 + 核准/拒絕含原因 prompt，比照原品牌頁；硬編繁中）；console nav 加「廠商審核」（第 3 項，品牌申請與師傅管理之間）。**品牌移除**：刪 `admin/vendor-approvals` 頁 + 側欄「廠商審核」項 + `Store` icon import + rolePolicy `/admin/vendor-approvals` 死條目 + i18n（`sidebar.nav.vendorApprovals` + `admin.vendorApprovals.*` 11 鍵，parity 2654）；`/vendors/register` 公開註冊端點保留（pytest 依賴 + 未來入口）。驗證：`test_platform_vendor_approvals.py` 5 項（RBAC 401/403、list→approve 冪等 409、reject、品牌 approve/reject 移除後 404）+ 既有 `test_cr_0029_vendor_*`（打 service 層，不受影響），全套 **1623 passed** 零回歸；tsc 0；重建 platform api+web、dispatch web。**已部署本機 docker**。

- **師傅管理完整移植到 platform console（:3003）+ 品牌頁完全移除（branch `feat/platform-technician-management`，CR-0114 §8 追補收尾，2026-07-06）**：業主指示把品牌 `:3000/technicians` 移植到 `:3003/platform`。**選項＝完整管理 + 完全移除品牌頁**。**後端**：復原上輪收斂時刪掉的師傅身分域寫函式（`technician_service.create_technician`/`update_technician`、`technician_certification_service.create/update/delete_certification`）—— 治理在 route 層,函式本身可攜；`platform_technician_service` 加 `_resolve_tenant_id` 跨品牌 wrapper（`get_technician_detail`/`create_technician`/`update_technician` + 認證 CRUD）;`platform_technicians.py` 新增 7 端點（`platformCreateTechnician`/`getPlatformTechnician`（merge `authorized_brands`）/`updatePlatformTechnician` + 認證 GET/POST/PATCH/DELETE），全 `require_platform_admin`，寫師傅權威庫 + 鏡射投影。新師傅掛預設租戶 `...0001`（對齊自助註冊）、status=pending_approval。**前端**：`/platform/technicians`（清單 + 狀態篩選 + 搜尋 q + 「新增師傅」modal + 連詳情 + 保留核准/停權等生命週期動作）+ `/platform/technicians/[id]`（個人資料 + 編輯 modal 含等級 S/A/B/C + 認證矩陣 CRUD + 生命週期歷史 + 授權品牌）；**排班/獎懲屬品牌營運（per-brand 工單/財務）不搬**、評分/工單數為 per-brand 統計亦不聚合。console layout nav「師傅審核」→「師傅管理」;刪舊 `/platform/technician-approvals`。**品牌端移除**：刪 `/technicians` + `/technicians/[id]`（實測已 404）+ 側欄「技師管理」項 + `Users` icon import + 6 個孤兒元件（TechniciansTable/CertificationMatrix/ProfileCard/WeeklySchedule/TechnicianDetailSidebar/PenaltyBonusLog）+ rolePolicy `/technicians` 死條目 + 孤兒 i18n（`sidebar.nav.technicians`/`pages.technicians.*`/`components.technicians.*`，parity 2666）；**品牌 `technicians_v2` GET 保留**（`admin/customers` 頁仍讀技師清單）。驗證：`test_platform_technician_management.py` 7 項（建立/詳情/編輯含 level/認證 CRUD/建立→核准/RBAC 403）+ 更新 router 斷言,全套 **1618 passed** 零回歸;tsc 0（清 `.next/types` 後）;**API curl 實測**（建立 pending→詳情含 authorized_brands→編輯 level=A→新增認證;品牌 8001 該端點 404）+ **Playwright 端到端**（3003 師傅管理清單→新增師傅 modal→建立出現在待審→詳情三區塊；3000 側欄無技師管理、`/technicians` 404）。**已部署本機 docker（platform api+web、dispatch web）**。

- **品牌申請導入頁移到 platform 站（:3003）+ 業界補充欄位（branch `feat/cr0114-brand-apply-platform-page`，CR-0114 延伸，2026-07-05）**：業主要求把「申請導入平台」畫面做在 platform 站（:3003）、從 landing（:3002）跳轉過來，並研究業界類似案例補欄位。**動線改造**：原本是 landing 的 **modal**（`BrandApplyForm`），改為 platform 站獨立公開頁 `/platform/apply`；landing 兩個品牌 CTA 由開 modal 改為 `<a href>` 外導 `:3003/platform/apply`（新 `appMode.brandApplyHref()` + `PLATFORM_PORTAL_URL` env / `web/Dockerfile` ARG / `docker-compose.landing.yml`）；刪 orphaned `BrandApplyForm.tsx` + landing modal/state/scroll-lock。**平台頁**：新 `/platform/apply/page.tsx`（公開，`AuthGuard` PUBLIC_PATHS + platform layout 不套 console 殼；硬編繁中、分區版面、返回上一頁），POST 平台 API `/brand-applications`（plain fetch 無登入態）。**業界補充欄位**（研究 SparkLayer/Jotform 夥伴導入/iLockey 智慧鎖經銷/B2B reseller onboarding；additive 非破壞）：現有 8 欄外加 `website`（公司網站）/`coverage_regions`（服務涵蓋地區，派工媒合用）/`store_count`（門市據點數）/`expected_monthly_orders`（預估月工單量級距）/`main_brands`（主營品牌）/`referral_source`（如何得知）—— `Schema_platform.sql` 加 6 欄（`ADD COLUMN IF NOT EXISTS` 冪等，已套本機 5435）、`BrandApplicationBody` + `brand_application_service.submit`/`_SELECT_COLS`/`_row_to_dict` 補欄、platform console 審核頁顯示新欄（網站可點連結）。驗證：`test_platform_brand_applications.py` +1（enriched 欄位入庫+list 回傳）、全套 **1610 passed** 零回歸；tsc 0；重建 platform-api/web + landing-web；**Playwright 端到端**（3002 品牌 CTA→3003/platform/apply（無 admin 殼）→填 enriched 表單→送出→平台庫入庫 6 新欄→平台管理員審核頁完整顯示）。測後清 e2e 記錄。CIA-additive（延伸 CR-0114 現有品牌申請流，無破壞契約）。**已部署本機 docker（platform+landing）**。

- **師傅註冊 KYC 擴充：登入/註冊分離 + 多步驟表單前端（branch `feat/cr0115-tech-register-form`，CR-0115 S6，2026-07-05）**：接續後端地基，落 §9 的 UI 步驟（文字欄位）。**登入/註冊分離**：`/tech-login` 移除註冊 tab、改為只登入 + 「還沒有帳號？申請成為師傅 →」連結（i18n `techLogin.noAccount`/`applyLink`，parity 2753）；刪頁內舊 `TechRegisterForm`。**新 `/tech-register` 多步驟表單**（硬編繁中，對齊 admin/staff 慣例，i18n 列後續）：4 步驟（① 基本：姓名/手機/Email/密碼/服務地區/交通工具 ② 專業：年資/可服務品牌/時段/證照/自我介紹 ③ 撥款與聯絡：緊急聯絡人+電話、敏感 PII（身分證/生日/地址/銀行代碼+帳號/統編，加密提示）、同意條款 ④ 確認：摘要 + **身分證/帳號遮罩顯示末碼**），進度條 + 逐步驗證（身分證 `A123456789`/銀行/手機格式）+「最小必填」（§8-4：年資/服務地區/緊急聯絡人/同意）於表單層強制、PII 選填。接線：`appMode.TECH_BUILD_ALLOWED` + `techRegisterHref()` 改指 `/tech-register`（原 `/tech-login?tab=register` 退場）、`AuthGuard` PUBLIC_PATHS 加 `/tech-register`。**修送出防呆 bug**：表單原靠 `<form onSubmit>`，中間步驟按 Enter/互動會提前送出跳過確認 → 改 form onSubmit 一律 `preventDefault`、「送出申請」改 `type=button`+onClick 手動觸發，杜絕隱式送出（僅確認步驟按鈕可提交）。驗證：tsc 0 + 重建 tech-api（新契約 openapi 到位）+ tech-web + **Playwright 端到端**（登入頁只登入+連結 → 多步驟填寫 → 確認頁遮罩（身分證 `•••••••789`／帳號 `822 ••••••3210`）→ 送出 → tech-db 加密入庫（Fernet 密文 `gAAAAAB…`、末碼正確）、品牌庫投影無 KYC；重驗中間步驟真點下一步不再提前送出）。測後清 e2e 記錄。純前端 + 既有後端，走 CR-0115 CIA 治理。**後續**：Tier 3 兩階段 token 文件上傳、平台審核頁顯示新欄+文件、dispatch api 部署、雲端套 089。

- **師傅註冊 KYC 擴充：後端 Tier 1/2（branch `feat/cr0115-technician-kyc-backend`，CR-0115，2026-07-05）**：CR-0115 §8 裁決後依 §9 實作第一步（後端契約先於 UI）。**migration 089**：`technicians` 加 Tier 1 非敏感欄（`years_experience`／`bio`／`vehicle_type`／`availability_note`／`emergency_contact_name`／`emergency_contact_phone`／`terms_accepted_at`）+ 新 `technician_kyc` 獨立表（§8-1）存 Tier 2 敏感 PII。**PII 加密**：新 `core/pii_crypto.py` —— app 層 Fernet 對稱加密（金鑰 env `KYC_ENCRYPTION_KEY`，任意字串經 SHA-256 正規化為 Fernet 金鑰；未設用具名 dev fallback + loud warning），身分證字號/銀行帳號**只存密文欄（`*_enc`）+ 末碼欄（`national_id_last3`／`bank_account_last4`）供遮罩顯示**，birth_date/address/bank_code/tax_id 存明文於隔離表；`decrypt_pii`/`mask_tail`/`last_n` helper。**`register_technician` 擴充**：Tier 1 欄存 technicians、敏感 PII 加密存 technician_kyc、自填證照落既有 `technician_certification`、**敏感 PII 不鏡射品牌庫**（§8-1 最小揭露；`technician_kyc` 不在 tech_mirror 白名單）。**`TechnicianRegisterBody`** 加大量**選填**新欄（加性非破壞，§4；身分證 `^[A-Z][12]\d{8}$`／銀行帳號 `^\d{6,16}$`／生日等格式驗證）——「最小必填」（§8-4）於後續 /tech-register 新表單層強制，本輪不改既有 6 欄註冊行為。`cryptography>=42` 由 python-jose 傳遞相依改為 pyproject 顯式宣告。089 已套本機 5433（品牌+測試，mirror 投影需 Tier 1 欄）+ 5434（tech 權威庫）。測試：`test_cr_0115_technician_kyc_register.py` 5 項（Tier1+加密儲存/末碼/解回、身分證不明文入 technicians、只送 6 欄向後相容、格式 422×2），全套 **1609 passed / 2 skipped** 零回歸。**後續輪**：/tech-login 拆登入 only + /tech-register 多步驟表單（S6）、兩階段 token 文件上傳、平台審核頁顯示新欄+文件、api image 重建部署、雲端套 089。

- **師傅／廠商主畫面重設計（branch `feat/portal-home-redesign`，2026-07-05）**：業主回報兩個 portal 主畫面「因之前合併的關係有點陽春」，要求重新設計。**① 師傅首頁 `/home`**：舊 hero 是「狀態膠囊＋上線開關各佔半欄白卡」的鬆散排版、全頁卡片同灰無層次 → 收攏為單一**深色漸層 hero 卡**（新 `TechHomeHero.tsx`：問候＋日期＋上線狀態 chip＋大字收入（tabular-nums）＋上線大開關；漸層自含固定色、亮/暗主題皆成立），`GoOnlineToggle` 加 `tone="hero"` prop（深底上提示文字改白系、**離線態改白底深字強 CTA**——深底灰鈕不夠醒目而「上線接案」是首頁第一行動）；「月度快照」由白卡內四灰格改為 **eyebrow 標題＋四張彩色 tint 統計卡**（毛額 emerald／完成率 blue／到場 amber／評分 violet,固定 hex＋低透明 tint 底,雙主題可讀）;「近期客戶評價」自 MonthlySnapshot 拆出獨立 `RecentFeedback.tsx`（無評價整卡隱藏,不擺空殼）;頁首 sticky 列改顯「首頁」（問候移入 hero）;刪被 hero 吸收的 `StatusEarningsPill.tsx`（全站無其他使用點）。資料面零變更（同一組既有端點）。**② 廠商專區 `/vendor`**：原本只有狀態卡＋帳號資料兩張卡＋一行小字 → 重排為完整 portal：**同語彙深色漸層 hero**（歡迎回來＋公司名＋類型 chip＋狀態 chip＋狀態提示,rejected 顯駁回原因）;**待審核狀態加「申請進度」三步視覺**（送出申請 ✓ → 平台審核中 ⏱ → 核准開通,連接線手機直向/桌面橫向）;帳號資料改 icon 列;新增「服務功能」區——發案管理/對帳與請款/營運報表三模組以虛線卡＋「規劃中」badge **誠實標示**（後端尚無 vendor 發案/對帳端點,不造假入口,但讓廠商知道功能將出現在哪）;支援條（聯絡平台管理員）。維持頁面既有硬編繁中（i18n 另輪）。驗證：tsc 0＋重建 tech/dispatch web image＋**Playwright 桌面＋手機雙尺寸實測**（師傅 `/home`：hero/統計條/雙欄卡渲染正確;廠商 `/vendor`：active 態 hero＋模組區、pending 態三步進度條、手機直向堆疊全正確;測試廠商帳號測後清除）。純前端視覺重排、無契約變更故 CIA 豁免。**已部署本機 docker web（tech＋dispatch）**。

- **品牌員工帳號申請（前端接線）：登入頁員工申請 tab + admin 待審區（branch `feat/staff-applications-web`，CR-0114 R5，2026-07-05）**：R5 後端（migration 088/service/router）已於前輪 merge，本輪補前端接線（裁決 4）。**① `StaffRegisterForm.tsx`（新）**：品牌員工於登入頁自助申請,只收姓名/Email/電話(選填)/密碼(**無角色選擇**——角色由 Admin 指派)；POST `tenantPath("/staff-applications")`（公開端點,tenantId 取自 path;前端未登入以 `auth.getTenantId()` 退回 `FALLBACK_TENANT_ID`＝本部署品牌租戶,與 X-Tenant-ID header 一致）。**② `login/page.tsx`**：註冊 tab 由 `VendorRegisterForm`（廠商自助註冊,含 vendor_type/公司/統編）換成 `StaffRegisterForm`；`login.tabRegister` 標籤「註冊」→「員工申請」（en「Register」→「Staff apply」）。**③ `admin/staff/page.tsx`**：新增「待審員工申請」琥珀卡區——`GET tenantPath("/staff-applications")?status=pending`,每列顯示姓名/Email/電話＋5 員工角色下拉（重用既有 `ROLE_OPTIONS`,對齊 `_STAFF_ROLES` 正典）＋「核准並指派」（`:approve {role}`,核准後 `Promise.all([loadApps,load])` 即時刷新待審與員工列表）＋「拒絕」（`window.prompt` 收原因 ≥3 字 → `:reject {reason}`）；pending 清空時整區隱藏。**④ 刪 `VendorRegisterForm.tsx`**（landing 早已改用 `BrandApplyForm`、login 換 `StaffRegisterForm` 後全站無使用點;品牌導入改走 landing → platform 意向申請,`/vendors/register` 後端仍保留供既有 vendor）。**⑤ i18n**：+`staffApply` 區塊 8 鍵（intro/name/email/phone/password/submit/submitting/success,zh-TW＋en,parity 2743→2751）。tsc 0＋重建 dispatch web/api image（api 重建把已 merge 的 R5 後端部署上線,openapi 3 路由到位）＋**Playwright 端到端實測**：登入頁員工申請 tab 只剩 4 欄、無統編/公司/類型 → 填表送出 → 綠字「申請已送出,待品牌管理員審核並指派角色後即可登入。」→ admin 登入 `/admin/staff` 見待審區（測試員工R5）→ 選「派工員」核准 → OK「已核准…並指派為『派工員』」、待審區消失、員工 5→6 位、新列 dispatcher/啟用 → 登出以新帳號登入 → role=`dispatcher`、tenant 正確。測後清 users/staff_applications 測試列。純前端接線＋既有後端部署,無契約變更故 CIA 豁免。**已部署本機 docker web+api**。**待辦**：R5 reject 的 `window.prompt` 未來可換 inline 表單;5 輪收尾（docs_html regen、雲端部署含 088、push）待後續。

- **品牌員工帳號申請（後端）：登入頁申請 → 品牌 Admin 審核指派角色（branch `feat/staff-applications`，CR-0114 R5，2026-07-05）**：業主裁決 4——品牌站內註冊改「員工帳號申請」：員工自助申請、品牌 Admin 審核並指派 UAT 版 5 員工角色之一（admin/operations_manager/dispatcher/customer_service/reviewer，裁決 5 正典）。**本輪僅後端**（前端 StaffRegisterForm／登入頁 tab 換裝／admin/staff 待審區／刪 VendorRegisterForm／i18n 留後續輪；業主指示先不動 web）。品牌庫新表 `staff_applications`（migration 088；pending (tenant,email) 部分唯一索引）——**不預建 users 列**（placeholder role 會漏進員工清單/角色統計，且 CR-0090 依角色去重無法定義；獨立表天然留拒絕審計）；申請時即 bcrypt 密碼（核准直接搬進 users 免二次 hash）。`services/staff_application_service.py`（submit 含 pending 去重 409＋email 已在職 409／list／approve 於 transaction 內 INSERT users（欄位對齊 `create_staff_user`：`tenant_type='platform'`、reuse 已 hash 密碼）並驗 role ∈ `_STAFF_ROLES` 單一正典／reject reason 必填，CAS 狀態機）；`routers/staff_applications.py`（全 tenant-scoped `/tenants/{tid}/staff-applications`：POST 公開（tenantId 取自 path）＋GET/:approve/:reject 走 `role_required(*FULL_ACCESS_ROLES)`，對齊 v2 慣例無 `/api/v1` 前綴）。申請中/被拒者無 users 列 → 登入自然查無，不需動登入路徑。`/vendors/register` 後端保留。測試：`test_staff_applications.py`（10 項：公開申請/pending 去重 409/驗證 422×3/不建 users/list gate（dispatcher 403）/核准指派角色後可登入/非法角色 super_admin 422/拒絕後可再申請），全套 **1604 passed** 零回歸。
- **品牌讀共用師傅庫：標示授權 + mirror-on-demand（branch `feat/brand-read-tech-authority`，CR-0114 R4，2026-07-05）**：品牌選師傅派工從「只讀本地投影＋授權過濾」改為「讀共用師傅庫 authority＋標示授權」（裁決 3/6/7）。**後端**：`dispatch_service._fetch_tenant_technicians`／`_brand_authorized_ids`／`_enrich_gis_performance`、`technician_service.list_technicians`／`get_technician` 全改 `require_tech_conn()`（單庫 fallback 同顆連線、SQL 不變 → 零回歸）；`list_dispatch_candidates` 鎖品牌授權由「過濾候選」改「**標示** `brand_authorized`（true/false/null）＋已授權排前」——全部啟用中師傅皆可見（裁決 3）；`auto_match_dispatch` **維持只選已授權**（裁決 7）；`technician_service` 清單/單筆附 `authorized_brands`（已授權鎖品牌，裁決 6，`_authorized_brands_map` 一次聚合；**服務層運算，GET 端點以 Technician schema 序列化會濾掉此附加欄，端點層曝露供前端 chips 屬後續前端輪**）。**FK 完整性**：`tech_mirror.ensure_technician_projection()`（指派前把 authority 師傅 pull 進品牌庫投影，單庫 no-op）掛 3 寫入點（`assign_order`／`reassign_order`／`accept_order` claim）——共用師傅庫模式下指派「未投影過的師傅」不再 FK 爆。**Web**：`dispatch-manual` 候選卡加「已授權／未授權」badge（`brand_authorized`）。測試：`test_cr_0114_brand_read_authority.py`（4 項：projection 單庫 no-op／`_authorized_brands_map` 回鎖品牌／service 層 list 附 authorized_brands／auto_match `_brand_authorized_ids` 過濾語義）＋既有 `test_cr_0060`／`dispatch_v2` 8 passed 零回歸。全套 **1594 passed**。
- **師傅審核搬遷到平台方 console（branch `feat/platform-tech-approvals`，CR-0114 R3，2026-07-05）**：師傅生命週期審核（核准/拒絕/停權/復權/終止）從品牌後台搬到 platform console（裁決 1）——師傅平台全品牌共用一顆身分庫，審核歸平台方統一管。**後端**：新 `routers/platform_technicians.py`（GET 跨品牌清單 `?status=&q=`＋5 生命週期端點＋GET lifecycle-events，全 `require_platform_admin`）＋`services/platform_technician_service.py`（**復用 `technician_lifecycle_service` 狀態機零改動**：先查師傅 tenant_id 再轉呼叫，`actor_role='platform_admin'`、initiator 取已驗簽 token sub —— 廢除品牌端「前端自報 X-Initiator」的偽造面）。品牌端 `technician_lifecycle_v2.py` **刪 5 個寫端點**、只留 GET lifecycle-events 唯讀 audit。**Web**：品牌 `technicians` 頁核准鈕移除＋頁首加「師傅生命週期由平台方統一管理」提示、`[id]` 詳情頁停權/復權鈕改狀態說明；console 新 `/platform/technician-approvals` 頁（狀態分頁＋依 `_ALLOWED_TRANSITIONS` 顯示可執行動作＋reason prompt）＋layout 導覽啟用。**測試改寫**：`test_technician_login_status_gate`／`test_technicians_onboard_v2_endpoint` 核准改打平台端點；`test_technicians_v2_endpoint` 驗品牌 `:suspend` 已 405；`test_technician_lifecycle` router 結構斷言更新（品牌 1 唯讀／平台 7）；新 `test_platform_technician_lifecycle.py`（6 項：清單 gate/過濾搜尋/approve-suspend-reactivate-audit/reason 必填/404/非法轉移 409）＋conftest `platform_admin_headers` fixture。全套 **1590 passed**、tsc 0。
- **品牌申請流：landing → platform console 審核（branch `feat/platform-brand-applications`，CR-0114 R2，2026-07-05）**：landing 品牌 CTA 從「打派工 API `/vendors/register`（送出即建帳號）」改為「打 platform API `/api/v1/platform/brand-applications`（意向申請，不建帳號、不收密碼）」。**後端**：平台庫新表 `brand_applications`（application_type/company_name/contact_name/tax_id/phone/email/address/notes/status/slug/review_notes/submitted_ip，pending email 部分唯一索引）；`services/brand_application_service.py`（submit 含 per-IP DB 計數限流 5 件/hr＋pending 去重 409／list／approve 回 `onboarding_guide` 純文字＋reject reason 必填，CAS pending→approved/rejected）；`routers/platform_brand_applications.py`（POST 公開＋GET/:approve/:reject platform_admin 限定）。核准回傳的開站指引為**純文字**（以 `brands/locksmart.env` 為模板產 `brands/<slug>.env` 建議值＋docker compose 手動步驟），實際開站全人工（裁決 2）。**Web**：`components/landing/BrandApplyForm.tsx`（無密碼，plain fetch 打 platform API，409/429 訊息直顯）取代 landing modal 的 `VendorRegisterForm`；`web/Dockerfile` +ARG `NEXT_PUBLIC_PLATFORM_API_BASE_URL`、`docker-compose.landing.yml` args 加對應 env；console 新 `/platform/brand-applications` 頁（pending/approved/rejected 分頁、核准填 slug 後彈開站指引 modal、拒絕填原因）＋layout 導覽啟用。i18n +3 keys（parity 2743）。測試：`test_platform_brand_applications.py`（12 項：公開申請/去重 409/驗證 422/限流 429/核准回指引/bad slug 422/拒絕後可再申請/404），全套 1584 passed、tsc 0。
- **platform console stack 骨架：第四個 stack + `API_SURFACE=platform` + `APP_MODE=platform`（branch `feat/platform-stack-skeleton`，CR-0114 R1，2026-07-05）**：平台方(Lock AI)自用管理系統的地基。**API**：`core/db.py` 加第三連線 `_platform_conn`（`PLATFORM_POSTGRES_URI`，未設=fallback 主連線，pytest/CI/雲端單庫零行為變化）；`core/auth.py` 安全狀態/jti 查詢加 role 路由（platform_admin 查平台庫）；`core/deps.py` 新 `require_platform_admin`（非 tenant-scoped）；`main.py` platform surface 過濾（只留 `/api/v1/platform` 前綴）+ worker 停用；新 `routers/platform_auth.py` + `services/platform_admin_service.py`（login 含 lockout/refresh rotate/logout/me，全落平台庫）。**新角色 `platform_admin`**（不活化 super_admin 死角色）：帳號池住平台庫、獨立 JWT secret（compose 同名不同值）→ 品牌 token 打平台端點 403、平台 token 打品牌端點 403（雙向 deny-by-default，pytest 驗證）。**基礎設施**：`docker-compose.platform.yml`（platform-db 5435 `lock_platform` 獨立容器/platform-api 8003/platform-web 3003）、`SQL/platform/Schema_platform.sql`（users+revoked_jti）、`scripts/db/init-platform-db.sh`（schema+seed 管理員，密碼參數傳入不落檔）。**Web**：`appMode.ts` 第 5 模式 platform（只服務 /platform 頁群）、`rolePolicy.ts` `/platform → platform_admin`（刻意不入 FULL_ACCESS）、AuthGuard `/platform/login` 公開頁+登入導向分流、`api.ts` refresh/登入導向依 role 分流 + `loginPlatformAdmin`/`logoutPlatformAdmin`、新 `/platform/{login,儀表板}` 頁群（獨立頂欄殼，不用品牌 Sidebar）。測試：`test_platform_surface.py` + `test_platform_auth.py`（28 項），全套 1572 passed 零回歸、tsc 0；compose 實測（3003 登入→儀表板→登出、8003 surface 過濾品牌端點 404、3000 不服務 /platform）。**對抗式審查修正 3 項**（3 視角 review + 逐項反駁覆核，4 confirmed/5 dismissed）：(1) compose 平台 JWT secret 改必填 `:?`＋main.py platform surface 啟動守衛（拒空/過短/dev 標記密鑰——god-role token 密鑰絕不可有 repo 內預設值，實測 dev 密鑰拒起/正式密鑰健康）；(2) init 腳本密碼從 argv 改互動 read -rs／env＋stdin 傳遞（argv 會洩入 ps/shell history）；(3) rolePolicy `/platform` 檢查移到 FULL_ACCESS 早退之前（否則品牌 admin 前端可進 console 殼，與 deny-by-default 對稱宣稱矛盾）。
- **landing 獨立容器：新增 `APP_MODE=landing` + `docker-compose.landing.yml`（branch `feat/landing-container`，2026-07-05）**：業主需求——把一頁式介紹網頁部署成獨立 container(對外門面與登入後系統關注點分離)。**同一份 web image**、以 `NEXT_PUBLIC_APP_MODE=landing` build：`appMode.ts` 加第 4 個模式,`crossModeRedirect` 在 landing 下只放行 `/`、其餘導派工 portal;CTA href 集中為 `techRegisterHref()`/`dispatchLoginHref()` 兩個 helper(landing → 絕對 URL 指師傅/派工 portal via `NEXT_PUBLIC_TECH_PORTAL_URL`/`NEXT_PUBLIC_DISPATCH_PORTAL_URL`;非 landing → 沿用站內/PEER 邏輯,零行為變化)。landing 頁在此模式下一律顯示「登入」(外導派工 portal,無登入態偵測)。**Dockerfile** +2 ARG(tech/dispatch portal URL)。**`docker-compose.landing.yml`**:單一 `landing-web` service(埠 3002)、**無 DB/後端依賴**——純 SSR + 瀏覽器打各服務,可完全獨立起;品牌申請 modal 打派工 API `/vendors/register`。埠位:landing 3002 / 派工 web 3000 / 師傅 web 3001。tsc 0;Playwright 實測(landing 容器 3002 渲染、師傅 CTA 絕對指 :3001、登入指 :3000、品牌 modal 送出 pending)。
- **一頁式平台介紹 landing（branch `feat/landing-one-pager`，2026-07-05）**：業主需求——介紹派工平台的一頁式網站，師傅要接單有按鈕跳師傅註冊、品牌方申請平台跳出品牌基本資料表單（對齊 20260702 會議 §三「landing 掛申請窗口、半自動化導入」）。**`/` 重寫為行銷一頁式**：頂欄（logo＋語言/主題切換＋「登入」→ /login 保留既有入口）、Hero＋雙 CTA、平台能力 ×4（LINE AI 客服/智慧派工與案件池/師傅行動工作台/報價與帳務閉環）、運作流程 ×3 步、師傅招募帶（3 賣點＋CTA）、品牌導入帶、footer。**師傅 CTA** → `/tech-login?tab=register` 深連結直落註冊表單（CR-0112 雙 stack：dispatch build 有配 PEER_PORTAL_URL 時指對方 portal 絕對 URL）。**品牌 CTA** → modal 彈出品牌基本資料表單——把 login 頁內嵌的廠商註冊表單抽成共用元件 `components/auth/VendorRegisterForm`（兩處入口同一份：登入頁註冊 tab＋landing modal），送出走既有 `POST /vendors/register` → `pending_approval` 進後台「廠商審核」佇列（半自動化：審核＋協助部署）。tech build 的 `/` 照舊直導 /tech-login（師傅 stack 不渲染行銷頁）。i18n 兩 locale +33 keys（parity 2740）。tsc 0；**Playwright 實測**：未登入 landing 全區渲染、師傅 CTA href 正確且深連結落在師傅註冊表單、品牌 modal 填表送出成功訊息、DB `vendors` 出現 `pending_approval` 列（測後清理）、右上登入連結通 /login。
- **技師身分庫物理拆分：權威庫＋投影雙寫（CR-0112 方案 B，branch `feat/tech-db-split`，2026-07-04）**：業主 2026-07-03 晚間改裁——師傅 stack 要有**自己的獨立 DB**（方案 B）。**架構（化解拆庫五斷點的關鍵設計）**：盤點發現 **35 張品牌表 FK 指向 users/technicians**（work_orders/dispatch_logs/notifications/revoked_jti…）→ 純搬走會讓品牌庫 FK 全爆＋派工/佣金 JOIN 全改。採**權威庫＋投影（mirror）**：`tech-db`（lock_tech，埠 5434）＝技師身分權威庫（users 技師列/technicians/排班/品牌授權/認證/技能/lifecycle 事件，登入閘與技師狀態以此為準）；品牌庫保留技師列作**同步投影** → FK 與所有跨域 JOIN **零改寫**、全站「讀」路徑不動。**同步＝寫入路徑雙寫**：`api/core/tech_mirror.py` 於身分寫入後以**實際值** upsert/刪除投影（SELECT * 鏡射而非重放 SQL → 兩庫 DB 端生成值不分岔；失敗大聲 raise，修復走 `scripts/db/split-tech-db.sh --verify/--force`）。**Fallback 安全閥**：`TECH_POSTGRES_URI` 未設＝單庫、行為與拆分前完全相同（雲端 UAT/CI/pytest 全走此模式）。**改造面**：`core/db.py` 雙連線（`require_tech_conn` 未配置時回主連線）；寫入路由＝auth（技師註冊 transaction/登入失敗計數/改密/admin 重設/自助重設 confirm）＋ technician_service（me/admin 編輯、admin 建技師、online_state）＋ lifecycle（核准/停權全交易入權威庫＋鏡射 users+technicians）＋ 排班（technician_schedule_requests **單一居所**技師庫）＋ 認證 CRUD＋ GDPR redact/hard-delete 依角色路由；lifecycle 事件單一居所技師庫。**特例**：排班 resolver FK 拆除（審核者是後台帳號不在技師庫）；雙庫模式技師帳號不可走 RBAC 角色指派（身分域不變量 422）；password reset confirm 跨庫失去原子性 → 改「先改密後燒 token」順序。**compose**：tech stack 加 `tech-db` service＋volume `lock-tech-pgdata`；品牌 api 以 env 選擇性接入（`TECH_POSTGRES_URI=... docker compose -f docker-compose.dispatch.yml up -d api`）。**遷移**：`scripts/db/split-tech-db.sh`（容器內 pg_dump schema 子集［補 uuid-ossp/pgcrypto extension］＋ FK 序全量搬遷＋雙庫比對；實搬 users 85 技師列/technicians 19/skill 24/brand_auth 60/lifecycle 10，零漂移）。**驗證**：api 全套 **1544 passed / 0 failed**（fallback 零回歸；db.py 健康檢查改 getattr 防禦以相容單元測試假連線、role 探查閘在 tech_db_enabled 之後）＋雙庫端到端（技師註冊→兩庫都有→核准→登入；見 CR-0112 §8-bis）。
- **師傅端/派工方雙 stack 拆分：兩份 docker compose + API_SURFACE + 前端雙 build（CR-0112，branch `feat/split-tech-dispatch-stacks`，2026-07-03）**：對齊 20260702 會議 §三「一品牌一 GCP 專案＋獨立 DB;師傅平台全品牌共用一套」的本機部署映射。**🛑 SoT 衝突正式記錄**：會議稱「師傅資料庫已獨立」，但 code 是**單一 DB 單一連線**（`api/core/db.py` 唯一 `POSTGRES_URI`；技師與工單 10+ 張表同交易/同 SQL JOIN 深度耦合，物理拆庫會斷搶單/指派/核准/月結/排班五條流程）→ 業主裁決**方案 A**：服務面拆到位、資料面此輪共用品牌庫，技師共用庫物理拆分（跨庫同步層）列 AI-2/AI-3 設計案。**① `docker-compose.dispatch.yml`（派工方，每品牌一套）**：db（每品牌獨立 Postgres 實例，`BRAND`/埠位參數化，volume `lock-dispatch-{brand}-pgdata`）＋ api（`API_SURFACE=dispatch` 完整面＋背景 worker）＋ agent（LINE bot 歸品牌側）＋ web（dispatch build）＋ `db-init` 一次性初始化 service（profile 隔離；`scripts/dev/compose-db-init.sh` 套 Schema→migrations→seeds＋追蹤表，解決「compose 空庫無 schema」的歷史遺留）。**② `docker-compose.tech.yml`（師傅，全品牌共用一套）**：tech-api（**`API_SURFACE=tech` 精簡面**——路由過濾只留技師 app 呼叫面（auth/technicians/work-orders/tech-statements/media/problem-cards＋pool・wo 兩條 WS），**背景 worker 全停**避免與品牌 api 接同 DB 雙跑重複 LINE 推播/告警；部署塑形非安全邊界，RBAC 仍把關）＋ tech-web（tech build）；經 external network 連品牌 DB、media volume 跨 stack 共享（技師上傳照片品牌後台可讀）。**③ api `API_SURFACE` env**（all|dispatch|tech，預設 all **零行為變化**）。**④ web `NEXT_PUBLIC_APP_MODE` 雙 build**（appMode.ts＋AuthGuard 跨端導向＋landing/登入頁跨端連結走 `NEXT_PUBLIC_PEER_PORTAL_URL`；未設 env = all 零影響）。埠位：派工方沿用 5433/8001/8000/3000，師傅 8002/3001。舊 `docker-compose.yml` 移除（redeploy-local.sh 改指 dispatch 檔）；預設品牌 pgdata 由舊 volume clone（舊留備援）。已知取捨：realtime WS 為 in-memory pubsub，雙實例後技師端即時推播只收品牌 api 側事件（頁面本有優雅降級）。`test_api_surface` 31 測試（預設全掛/技師面保留/派工面剔除/真路由表模擬過濾）；api 全套 **1544 passed**；tsc 0。
- **報價主檔三類目 CRUD（CR-0110 依會議裁決簡化落地）（branch `feat/quote-catalog-crud`，2026-07-03）**：報價修改先前被 CIA/tenant 隔離卡住（CR-0110 §8 九項 open 決策）。20260702 會議拍板「**一品牌一 GCP 專案＋獨立 DB**」→ 單品牌 DB 內 code 天然唯一，global+override 資料模型/複合鍵 migration/引擎 fallback 全數不需要，`quote_engine.add_line` 按 code 查價**不變**。業主 2026-07-03 裁決殘餘三項：三類目一次做／軟刪 `deleted_at`（migration **087**；同 code 重建=復活該列——code 是物理 PK，軟刪列仍佔用）／租戶編輯後 `is_mock=FALSE`＋`decision_status='已確認'`（自填價即正式值）。**後端**：`quote_catalog_service` 加通用 CRUD（白名單欄位驗證、金額≥0、code 唯一 409 CODE_TAKEN、NOT_FOUND 404）＋三個 list 過濾 `deleted_at IS NULL`；`catalog_v2` 加 3 端點 ×3 類目（`POST/PATCH/DELETE /tenants/{tid}/quote-catalog/{services|materials|surcharges}[/{code}]`，守衛對齊 pricing_rules_v2：OPS_ROLES＋POST 強制 Idempotency-Key）。**前端**：報價主檔頁三區各加「新增」鈕＋每列 編輯/刪除（modal 表單、`cacheInvalidate` 後即時反映；刪除有 confirm）；移除「示意資料」徽章（現可含租戶確認值）。CR-0110 frontmatter 轉 `implemented`＋§8 補裁決記錄。`test_quote_catalog_crud` 9 測試（三類目 CRUD 生命週期含軟刪列保留/刪後 404/軟刪 code 復活、缺名 422、負數 422、未知類目 404、技師 403）＋ quote/catalog/pricing 回歸 **86 passed**；tsc 0＋i18n parity 2707；**Playwright 實測**：新增服務 32→33 即時出現 → 編輯 2500→2800 即時反映 → 刪除 33→32（DB 軟刪列在）。**⚠️ 雲端部署需套 migration 087**。

- **UI 入口濃縮為兩條 + 登入註冊同框（branch `feat/two-entry-auth`，2026-07-03）**：20260702 會議決議 2——現行 4 個入口（/login、/tech-login、/vendor-login、/register 全獨立頁）概念太散，一律濃縮為兩條、登入與註冊放同一個框（仿 Google）。**① Landing `/`**：4 卡 → 2 卡（「品牌/經銷/鎖店登錄」派案方 →後台、「鎖匠師傅登錄」接案方→工作台）。**② `/login` = 品牌/經銷/鎖店入口**：同框 登入/註冊 tab——登入為**單一表單涵蓋兩種派案方帳號**（先試 loginAdmin，401 才退試 loginVendor；非 401 如鎖定/停用直接顯示不重試、避免爆破計數污染），成功後依角色導向（`fallbackRouteForRole`：vendor→/vendor、後台→/dashboard）；註冊 tab = 原 /register 廠商表單搬入（類型/聯絡人/公司/統編/手機/Email/密碼/地址 → `/vendors/register`）。**③ `/tech-login` = 鎖匠師傅入口**：同框 登入（手機/Email identifier）/ 註冊（原技師表單：姓名/手機/Email/密碼/服務地區/可服務品牌 → `/technicians/register`，成功顯示待核准訊息——配合登入狀態閘，核准前不可登入）。**④ 舊路由不斷鏈**：`/vendor-login` → redirect `/login`、`/register` → redirect `/`（landing 選身分）；`?tab=register` 深連結支援；vendor 頁登出與 api.ts 401 兜底導向同步改指新入口。後端登入/註冊端點**零改動**（帳號體系三層重整屬 AI-2 另輪設計）。i18n 兩 locale：landing 改 2 卡鍵、login/techLogin 加 tab 鍵、清 6 個孤兒鍵（parity 2707）。tsc 0；**Playwright 四流程實測**：admin 同框登入→/dashboard、廠商註冊→待審核訊息、**新註冊 vendor 用同一表單登入→自動退試 vendor 端點→/vendor**、技師註冊 tab 表單齊、兩箇 redirect 生效。

- **UAT 前假流程強隱藏 + 隱藏功能報告（branch `feat/uat-hide-fake-flows`，2026-07-03）**：20260702 會議決議 7——上線給 Johnson UAT 前，把所有「畫面有反應但金流/後端沒實際接通」的功能先隱藏（業主 2026-07-03 裁決：全依盤點清單強隱藏）。盤點確認**全站金流 0 真接通**（`payment_service` 純 mock 且無 router 掛載）。**統一開關** `web/src/lib/uatFlags.ts:UAT_HIDE_FAKE_FLOWS`（單旗標可整批恢復，後端 API/DB 不動）。**隱藏**：① `/admin/refunds` 整頁（側欄入口移除＋直連 404）——審批是真狀態機（DB/SoD/稽核）但核准不會真的退錢，誤導性最高；② 工單詳情右欄：設備遙測示意（電量/連線/最近操作）、S/N「—(示意)」佔位、遠端開鎖/重置密碼 disabled 死鈕（IoT 未接、對鎖業客戶敏感）、標記異常死鈕（派工模組未接）、「明細將於派工計費模組接入後顯示」佔位說明；③ 工單詳情：時間軸「全部」disabled 篩選鈕、「示意」banner（唯一提及的設備面板已隨之隱藏，banner 失去存在意義）；④ 儀表板 SLA 達標率卡（88% 為寫死示意值）；⑤ KPI 報表 SLA/滿意度/NPS 待接入佔位塊；⑥ 營收報表「切片:按品牌」「與上期比較」即將推出控制＋「樞紐切片待擴充」預告文字。**保留＋警語**：月結批次鈕旁加黃標「金流未接:僅建立帳務紀錄,實際撥款由財務手動匯款」（HD-3 半真功能，ledger 是真的）。**真資料區塊全保留**：工單歷程/完工報告/報價明細/成本明細（CR-0027）/異常記錄/品牌營收占比等。i18n 兩 locale +1 key（parity 2706）。**產出報告** `docs_html/20260709/UAT-隱藏功能清單與可用功能報告.html`（隱藏清單＋加註警語＋十模組實際可用功能，下週三給 Irene；未入版控）。tsc 0；**Playwright 逐頁驗證**：dashboard 無 SLA 卡＋側欄無退款、refunds 直連 404、wo-detail 假控制全消失＋真區塊在、revenue 切片/比較/預告全消失＋品牌表在、accounting 警語顯示、KPI 佔位塊消失。

- **LINE 圖片訊息接入 vision 管線（VLN Bug 修復）（branch `fix/line-image-vln`，2026-07-03）**：20260702 會議待辦——「用戶直接上傳一張照片，系統目前還不會知道」。**Root cause**：`lockcore/channels/line_gateway.py` 的 webhook 迴圈只放行 `TextMessageContent`，圖片/貼圖/語音等型別一律 `continue` **靜默丟棄**（webhook 回 200 但 agent 不跑 turn、不回覆、對話管理也看不到）——而 lockcore 內部 vision 管線**早已完整**（`InboundMessage.media` → `_build_user_content` base64 `image_url` → LiteLLM；不支援 vision 的模型有降級保護；history 重播只留 `[image: path]` 文字麵包屑不重讀檔）。**修正（單檔 + 啟動器不動）**：① 放行 `ImageMessageContent` → `AsyncMessagingApiBlob.get_message_content(message_id)` 二次拉取 bytes → 依 magic bytes 判副檔名落地 `get_media_dir("line")` → `InboundMessage(media=[路徑])` 走既有 vision 管線；下載失敗 fail-soft 回「請再傳一次」。② `handle_text_turn` 加 `media` 參數（純圖片 turn 不被空訊息 guard 擋掉）。③ 其他型別（貼圖/語音/影片/檔案/位置）回友善話術「只看得懂文字和照片」＋以 `[貼圖]` 等標記旁路持久化（不再已讀不回）。④ 接管中（handover）與 escalation 兜底路徑同步支援圖片 turn（persist 用 `[照片]` 標記）。新增 6 測試（media 傳遞／純圖不擋／無文無圖跳過／下載落地＋副檔名／空內容 None／Blob 例外 fail-soft）；agent 全套 **143 passed**。註：需部署後在 LINE 實傳照片做端到端驗證；正式環境 model 需支援 vision（預設 claude-sonnet-4-5 支援）。

- **技師/廠商誤入後台路由白畫面死鎖修正（branch `fix/tech-route-policy`，2026-07-02）**：`AuthGuard` 被拒一律導 `/dashboard`，註解宣稱「對所有後台角色開放不會無限重導」——但 **technician/vendor 不在 `/dashboard` 允許清單** → 導過去再被拒 → 重導自己 → `checked` 永不為 true → **整頁永久白畫面**（技師誤開任何後台網址即卡死，只能手改網址）。**修正**：**①** `rolePolicy` 新增 `fallbackRouteForRole()`（technician→`/home`、vendor→`/vendor`、其他→`/dashboard`），AuthGuard 被拒與「已登入進公開頁」兩處都改用角色化落點；**②** 技師 portal 四路由（`/home` `/pool` `/my-orders` `/account`）補進 `ROUTE_POLICY` 為 technician-only（原「未列→預設放行」，後端資料端點本就 technician-only，前端對齊；FULL_ACCESS_ROLES 仍全放行供 admin 除錯）。tsc 0；Playwright 驗證：技師登入後手動開 `/dashboard` → 即刻彈回 `/home`，不再白畫面。
- **後台技師頁三處假/壞顯示修正（branch `fix/technician-admin-polish`，2026-07-02）**：**① 技師詳情「本週排班」hardcoded mock 換真資料**：原寫死固定日期 2026/04/20-26＋假早晚班（beta B1 宣稱「無假資料」的落差點）。新增唯讀端點 `GET /tenants/{tid}/technicians/{techId}/schedule?month=`（getTechnicianScheduleV2，CIA-additive；與技師自助 me/schedule 同源——每日工單數 keyed by `technician_id`（容忍展示技師 user_id NULL）＋核准休假/備勤），前端 `WeeklySchedule` 改算真實本週（週一起算、跨月抓兩月）逐日顯示「工單 N／休假／備勤／無排班」；移除 MockBanner。**② 儀表板「最近工單」技師欄全顯「技師 7777」**：原 fallback 只取 `technician_id` 前 4 碼，而 seed 技師 id 全為 7777 開頭 → 每列同字無法辨識。dashboard 本就載入技師清單 → 建 id→姓名 map 傳入 `RecentWorkOrders` 顯示真姓名（查無 id 才退回短碼）。**③ `/admin/technicians-lifecycle` 檔頭註解過時**：宣稱本頁有 5 個 lifecycle 動作端點且路徑寫 `/approve` 等舊式——實際本頁**唯讀** audit log，動作是冒號動詞式 `:onboard-approve` 等、在 /technicians 列表與詳情頁操作 → 註解改為如實描述。新增 `test_technician_schedule_admin.py` 4 測試（200 shape／404／422 缺 month／cross-tenant 403）＋technician 回歸 22 passed；tsc 0；Playwright 驗證：詳情頁顯示真實本週（06/29-07/05）逐日狀態、示意 banner 消失、儀表板顯示翁美玲/陳美惠/李麗華真姓名。
- **技師端死控制清理 + 到場回報補實作（branch `fix/tech-detail-dead-controls`，2026-07-02）**：師傅端 Playwright 全頁測試發現四處問題。**① 急迫度 badge 誤標（pool＋我的工單＋詳情全中）**：i18n `mediumBadge="急件"` → **所有一般單（DB normal→API medium）被標成急件**、high/urgent 顯示英文「Red Code」。對齊設計 spec（11_tech_pool §urgency_badge：normal **不顯示**、urgent 橘「急件」）：medium → 無 badge＋綠左框，high → 橘「急件」＋pulse；「Red Code」屬 spec 的 emergency 檔而 **DB priority 尚無 emergency 值**（不可達）→ 退場，待引入該檔再接。**② 撥打客戶電話死按鈕**：原 disabled＋註解「電話需從 ProblemCard 取，MVP 先省」，但 WO payload **本來就有 `customer_phone`** → 改真 `tel:` 連結（顯示號碼、無電話時隱藏）＋補顯示 `customer_name`。**③ 到場回報缺失（door-check 死路）**：後端 `/onsite/arrival` 存在且 door-check 有「**需先有 arrival 事件**」前置閘（CR-0007 HD-01），但前端**完全沒有到場入口** → 技師填完門面檢核表單、傳完照片後必 409 死路。補「**已到達現場**」CTA（設計 spec 12_tech_my_orders §accepted `arrived_btn`：MapPin＋橘色）：確認對話框 → 瀏覽器 GPS → POST arrival → 顯示「已到場：{時間}」（讀 `actual_arrival`）；GPS 拒絕/不支援有明確錯誤訊息。**④ 實收金額死輸入**：完工表單收值但 `submitCompletion` 從未送出——後端 `/onsite/completion` **刻意** `actual_amount=None`（完工金額由後續 AR/Payment 模組確認）→ 移除輸入框（admin 端完工的金額欄走 `/complete`、不受影響）。i18n 兩 locale 同步（+6 到場鍵、−3 死鍵，parity 2705）。tsc 0；Playwright 端到端：搶 created 單（Round A）→ 詳情頁 tel: 連結真號碼＋已到達現場鈕在＋實收金額欄消失＋normal 單無急件 badge。
- **技師登入狀態閘（branch `fix/tech-login-status-gate`，2026-07-02）**：師傅端測試發現 `technicians.status`（生命週期 source of truth）與 `users.is_active`（登入/refresh 檢查點）**完全脫鉤**——`register_technician` 以 `is_active=TRUE` 建 user、lifecycle `:suspend`/`:onboard-approve` 只改 `technicians.status` → **註冊未核准即可登入、被停權後仍可重新登入**（違反 BR-M07-01 上線審核 gate、beta checklist A6「停權即時失效」語意、register「不簽 token」的設計意圖）。**修正**：**①** register 改 `is_active=FALSE`（核准前不可登入）；**②** lifecycle 每次狀態轉移同步 `users.is_active = (status=='active')`（approve/reactivate → 開，suspend/terminate/reject → 關；自動獲得既有 A2 refresh 重查基建 → 停權後 refresh 也失效）；**③** 技師登入對停用帳號回**精確 403**：`ACCOUNT_PENDING_APPROVAL`（帳號待核准）/ `ACCOUNT_SUSPENDED`（帳號已停權）/ 其他 `ACCOUNT_DISABLED`，中文訊息由 `friendlyError` 直接顯示（前端零改動）；**④** migration `086-technician-login-gate-backfill.sql`（冪等）校正存量資料（非 active 技師 is_active→FALSE）。**已知限制**：已簽發的 access token（1hr TTL）到期前仍有效（完整即時撤銷需 per-user jti 追蹤，另案）。新增 `test_technician_login_status_gate.py` 3 測試（register→403→核准→200→停權→403→復權→200 全 HTTP 閉環／register 建 inactive user／terminate 拒登）；auth+lifecycle 回歸 **48 passed**；docker 實測含 seed 停權技師 `tech-03` 登入 403。**雲端部署時需套 086**。
- **技師搶單 claim + 派工資格檢查（branch `fix/tech-pool-claim`，2026-07-02）**：師傅端全面測試發現搶單流程從未真正可用——pool 列出 `created` 單但 `accept` 只允許 `assigned` 起轉（搶 created 單必 409、前端誤報「已被其他技師接走」且卡片被移除、重整又出現），且 accept 不寫 `technician_id`（搶單語意未實作）、不驗技師狀態（**停權/待核准技師仍可接單**，違反 BR-M07-01「阻擋派工」）。**修正（`accept_order` 行為矩陣）**：技師×created → **claim**（`technician_id`=自己＋accepted）；技師×assigned（給自己）→ 正常接單；技師×assigned（**給他人**）→ 409（FR-0005 A9）；非技師×assigned → 後台代操作不變；非技師×created → 409 不變；技師 `technicians.status != active` → **403 `TECHNICIAN_NOT_DISPATCHABLE`**。**pool 技師視角過濾**：只列 created＋派給自己的 assigned（派給他人的單列出來只會產生必 409 的假搶單）；admin 視角全列。flat 與 v2 端點同步接 actor 參數。順修 `test_cr_0051_dispatch_eligibility` 既有腐化（mock tuple 12 欄 vs CR-0104 後 `_TECH_SELECT` 13 欄 → IndexError）。新增 `test_pool_claim_accept.py` 9 測試（claim 寫入／他人單 409／自己單 200／suspended・pending・terminated 403／admin 行為不變×2／pool 過濾）＋ WO・technician 回歸 **312 passed**。實測：技師 pool 9→6（濾掉他人 3 張）、搶 created 單 200＋technician_id 正確寫入、admin pool 仍 8 張。
- **設定頁「個人資料」可編輯可存 + 清死控制（新 `GET/PATCH /auth/me`）（branch `feat/settings-profile-editable`，2026-07-01）**：業主盤點 `/settings` 後要求「全部修正」死控制。稽核四分頁——**系統設定 / 報價已真接**（讀寫 `/api/v1/config`、pricing CRUD＋試算）；**個人資料**是唯讀死頁（時區寫死、上傳頭像／取消／儲存全 disabled、電話恆「—」）；**安全**的 2FA 是死 placeholder。**修正**：**① 新增自助端點** `GET /api/v1/auth/me`（回 display_name/email/phone/role）＋ `PATCH /api/v1/auth/me`（更新 display_name/phone；本人、`get_current_user` 守衛、去空白＋長度驗證；users 表既有欄→**無 schema migration**）。**② 個人資料頁**：display_name/phone 改可編輯 input（值由 /auth/me 載**真實 DB 值**、非只 JWT），啟用「取消／儲存變更」（PATCH ＋ `cacheInvalidate("GET:")` ＋ 成功訊息），移除「上傳頭像」死鈕，時區改**唯讀靜態**（台灣固定 UTC+8、無欄位可存），清掉 unused `SelectField`/`deriveName`/`avatarChar`/`Upload`/`ChevronDown`。**③ 安全頁**：移除 **2FA 死 placeholder** 區塊（無後端）；變更密碼維持真接。i18n 兩 locale 加 `nameRequired`/`savedMsg`/`saving` ＋ 修過時文案「待 /users/me 上線」。**未動（獨立功能、非本次死控制清理範圍）**：頭像上傳（需檔案儲存）、2FA（需 TOTP 後端）、時區持久化（需加欄）。`test_auth_me_profile`（4 新：GET／PATCH roundtrip／空名 422／未認證 401）＋ login 回歸 **10 passed**；tsc 0 ＋ next build ＋ JSON parity 2703 ＋ **Playwright 端到端**（個人資料 2 可編輯欄載真值、改名→存→成功→**重載持久化**；頭像／2FA／時區下拉皆消失；安全頁 3 密碼欄＋改密碼鈕在、無 2FA）。CIA：新 `/auth/me` 為 additive 自助端點（非破壞、本人限定）→ CIA-additive。**已部署本機 docker api+web**。

- **RBAC 授權判斷 primitive `has_permission` + shadow 稽核（log-only，為「矩陣真正生效」鋪路）（branch `feat/rbac-has-permission-shadow`，CR-0111 後續，2026-07-01）**：業主追問「權限矩陣是不是只有定義、沒真的生效」→ 查證確認**目前矩陣完全沒接授權**：`role_required(*roles)` 只比對 JWT role vs 寫死角色清單（195 處）、`get_flat_permissions` 只在編輯矩陣時自我參照、無 `has_permission`／授權 middleware（連 read/write/delete 都不是強制來源，非只有 approve）。業主選「先做安全半（不用 CIA、加性不改行為）」。**① `role_service.has_permission(tenant, role, resource, action)`**——讀 `_MATRIX` 預設 + `role_permissions` overrides 判斷授權，未知 resource/action fail-closed → False；純判斷、不擋。**② `core.deps.permission_shadow(resource, action)`**——FastAPI dependency，掛在既有 `role_required` 守衛**之外**當 side-effect，計算「矩陣是否允許此 user 的 (resource, action)」，矩陣會拒但端點放行 → 記 `RBAC_SHADOW_DENY`（**永不 raise、永不改變請求結果**），蒐集『矩陣 vs 現行寫死授權』落差供未來收斂到矩陣（CIA / CR-0092）安全 rollout。**③ pilot**——refunds 端點 create（`refunds.write`）/ decision（`refunds.approve`）掛上 shadow。**落差立現**（對 `REVIEW_ROLES`）：`tenant_admin`／`super_admin`／`operations_manager` 端點放行、但矩陣無其列 → 若貿然翻強制會**鎖死這三個高權角色**（正是「先對帳再翻」的鐵證，也是 shadow 要蒐集的資料）。`test_cr_0111_has_permission_shadow`（6 新：矩陣判斷／Q113／fail-closed／shadow 永不擋／落差時 log／允許時不 log）+ refunds + rbac 群 **77 passed**。純加性、log-only、不改任何授權行為 → CIA 豁免；把授權真正收斂到矩陣（讓 `has_permission` 成強制來源）仍需 CIA（對帳 195 條 role_required + rollout）。**已部署本機 docker api**。順帶清掉 rbac 測試在 dev DB 遺留的 reviewer override 污染（16 筆）。**待辦**：矩陣補上 tenant_admin／super_admin／operations_manager 列（消除高權角色 shadow 落差）；shadow 擴到更多 router；rbac 測試 teardown 未清 `role_permissions` 的 hygiene。

- **RBAC 權限矩陣補齊：新增 can-approve 維度 + 角色補至 12 個（對齊權威規格，可操作可持久化）（branch `feat/rbac-matrix-operable`，CR-0111，2026-07-01）**：業主盤點 `/admin/roles` 後追問「文件規劃的權限是不是都包含了」→ 逐項對照 `20260617資料/01-workorder-erp-final-spec-20260520.xlsx`（sheet 11 權限角色矩陣 + sheet 36 M17 `BR-M17-01`「can-view/can-edit/can-approve」，標**阻擋 Coding=是**）確認**沒有全包含**：維度缺 approve（實作只有 read/write/delete）、14 角色只落地 4 個完整。業主經 AskUserQuestion 選「真可操作（碰一點後端）」路徑 → 採 CR-0111 §8 建議：**① 權限維度加 approve（4 維，additive 保留 delete）**——`role_service._perm` 加 `approve` 參數、`_ACTIONS` 加 approve、`_parse_permission_code` 驗證放行；`role_permissions` 表存扁平 `*.approve` code（**無需 schema migration**，`permission_code` 本為自由 text）故 approve 可真持久化；approve 目前**僅供矩陣配置與呈現、尚未接端點強制授權**（端點改讀 RBAC 屬 CR-0092 脈絡、另 CR，UI matrixHint 已標「僅配置」）。**② 角色補齊至 12**——依 final-spec 職責 + owner Q&A 規則新增 `accounting`（會計，Q113 工單唯讀、可核准退款/月結/發票）、`supervisor`（主管，全域核准）、`dispatcher`（派工）、`customer_service`（客服，退款可看不可核准）、`auditor`（稽核，全唯讀）、`family_reviewer`（家族覆核，合約 4.4(d)）、`distributor`（經銷/門市/建商）；`_MATRIX`/`_ROLE_META`/`ROLE_HIERARCHY`（前後端）同步。**③ 前端**——權限矩陣加「核准」欄（順序 檢視/編輯/核准/刪除）、`RolePermissionsEditor` ACTIONS 加 approve、**預設選「第一個可編輯角色」**（原本一進來停在最高階 admin、編輯鈕灰掉看起來像壞掉）、i18n 兩 locale 加 `cols.approve`/`actions.approve` + 修過時文案「F-019 啟用後可調整」→「核准欄依 BR-M17-01 補上、僅配置」。Pydantic `RolePermission` + `api.generated.ts` additive 補 `approve`。**驗證**：後端 `test_cr_0111_rbac_approve_roles`（6 新，approve 維度/12 角色/會計 Q113/approve 分層/階層）+ `test_rbac_v2`（補 approve+新角色斷言）+ rbac 群 **23 passed** 無回歸；tsc 0 + `next build` 成功 + JSON parity 2702；**Playwright 端到端**（12 角色渲染、矩陣 4 欄、編輯鈕預設 enabled、會計編輯器 48 開關、勾 `disputes.approve`→存→**DB `role_permissions` 真寫入**→重載仍在→清測試 override 還原）。CIA：CR-0111 §8 業主已裁（走建議路徑）。**已部署本機 docker api+web**。**延後（另 CR）**：approve 端點強制授權（CR-0092 脈絡）、臨時 IT 權限（BR-M17-03）、AI Bot/System Setup/IT Support/AI Ops 4 個非業務資源角色、audit retention 期限（§8-9 owner 待定）。同時修正 FR-0019 changelog「code 全部實作」失準（實為維度 3/4、角色 4/14）。

- **營收頁 日/週/月/季 真實粒度切換（趨勢圖 date_trunc 分桶）（branch `feat/revenue-real-granularity`，2026-07-01）**：承上一輪——業主澄清 revenue 的 日/週/月/季 時間按鈕不是要移除，而是要**能用**（趨勢圖真的依粒度分桶）。**後端** `revenue_service`：`_query_trend_monthly` → 泛化為 `_query_trend(granularity, ...)`，依 `_GRANULARITY_SPEC`（day/week/month/quarter → `date_trunc` 單位 + `to_char` 期別格式 + 無 range 時的預設回溯視窗）以 `date_trunc` 分桶；`_VALID_GRANULARITY` 加入 `quarter`；`get_revenue_summary` 移除 `effective='month'` 寫死、`granularity` 原樣回傳、trend 真實分桶（kpis/by_brand/by_category 為區間彙總、與粒度無關故不變）。**注意** date_trunc 單位與格式**內插為字面值**（源自白名單 dict、無用戶輸入）而非 `%s` 參數化——否則 SELECT 與 GROUP BY 的 `date_trunc` 綁到不同 `$n`、Postgres 報 `GROUPING` 錯（測試時踩到、已修）。`Granularity` enum（Pydantic + `api.generated.ts`）additive 補 `quarter`。**前端** revenue 頁：還原 日/週/月/季 分段控制為**可點的真控制**（onClick→`handleGranularity`）；切粒度時同時套用該粒度的**預設視窗**（日→近30日、週→近12週、月→近12月、季→近8季，batched 單次 refetch），避免點「日」在近12月區間下跑出約 365 根柱；`fetchSummary` 送 `granularity` state；`periodLabel` 依期別字串格式化（`YYYY-MM`→`M月`、`YYYY-QN`→`QN`、`YYYY-MM-DD`→`M/D`）；subtitle 顯示當前粒度；匯出/排程 modal 帶當前 granularity。`test_cr_0063_coverage_batch1` 新增 `test_revenue_summary_granularity_buckets`（四粒度皆回真實 trend、granularity 原樣回傳、不合法→422）+ `test_reports_v2`/`test_export_report` 共 23 passed + tsc 0 + `next build` 成功 + **Playwright 四粒度實測**（日→`granularity=day` range 近30日、x軸 6/3·6/5·6/7；週→`week` 近12週、x軸 4/6·4/13 週起日；月→`month` 近12月、x軸 3月·4月·5月·6月；季→`quarter` 近8季、x軸 Q1·Q2；subtitle 同步顯示粒度、active 按鈕高亮正確）。CIA：`Granularity` enum additive 補值 + 讓既有 `granularity` 參數真正生效（原契約既有欄位）故 go-live polish／CIA-additive。**已部署本機 docker api+web**。**待辦**：季別 x 軸標籤 `QN` 未帶年份（跨年 Q1 會同名，目前資料稀疏無虞）；匯出 CSV 後端仍恆用月度（`_build_rows` revenue 分支未帶 granularity），若要匯出跟隨畫面粒度需另接。

### Fixed

- **平台 console 各頁 mutation 後不刷新 —— 30s GET 快取未失效（branch `fix/platform-console-cache-invalidation`，2026-07-06）**：業主回報 `/platform/technicians` 師傅核准後清單不自動刷新。**Root cause**：api client 的 GET 有 30s staleTime 快取（cache key `GET:<完整URL>:<tenant>`），mutation 後須手動 `cacheInvalidate("GET:")`（全站既定模式,accounting/quotes/cases/inventory 皆如此）—— 但本階段新建的 platform console 頁**全漏了這行**,故核准後 `load()` 命中 30 秒舊快取、回傳含該師傅的舊清單,畫面不變。**修正**：5 個 platform 頁的所有 mutation 在 reload 前補 `cacheInvalidate("GET:")` —— technicians（生命週期動作＋新增師傅）、technician 詳情（編輯＋認證 CRUD＋刪除）、VendorsPanel（approve/reject）、BrandApplicationsPanel（approve/reject）、OpsMonitorPanel（目標 CRUD）。純前端 bug fix,無 contract/schema 變動 → CIA 豁免。驗證：tsc 0；重建 platform-web；**Playwright**（建 pending 師傅→核准→**立即**從待審核消失、同步出現在啟用中清單）。**已部署本機 docker（platform web）**。

- **品牌端師傅職權全面收斂為唯讀 —— CR-0114 裁決 1 收尾（branch `fix/cr0114-brand-tech-readonly-closure`，2026-07-06）**：業主發現品牌後台仍有「師父註冊/建立」功能，裁示此類功能只該存在平台方後台，並要求全面盤查同類情形。以多 agent 盤查（4 維度 29 項發現、逐項逆向驗證、22 項確認/7 項剔除）後收斂：**後端** ① 刪品牌端 `POST createTechnician`（直建師傅身分＋users 列，admin/ops/dispatcher 皆可打）與 `PATCH updateTechnicianV2`（可改技能/區域/等級 S-A-B-C，直寫師傅權威庫）；② 刪認證 CRUD 三寫端點（`technician_certifications_v2` 只留 GET）；③ `admin-reset-password` 對 `role=technician` 帳號改回 403 `FORBIDDEN_TECHNICIAN_ACCOUNT`（品牌 admin 不可接管全平台唯一師傅帳號憑證）；④ 新增 `API_SURFACE=dispatch` 路由剔除過濾（`/api/v1/platform/*` 平台端點 + `/api/v1/technicians/register` 師傅自助註冊不再掛品牌 8001——後者在 fallback 部署會產生平台 console 看不到的「品牌庫幽靈師傅」）；⑤ RBAC 矩陣 admin/supervisor 對 technicians 由 RWD+approve 改唯讀宣告。**前端（3000）** ⑥ `/technicians` 移除「新增技師」按鈕＋刪 `CreateTechnicianModal`；⑦ 詳情頁移除「編輯」modal（姓名/電話/等級/技能/區域），提示改「師傅資料與生命週期由平台方管理」；⑧ 技能認證矩陣改全唯讀（移除新增/編輯/刪除）；⑨ dispatch build 的 `/tech-register` 改導對方 tech portal（不再於品牌站服務師傅 KYC 註冊表單）；⑩ 清死碼與誤導文案（TechniciansTable 死 props `onApprove/approvingId`、i18n 死鍵 `approveSuccess/approveFailed/addTechnician/register.*` 30 鍵×兩語系 parity 2723、technicians-lifecycle 頁首過期註解）。測試同步：刪 `test_technicians_onboard_v2_endpoint.py`（整檔測已廢端點）、四檔測資建立改直插 DB、新增 405/403 契約回歸守衛與 dispatch 面過濾測試。**全套 pytest 1611 passed 零回歸**；`all` 模式（pytest/雲端單體）不過濾、行為零變化。盤查另確認兩項**維持現狀、待業主裁決**（記入 CR-0114 §8 追補）：品牌 GDPR 被遺忘權可對師傅身分列 REDACT/實刪（CR-0112 刻意設計，平台 console 尚無 GDPR 承接）；排班/獎懲/佣金屬品牌營運職權不動。**遺留後續輪**：platform console 補師傅建立/編輯/認證管理與代重設密碼功能。

- **師傅登入頁移除「品牌入口」連結與版本 footer（branch `fix/tech-login-remove-brand-link-footer`，2026-07-05）**：業主要求 `/tech-login` 移除底部「我是品牌 / 經銷 / 鎖店 →」跨站連結（`brandEntryLink`）與「v0.1 — 技師端 PWA」版本字（`footerVersion`）。移除兩個 JSX 區塊，頁面底部收在「還沒有帳號？申請成為師傅」連結（登入/忘記密碼/申請保留）；i18n 鍵保留不動（無其他使用點、不影響 parity）。tsc 0 + 重建 tech-web（3001）與 dispatch web（3000，同元件保持一致）+ Playwright 實測（3001/tech-login 兩者皆消失、申請/忘記密碼保留）。純前端刪元素 CIA 豁免。**已部署本機 docker web（tech+dispatch）**。

- **師傅註冊連結收斂 + 「返回首頁」改「返回上一頁」（branch `feat/cr0115-register-links-and-back`，CR-0115，2026-07-05）**：業主三項要求——① landing（:3002）師父 CTA 導 `:3001/tech-register`；② `:3000/tech-login` 移除註冊功能、改連 `:3001/tech-register`；③「返回首頁」一路改「返回上一頁」。①② 的路由 code 於 S6 已 merge（`techRegisterHref()`→`/tech-register`、tech-login 拆分 + dispatch registerHref→`PEER/tech-register`），本輪**重建 landing/dispatch web image** 讓運行容器套用（實測 3002 兩處師父 CTA 皆 `:3001/tech-register`、3000/tech-login 無註冊 tab + 連結 `:3001/tech-register`）。③ `BackToHome` 由 `<Link href="/">`（固定回首頁）改為 `<button onClick={router.back()}>`（返回上一頁，無歷史時退回 `/`），i18n `landing.backToHome`「返回首頁」→「返回上一頁」（en「Back to home」→「Back」，parity 2753），一處改全站生效（login/tech-login/tech-register/DesktopMobileGuard 共用）。**順修一個 S6 引入的 bug**：`/tech-register` 的「返回登入/前往登入」原用 `PEER_PORTAL_URL`（對方 portal）→ tech 站上誤指 `:3000/tech-login`，改為同源相對 `/tech-login`（tech-register 與 tech-login 恆同站台）。tsc 0 + 重建 landing/dispatch/tech web + **Playwright 端到端**（3002 師父 CTA→3001/tech-register→返回上一頁→回 3002；3000/tech-login 拆分；tech-register 返回登入→3001/tech-login）。純前端路由/文案，CIA 豁免。**已部署本機 docker web（三 stack）**。

- **跨 stack 殘留 token 把使用者一路彈到別站台登入頁的防呆（branch `fix/auth-guard-foreign-token`，2026-07-05）**：業主回報「在 :3002 點『我是鎖匠我要接』跳去 `:3000/login`」「手動輸入 :3001 也跳去 `:3000/login`」。**Root cause**（非 code bug，是跨 origin 殘留 token 觸發的錯誤導向鏈；四 stack 各埠為獨立 origin、`localStorage` token 各自獨立）：師傅站（:3001）若 origin 留有**別站台的 token**（如品牌 token，role 非 technician），`AuthGuard` 的 `token && isPublic` 分支會 `router.replace(fallbackRouteForRole(role))` → role 為 null/品牌 → 落點 `/dashboard`（tech build 不服務）→ `crossModeRedirect` 把 `/dashboard` 導去 PEER `:3000/dashboard` → :3000 該 origin 無 token → `/login` → 最終停在 `:3000/login`。:3002 點師父 CTA 會先導到 `:3001/tech-login`（`techRegisterHref()`），故兩個回報症狀其實**同一條鏈、都在 :3001 上發生**。**修正**：`AuthGuard` 的 `token && isPublic` 分支在導向前先判斷落點是否屬本 stack——`if (crossModeRedirect(home))` 為非 null（代表該落點會被導去別的 origin，即此 token 不屬本 stack）時，改為 `auth.clear()`（清掉本 origin 的 access/refresh/email，與 `logout()` 一致、不動 tenant）並**留在當前公開頁**，不再據殘留 token 把人彈走；落點屬本 stack（如師傅站的 technician→`/home`、品牌站的 vendor→`/vendor`）才照舊 `router.replace(home)`。`all` 模式 `crossModeRedirect` 恆回 null → 此防呆分支永不觸發、**單庫部署行為零變化**。tsc 0 + 重建 tech web image + **Playwright 三情境實測**（① 無 token 進 :3001 → 留 `:3001/tech-login`；② **外站殘留 token 進 :3001 → 留 `:3001/tech-login` 且 token 已清**（修正前=彈 `:3000/login`）；③ 合法 technician token 進 :3001 → 導 `:3001/home`，合法轉址保留未被誤清）。連帶重建 landing/platform/dispatch web image 讓四 stack 一致套用同一份共用碼。純前端路由防呆、無契約變更故 CIA 豁免。**已部署本機 docker web（四 stack）**。

- **品牌 stack 首頁（localhost:3000）誤渲染對外導流 landing，「點師父跳品牌」錯覺（branch `fix/dispatch-root-to-login`，2026-07-05）**：業主回報「localhost:3000 點師父還是跳轉到品牌」。**Root cause**（非師父 CTA 接線問題，實測 3 條動線師父 CTA 皆正確導向 `http://localhost:3001/tech-login?tab=register`）：① 3000 是**品牌 dispatch stack**（非導流站，導流 landing 在 3002），瀏覽器留有品牌 `smartlock.access_token` → `AuthGuard` 的 `token && isPublic` 分支把 `/` 依角色送 `/dashboard`（品牌後台）→ 使用者一進 3000 就看到品牌、根本沒走到師父流程；② 更深層問題：dispatch build 的 `/`（`crossModeRedirect` 回 `null`）仍會渲染那份含「師父/品牌」CTA 的對外 landing，與專屬導流站 3002 **重複**，一個品牌後台入口卻擺著「我是師傅」招募按鈕，語意錯置。**業主裁決（2026-07-05）**：3000 首頁改直接進品牌登入，導流 landing 只留 3002。**修正**：① `appMode.ts` `crossModeRedirect` 的 dispatch 分支新增 `if (pathname === "/") return "/login"`（未登入 → 品牌登入；已登入者續由 `token && isPublic` 分支依角色送 `/dashboard`），並把該分支 `/platform`、tech-prefix fallback 的 `return "/"` 一併改 `"/login"`（deny-by-default 回登入而非落到已不渲染的 landing）；② `page.tsx` landing 元件的 `return null` 守衛由 `APP_MODE === "tech"` 擴為 `tech || dispatch`（dispatch build 不再渲染 landing、避免 redirect 前的 landing 閃現；redirect 交給 `crossModeRedirect` 以免與 AuthGuard 已登入→`/dashboard` 競態）。tsc 0 + 重建 dispatch web image + **Playwright 端到端實測**（未登入打 3000 → 導向 `/login`（h1「品牌 / 經銷 / 鎖店」、無主 CTA landing）；3002 導流站不受影響、`/` 仍顯示 landing 且師父 CTA → 3001；3001 tech-login 直達正常）。純前端路由行為調整（CR-0114 導流架構細化）、無契約變更故 CIA 豁免。**已部署本機 docker web**。

- **庫存頁新增/補貨/編輯後需手動刷新才顯示 → 改為立即反映（branch `fix/inventory-refresh-after-mutation`，2026-07-01）**：業主回報 `/admin/inventory` 建立物料後要「重新整理畫面」才看得到新品項。**Root cause**：`CreateInventoryItemModal` 送出 `api.post` 後只呼叫 `onSuccess?.()`（→ 頁面 `fetchItems()` refetch），但 `api.get` 有 30s staleTime 的 GET 快取、mutation 後未清 → 立即 refetch 打到**建立前的舊快取**、拿回不含新品項的清單；整頁 reload 才因新頁面無快取而取到新資料。同源問題另見 `RestockInventoryModal`（補貨）與 `EditInventoryItemModal`（編輯）——皆 mutation 後直接 `onSuccess` 未清快取，補貨數量/編輯變更同樣要手動刷新才反映。**修正**：三個 mutation modal 於成功後、`onSuccess?.()` 前補 `cacheInvalidate("GET:")`（對齊 `admin/quotes`/`accounting`/`technicians` 等既有 mutation 慣例；cache key 含完整 URL 故用廣域 `GET:` prefix 清）。純前端快取失效修復、無契約變更故 CIA 豁免。`next build`/tsc 0 + **Playwright 實測**（新增「自動刷新驗證品項」不 reload 即出現、本頁品項 1→2；測試列已從 `saas.inventory_item` 清除）。**已部署本機 docker web**。**待辦**：全站其餘 create/mutation modal 若有相同「mutation 後 refetch 卻未 `cacheInvalidate`」模式可一併盤點（本次僅修庫存三個）。

- **技師排行捲軸失效修復 + 移除營收頁失效的日/週/月/季粒度按鈕 + SOP 績效捲軸同類修復（branch `fix/reports-scroll-and-granularity`，2026-07-01）**：業主回報 `/admin/reports/technician-ranking`「捲軸失效」、`/admin/reports/revenue`「日週月季時間按鈕失效」。**① technician-ranking 捲軸**（真 bug）：外層捲動容器 `flex flex-1 flex-col gap-6 overflow-auto` 是 flex column，flex 子層預設 `flex-shrink:1`，podium + 25 列表格（含內層 `overflow-hidden` 表格 wrapper）被壓縮塞進視窗高度（Playwright 實測 `scrollHeight==clientHeight==773`、`canScroll=false`）而非自然溢出 → overflow-auto 無從捲動、底部列與分頁鈕被裁切（與 refunds/warranty/disputes 同類 bug、pre-existing，本 session 移除篩選控制只是讓它更明顯）。改 `flex-1 space-y-6`（block 堆疊、子層取自然高度、容器自然溢出）→ 修後 `scrollHeight=1521`、`canScroll=true`、可捲到底。**② revenue 粒度按鈕**（失效控制）：日/週/季在上一輪已 disable+「即將推出」（後端 `effective='month'` 四鍵回同資料），但業主仍視為「失效」；因**時間篩選已由已接後端的 `DateRangePicker` 統一負責**（上一輪接上 `start_date/end_date`），該粒度分段控制已冗餘，**整段移除**（連同 unused `SEGMENTS` const），留 DateRangePicker 為唯一且可用的時間控制、趨勢固定月度（subtitle 標「月粒度」）。**③ sop-performance 捲軸**（連帶預防）：同 `flex flex-col gap-6 overflow-auto` 容器、同 `scrollHeight==clientHeight==773 canScroll=false` latent bug（此租戶「近期發布」空故目前剛好塞得下、有資料即裁切），一併改 `flex-1 space-y-6`；連帶把 `!metrics` 空狀態卡由 `flex-1`（原依賴 flex-col 父層撐高）改 `min-h-[400px]`（父層改 block 後仍維持卡片高度）。純前端屬 CIA 豁免。tsc 0 + `next build` 成功 + **Playwright 三頁實測**（technician `canScroll=true` 可捲到底；revenue 無 日/週/月/季 按鈕、DateRangePicker 與區間營收/月粒度保留；sop 容器改 block、空狀態卡正常）。**已部署本機 docker web**。**待辦**：revenue 的「切片：按品牌」「與上期比較」仍為 disabled 誠實 placeholder（業主本次僅點名粒度按鈕、未動這兩者），若要完全清空工具列可一併移除。

- **三報表頁盤點寫死並接真實資料：技師排行 / 營收 / SOP 績效（branch `feat/reports-connect-dead-controls`，2026-07-01）**：業主要求 `/admin/reports/technician-ranking`、`/admin/reports/revenue`、`/admin/knowledge-base/sop-performance` 三頁「一樣有寫死就接上真實資料」。**盤點方法**（ultracode workflow：每頁一 Explore agent 盤點死控制/寫死 + 逐 finding 對抗式 verifier 複查後端可接性）——verifier **推翻我數個初判**，避免了誤刪：(a) revenue 我原判「移除 DateRangePicker」，複查證實後端 `reports_v2.get_report_revenue` + `revenue_service.get_revenue_summary` **端到端已支援 `start_date/end_date`**（F-021，前端 TODO 註解「後端還沒有 from/to」已過時）→ 改判**接上**；(b) revenue「切片：按品牌」「與上期比較」「樞紐切片」本就是 `disabled + 即將推出` 的**誠實 placeholder** → **保留**、非移除；(c) sop-performance 我原以為全接真實，複查抓到 **3 個真 bug**。**① technician-ranking（前端 + 後端）**：移除 `period`（本週/月/季/年）死切換（`/technicians` 無任何 date 參數、`rating`/`completed_orders` 為 lifetime 累計欄，切換不改 query）與失效 `DateRangePicker`（列表與匯出皆不吃日期）；**修 `limit:100` 寫死**——排名/排序/分頁全在前端對「最多 100 筆（依 created_at）」算，>100 技師時 #1 名不保證全租戶最高分，改為 **cursor 迴圈抓齊全租戶技師**（`RANKING_FETCH_CAP=500`、超過標「僅列前 N 位」）；**CSV 匯出接真實資料**——`report_export_service._technician_ranking_to_rows` 由 stub（回「尚未實作技師排行 service」佔位列）改成真實技師列（`rank/technician_id/name/level/completed_orders/avg_rating/composite_score/service_areas`，依 rating desc 排序、`composite_score = rating×20` 與前端同公式），`_build_rows` 改呼 `technician_service.list_technicians`（單批 `_TECH_EXPORT_LIMIT=500`、超過標 truncated）。**② revenue（前端）**：**接 `DateRangePicker`**——`fetchSummary` 帶 `start_date/end_date`（由 `range` 導出、預設近 12 自然月，保留「營收趨勢」12 根月柱），trend/by_brand/KPI 皆改後端 DB-side range filter；標籤改區間語意（本月營收 → **區間營收**、已開立工單（近12月）→ **（區間）**、subtitle 顯示 `{區間}（月粒度）`、圖表空狀態/品牌表頭同步）；日/週/季粒度**改 `disabled` + 「即將推出：後端目前僅提供月粒度」**（後端 `effective='month'`、四鍵回同資料 = 假切換），保留月為 active；切片按品牌/與上期比較/樞紐文字保留為誠實 placeholder（後端無 time×brand / period-over-period 資料源）。**③ sop-performance（前端）**：移除「版本」欄——後端 `top_recent_published` 從不回 version（`sop_drafts` 無此欄）、前端型別誤宣告 `version:number` → render 出字面 `vundefined`；「發布日期」改讀 `reviewed_at`——後端回的 key 是 `reviewed_at`（`sop_drafts.reviewed_at`、已當發布日 proxy），前端誤讀 `published_at` → `formatDate` 恆回「—」；核准率/發布率 hint「`{windowDays} 日累計`」→「**全期累計（不受區間影響）**」——兩率由全期 `status_distribution` 計算、不吃 window_days，原標籤說謊（停用候選確為 `reviewed_at` 窗口化故保留「N 日未審查」）。**驗證**：`test_export_report`（technician 匯出測試改斷言真表頭 `rank`/`composite_score` + 無 stub 佔位）+ `test_reports_v2` + `test_sop_performance` **20 passed** + tsc 0 + `next build` 成功 + **Playwright 三頁實測**（revenue：`區間營收`/`已開立工單（區間）`/月粒度 subtitle、日週季 `disabled=true` 月 active、請求帶 `start_date=2025-08-01&end_date=2026-07-31` 回 200；technician：period 切換消失、17 位技師真實載入、排序/區域控制保留；sop：版本欄消失、無 `vundefined`、hint「全期累計」、停用候選仍窗口化）+ **CSV 匯出實測**（技師排行真實 8 欄、依評分排序、真名如李麗華/黃師傅/韓建華、`尚未實作` stub 消失）。CIA：technician 匯出屬 stub→真資料 bug fix（CSV 欄位非契約）、revenue 純前端接既有端點、sop 前端 bug 修復——皆無契約變更故豁免。**已部署本機 docker api+web**。**待辦**：① revenue 接日期後 `month_revenue` 語意由「本月」變「區間」（與 `revenue_service` docstring「month_revenue 保持本月語意」註記衝突、採 code 行為 + 區間標籤揭露），若業主要固定「本月營收」headline 可一行拆回；② sibling `accounting/revenue` 頁仍有「後端無日期參數」過時註解且無日期選擇器、宜一併接上對齊；③ sop 核准/發布率若要真窗口化（依 reviewed_at 在窗內 count）為後端小改。

- **KPI 儀表板 FTFR 接真實資料 + 移除品牌切片死控制 + 公式 hint 去 snake_case（branch `feat/kpi-connect-ftfr-dead-controls`，2026-07-01）**：業主要求檢查 `/admin/reports/kpi` 有無寫死、有就接真資料。**盤點**：頁面主要指標（轉換漏斗、異常率三項、平均處理時長）**本就接真實** `KpiReport` 端點（`kpi_service.get_kpi_report` 全部 DB 計算）；真正未接/寫死的有——① FTFR 一次修好率顯示「—／尚未接入」placeholder；② 工具列「切片：全部／按品牌」下拉是**死控制**（`sliceBy` state 設了卻無人讀、`fetchReport` 與後端 `get_kpi_report` 都無 brand 維度）；③ `DisputeRow` 三個公式 hint 露 snake_case（`refund_requests / work_orders`、`warranty_claims / work_orders`、`disputes / work_orders`）；④ SLA／客戶滿意度／NPS／差評率為 placeholder 卡。**查可接性（DB schema）**：`work_orders` **已有 `is_rework` + `rework_of_id` 欄位**（後端 notes 寫「FTFR：尚無 rework 標記欄位」已過時）→ FTFR 可計算；`conversations` 表**無 brand 欄**（漏斗第一階段無法依品牌切）→ brand-slice 不可乾淨接；查無 SLA 規則表／rating／review／NPS 表（只有 SOP 用的 `family_reviews`）→ SLA/NPS/滿意度確無資料源。**修正**：① **FTFR 接真值**：`kpi_service` 新增 `_ftfr()`——分母 = 期間內完工（status `completed`/`confirmed`）且非返工（`is_rework=FALSE`）的原始工單；分子 = 上述中未被任何工單以 `rework_of_id` 指回者（即未被重做）；`FTFR = first_time / originals`（沿用 `_ratio` 四位小數、分母 0 回 null）。寫入 `technician_efficiency.ftfr` + `ftfr_sample`，並自 `notes` 移除 FTFR 那條。Pydantic `KpiTechnicianEfficiency` 與前端 `api.generated.ts`（TS 生成器停用、手動加，同 RevenueByCategoryPoint/SettlementStatus 策略）補 `ftfr`/`ftfr_sample` 兩欄。前端 FTFR 卡由「—／尚未接入／待接入 PendingTag」改為 `formatPercent(report.technician_efficiency.ftfr)` + 樣本數。② **移除 sliceBy 死控制**（`<select>` + `sliceBy`/`setSliceBy` state）。③ `DisputeRow` 三個 hint snake_case → 繁中（「退款申請數 / 工單總數」等）。④ **SLA／NPS／滿意度卡誠實保留 placeholder**（後端 `notes` 仍據實列出三項缺的資料源；非假資料、不動）。**FTFR 定義為標準業界算法**（first-time fix rate），目前 DB 全 `is_rework=false`／0 返工故計得 100%（真實反映「目前無返工記錄」、有返工即降）；定義已透明標於卡片 hint，業主可一行調整（如改 is_rework 比率法）。`test_reports_v2_endpoint`（強化斷言：`technician_efficiency` 含 `ftfr`/`ftfr_sample`、ftfr 介於 0~1、notes 不再含 FTFR）+ `test_operational_kpi` 共 12 passed + tsc 0 + `next build` 成功 + **Playwright 實測**（FTFR 卡顯示 `100.0%` 樣本 9 筆、無「待接入」；「切片：」下拉消失；hint 無 snake_case；SLA 卡仍為 placeholder）。CIA：後端 `ftfr`/`ftfr_sample` 為純加性回應欄位（非破壞）。**已部署本機 docker api+web**。**待辦**：SLA 達成率（需 SLA 規則引擎）／客戶滿意度・差評率（需評價回傳機制）／NPS（需調查管道）各需獨立模組才能接。

- **知識庫分頁 sidebar 去重 + 三頁 tab count 接真實總數 + 匯出筆數/匯出 404 修復（branch `feat/kb-real-counts-sidebar-dedup`，2026-07-01）**：業主兩項要求——① 知識庫頁面已有的分頁（案例/手冊/SOP 草稿）不要在 sidebar 重複列出；② 盤點頁面還有哪些寫死的資料、直接接上。**盤點**（ultracode 多 agent workflow，5 partition 平行盤點 + 對抗式 verify）：9 筆確認假資料、0 誤報，分三類——**needs_backend**（5 筆：cases/manuals/sop-drafts 三頁各自把「別的分頁」的 badge count 寫死 128/23/7/6，且同一分頁在不同頁顯示的數字互相矛盾，證明全是 placeholder）、**blocked_by_constraint**（2 筆：cases/manuals 品牌篩選下拉硬編品牌陣列）、**yes_frontend**（1 筆：cases 匯出成功 toast 的 `count` 永遠傳 0）。**修正**：① **Sidebar 去重**：`knowledgeBase` 群組移除與分頁重複的 `kbCases`/`kbManuals`/`kbSopDrafts` 子項，只保留非分頁的 `kbFamilyReviews`（家族覆核）；`NavItem` 新增 `matchPrefix` 欄位並修 `isParentActive`，讓父項在整個 `/knowledge-base` 區段保持高亮（直接刪子項會讓 manuals/sop-drafts 頁的父項失去 active、submenu 不展開、家族覆核從那些頁無法到達）。② **tab count 接真實總數**：後端 `case_service.list_cases`/`manual_service.list_manuals`/`sop_draft_service.list_drafts` 各補一句 `SELECT COUNT(*)`（用基礎過濾、不含 cursor predicate）回 `total_count`，`/kb/documents`（doc_type 指定時為該型總數、未指定為 case+manual 之和）與 `/tenants/{tid}/sops/drafts` router 透傳；前端新增共用 hook `web/src/hooks/useKbCounts.ts`（三分頁各以 `limit=1` 輕量請求讀 `total_count`，`usePaginatedFetch` 本就支援同名欄位），三頁 tab badge 全改真數、刪掉寫死的 128/23/7/6 與不精準的 `showCount`（items.length+）。③ **匯出筆數**：cases `handleExport` 從已下載的 CSV blob 算真實列數（`blob.text()` → 總行數 − 表頭；後端已把自由文字內換行替成空白故行數法安全）取代恆 0。④ **匯出 404（連帶修）**：`handleExport` 的 base URL `process.env.NEXT_PUBLIC_API_BASE_URL ?? "http://localhost:8001"` 改 `||`——該 env 在 docker build 被烤成空字串，`??` 不對空字串退回 → `fetch` 收到相對 URL → 打到 web origin（:3000）而非 API（:8001）→ 404；改用 `||`（對齊 `api.ts:BASE_URL`，空字串 falsy 時退回）。**保留不動**：cases/manuals 品牌篩選硬編——`web/src/lib/constants/brands.ts` docstring 明文記載 KB 品牌 filter 綁後端已存品牌字串、資料層大小寫/中英不一致（dormakaba vs Dormakaba、美樂 vs Milre），統一/動態化會打到搜尋結果、屬「資料正規化」CR、不在前端 paper over（workflow 判 `blocked_by_constraint`）→ 回報業主、不就地改。`test_kb_v2_endpoint` 13 passed（含 2 新增 `total_count` 參數化測試）+ `kb/case/manual/sop/draft` 相關 92 passed 無回歸 + tsc 0 + `next build` 成功（lint+typecheck）+ **Playwright 端到端實測**（三頁 tab 顯示真數 案例 2 / 手冊 83 / SOP 0，對上 DB；sidebar 僅剩「知識庫 + 家族覆核」、父項在 manuals 頁 `aria-current=page` 且家族覆核 submenu 可見；匯出按鈕下載成功 = CSV 1 表頭 + 2 資料列 → toast 顯示「已匯出 2 筆」）。清掉本次 pytest 造的 2 個測試 case。CIA：後端 `total_count` 為純加性回應欄位（非破壞）。**已部署本機 docker api+web**。**待辦**：① 品牌 filter 正規化 CR（資料層）；② `manuals` 表有 ~77 筆跨 session 累積的 `Test %` 測試污染（count 真實反映但資料品質另議）；③ `scope-change`/`quotes`/`track`/`consent`/`MediaGallery` 同樣以 `?? NEXT_PUBLIC_API_BASE_URL` 取 base URL，有相同空字串→相對 URL 隱患，宜一併改 `||`。

- **全站 user-facing 字串內部識別碼洩漏盤點 + 修正 53 處（branch `fix/user-facing-jargon-leaks`，2026-06-30）**：業主回報異常頁錯誤訊息「請先選擇處理方式（return_path）」把後端欄位名 `return_path` 直接露給使用者，要求全盤再盤點一次並修正。**盤點方法**（ultracode 多 agent workflow）：8 個 partition 平行掃 `web/src/app` + `web/src/components` 的 user-facing 字串面（`setError`/`toast`/`alert`/`setMsg`/JSX 文字/`placeholder`/`title`/`aria-label`/option label），找內嵌的內部識別碼與開發者 jargon；每筆再以獨立 agent 對抗式 verify（實際讀檔確認真渲染給使用者、排除程式碼註解／控制流 `=== "CODE"` 比較／通用詞 JSON·CSV·Email·LINE·AI·PDF·URL）。76 個 subagent、候選 68 筆 → **確認 53、濾除 15 誤報**。**洩漏類型**：snake_case 後端欄位名（`return_path`/`user_id`/`work_order_id`/`door_type`/`Notification.source` enum）、HTTP 標頭名（`X-Approver`）、環境變數名（`NEXT_PUBLIC_REALTIME_BASE_URL`）、原始 enum 碼（`instant`/`brand`/`locksmith`/`platform`/`weekly`/`websocket`/`line_push`）、內部流程／模組／ADR ID（`F-014`/`F-015 dual-trigger`、`M18 Config`、`ADR-0052`/`ADR-0053`、`FR-0007`/`FR-0044`、`S3/S4`）、協定 jargon（`JWT`/`access token`/`/users/me endpoint`/`payload`/MIME `text/csv`/原始 HTTP `{status}`）、UUID 片段當顯示名（`title={id}`、`User ${id.slice(0,6)}`、`已建立問題卡 ${id.slice}`）、英文欄位括註（`capabilities`/`profile`/`chars`）。**修正分三路**：① **25 處純字面替換**以確定性 codemod（每筆斷言 old 在該檔「恰好 N 次」才換，dry-run→apply，0 miss）；② **15 處 i18n 兩 locale 同步**（`zh-TW.json`+`en.json`：移除消費者錯誤文案的 `（{status}）`、改寫 `JWT`/`2FA`/`access token`/`/users/me endpoint` 文案、`租戶 ID`→`所屬機構代碼` / `Tenant ID`→`Organization code`，並新增 4 個 `pages.notifications.sourceLabel.*` 鍵把通知來源 enum 轉中文）；③ **複雜案例手動**：notifications source／WorkOrderDetailSidebar `door_type`／ScheduleReportModal cadence·format 各建中文對照表後內插；EventTimeline `default` 分支不再 `JSON.stringify(payload)` dump 原始 snake_case，改友善提示；problem-cards `FIELD_LABEL` 補全 5 欄 + fallback 由 `?? m.field` 改 `?? "其他必填欄位"`；conversations `title={id}`／`title={pc.id}` 由內部 UUID 改公單號／品牌型號（對齊 sop-drafts hover 不洩 UUID 政策）；config-governance namespace 清單改「中文說明為主標、原始鍵降 font-mono 小字副標」（NS_HINT 缺漏才以鍵為主標，保留管理者對照需求）；RolePermissionsEditor `aria-label` 用既有 `RESOURCE_KEY`/`ACTION_KEY` 轉可讀；移除 ReportExportModal 的 Content-Type MIME 說明（格式上方已標 CSV／PDF）+ 連帶移除 unused `FORMAT_CONTENT_TYPE`。**保留**：控制流 `e.errorCode === "CODE"` 分支判斷不動（是邏輯非顯示）；少數無 NS_HINT 的 namespace 仍顯原始鍵（管理工具刻意）。tsc 0 + `next build` 成功（lint+typecheck）+ JSON parity 2700=2700 + **Playwright 三頁端到端實測**（① exceptions 點「處理」未選處理方式 → banner「請先選擇處理方式」、`leaksReturnPath=false`；② config-governance 標題「設定治理」、全頁無 `X-Approver`/`tenant_id`/`NEXT_PUBLIC`/`（instant）`；③ settings 個人資料分頁無 `JWT`/`access token`/`租戶`、正確顯示「所屬機構代碼」與新文案）。共 26 檔（24 程式 + 2 i18n），淨 +19 行。純前端屬 CIA 豁免。**已部署本機 docker web**。

- **全站錯誤訊息收斂為使用者看得懂的繁中，移除 `errorCode (status)：英文 message` 外洩（branch `fix/friendly-error-messages`，2026-06-30）**：業主回報「NOT_FOUND (404)：Exception not found or already closed」這類錯誤直接把開發者導向的英文錯誤碼、HTTP 狀態碼與後端英文 message 丟給使用者看，要求全盤盤點一起修。**盤點**：全前端約 **109 個檔**以 `${e.errorCode} (${e.status})：${e.message}` 之類的字串呈現錯誤，其中 22 個是各頁本地 `formatError` 函式、4 個是 i18n 模板（`components.admin.*` 的 `toast.errorBody`／`errors.generic`／`server`／`forbidden`，內容同樣內嵌 `{code} ({status})`）。**修**：新增單一共用工具 `web/src/lib/apiError.ts` 之 `friendlyError(e)`，依序——① 後端 `message` 已含中文（業務刻意寫給使用者的 detail）→ 直接採用（最精準）；② 否則用錯誤碼 → 繁中映射 `CODE_MESSAGES`（對齊 api/ 實際使用碼：`DB_UNAVAILABLE`／`VALIDATION_ERROR`／`NOT_FOUND`／`STATE_CONFLICT`／`UNAUTHENTICATED`／`FORBIDDEN`／`CROSS_TENANT_*`／`HIGH_RISK_HOLD`／`SIGNATURE_*`… 40+ 碼）；③ 再否則退回 HTTP 狀態碼族群 `fallbackByStatus`（401 重新登入／403 無權限／404 找不到／409 狀態衝突／422 輸入有誤／429 過於頻繁／5xx 系統問題）。另處理 `AbortError`（已取消）與 fetch 連線失敗（連線失敗）。以**確定性 codemod**（Node script，dry-run → apply）全站收斂 **105 個標準寫法檔**：把 `${e.errorCode} (${e.status})：${e.message}` 整段 ternary／裸 template 換成 `friendlyError(e)`、收斂標準 `formatError` body、補 `import { friendlyError } from "@/lib/apiError"`、移除隨之失效的 `ApiError` import。**手動處理 12 個非標準殘留**：中文前綴 template（`儲存失敗：…`／`刪除失敗：…`／`結束接管失敗：…` → `\`儲存失敗：${friendlyError(e)}\``）、4 個 i18n 模板（改用 `friendlyError(e)`，未用的 key 保留於兩 locale → parity 不破）；**保留** 6 處控制流 `e.errorCode === "CODE"` 分支判斷（如 `TECHNICIAN_CIRCUIT_BREAKER_OPEN`／`RESCHEDULE_LIMIT_EXCEEDED`／`RESET_TOKEN_INVALID`／`DUPLICATE_CUSTOMER` — 是邏輯不是顯示，不可改）。tsc 0 + eslint 0（無未用 import）+ `next build` 成功 + **Playwright 重現業主情境**（建異常 → DB 刪除該列 → 點過時列「升級」→ 後端回 404 → banner 由「NOT_FOUND (404)：Exception not found or already closed」變為「找不到資料，可能已被刪除或狀態已變更，請重新整理後再試。」、`leaksRawCode=false`）。淨 −333 行（移除重複錯誤格式樣板）。純前端屬 CIA 豁免。**已部署本機 docker web**。**待辦**：6 個 i18n error key 改用 friendlyError 後成 unused（保留兩 locale、不影響 parity），日後可整批清除。

- **異常管理頁（`/admin/exceptions`）「無法開立」+ subtitle 內部黑話外洩 + 工單欄只有 UUID（branch `fix/exceptions-wo-picker-and-copy`，2026-06-30）**：業主回報三件——無法開立異常、subtitle 露「M15 control tower / high_risk_hold」黑話「不該出現在前端」、要盤點寫死資料接上。**Root cause（無法開立）**：開立表單「工單 ID（選填）」是 raw UUID 文字框，使用者填公單號/任意字串（如 `WO-20260628-001`）→ 後端 `open_exception` 的 `INSERT saas.exception_case`（`work_order_id` 為 uuid 欄、以 `%s` 帶入）噴 `psycopg.errors.InvalidTextRepresentation: invalid input syntax for type uuid` → 未捕捉 500 → 瀏覽器顯示 `net::ERR_FAILED`、表單卡住＝「無法開立」（空欄或合法 UUID 其實成功 201，故此 bug 隱性、易誤判為整功能壞）。**修**：① 前端 raw UUID `<input>` → 重用 `WorkOrderPicker`（公單號 `TP-000001`/客戶名搜尋、debounce 反查、對外回工單 UUID），從輸入端根除壞值；② 後端 `exception_service.open_exception` 在 INSERT 前補 `uuid.UUID()` 格式 guard，非 UUID 回 422 `VALIDATION_ERROR`（防禦縱深，把 500 轉成乾淨 422）；③ subtitle 由「M15 control tower — …；high/critical 會暫停工單（high_risk_hold）」改寫為正常使用者文案（去內部代號／英文旗標名）「統一追蹤並處理工單異常：缺料、加價拒絕、取消、爭議、安全風險等。標記為「高 / 緊急」的案件會自動暫停關聯工單，待處理後恢復。」；高/緊急暫停工單由純文字改為 **「已暫停工單」紅徽章**（active + 高/緊急 + 有關聯工單時於狀態欄顯示）；④ 工單欄原顯示截斷 UUID（純文字、未接資料）→ `list_exceptions` 加 `LEFT JOIN work_orders` 回傳 `work_order_no`、前端顯示**公單號**（反查不到才退回短 UUID）。**盤點結論**：admin 區 `quotes`/`payout-rules` 命中的 `is_mock` 是後端真實資料來源旗標（只在該列真為 mock 時標「示意資料」徽章）、非寫死假資料；本頁除 raw UUID input 外列表資料皆接 `/exception-cases` 真實端點，無其他寫死。`test_cr_0041_exception_framework` **8 passed**（含 2 新增：非 UUID work_order_id→422、list 帶回 work_order_no）+ tsc 0 + docker api/web 重建 + **Playwright 端到端實測**（subtitle 無黑話外洩；picker 搜 `TP-000144` 選取；類型缺料/嚴重度高/選工單開立 → 新列工單欄顯示公單號 `TP-000144` + 狀態「已暫停工單」徽章；DB 驗 `work_orders.high_risk_hold=t` 確認暫停機制真實寫入）。測後清理測試 exception + 還原 `TP-000144` 的 hold。後端屬 bug fix（單函式 uuid guard）+ 純加性顯示欄（`work_order_no`），CIA 豁免。**已部署本機 docker api+web**。

- **結算記錄「批次確認」按了沒反應（batch_action 寫空 saas.settlement、資料在 public.settlements）（branch `fix/settlement-batch-write-public`，2026-06-30）**：業主澄清是問結算記錄的「批次確認」（非對帳核准）。**Root cause**（同對帳的 v2/legacy schema 雷、方向相反）：結算**列表**（`list_settlements`，含 v2）讀 `public.settlements`（28 筆，UI 顯示的資料），但**批次確認/標記已付** `settlement_service.batch_action` 做 `UPDATE saas.settlement`（**0 筆、空表**，是 generate 月結批次的 v2 目標）→ 批次更新 0 筆 ＝ 看似沒反應；且 public 是 3 態（pending/paid/failed）、saas 是 4 態（多 confirmed 中間態）。**業主裁決 A：batch_action 改寫 public**。**實作**：① `batch_action` 由 `UPDATE saas.settlement` 改 `UPDATE settlements`（public，與 list 同表）+ 透過 `technicians.tenant_id` 子查詢過濾（public.settlements 無 tenant_id 欄）+ 移除 `manual_paid_at`（public 無此欄）；② public.settlements.status 為自由 varchar（無 CHECK）→ `'confirmed'` 可直接寫入，Pydantic `SettlementStatus` enum 補 `confirmed`（手動 additive，否則 list 回 confirmed settlement 序列化 422，同 RevenueByCategoryPoint 策略）；③ 前端 `SettlementTable` `STATUS_TONE`／`statusLabels` 補 `confirmed`（藍）+ lookup `?? fallback`（前端生成型別尚未含 confirmed）；④ `doBatch` 補 `cacheInvalidate("GET:")`（否則狀態更新被 30s GET 舊快取蓋住看不到）；⑤ i18n `status.confirmed`（已確認／Confirmed，zh+en）。`test_settlements_v2`／`test_cr_0012`／`test_cr_0073`／`test_reports_v2` 共 27 passed 無回歸 + tsc 0 + JSON parity 2696=2696 + docker api/web 重建 + **Playwright 端到端實測**（全選 → 批次確認 → POST 200 `updated:7`、待付款 7→已確認 7（新藍徽章即時顯示）；再批次標記已付 → POST 200 `updated:7`、已確認 7→已付款 19）。**已部署本機 docker api+web**。**待辦**：`generate_monthly_batch`（觸發本月月結）仍寫 `saas.settlement`（v2 路徑），未來宜統一收斂到單一 settlement 表。

- **結算頁對帳區空白看不到待核准（list 讀空的 saas、approve 寫有資料的 public）（branch `fix/accounting-recon-list-legacy-source`，2026-06-30）**：業主回報 `/accounting` 結算頁「核准」按了沒反應。**Root cause**（記憶中的 v2/legacy schema 雷）：前端對帳 **list 走 v2** `tenantPath("/accounting/reconciliations")` → `reconciliation_v2_service.list_reconciliations_v2` 讀 **`saas.reconciliation`（0 筆，從未 seed）** → 整個對帳區顯示「共 0」、根本沒有待核准列可按；但真實 25 筆對帳（5 pending）在 **`public.reconciliations`**（legacy 表），且前端 **approve 仍走 legacy** `POST /api/v1/accounting/reconciliations/{id}/approve`（讀寫 public）。**list 讀空 saas、approve 寫有料 public ＝ 來源錯位**——list 被提前遷 v2 但 saas 未 seed、approve 依 code 註解 P3.5-KEEP 仍 legacy 單簽（v2 dual-sign `:review`→`:co-sign` UX 待重做）。**修**：① 對帳 list 從 v2 `tenantPath` 改回 legacy `/api/v1/accounting/reconciliations`（讀 public、與 approve 一致）；② `handleApprove` 補 `cacheInvalidate("GET:")`（否則核准完那筆從 30s GET 舊快取讀回、續留 pending 列表，同 quotes/intake 修法）。純前端屬 CIA 豁免。tsc 0 + docker web 重建 + **Playwright 端到端實測**（對帳區 0 → 顯示 5 筆待核准；點列級核准 → modal 確認核准 → POST 200 帶 Idempotency-Key → 列表 5→4、toast「已核准」）。**已部署本機 docker web**。**待辦**：v2 dual-sign（`:review`→`:co-sign`）兩步 UX 落地 + `saas.reconciliation` seed 對齊後，再把對帳整體遷 v2。

- **結算頁「觸發本月月結」按了無反應（api.post 不帶 Idempotency-Key → 400）+ 批次按鈕 disabled 無提示（branch `fix/settlement-batch-idempotency-ux`，2026-06-30）**：業主回報 `/accounting` 結算記錄「批次」按了沒反應。**查出兩件事**：① **「觸發本月月結」是真 bug**——後端 `POST /accounting/monthly-settlements:generate` 掛 `idempotency_guard` 強制 `Idempotency-Key` header，但前端 `api.post` 的共用 `rawRequest` 僅在 caller 傳 `opts.idempotencyKey` 時才加（不自動生成），`generateMonthly` 未傳 → 每次點擊都回 400 `MISSING_IDEMPOTENCY_KEY`，錯誤只顯示在按鈕旁小字 `genMsg` 易被忽略（＝「沒反應」）。② **「批次確認／標記已付」非 bug**——`settlements:batch` 端點無 idempotency_guard，但兩鈕 `disabled={selectedIds.size === 0}`，未勾選任何結算列時 disabled、點了無反應且無說明。**Root cause**：api client 對需要冪等的 POST 端點未送 Idempotency-Key。**修**：① `rawRequest` 改為**非 GET 一律帶 Idempotency-Key**（caller 給就用其值、否則 `newIdempotencyKey()` 自動生成，與既有 upload/download 一致；未掛 guard 的端點忽略此 header 無害、有顯式 key 者保留）——系統性修掉此類 bug 而非單點；② `SettlementTable` 批次鈕在 0 選取時加 `title` tooltip + 旁顯提示「請先勾選結算列以啟用批次操作」（新 i18n `batchHint` zh+en）。純前端屬 CIA 豁免。tsc 0 + JSON parity 2695=2695 + docker web 重建 + **Playwright 實測**（觸發本月月結 status 400→**201**、request 帶 Idempotency-Key、`genMsg`「已觸發 2026 年 6 月月結」回 batch_id `34e3061f…`；批次提示 0 選取時顯示、勾選 1 列後兩鈕 `disabled=false` 啟用、提示消失）。**已部署本機 docker web**。

- **退款／保固／爭議三頁列表捲軸失效（內容被 flex-shrink 壓扁裁切）（branch `fix/admin-list-scroll-flex-shrink`，2026-06-30）**：業主回報 `/admin/refunds` 退款列表捲軸失效、看不到下半部。**Root cause**：與先前 `/admin/customers` 捲軸 bug 同一類——捲動容器寫成 `flex flex-1 flex-col gap-5 overflow-auto`（flex column），flex 子層預設 `flex-shrink:1`，資料撐高（20 筆退款）後子層被壓縮塞進視窗高度（實測 `scrollHeight==clientHeight==773`、`canScroll=false`）而非自然溢出觸發捲動。`/admin/warranty-claims`（line 163）與 `/admin/disputes`（line 232）**共用完全相同的容器字串**、有同一 latent bug，一併修。**修**：三頁捲動容器 `flex flex-1 flex-col gap-5` → `flex-1 space-y-5`（移除 flex column 改 block 堆疊、子層自然溢出，與 customers-scroll 同修法）。純前端屬 CIA 豁免。tsc 0 + docker web 重建 + **Playwright 實測**（修後 refunds `scrollHeight` 773→1376、warranty→1950、disputes→1727，三頁 `canScroll` 皆 true、可捲到底）。**已部署本機 docker web**。

### Changed

- **三個 portal 主測試帳號 email 統一為 `test@lock-ai.com`（branch `chore/unify-test-account-email`，2026-07-06）**：延續密碼統一,業主要求帳號也統一。因登入以 `(email, role)` 解析（`auth_service.py:42`）、且 `users.email` 無全域唯一約束（僅 id/line_user_id 唯一）,同角色多帳號不能共用 email。故採「三個 portal 主登入帳號 → `test@lock-ai.com`」：品牌 admin（`admin@example.com`）、師傅主帳號（`demo-tech@example.com`，手機 `0911222333` 仍可登入）、平台管理員（`admin@lock-ai.com`）三者改為同一 `test@lock-ai.com`（各在不同 DB 或不同 role,登入不衝突）。`dispatcher/ops/cs/reviewer`（角色隔離用）與 4 名 placeholder 展示技師（`tech-chen` 等）**保留原 email**——它們靠不同 email 區分,共用會使登入無法辨識。**兩個負向測試改用保持獨立 email 的 `tech-chen@example.com`**：`test_technician_cannot_login_admin_web` 與 `test_reset_technician_account_forbidden`（品牌 DB 含師傅投影,admin 與 demo-tech 同為 `test@lock-ai.com` 會讓「技師不能登後台/不能被重設」的負向斷言誤中 admin 列;`admin_reset_password` 查詢無 role 過濾尤然）。範圍:seed（`_admin_user.sql`/`technicians.sql`）+ 三顆本機 DB（users + technicians 表 email）+ 前端 devHint（zh/en i18n）+ `api-status` 預設值 + 59 檔測試/e2e/文件同步 + `init-platform-db.sh` 範例 email;歷史記錄（CHANGELOG/completion-status 舊條目）與 `docs/_archive` 不改寫。順修 `technicians.sql`/`CR-0099` 兩處上一輪漏改的 `techpass123` 註解。驗證：登入 22 測試 + 全套 **1611 passed**;四面 curl（8001 admin／8002 email+手機／8003 平台）皆以 `test@lock-ai.com`/`changeme123` 登入成功、舊 `admin@example.com` 回 401;重建 dispatch web 讓登入頁 devHint 更新。測試資料/文件變更 CIA 豁免。

- **測試帳號密碼統一為單一組 `changeme123`（branch `chore/unify-test-account-passwords`，2026-07-06）**：業主要求所有測試帳號密碼統一。盤點後採 `changeme123`（6 個可登入種子帳號中 5 個已是它、登入頁 devHint 與 20+ 處 e2e/文件已引用，反向統一代價最小）。變更：① `SQL/seeds/technicians.sql` demo-tech 的 hash 由 techpass123 改為與 `_admin_user.sql` 同一 changeme123 hash（4 個 placeholder 展示技師維持不可登入的刻意設計）；② 三顆本機 DB 直接 UPDATE（品牌 5433：demo-tech/tech-chen/測試 vendor；師傅庫 5434：demo-tech/tech-chen；平台庫 5435：admin@lock-ai.com 由 platadmin123 改統一，並 bump `password_changed_at` 撤舊 session）；③ 測試同步（`test_login_roles`/`test_technician_login_identifier`/e2e `tech-flow.spec.ts` 的 techpass123 → changeme123，technician 打後台登入的 401 測試因此改為驗證純角色閘）；④ 文件同步並**修正兩處本來就寫錯的帳密**（`SQL/seeds/README.md` 的 `demo-admin@example.com/adminpass123`→實際 `admin@example.com/changeme123`；`scripts/dev/QUICKSTART.md` 的 `demo-dispatcher@example.com/dispatcherpass123`→實際 `dispatcher@example.com/changeme123`）。`init-platform-db.sh` 維持部署時輸入密碼的設計（無預設明文）；雲端 DB 未動。驗證：登入相關 22 測試 + 全套 **1611 passed**；8001 admin／8002 demo-tech+tech-chen／8003 平台管理員四面 curl 實測 changeme123 皆可登入。測試資料/文件變更 CIA 豁免。

- **負面情緒告警頁：移除「狀態流向」說明 banner（branch `fix/sentiment-alerts-remove-flow-notice`，2026-07-01）**：業主要求移除 `/admin/sentiment-alerts` 頂部藍色說明框「狀態流向：未處理 → 已確認 → 已結案。確認/結案後會寫入備註並記錄到後端，已結案無法回退。」移除 banner div + 兩 locale 的 `sentiment.flowNotice` key（parity 2701→2700）。純前端屬 CIA 豁免。next build + **Playwright 實測**（頁面無「狀態流向」/「已結案無法回退」、藍 banner 消失、標題與狀態篩選保留）。**已部署本機 docker web**。

- **角色權限頁：移除洩漏內部識別碼的「F-019」藍色提示 banner + 清 matrixHint 的「BR-M17-01」（branch `fix/roles-remove-f019-notice`，2026-07-01）**：業主指出矩陣上方藍框「F-019：admin/tenant_admin 可動態調整…透過 `/realtime/rbac` 即時推送給所有相關 session…actor 必須嚴格高於目標角色」把內部 flow ID（F-019）、WS 路徑（`/realtime/rbac`）、開發者術語（session/actor）洩漏給使用者（同 `fix/user-facing-jargon-leaks` 類）。移除整個 banner div + 兩 locale 的 `f019Notice` key（parity 2702→2701）；順帶把上一版 CR-0111 在 matrixHint 引入的「BR-M17-01」內部 ID + 「端點強制授權」術語改白話「核准欄目前僅供設定與檢視，尚未套用到實際操作權限」（保留「approve 僅配置、未生效」的誠實訊息）。純前端屬 CIA 豁免。next build + JSON parity 2701 + **Playwright 實測**（頁面已無 F-019／`/realtime/rbac`／actor／session／BR-M17 任一字串、藍 banner 消失、matrixHint 白話版正常）。**已部署本機 docker web**。

- **角色權限頁：權限矩陣 block 放大（`min-h-[620px]`）（branch `style/roles-matrix-enlarge`，CR-0111 後續排版，2026-07-01）**：CR-0111 角色補至 12 個後，角色卡 grid（`grid-cols-5` × 12 = 3 排）吃掉垂直空間，權限矩陣卡（`flex-1`）被壓成 125px、12 個資源列擠在裡面要捲。業主要求把「系統管理員 權限矩陣的 block 放大」。矩陣卡加 `min-h-[620px]`（flex-1 仍保留、有空間時續撐大、但不再被壓縮），**Playwright 量測**：矩陣 block 125px → 620px、12 個資源列（×4 動作 檢視/編輯/核准/刪除）完整顯示、內層無裁切無捲動（`scrollH == clientH == 551`）。純前端排版屬 CIA 豁免。**已部署本機 docker web**。

- **保固索賠建立 modal 兩個 raw UUID 欄接真實資料 + 清死搜尋框；爭議仲裁查核確認已全真（branch `feat/warranty-create-modal-pickers`，2026-06-30）**：業主要求一起檢查 `/admin/warranty-claims` 與 `/admin/disputes`。**爭議仲裁**：盤點後**已全真**——列表／表格／詳情／證據面板皆接真實端點、決議表單操作列表選中的 dispute 且 `X-Initiator` 用真實 session、**無 create modal 無 raw UUID 欄**（爭議由後端 payment 事件自動建立，本頁只審查/co-sign），**不需改**（實測 18 筆正常渲染）。**保固索賠**：顯示資料已真（列表走 `usePaginatedFetch /warranty-claims`），但「建立保固申訴」modal 有兩個 raw UUID 手填欄 + 一個 disabled 死搜尋框。**接上**：① 客戶 ID（原手貼 UUID、必填）→ `<select>` 拉真實客戶主檔（`GET /tenants/{tid}/customers?limit=100`，顯示「姓名（電話）」、54 名）；② 工單 ID（原手貼 UUID、可選）→ 重用既有 `WorkOrderPicker`。**清掉** status tabs 右側 disabled「關鍵字搜尋」死控制（coming soon）並移除無用的 `Search` import。建立保固契約（`POST /tenants/{tid}/warranty-claims` 欄位）不變，純前端改輸入方式屬 CIA 豁免。tsc 0 + docker web 重建 + **Playwright 實測**（死搜尋框消失；客戶下拉 54 名真實客戶、選「陳美玲（0921123456）」回真實 UUID `cccc0000-…`；工單 picker 搜尋框在；無殘留 raw UUID 欄；disputes 18 筆正常）。**已部署本機 docker web**。**附帶觀察**：客戶清單含測試資料雜訊（初始／更新後名稱／測試客戶_…，DB 資料潔淨議題、非本次 code 缺陷）。

- **退款審批「建立退款申請」modal 兩個 raw UUID 手填欄接上真實資料（branch `feat/refunds-create-modal-pickers`，2026-06-30）**：業主要求盤點 `/admin/refunds` 寫死資料並接上。**盤點結論：顯示資料已全真**——端點回 20 筆真實退款、SLA 卡 1/0/5 由 `items` 即時計算、`RefundReviewTable` 全 `row.*` 欄位驅動，無假/寫死的顯示資料。唯一「未接真實資料」處是「建立退款申請」modal 的兩個 raw UUID 手填欄。**接上**：① 工單 ID（原須手貼 `00000000-...`）→ 重用既有 `WorkOrderPicker`（以公單號／客戶名搜尋、免貼 UUID，同先前報價頁 UX）；② 覆核主管 ID（原須手貼主管 UUID）→ 改 `<select>` 拉真實後台員工清單（`GET /api/v1/staff`，顯示 `姓名（角色）`，**排除發起人自己以符 SoD**、只列啟用帳號）。建立退款契約（`POST /tenants/{tid}/refunds` + `X-Initiator`／`X-Approver` header、送出欄位）皆不變，純前端改輸入方式屬 CIA 豁免。tsc 0 + docker web 重建 + **Playwright 實測**（工單 picker 搜尋框在；主管下拉列 29 名真實員工＋角色標籤、選「示範覆核-黃覆核（審核員）」回真實 UUID `0a000003-…`；無殘留 raw UUID 手填欄；當前 admin 自己被排除）。**已部署本機 docker web**。**附帶觀察**：staff 清單含約 25 個「重設測試（客服）」測試帳號雜訊（DB 資料潔淨議題、非本次 code 缺陷，可另行清理）。

- **會計傳票頁空白 → 擴充 demo seed（含 6 月可見資料）並灌入本機 DB（branch `chore/seed-vouchers-demo-data`，2026-06-30）**：業主問 `/accounting/vouchers` 是沒資料還是顯示有問題。**診斷結論：沒資料，非 bug**（端點 200/`count=0`、頁面正確空狀態無錯、查詢 `FROM vouchers WHERE tenant_id=%s` 正常）。`vouchers` 表全表 0 列，對照 invoices 60／settlements 25／reconciliations 25 皆有資料。**兩層成因**：① 傳票功能 app 端為**唯讀** —— `voucher_service` 僅 `list_vouchers`／`get_voucher`／`render_voucher_pdf`，全專案**零個 `INSERT INTO vouchers`**（對帳核准／結算／退款／開票事件不會自動生傳票，唯一資料源是 seed SQL）；② 既有 seed `SQL/seeds/vouchers.sql`（4 筆、本就列於 `quickstart.sh` SEED_ORDER）從未套到此 DB（此 DB 以 `realistic_demo_seed.py` 灌、不含 vouchers）；③ 即使灌入，原 4 筆皆 4 月、頁面預設「近 30 天」範圍會濾掉。**業主裁決：灌 seed + 補 6 月資料**。擴充 `SQL/seeds/vouchers.sql` 新增 6 筆 6 月份傳票（reconciliation／settlement／refund／invoice 四型俱全、含負數退款沖銷、`posting_date` 落 2026-06-05~29 預設範圍內，deterministic UUID + `ON CONFLICT DO NOTHING`），套用至本機 DB（共 10 筆）。Playwright 實測（預設範圍即顯示 6 筆、四型徽章齊全、退款 -NT$2,800 紅字、每列 PDF 匯出鈕）。無 code／schema 變動屬 CIA 豁免。**已套用本機 DB（無需重建容器）**。**治本待辦**：傳票自動產生（對帳核准／結算／退款／開票 → 自動記帳分錄寫入 vouchers）為後端功能缺口，另立 CR。

- **營收頁 CSV 匯出鈕從底部移到頂部 header（比照 /accounting 結算頁）（branch `chore/accounting-revenue-csv-to-header`，2026-06-30）**：業主要求「匯出 csv 功能比照 `/accounting` 放到上方」。營收頁原 CSV 匯出鈕在捲動內容最底部（`justify-end`），結算頁則置於頂部標題列 refresh 旁。將營收頁匯出鈕搬到 header（順序：標題 → refresh → CSV匯出 → 連線徽章），樣式比照結算頁匯出鈕（`border + bg-surface + Download icon + text-[13px]`）；功能不變（客戶端月度趨勢 CSV 下載、無資料 disabled），移除底部匯出區（含已失效的舊 Excel 移除註解）。純前端屬 CIA 豁免。tsc 0 + docker web 重建 + Playwright 實測（匯出鈕只在頂部 top=16 與 refresh 同列、底部無殘留、點擊下載 `revenue-month-2026-06-30.csv`、內容為真實趨勢 `period,revenue,order_count` + 3 個月資料）。**已部署本機 docker web**。

### Removed

- **移除 sidebar 重複的「會計傳票」導覽項（branch `chore/sidebar-remove-duplicate-vouchers`，2026-06-30）**：業主指出 `/accounting` 的 tab bar（結算管理／發票管理／會計傳票／營收報表）已有「會計傳票」可點過去，sidebar 帳務群組又列一個「會計傳票」是重複——且「發票管理」「營收報表」都只在 tab bar、未進 sidebar，會計傳票進 sidebar 反而是唯一不一致者。移除 `Sidebar.tsx` 帳務群組的 `{ id: "vouchers", href: "/accounting/vouchers" }` 子項；連帶清掉移除後即無人引用的死 key `sidebar.nav.vouchers`（zh+en，維持 parity 2694=2694）。**保留** `accounting.tabs.vouchers`（tab bar 標籤）與傳票頁本身——仍可從 `/accounting` 的 tab 進入。純前端屬 CIA 豁免。tsc 0 + JSON 合法/parity + docker web 重建 + Playwright 實測（sidebar 帳務群組剩 月結算總覽／退款審批／保固索賠／爭議仲裁、無會計傳票；tab bar「會計傳票」tab 仍在、`/accounting/vouchers` 仍可達）。**已部署本機 docker web**。

- **移除帳務 revenue/invoices 兩條揭露警語 + 發票頁孤兒死控制（branch `chore/accounting-remove-disclaimer-banners`，2026-06-30）**：業主要上線、不需使用者看到「即時資料 vs coming soon」這類內部揭露警語。移除 ① revenue 頁警語（`accounting.revenue.banner`）② invoices 頁警語（`accounting.invoices.banner`）兩個琥珀色 banner。**連帶清掉**發票頁「僅顯示逾期」disabled toggle —— 它本就是靠 invoices 警語的「coming soon」說明而存在的死控制，警語一移除就成了沒說明的灰 toggle，上線更難看故一併移除（搜尋／狀態／付款方式／日期範圍四個篩選完整保留）。i18n 同步移除 `accounting.revenue.banner`／`accounting.invoices.banner`／`accounting.invoices.overdueOnly`（zh+en，parity 2695=2695；`accounting.common.comingSoon` 別處仍用故保留）。純前端屬 CIA 豁免。tsc 0 + JSON 合法/parity + docker web 重建 + Playwright 實測（兩頁警語文字與琥珀 banner div 皆消失、死 toggle 消失、三個下拉篩選與搜尋框保留）。**已部署本機 docker web**。

### Added

- **帳務頁寫死資料轉真（問題類別營收佔比）＋ 四頁重新整理按鈕尺寸統一（branch `feat/accounting-hardcoded-and-refresh`，2026-06-30）**：業主盤點 `/accounting`、`/accounting/invoices`、`/accounting/vouchers`、`/accounting/revenue` 四頁，要求「把所有寫死的資訊都接上、統一上方重新整理 button 大小」。**盤點結果**：四頁表格（結算/發票/傳票）皆 `listInvoices`／`usePaginatedFetch` 即時資料，唯一真正寫死的「資料」是營收頁 `ServiceTypeChart`（無 props／無 fetch，寫死 4 筆假數字：維修 NT$520,000／清潔 380,000／安裝 240,000／諮詢 144,500）。**後端能否接真資料查證**：`work_orders.service_category`（install/repair/warranty enum）**80 筆全 NULL**（接了會空圖）；唯一有資料的維度是 `problem_cards.category`（卡片/指紋辨識/密碼/WiFi 連線/電池/聲音異常/故障/安裝），經 `invoices→work_orders→problem_cards` JOIN（與 `by_brand` 同條 `_TENANT_JOIN`）可聚合出真實營收分佈。**業主裁決「接問題類別＋更名」**（避免語意不符：圖標題原寫「服務類型」但唯一有資料的維度其實是「問題類別」）。**實作**：後端 `revenue_service._query_by_category`（clone `_query_by_brand`，`GROUP BY pc.category`，NULL/空→「未分類」，回 `category/revenue/share` DESC）+ `get_revenue_summary` 回應加 `by_category`；`RevenueSummary` Pydantic model 加 `RevenueByCategoryPoint` + `by_category: list[...] = []`（**手動 additive**，`openapi.yaml`／web `api.generated.ts` 待 TS 產生器修復後回填同步，前端以 inline 型別消費 —— 同 customer-stats 策略；CI `api-types-sync` 只看 `openapi.yaml`／`api.generated.ts` 故不踩 gate，且 `models/generated.py` 非 CI-gated）。前端 `ServiceTypeChart` → 新元件 `CategoryRevenueChart`（接 `by_category`、條寬以最大值正規化、空/載入狀態、更名「問題類別營收佔比」），revenue 頁改傳真資料 + 警語去「服務類型示意」改「問題類別占比即時」（zh+en）。**重新整理按鈕統一**：四頁中 `/accounting`（結算）原為 36×36／icon 16×16 異類，改 32×32／14×14 對齊其餘三頁。`test_reports_v2_endpoint` +`by_category` 結構斷言（整檔 6 passed；`test_export_report`／`test_cr_0063` 16 passed 無回歸）+ tsc 0 + JSON 合法 + docker api/web 重建 + **Playwright 實測**（端點回 8 類別真實營收、share 加總 ≈100%、圖表 DOM 渲染 卡片 NT$29,408…安裝 NT$19,950 與 DB 一致；四頁按鈕量測皆 32×32／14×14）。**版面修正**：問題類別圖 8 列（舊服務類型圖僅 4 列）在 flex-column 捲動容器內被底部 row 的 inline `minHeight:320`（覆蓋 flex item 預設 `min-height:auto`、放行壓縮）壓到 320px → 內容溢出跑版；改 `shrink-0 + min-h-[320px]`（高度回內容自然值、空資料仍有地板），實測兩卡同高 536px、末列「安裝」完整落在卡內。**已部署本機 docker api+web**。

- **客戶主檔統計卡接真實聚合（活躍/高風險/已過保固）（branch `feat/customers-stat-cards`，2026-06-29）**：`/admin/customers` 上方 4 張統計卡原本 3 張是 placeholder「—」（灰底 + 「待活躍判定規則 / 待風險引擎 / 待保固模組」），盤點時業主要求接真實資料。**新增** read-only 聚合端點 `GET /tenants/{tid}/customers/stats`（`customer_service.customer_stats`，單次掃描 `users` 以 FILTER 聚合，cross-tenant guard，回 `{total, active_30d, high_risk, expired_warranty}`）。**業主裁決定義（2026-06-29，避免腦補業務規則）**：① 活躍 = `last_active_at` 近 30 天有互動（`is_active` 欄全為 true 不具鑑別度故不採）；② 高風險 = `risk_level IN ('high','critical')`；③ 原「保固即將到期」**因客戶層級無到期日資料**（`work_orders.warranty_expiry_date` 全空）改為「**已過保固**」= `warranty_status='expired'`（再行銷名單）。前端去掉 3 卡的 `opacity-60` placeholder、接 stats 值，i18n 標題/註解同步更新（保固卡 zh「已過保固」/en「Warranty expired」）。端點為 additive read-only、reuse 既有欄位（無 migration、無 schema 變動）；inline 型別（產生器 Node26 本機壞、暫未進 `openapi.yaml`，待 CI Node20 或工具修復補入 spec）。`test_customers_v2_endpoint` +2（stats 200 結構/子計數≤總數 + cross-tenant 403），整檔 19 passed 無回歸；`tsc --noEmit` 0 + JSON 合法 + docker api/web 重建 + **playwright 實測**（活躍 9 / 高風險 10 / 已過保固 7，與 SQL 聚合完全一致、端點 200）。**已部署本機 docker api+web**。

- **CR-0109 M09 媒體法務保留（legal_hold）手動設定面（branch `feat/cr-0109-legal-hold`，2026-06-28）**：Phase I backlog 批次 3 之 M09。`media_files.legal_hold` 欄與 retention cron 排除早已存在（CR-0067）但無設定面 —— 運營端無法在爭議/保固/訴訟時鎖證據。新增手動設定面（admin 自行判斷何時鎖，**不腦補自動觸發規則**，後者待業主）：`media_service.set_legal_hold`（UPDATE + 404 防呆 + 稽核 `media_legal_hold` event）+ `list_media_for_work_order` 補回 `legal_hold` 欄 + `media_v2` 新端點 `PATCH /tenants/{tid}/media/{mediaId}/legal-hold`（REVIEW_ROLES：admin/ops/reviewer，cross-tenant guard）+ 前端 `MediaGallery` 🔒「保留」徽章 + 鎖/解 toggle（有權角色才顯，樂觀更新）。additive 端點 + 既有欄位（無 migration）。API `test_cr_0109_legal_hold` 3/3（設定→列表反映→解除 / cross-tenant 403 / 404）+ 全套 1464 passed 無回歸 + tsc 0。**待部署 api+web**。詳見 `docs/4-exploration/CR-0109-media-legal-hold-manual.md`。

- **CR-0108 S3 LINE 進線自動帶 Case（branch `feat/cr-0108-s3-line-case`，2026-06-28）**：M01 收尾。原 LINE 進線直接建 problem_card 不掛 Case，與手動渠道（電話/官網/熟客介紹）資料模型不一致。`problem_card_service` 新 best-effort `_ensure_line_case` —— escalation 建/併草擬卡時 ensure 一張 `source_channel=line` 的 intake_case 並連 `problem_cards.case_id`（一卡一 Case、已連則略過、客戶資訊取對話 user 沿 D6 phone+LINE ID、失敗不阻斷建卡，比照 CR-0102 電話回填的 fail-soft）。**LINE 進線至此也收斂到 Case 模型**，M01（CR-0108）後端+前端+LINE 三段全到位。API `test_cr_0108_s3_line_case` 2/2（卡連 case_id + intake_case source_channel=line / 同 session 併卡仍同一 Case 不重建）+ 全套 1461 passed 無回歸。**待部署 api**。

- **CR-0108 S2 M01 進線建案前端頁 + Case 列表（branch `feat/cr-0108-intake-frontend`，2026-06-28）**：接續 CR-0108 後端核心，補前端讓客服可實際操作。新頁 `web/src/app/admin/cases/page.tsx`：**代客建案**（渠道 phone/web/referral + 客戶姓名/電話 + 需求摘要 → 建案發案號 C-NNNNNN + 啟動 first-response SLA；LINE 進線由 agent 自動建案不在手動表單）+ **Case 列表**（案號/渠道/客戶/摘要/狀態/SLA + 逾時紅標 + 標記處理中/結案動作 + header 顯逾時件數）。Sidebar 加「進線案件」入口（開單流程群組第 2 位，Inbox icon）+ rolePolicy `/admin/cases`（admin/ops/dispatcher/**customer_service** —— 對齊後端 BACKOFFICE_ROLES，避開 `/admin` catch-all 排除客服）+ 中英 i18n `sidebar.nav.intakeCases`。tsc 0 + 重建 api+web docker + **Playwright 實機驗證**（頁面渲染、UI 建案 C-000006 即時入列、curl POST/GET 通；測試資料已清）。**S3 小 follow-up**：LINE escalation 自動 ensure 一張 Case。詳見 `docs/4-exploration/CR-0108-m01-intake-case-entry.md` §13。

- **CR-0108 M01 進線 Case 入口（後端核心）—— 全渠道 Case/Inquiry 實體 + first-response SLA（branch `docs/cr-0108-m01-intake-cia`，2026-06-28）**：Phase I backlog 批次 1。M01 進線完成度 12%（Phase I 最低），業主核心目標「接單從客服一路串到工單」最前端斷點 —— 現況無跨渠道可追蹤 Case 實體。**先產 CIA（CR-0108）停 §8**，業主裁決：D1 渠道=line/phone/web/referral（partner 4 渠道 Phase II）、D2 SLA 級距入 config 暫定 30 分待業主、D3 漸進（case_id nullable 不硬擋）、D4 Case 為上游容器（一 Case 多 problem_card/work_order）、D5 可讀號 C-NNNNNN、D6 客戶去重 phone+LINE ID 為主（修正 Q008「依地址」）。**後端核心實作**：migration 085（`saas.intake_case` + 號碼序列 + `problem_cards/work_orders +case_id`）+ `intake_case_service`（建/列/詳/改 + SLA due 計算 + 渠道/狀態守門 + status=in_progress 記 first_responded_at）+ `intake_cases_v2` router（GET/POST/GET-by-id/PATCH `…/cases`，BACKOFFICE_ROLES 含客服代建、cross-tenant guard、POST idempotency，main.py 註冊）+ `core.config` 加 `intake` 區段 + `[intake].first_response_sla_minutes`。API `test_intake_case` 5/5（建案+可讀號+SLA、列表篩選、詳情 sla_overdue、in_progress 記首回、非法渠道 422、cross-tenant 403、404）+ 全套 1459 passed 無回歸。**S2 待續（同 CR）**：前端進線建案頁 + Case 列表、LINE escalation 自動帶 Case。**待部署 api（含 migration 085）**。詳見 `docs/4-exploration/CR-0108-m01-intake-case-entry.md`。

- **Phase I 帳號安全 A1+A2+A3 —— 登入防爆破 / 停權即時失效 / 改密碼撤 session（branch `fix/account-security-phase1`，2026-06-28）**：Phase I 缺口 backlog 批次 0（業主裁決帳號安全先補、逐一補），Beta 對外開放前的最低底線。三件同屬 auth 子系統、共用 migration 084 + `get_current_user` 熱路徑，故一分支內依序完成。**A1 登入防爆破**：migration 084 `users +failed_login_attempts +locked_until`；`auth_service.login`/`login_with_identifier` 連續密碼錯誤達 `[auth].login_max_attempts`（預設 5）即設 `locked_until=NOW()+login_lockout_minutes`（預設 15 分），鎖定中即使密碼正確也回 **429 LOGIN_LOCKED**，成功登入歸零；門檻入 config 不寫死。僅對既存帳號生效（查無帳號無 row 可記，回 401 同枚舉防護；per-IP/global spray 防護列後續）。**A2 停權即時失效**：新 `core.auth.load_user_security_state`（fail-open：DB 不可用/非 uuid/查無 → None 維持 claims-only，相容既有用假 user_id 的元件測試）；`get_current_user` 與 `auth_service.refresh` 每請求重查 —— 既存帳號 `is_active=FALSE` → **403 ACCOUNT_DISABLED**（原停權後 access 仍可用 1h、refresh 續 30d）。**A3 改密碼撤 session**：migration 084 `users +password_changed_at`；`change_password`/`confirm_reset`(自助重設)/`admin_reset_password` 皆設 `password_changed_at=NOW()`，token 驗證比對 `iat < password_changed_at` → **401 TOKEN_STALE**（改密碼即全域撤該 user 既有 access/refresh）。API `test_account_security_phase1` 5/5（A1 鎖定+歸零、A2 停權 403、A3 stale 401+控制組）+ **全套 1454 passed 無回歸**（3 個 pre-existing seed 失敗 test_cr_0051/0060 與本批次無關，git stash 驗證）。**待部署**（含 migration 084）。詳見 `docs/4-exploration/phase1-gap-backlog-20260628.md` §1。

- **Phase I 缺口 backlog + 給主管報告增補 Phase II/III 延後清單 + 修正派工模式誤述（branch `docs/phase1-backlog-phase2-report`，2026-06-28）**：業主交辦「Phase I 記下來逐一補帳號、Phase II 補進給主管確認的文件、順便修報告錯誤」。**①** 12-叢集對抗式現況驗證（對 dev_new_arch HEAD 實查 280 次）→ 新增 `docs/4-exploration/phase1-gap-backlog-20260628.md`：把 Phase I 純工程缺口立成可打勾 backlog，**業主裁決帳號安全 3 件（A1 登入防爆破 / A2 停權 token 即時失效 / A3 改密碼撤 session）排最前、一件一分支逐一補**，其餘（M01 進線 Case 入口、M03 分診 5-state/completeness_score、M08 客戶獨立簽收、M09 legal_hold 設定面、M02 Device 主檔、M05 狀態詞彙雙軌）依批次排程。**②** 給主管報告 `docs/_audit/owner-spec-compliance-and-pending-decisions-20260627.html` 新增 **§十一 現況缺口驗證**（Phase I 工程待補 + Phase II/III 延後清單供主管確認簽核：金流 M11 router 未註冊、自動媒合 M06 引擎 stub、multi-tenant 三類租戶含 vendors 隔離破口 `tenant_id=%s OR IS NULL`、Partner Portal M14、進階治理）。**③ 修正報告錯誤**：原 §二 step 5「派工模式前端 UI 待確認補齊」**誤述** —— 經實查前端 UI 已存在（`web/src/app/admin/dispatch-queue/page.tsx` 的 `DispatchModeSelector`，CR-0030 manual/platform_paid/auto_match），改判 ✅ 就緒（唯自動媒合引擎屬第二階段）。重跑 `gen_docs_html.py` 同步 md + docs_html 鏡像。純文件，無 code/contract 變動。

- **CR-0107 師傅獎懲紀錄轉真 — 後台手動登錄 ledger + 自動帶取消失約扣款（branch `feat/technician-detail-real-fields`，2026-06-27）**：業主問「獎懲為何還是寫死的」。**源頭查證**（窮盡翻 `20260617資料/` 全 xlsx + 會議記錄）：前端 mock 4 筆（高評價獎金+200/準時完工+150/遲到扣款-200/客戶推薦+500）**來源完全無此規則（捏造）**；唯一規則明確者為**取消失約扣款**（測試計畫 TI-M07-02/BR-CANCEL-007：同月首次免責/≥2次扣NTD500+weight-10/不可抗力免責），已存於 `public.cancellation.technician_penalty` 欄；獎金與其他扣款類型（未繳現金/未退料/客訴/返工…）僅 ERP spec P2-23「建議」未拍板；**關鍵紅線 Q121「主管拍板 all：…師傅扣款…這些都不能交給 AI 或工程師自行假設」**→ 不可照 mock 編自動獎懲規則。**業主裁決「後台手動登錄 + 自動帶取消罰」**。實作：migration 083 新表 `saas.technician_penalty_bonus_ledger`（entry_type bonus/penalty + title + amount≥0 + occurred_date + source_work_order_id + created_by，per-tech FK ON DELETE CASCADE）；`technician_penalty_bonus_service`（`list_entries` UNION ①手動 ledger（editable）②自動取消罰[`cancellation` JOIN `work_orders` 取 technician_id、排除 goodwill_waiver、id 帶 `cancel:` 前綴/editable=false]，依日期新→舊；`create_entry`/`delete_entry` 限手動，`cancel:` 前綴擋下不可刪自動帶入）；`technician_penalty_bonus_v2` router（GET/POST/DELETE `/tenants/{tid}/technicians/{techId}/penalty-bonus`，DISPATCH_ROLES —— 財務敏感+Q121 主管拍板，POST idempotency，created_by=actor，main.py 註冊）；前端新元件 `PenaltyBonusLog`（抽離自 TechnicianDetailSidebar，去寫死 4 筆 + 後台登錄 modal[類型/事由/金額/日期/說明] + 刪除[手動限定] + 自動帶入標「自動」徽章不可刪 + 空狀態），連帶清掉 sidebar 已無用的 `MockBadge`；MockBanner 文案更新（**右欄全真，僅本週排班仍示意**）。**範圍/限制**：獎金與其他扣款不自動計算（Q121 須主管拍板、規則未定）→ 主管手動登錄；自動帶入僅取消失約扣款（唯一規則明確+有資料源）；多數技師獎懲「真但空」（cancellation 表 0 列）。API `test_technician_penalty_bonus` 4/4（手動建/列/刪、自動帶取消罰 source=cancellation/不可刪 422、非法 entry_type 422、cross-tenant 403）+ 技師相關 38 passed 無回歸 + Live E2E（curl + Playwright：丁啟恆 POST 手動獎金+300 + DB 插取消罰500 → 卡片顯「取消失約扣款 -NT$500【自動】無刪除鈕 + 高評價獎金 +NT$300 可刪」；seed 已清）。**師傅詳情頁右欄至此全部轉真**（可用狀態/佣金/獎懲+進行中工單）；主區僅本週排班仍示意（CR-0105 §3，需班別定義）。詳見 `docs/4-exploration/CR-0107-technician-penalty-bonus-ledger.md`。

- **CR-0106 師傅佣金摘要轉真 — 固定工資制月結引擎（branch `feat/technician-detail-real-fields`，2026-06-27）**：接續 CR-0104/0105。CR-0105 §0 源頭查證後佣金原判「第二階段(Beta 後)+Q-09 待業主」，**業主裁決「提前做+核准 21 拆帳規則 66 條草稿費率」**。**源頭兩大發現**（窮盡翻 `20260617資料/PhaseII_FinanceSettlement.xlsx`）：① 費率 Q-09「師傅分潤」業主自己在「13 待決策 Q&A」標「待回答」、payout_rule 69 筆全 draft/is_mock → 業主核准後翻 accepted；② **前端 mock 模型概念性錯誤** —— mock 顯示「維修佣金 70%/安裝 60%/客製料 80%」=**抽成制**，但來源（21 拆帳規則 + 19 鎖匠等級 + 28 AP Ledger）實為**固定工資制**：每服務每等級固定 base_payout（電子鎖安裝 LV-B=500，等級係數 A1.25/B1.0/C0.85 已內含於分級費率），月結走 AP Ledger（Σ核准工資+車馬費+檢測費+可報銷材料−平台費−未繳回現金−爭議暫扣=應付）。**實作照固定工資制重建**：資料鏈 `work_orders`(completed)→`quote_line_items`(service_code)→`technician_payout_rule`(service_code, level_id)；migration 082 把 payout_rule 草稿列翻 `decision_status='accepted', is_mock=FALSE`（69 筆，記錄 Q-09 決議、費率值不變）；`technician_commission_service.compute_monthly_commission`（技師等級 S/A/B/C→LV-A/B/C，當月 completed WO 服務明細 LEFT JOIN payout_rule → `gross=Σ(base_payout×qty)`，回 lines/completed_orders/unmapped/誠實 notes）+ `technician_commission_v2` router（`GET /tenants/{tid}/technicians/{techId}/commission-summary?year=&month=`，預設當月，DISPATCH_ROLES —— base_payout 為內部敏感成本，cross-tenant guard，main.py 已註冊）；前端 `CommissionSummaryCard` 抽成制 mock → 固定工資制真資料（本月工資 gross + 服務明細 service_name×qty=line_total + 應付 net + 空狀態 + 誠實註記），移除假 45,600/百分比/Mock 標籤，MockBanner 文案更新。**誠實限制（已標於 UI/回應）**：夜間/急件加成不自動套用（觸發 Q-03/Q-04 待回答）、扣項暫 0（待月結模組）、S→LV-A（來源「19 鎖匠等級」無 S 級費率）、多數技師佣金「真但空」（work_orders 多無 service_code 明細，有帶碼工單完工後自動填，同認證矩陣）。API `test_technician_commission` 4/4（固定工資制 LV-B SVC-ELK-001×2=1000、空案例、無費率 mapped=false/unmapped、cross-tenant 403）+ 技師相關 51 passed 無回歸 + Live E2E（curl + Playwright：丁啟恆 seed level=B + 完工單 SVC-ELK-001×1+SVC-RES-002×2 → 卡片顯「2026年6月·B級 本月工資 NT$1,100＝電子鎖安裝500+住宅開鎖600」+應付1,100+誠實註記；獎懲仍標示意；seed 已清、丁啟恆還原 level=C）。**下輪候選**：獎懲明細（CR-0105 §2，同屬月結 AP）、夜間/急件加成（Q-03/Q-04）、扣項接月結。詳見 `docs/4-exploration/CR-0106-technician-commission-fixed-payout.md`。

- **CR-0104 師傅詳情頁假資料轉真 — 可用狀態 / 等級 / 技能認證矩陣（branch `feat/technician-detail-real-fields`，2026-06-27）**：業主要求師傅詳情頁「全部轉接成真」。**40+ 子代理對抗式深掃 6 假資料區塊**判定：只 2/6 有可立即接的真資料源、4/6 缺的是**業務規則**（分潤%/等級門檻/班別/獎懲觸發 —— AI 不可腦補，change-governance 紅線）。**業主裁決**：D1 等級=後台手動指派、D2 認證矩陣=建完整模組、D3 佣金/獎懲/排班三大模組本輪不做（下輪各立 CR）。**① 可用狀態（假綠修復）**：`technician_service._TECH_SELECT` 接真 `online_state`（取代硬補 `_DEFAULT_AVAILABILITY`，online_state CHECK 值域與 TechnicianAvailability enum 本就相同）；前端 `AvailabilityCard` 顯示真值（綠點/標籤依狀態）+ 移除假 toggle/「上次上線」/「30 分自動離線」/Mock 標籤，加誠實註記「由技師端 App 切換、管理後台僅檢視」；header 徽章同步變真值。**② 等級（手動指派）**：migration 080 `technicians +level VARCHAR(2) DEFAULT 'C'`（值域 S/A/B/C，`TechnicianLevel` enum 守門）；`_TechnicianUpdateRequest` +level + `update_technician` SET level；前端編輯 modal 加等級下拉。取代後端對全技師硬補常數「C」。**③ 認證模組（完整）**：migration 081 新表 `technician_certification`（cert_name/brand/obtained_at/expires_at/is_mock，per-tech FK ON DELETE CASCADE，與 063 `technician_brand_authorization` 職責分離 —— 後者 dispatch 品牌過濾 UNIQUE(tech,brand)、本表一技師多筆具名認證）+ `technician_certification_service`（list/create/update/delete + 狀態 computed：valid/expiring_soon/expired 依 expires_at vs 今日）+ `technician_certifications_v2` router（GET/POST/PATCH/DELETE，require_tenant + cross-tenant guard + DISPATCH_ROLES 寫 + POST idempotency，main.py 已註冊）+ 前端新元件 `CertificationMatrix`（讀真實認證 + 後台維護新增/編輯/刪除 modal + 空狀態「尚無認證資料」+ 狀態色碼徽章），取代寫死的 5 列 mock。**業務假設（§4.1，待業主確認、非財務規則可動態調整）**：等級預設 'C'（入門級）、認證即將到期門檻 30 天、認證 admin 後台登錄（非技師上傳憑證）。API 技師相關 **83 passed + 1 skipped**（新增 cert 4 + level/availability 4，_TECH_SELECT 改動對 lifecycle/onboard/v2/availability/statement 無回歸）+ Live E2E（curl + Playwright：認證 modal 新增即時顯示「有效」、過期認證顯示「已過期」、等級 PATCH=A 顯示 A、可用狀態真值；已清測試資料、丁啟恆還原 level=C/0 認證）。**下輪候選**：佣金結算（需 Q-09 分潤公式）、獎懲明細（規格 09 已有 contract）、本週排班（需班別定義）。詳見 `docs/4-exploration/CR-0104-technician-detail-real-fields.md`。

- **CR-0099 技師登入支援「手機號或 Email」（branch `feat/technician-phone-login`，2026-06-23）**：業主測到「指派師傅 → 用師傅登入」卡 `VALIDATION_ERROR (422)`。**根因**（log 鐵證）：前端技師登入框標籤寫「手機號碼或 Email」（i18n `identifierPlaceholder`）叫人用手機登入，但後端 `/technicians/login` 的 `LoginBody.email` 是 `EmailStr` → 手機號不是合法 email，**在驗證帳密前就被 422 擋下**；`auth_service.login` 只 `WHERE email=%s`，無手機查詢路徑——UI 承諾的能力後端從未實作。**業主裁決方案 A**（後端支援手機或 email）+ 子決策：§8.1 手機非唯一（`users.phone` 無唯一約束），多筆相符 → 擋下 409 `AMBIGUOUS_IDENTIFIER` 要求改用 email（最安全、免 migration）；§8.2 欄位改名 `identifier`（契約語意正確）。**實作**：後端 `auth_service.login_with_identifier`（手機格式 `^09\d{8}$` → `_find_users_by_phone` 回全部相符偵測歧義；否則走既有 email 查詢）+ `TechnicianLoginBody{identifier,password}`；前端 `loginTechnician` 改送 `{identifier}` + tech-login 密碼 minLength 4→8 對齊後端；OpenAPI `/technicians/login` requestBody email→identifier + 加 409。**守備**：僅技師登入，admin/vendor 維持 email-only。test_technician_login_identifier 5/5（手機/email 登入、密碼錯/查無 401、歧義 409）+ test_login_roles/auth_guards 回歸 10 passed。**無 DB migration**（純應用層）。**技術債**：`users.phone` 仍無唯一約束，本 CR 以「多筆擋下」緩解未根治（日後手機為主帳號需加 `(tenant,phone)` 唯一索引 + migration）。詳見 `docs/4-exploration/CR-0099-technician-phone-login.md`。

- **CR-0097 AI 進線「說了轉接卻沒呼叫工具」兜底（branch `fix/agent-handoff-fallback`，2026-06-23）**：業主 LINE 實測報修「門鎖壞了 Chatlock A90 鎖舌卡住 0922371211」（品牌+症狀+電話齊全），AI 回「已幫您轉接給真人專員」但**問題卡沒生成**，重傳一次穩定重現。**根因**（agent log 鐵證）：只 iteration 0、零工具呼叫——AI 生成「已轉接」**話術**卻**沒實際呼叫 `transfer_to_human`**，escalation 未新增 → 不建卡。**不是 SOP 沒寫清楚**（SKILL.md §0 鐵律 + booking.md:33 都明示），是 **LLM tool-calling 穩定地不遵守**，文字 SOP 壓不住——「進線→建卡」這個最關鍵起點 100% 靠 LLM 自覺呼叫工具，客人報修可能靜默蒸發。**業主裁決方案 A**（話術-行為一致性兜底）。**實作**（守 architecture lock：不加工具/不改白名單/不 fork 核心，改在 channel 層 `line_gateway`）：`_promised_handoff` 偵測承諾話術（完成式/指派式，降誤判）+ `_apply_handoff_fallback_safe`——turn 後若「escalation 未新增（=沒呼叫工具）且 AI 承諾轉接」→ 補一筆 `escalation_store.log`（模擬 transfer_to_human 的唯一動作），讓既有 `_forward_escalation` 仍建 AI 草擬問題卡；fail-soft。**取捨**：誤判寧可多建一張卡（後台可關），也不讓報修靜默蒸發。test_line_gateway +6 + agent 全套 126 passed。**Follow-up**：監控兜底觸發率（頻繁觸發=該調 SOP/模型/加方案 B 意圖兜底）；兜底卡標 `facts_snapshot.fallback=true`；部署 agent 後須 live 重測。詳見 `docs/4-exploration/CR-0097-handoff-fallback.md`。

- **CR-0096 同一 LINE 客人的不同問題各自獨立成卡（branch `fix/problem-card-per-issue`，2026-06-23）**：業主實測透過 LINE 講新問題、卻發現「新問題卡」內容跟第一張寫在一起。**根因三連鎖**：①LINE 同用戶永遠同 conversation（`session_id={tenant}:{user_id}` 永久復用）②`problem_cards.conversation_id` 全唯一（一 conversation 一卡）③`escalation_to_draft_pc` 對既有卡無條件 append 新症狀（回 `created:False`，連已轉工單的卡也照 append）→ **一個客人一輩子只有一張卡**，無法分單/分別派工/報價。**業主裁決方案 A**：舊卡「已轉工單 或 已結案」才開新卡；仍 draft/已確認未派工 → 新訊息視為同問題補充併入。**做法**（最小破壞，不動 status 語意）：**migration 077** `problem_cards +converted_at`（轉工單時標記）+ DROP 全唯一 + 部分唯一索引 `uniq_pc_conversation_active`（`WHERE status NOT IN (resolved,escalated) AND converted_at IS NULL`，同 conversation 同時只一張 active 卡）；service 5 處加 active 條件（escalation idempotency + existing 查詢、create_card 409、work_order_service 轉工單 set converted_at、sop_draft conversation→PC 加 ORDER BY）。test_cr_0096 3/3（active→append、已轉工單→新卡、已結案→新卡）+ 轉工單/工單/escalation/convert/create_card/sop_draft/ingest 回歸 64 passed 0 fail。**Follow-up**：對話訊息串仍共用（卡/工單已分開，但問題卡詳情的 LinkedConversation 仍顯示整段對話，要完全分需「每問題開新 conversation」更大改動）。詳見 `docs/4-exploration/CR-0096-problem-card-per-issue.md`。

- **報價頁建報價改「選工單」免貼 UUID（branch `feat/quote-wo-picker`，2026-06-23）**：業主實測回報「建報價草稿那欄還是要貼工單 UUID `45423b41-…` 才能搜尋」。釐清現況：工單早有可讀公單號 `{2碼地區}-{6碼流水}`（如 `TP-000001`，CR-0020 / migration 031 依 `customer_address` 發號、per-region 原子遞增），列表/詳情顯示層也已優先公單號，但**報價頁「建報價」唯一入口仍是 `placeholder="work_order uuid"` 的手貼輸入框**（CR-0095 UX 只補了「列表開既有報價」、沒補「選工單建新報價」）。**① 後端**：`work_order_service.list_orders` 的 keyword 過濾加 `OR wo.document_number ILIKE %s`（原只搜 customer_name/address/phone）→ 公單號可當搜尋字；additive query filter，response schema 不變。**② 前端**：新元件 `web/src/components/quotes/WorkOrderPicker.tsx`（受控、debounce 300ms 打 `GET /work-orders?keyword=`、下拉顯示 `公單號　客戶名　狀態`、選定回拋工單 UUID 給既有 `create_quote` 端點、深連結 `?wo={uuid}` 自動反查公單號顯示不露 UUID），報價頁把 UUID `<input>` 換成 `<WorkOrderPicker>` + 中英 i18n（admin.quotes.woPicker* 5 key）。**性質**：前端 wire-up + 後端搜尋微增強，不動 response schema / domain model / 派工流程 → 比照 CR-0091/0095 前端補接，未跑完整 CIA。test_wo_keyword_doc_number 2/2（公單號完整＋前綴搜得到、客戶名搜尋回歸）+ 工單 v2/報價回歸 24/0 + tsc 0。**Follow-up**：`SQL/Schema_doc_numbering.sql` 註解仍寫工單 `WO-YYYYMMDD-NNNN`（ADR-009 舊設計）實際早被 CR-0020/ADR-0110 地區制取代 → 待修註解；網址列與 Kanban/地圖 hover 仍露 UUID（顯示層獨立改點）。

- **派工單流程設計說明文件 + CLAUDE.md「文件雙版本」規則（branch `docs/arch-0006-wo-flow`，2026-06-22）**：業主問「問題卡 → 開單 → 工單 6 模組」這套流程為何這樣切。新增 `docs/architecture/work-order-flow-design-rationale.md`（ARCH-0006）說明四層設計用意（漸進式蒐集 / 問題卡-工單分離 / 開單最低門檻+完整度 gate / 唯讀防偽 + single source of truth），含 6 模組對照表 + 程式碼與 CR 追溯。CLAUDE.md「非預期工具鏈」新增規則：**文件一律寫 `docs/` 的 .md，再跑 `tools/gen_docs_html.py` 同步 `docs_html/`（整包 rmtree 重建，絕不手寫 HTML）**。同時重跑 `gen_docs_html` 讓 `docs_html/` 反映全部最新（含本輪 CR-0095 報價流程 + 先前未生成的 CR-0088~0094 / audit 報告 HTML）。原 `docs/wo-flow-rationale` 分支因含早期 docs_html 快照（與現況衝突），改以 cherry-pick 源檔 + 重生鏡像方式併入。

- **技師「核准」按鈕 — 後台可核准 pending_approval 技師（branch `fix/technician-approve-button`，2026-06-22）**：業主測試派工時派不出去，根因技師為 `pending_approval` 且後台無核准入口。後端 FR-0044 lifecycle 端點（`approveTechnicianOnboarding` 等）早就在，但 `Technician` 回應 schema **無 `status` 欄**（Pydantic `extra=ignore` 把 service 已撈的 `t.status` 丟掉）→ 前端看不出誰 pending、列表 Actions 欄只有無功能的 Ellipsis 圖示。**修**：(a) `Technician` model + 前端型別 `api.generated.ts` 補 `status`（additive，非破壞）；(b) `technician_service._tech_row_to_dict` 把 `row[9]` status 放進 dict；(c) `TechniciansTable` Actions 欄：`pending_approval` 顯「核准」鈕（POST `:onboard-approve` + `X-Initiator`，admin∈DISPATCH_ROLES）、其他非 active 顯 onboarding 狀態徽章；(d) 頁面接 `handleApprove`（成功 toast + 清 GET 快取 + refresh）+ 中英 i18n。test_technicians_v2 加 `test_technician_response_exposes_onboarding_status`（list/get 帶 status）共 9 passed + 技師/派工相關 175 passed 0 回歸 + tsc 0。**性質**：additive 回應欄 + 既有 FR-0044 端點前端接線，無新端點/狀態機/流程 → 比照 CR-0091/0035/0039 wire-up，未跑完整 CIA。**技術債**：`api/scripts/generate_models.sh` 指向已不存在的 `docs/02-design/specs/openapi.yaml` 且 `datamodel-codegen` 未裝 → 本次手動同步 model；codegen 待修。

- **CR-0095 初始報價 LINE 送單 + 客戶 LINE 同意 + 同意才派工（branch `feat/quote-line-approval`，2026-06-22）**：業主要的派工流程「開單→內部估價→外部報價→**推 LINE 報價給客戶**→**客戶 LINE 點同意/拒絕**→**同意才派工**」原只做一半。CIA `CR-0095` §8 業主裁決：D1 真 LINE 點按+網頁 fallback、D2 硬擋（一律需報價同意）+主管 override、D3 拒絕保留工單重送、D4 過期擋派工。**實作**：`PushKind +quote_proposal` + `render_quote_proposal`（postback `q:a|`/`q:r|` + URI fallback）+ `transition(send)` enqueue + worker `quote` 反查路徑；`work_order_service._assert_quote_accepted`（無 accepted 報價→409 `QUOTE_NOT_ACCEPTED`，過期不算）+ assign 整合 + 主管 override（`assignWorkOrderV2` query `override_reason`，audited）；`customer_respond_to_quote`（驗 line_user 擁有權→403）+ internal `POST /internal/quotes/{id}:customer-respond`；agent `line_gateway` 加 `PostbackEvent`（`q:a|`/`q:r|`→內部端點 + reply_token 即時回覆）；前端 `/admin/quotes` 送單文案+lineApprovalHint+i18n。test_cr_0095 8/8 + 全套件 **1403 passed / 0 回歸** + agent 120 passed + tsc 0。**Follow-up**：scope_change postback 救活（仍走網頁 fallback）。詳見 CR-0095。

- **CR-0093 Phase II 後半 — config 治理 UI / 廠商專區 / 轉單 override / 拆帳頁 / MED 收緊（branch `feat/perm-i18n-dispatch-20260621`，2026-06-21）**：業主「依序做完」。**CR-0036/0044/0045/0046**（686f2431）新 `/admin/config-governance`：列 M18 namespace（訂金/佣金/月結/取消費/稅率/公司檔/折扣）+ active 值檢視 + JSON 編輯建草稿（PUT，X-Initiator）+ 雙簽啟動上線（POST start-rollout，X-Initiator≠X-Approver SoD + instant/canary）—— 一頁涵蓋全部 config CR，業主可自助改值不改 code。**CR-0029**（2f923b30）廠商專區：後端 `GET /vendors/me`（role_required vendor）+ 前端 `/vendor` 專區頁（狀態卡 + 帳號資料）+ vendor-login 導向改 /vendor + rolePolicy /vendor。**CR-0042**（92af53f1）轉單完整度 422 結構化顯示（缺漏欄位 + tier 徽章）+ 主管 override UI（?override_reason=）+ DUPLICATE_CUSTOMER 提示。**CR-0037**（6069ca4f）師傅拆帳規則唯讀頁（base_payout RBAC 遮蔽）。**MED 收緊**（2c44fc47）7 個治理/稽核讀取端點 require_tenant→role_required（audit-logs/roles/rbac/ai-traces；其餘 cross-role 讀取保留逐類審查）。test_cr_0029 3/3 + 全套件 **1396 passed / 0 回歸** + tsc 0。詳見 CR-0093。

- **CR-0094 完整角色系統 — 後台員工帳號建立 + reviewer 三方對齊（branch `feat/perm-i18n-dispatch-20260621`，2026-06-21）**：業主裁決「建完整角色系統」（解「5 種角色只有 admin」）。稽核發現角色三方不一致（`role_service._MATRIX` 5 角色 / `auth._ADMIN_WEB_ROLES` 登入集 / `rolePolicy.ts` 缺 reviewer）且無建立非-admin 角色帳號的路徑。以 `_ADMIN_WEB_ROLES` 為操作正典：`auth_service.create_staff_user/list_staff_users` + `POST/GET /api/v1/staff`（FULL_ACCESS_ROLES 守衛，role∈operations_manager/dispatcher/customer_service/reviewer/admin，即時 active）；新 `REVIEW_ROLES = OPS_ROLES + reviewer`，refunds/disputes/warranty 6 router 改套（對齊 _MATRIX reviewer 可寫三域）；前端 `rolePolicy.ts` 補 reviewer + `/admin/staff` 路由 + 員工管理頁（列表+建立）+ Sidebar 入口 + i18n。brand_oem（外部夥伴）留夥伴入口 CR-0029。test_cr_0094 7/7 + 全套件 1393 passed / 0 回歸。詳見 CR-0094。

- **CR-0039 技師完工頁改走正規完工硬閘端點（branch `feat/perm-i18n-dispatch-20260621`，2026-06-21）**：後端 CR-0039 完工硬閘 + 技師打 :complete 的 403 guard 都做了，但技師完工頁仍 POST :complete → 撞 403 → 技師主完工流程壞掉（稽核 §三 HIGH）。改打 `/onsite/completion`（送 signature_evidence_id + photo_evidence_ids + notes）+ 客戶簽名上傳（→ /media 取 id）+ 完工照片計數≥3 + submit gating + 422 友善訊息（後端訊息已繁中）+ 成功後 refetch。無 contract 變更。

- **CR-0035 報價→應收發票前端動作（branch `feat/perm-i18n-dispatch-20260621`，2026-06-21）**：後端「報價→發票」三條寫入路徑全做完但前端純唯讀。admin/quotes：報價 accepted 時加「開立應收發票」鈕 → POST /accounting/invoices:from-quote。accounting：加「觸發本月月結」鈕 → POST /accounting/monthly-settlements:generate（帶 X-Initiator + 當前年月，冪等）。無 contract 變更。

- **CR-0091 標準化派工單 6 模組視圖 + admin 免責同意 GET（branch `feat/perm-i18n-dispatch-20260621`，2026-06-21）**：業主「開單頁面欄位沒按 PDF 做」。稽核發現 work_orders 早有「電子鎖派工單整合分析報告.pdf」全部 6 模組欄位（CR-0026/0043/0047 加的 brand/serial/door/warranty/billing 等）+ `PATCH /work-orders/{id}/fields` 端點，但**前端零編輯 UI**（`grep api.patch` 無 work-orders 消費者）→ 典型「後端做了前端沒跟」。新增 `DispatchOrderView` 依 6 模組組織：模組 1/2/3/5 標準化欄位**內嵌編輯**（首個 api.patch /fields 消費者，只送變更欄位）、模組 4 免責同意串新 admin 唯讀 `GET /work-orders/{id}/consents`（複用 consent_service，role_required(_DISPATCH_ALLOWED_ROLES)）、模組 5 計費沿用 quote-items、模組 6 簽認依狀態顯示。唯讀欄位（problem_type/completion_status/warranty_expiry_date）標示來源。test_cr_0091 5/5（含角色隔離 technician 403）+ tsc 0 錯 + 全套件 1386 passed。無 schema 變更。詳見 CR-0091。

- **CR-0092 RBAC 硬化 80 個敏感寫入端點（branch `feat/perm-i18n-dispatch-20260621`，2026-06-21）**：業主「連權限設定都沒改好」。多 agent 稽核 407 端點發現 `require_tenant` 只驗「登入+同租戶」**不檢查角色**，80 個敏感寫入（accounting 45/admin 14/pricing 10/dispatch 5/data-correction 2/role-mgmt 2/audit 1/cs 1）任何登入者含 technician/vendor 可寫金流/設定/派工/結算/退款/GDPR 刪除；`_require_initiator`/`_require_sod_two` 只檢查 header 存在/相異可填任意字串繞過。`core/deps.py` 新增單一真相源角色常數（FULL_ACCESS/OPS/DISPATCH/BACKOFFICE_ROLES＝既有 `_BILLING_ROLES`/`_APPROVE_ROLES` 超集+super_admin，不破既有存取）。80 端點 `require_tenant`→`role_required(*CONST)`（drop-in，保留 SoD/idempotency/cross-tenant guard/docstring）。technician/vendor 打這些端點由寫入成功→**403**（修補非破壞）。MED 193（多 cross-role 讀取）列 Phase II。test_cr_0092 角色隔離 + 全套件 1386 passed / 0 回歸。逐端點矩陣 + 23-CR 前後端對照見 `docs/4-exploration/audit-backend-frontend-rbac-20260621.md`。詳見 CR-0092。

- **修 66 個缺失 i18n key（UI 直接顯示 raw path，branch `feat/perm-i18n-dispatch-20260621`，2026-06-21）**：業主見工單詳情按鈕顯示 `pages.workOrderDetail.actions.requestReschedule` 等原始 key（translate() 缺 key 時 fallback zh-TW→en 仍缺則回傳 path 本身）。掃全 207 前端檔（就近 namespace 綁定 + 動態父層複查兩輪）確認 54 static + quotes 12 動態 state/action 共 **66 個真實缺口**（admin.quotes/quoteCatalog/vendorApprovals 三段整段空 + 工單詳情新 ops v2 action），排除 4 個 false positive。zh-TW + en 各補 66 個。

- **CR-0090 技師與廠商可共用同一 email（branch `feat/tech-portal-dashboard`，2026-06-21）**：業主測試時同一人想兼具接案(技師)+發案(廠商)身分，但註冊回 `EMAIL_TAKEN 409`。email 唯一性不變式由「全域唯一」放寬為「**每角色唯一**」：register_technician/vendor 檢查由 `WHERE email=%s` 改 `+AND role='technician'/'vendor'`。**零 migration**（users.email 本就無 UNIQUE 約束）；登入天然分辨（`_find_user_by_email` 已 role 過濾、/technicians/login=["technician"]、/vendors/login=["vendor"]，兩列獨立 users/密碼）。test_cr_0090 4/4（component）+ 回歸 267 unit；實機技師201→同email廠商201→同email技師再註冊409、DB 同 email 兩列。**Phase II**：密碼重設遇重複 email 之 role-aware 化（現 LIMIT 1）。詳見 CR-0090。

- **CR-0089 廠商註冊新增統一編號 + 公司名必填（branch `feat/tech-portal-dashboard`，2026-06-21）**：業主逐頁測試廠商註冊先遇 404（路徑缺 `/api/v1`，已 cherry-pick `fix/register-path-and-fields` 修復），再裁決補台灣 B2B 關鍵欄位。**migration 076** `vendors +tax_id VARCHAR(8)`（ADD COLUMN IF NOT EXISTS，既有列 NULL 不回溯）+ `VendorRegisterBody` +tax_id（`^\d{8}$` 必填）+ company_name 改必填 + `register_vendor` INSERT/return 含 tax_id + 前端廠商分頁加統編欄(required)+公司名必填 + i18n。test_cr_0089 9/9（Pydantic 驗證）+ 回歸 267 unit 全綠；實機帶統編 201 落庫、缺統編/非 8 碼 422。**Out of scope（Phase II）**：負責人/職稱/銀行帳戶/營登上傳、統編財政部勾稽。詳見 CR-0089。

- **技師端響應式版型 + 登入後儀表板決策屏（branch `feat/tech-portal-dashboard`，2026-06-21）**：業主逐頁測試指出技師端桌面是固定 480px 置中欄（「手機擺中間」）且登入直落 `/pool` 搶單缺儀表板。研究 DoorDash / Uber Driver / ServiceTitan / Housecall Pro / foodpanda 後採「決策屏」設計（第一屏只答「現在要不要上線接案」）。**P0（零後端、僅用現成端點）**：響應式 `TechShell`（純 CSS `md:` 切版，手機底部 4-tab / 桌面 `TechSidebar` 側欄；既有技師頁包入即自動響應式）+ 新 `/home` 決策屏（登入導向 `/pool`→`/home`）+ 5 widget（狀態收入膠囊 / 上線大鈕接 `PATCH /technicians/me/availability` / 今日行程 / 需注意 / 案量熱力圖）+ account 上線開關接通真實端點 + 中英 i18n。驗證：tsc 0 錯 + docker build + 5 角度對抗式審查（SSR/端點/版型/崩潰/i18n）+ 實機 register→login→打端點全 200。**P1（即時收入累計 / 月度快照 KPI / 排名，需新聚合端點）待業主裁決** → CIA `docs/4-exploration/CR-0088`（§8 待決，含 workload-heatmap IDOR 安全債）。

- **CR-0088 技師決策屏 P1 聚合（即時收入 / 月度快照 / SLA / 客戶評價，branch `feat/tech-portal-dashboard`，2026-06-21）**：業主裁決 §8 全做。盤點發現所需資料 work_orders 皆已具備（`estimated_price / accepted_at / started_at / completed_at / scheduled_at / rating(1-5) / feedback`，後者有 `confirm_order` 寫入路徑）→ **零 migration**。新增 `GET /api/v1/technicians/me/dashboard-summary`（今日/本週收入、本月毛額含未結預估、完成率、今日新單、平均到場、客戶評分 + 近期評價；self-scoped；**不含租戶內排名**＝§8-3）+ `GET /api/v1/technicians/me/workload-heatmap`（self 版，修 A37 端點 require_tenant 未鎖 self 的 IDOR）。前端：收入膠囊改今日/本週、新增 MonthlySnapshot widget、TodayScheduleSummary 加預約時間+逾時徽章、workload 改打 /me。**踩雷修正**：/me/* 路由須註冊於 /technicians/{id}/* 之前（否則 {id}="me" → uuid cast 500）。test_cr_0088 6/6 + 258 unit 全綠 + 實機 register→login→兩端點 200。收入口徑為 estimated_price 預估（UI 標註非實收）。

- **修技師工單/對帳單查詢假綠（branch `feat/tech-portal-dashboard`，2026-06-21）**：`my-orders` 與 `account/statements` 以 `getCurrentSession().userId`（= JWT sub = `users.id`）當 work-orders / statements 的 `technician_id` 過濾，但 `work_orders.technician_id` = `technicians.id`、且對帳單路由實為 `/tenants/{tid}/tech-statements?technician_id=`（前端打的 `/me/statements` 後端無此路由 → 404）→ 技師永遠查無自己的工單/對帳單（標 done 卻端到端壞）。改為先 `GET /technicians/me` 取 `technicians.id` 再過濾、對帳單改打正確路由。實機 register→login→打端點驗證 200。

- **CR-0087 測試尾巴補強 — skill 合規 / loop 上限 / ingest 不變式（branch `feat/cr-0087-test-tails`，2026-06-20）**：補「部分」項中**無外部依賴**的測試尾巴。**TI-A04-01** SKILL.md frontmatter Agent Skills 標準合規（恰 2 builtin skill + name/description/version + name==目錄名 + 無非標準欄位破壞可攜性 + semver）。**TI-A03-02** loop 安全上限（max_tool_iterations 有界正整數防無限迴圈；**據實標 spec「max_iter=4」為舊 ReAct 數字、lockcore 重寫後為 200**，不假裝舊數字成立）。**TI-M01-03** ingest message_count 3 輪累加到 6（非單輪覆蓋）。**TI-SYNC-01** no-silent-fail（空 assistant turn 只計 1 則不 over-count）。test_cr_0087 agent 4/4 + api 2/2 + 回歸 api 914 / agent 120 passed。**誠實標註**：TI-A08-01/AIOPS-11 AI 影像辨識禁用的 runtime image-strip 因 lockcore（fork nanobot）平台層保有 vision pathway，屬 agent 核心改動需 CIA，不寫 static analysis 假裝紅線守住（避免製造假綠）。

- **CR-0086 AI 進線 live E2E + 三套 eval 實機驗證（branch `feat/cr-0086-ai-intake-live`，2026-06-20）**：業主提供 Vertex 憑證（cedar-scope-489604-g3）後，把先前標 needs_external 的 live 段全部實機跑通：**redline_gate 9/9 全守線**（TI-A05-01/AIOPS-02：詢價/退款/付款/真人 → 全 transfer 且零報價）、**multiturn LLM-as-Judge overall 0.975**（TI-A09-01：redline 1.0/幻覺 0/12）、**eval_reply_quality 15 題 safety 0.90**（TI-AIOPS-08）。新增 `agent/tests/test_cr_0086_ai_intake_live.py`（live-gated pytest：有憑證跑真 agent turn 驗詢價轉真人不報價 + 領域外婉拒；無憑證自動 skip 不阻斷 CI）。live 結果存 `agent/evals/LIVE_EVAL_RESULTS_20260620.md`。agent 全套 116 passed（含 2 live）。**註**：followup_correct 0.067 偏低為單輪 Q&A 評測不利追問之已知模型行為觀察，非缺陷。

- **CR-0085 TI-M03-14 營運主流程 E2E（非 AI 段）（branch `feat/cr-0085-ops-e2e`，2026-06-20）**：進線→AI→PC→WO→結案端到端主流程的**非 AI 營運段**用 api component 測試串全鏈（live DB，無需 LLM/stack）：confirmed PC → create_from_problem_card → assign → accept → arrival → door-check → complete(admin override) → confirm，斷言走到 confirmed 終局 + 關鍵狀態機 gate（accept 須在 assign 後 409、door-check 須在 arrival 後 409、confirm 只能從 completed 409）。test_cr_0085 4/4 + 回歸 912 passed 0 fail。**needs_external**：AI 進線段（LINE webhook→lockcore agent→LiteLLM 判斷 escalate→產 PC）需 live LLM + 跑 stack。

- **CR-0082/0083/0084 TI-SYNC-02/NFR-01/PORTAL-01 — facts↔ERP / 批次效能 / partner 隔離（branch `feat/cr-0082-facts-erp-sync` 等，2026-06-20）**：**CR-0082** facts↔ERP 同步（Sync-M02，mock-first）：`facts_erp_sync_service`（純函式 `reconcile_facts` ERP-wins + `sync_user_facts` SCD2 關舊開新 + audit；缺值 no-op 非破壞；冪等）。**繞 CIA**：api 側服務不動 agent CS_TOOL_ALLOWLIST。**CR-0083** 月結/對帳批次效能（TI-NFR-01）：`test_cr_0083` seed 200 筆 reconciliation 計時 generate_monthly_batch（<20s 門檻）+ export_batch_csv eligible 過濾 + 冪等。**CR-0084** Partner Portal scope 隔離（修假綠：vendor 可讀全 tenant 全品牌 statement）：**migration 075** vendors +brand_partner_id；`partner_scope_service.resolve_partner_scope`（fail-closed：未綁 partner→403 PARTNER_NOT_BOUND、跨 partner→403 CROSS_PARTNER_READ、admin 不受限）。test 6+2+5 = 13/13 + 回歸 908 passed 0 fail。**needs_external**：正式 ERP client（真 ERP 主檔+憑證）、agent update_user_info tool 接線（CIA）。

- **CR-0080/0081 TI-A09-01/AIOPS-08/AIOPS-02 eval 套件可測核心（branch `feat/cr-0080-eval-scoring-pure`，2026-06-20）**：eval 套件的 scoring/aggregation/parse 邏輯原 **100% 埋在 main_async（0 pytest 覆蓋=假綠）**。**CR-0080** 抽 module-level 純函式：`multiturn_sim_eval.score_scenario`（4 維 + transfer/price 確定性覆寫）、`eval_reply_quality.aggregate_scores`（5 維）、`aggregate_by_category`（AIOPS-08 觀測）；main_async 改呼叫（行為不變）。**CR-0081** 新增 `forbidden_eval.py`（TI-AIOPS-02 K8 Forbidden Eval）：`judge_forbidden_case`（案例判定，_PRICE_RE 引自 redline_gate 單一來源）+ `validate_corpus_structure`（7 分類配額 200 題 + rotating 不重疊）+ `compute_forbidden_eval_gate`（≥95% pass-rate gate）。test_cr_0080 10/10 + test_cr_0081 10/10 + agent 全套 114 passed。**needs_external**：真 200 題對 agent 跑 reply 的 live runner 需 GEMINI_API_KEY/Vertex；本批只測 judge 評分數學/門檻/結構（mock fixture，零 LLM）。

- **CR-0079 TI-A10-02 高風險 SOP 雙審 + 不可篡改 ledger（合約 4.4d 紅線）（branch `feat/cr-0079-sop-dual-review-ledger`，2026-06-20）**：合約紅線（違反=合約終止）。家族覆核兩階段 substrate 已在（admin 初審→family review），但缺 (1) 雙審 distinct 強制 (2) ledger 不可篡改。**migration 074** family_reviews +prev_hash +entry_hash（hash chain，複用 CR-0068 audit 機制）。`family_review_service.create_review` 加雙審四眼（家族覆核者≠初審 admin reviewed_by → 403 SOD_VIOLATION）+ 每筆覆核計 entry_hash 接前列；新增 `verify_family_review_ledger`（走鏈偵測篡改）。test_cr_0079 3/3（四眼 403 + ledger hash + 篡改偵測）+ 回歸 895 passed 0 fail。**註**：4.4(d) 家族覆核 100% 的 publish gate + reviewer 缺席 fallback 屬 adopt 流程整合 follow-up。

- **CR-0078 TI-FIN-BOM-03 兩層 BOM + material owner + 材料費歸屬（branch `feat/cr-0078-bom-two-layer`，2026-06-20，P2 mock-first）**：兩層 BOM 原無 substrate。**migration 073** saas.product_model（第一層 brand/model）+ saas.bom_line（第二層子件，material_owner CHECK brand/company/locksmith/customer 對齊 BR-M10-02、cost_attribution CHECK customer/brand/technician/company 對齊 Q087、return_deadline_days nullable）。新增 `bom_service`：create_product_model（重複 409）/ add_bom_line（enum 驗證 422 + model 404）/ list_bom（兩層 + tenant 隔離）/ _validate_material_owner / _validate_cost_attribution（純函式）。test_cr_0078 6/6 + 回歸 892 passed 0 fail。**註**：Phase I 只建資料模型，退回狀態機（Q086 part-level warranty）+ material_ref 硬 FK 到主檔 + BOM 維護者 RBAC 屬 Phase II/待裁決。

- **CR-0077 TI-M01-04 進線 debounce + flood 24h dedup（branch `feat/cr-0077-inbound-debounce`，2026-06-20）**：A01 進線 1.5s debounce / flood dedup（合併文字/媒體/貼圖為 single logical turn）原**無實作**（line_gateway 對每則 event 立刻 reply，圖/貼圖直接丟棄）。新增 `agent/lockcore/channels/inbound_debounce.py`（通道層純邏輯，守 architecture lock 非 loop 核心）：`merge_inbound_items`（多則合併、媒體入 metadata）+ `InboundDebouncer`（滑動視窗，1.4s 合併/1.6s 分裂）+ `EventDeduplicator`（同 tenant+event_id 24h in-process TTL dedup，多租戶鍵正確）。時鐘以 now 參數注入，純記憶體可單元測。test_cr_0077 9/9 + agent 全套 94 passed。**註**：runtime 接線（line_gateway 改 buffer 驅動 + reply_token 時效處理 + 跨實例落地 webhook_idempotency 表）屬整合層 follow-up（reply_token 時效衝突需設計）；媒體下載餵多模態屬工具白名單變更需 CIA。

- **CR-0075/0076 TI-A12-01/A03-04/A04-02/A03-03 AI 治理測試批次（branch `feat/cr-0075-agent-governance-tests`，2026-06-20）**：**CR-0075（api）** A12-01 治理 trace（`ai_governance_trace_service` log_decision/list_traces/get_trace_summary 改打真 saas.ai_decision_trace，驗 prd_source+charter_rule+owner_decision_ref 三軸 provenance + filter + 聚合 + 非法 decision_type 422）+ A03-04 對話後台 list envelope。**CR-0076（agent）** 新增 `agent/scripts/governance_checks.py`：A04-02 `scan_references_sourcing`（掃 45 references 185 URL，驗 bronze 可溯 + GDrive link-only，**實跑 0 violation 確認 bronze-only 合規**）+ A03-03 `compute_agent_kpi_gate`（四指標門檻純邏輯 auto-resolve≥0.70/eval≥0.85/esc≥0.95/p95≤8000ms）。test_cr_0075 5/5 + test_cr_0076 8/8 + 回歸 api 886 / agent 85 passed。**揪出假綠（待 CIA）**：A12 trace service 完整但 lockcore runtime 從未呼叫 log_decision（saas.ai_decision_trace row=0），「每個 AI 行為可追溯」端到端契約未成立 — 接線屬 agent 核心架構變更需 CIA。

- **CR-0074 TI-M04-04/A05-01/A05-02 AI 安全紅線結構守線測試（branch `feat/cr-0074-agent-redline-tests`，2026-06-20）**：紅線（no final quote/no discount/NTD 不複誦→transfer_to_human）原無 pytest。新增 `agent/tests/test_cr_0074_redline.py` 6 結構測試（不需 live LLM）：SKILL.md 決策樹存在、transfer_to_human 在白名單且白名單最小（無寫入/執行工具）、轉接關鍵字涵蓋金錢+人工、tool description 帶紅線指引、報價偵測 regex 有效、**_PRICE_RE 單一事實來源（兩處定義一致防 drift 假綠）**。test_cr_0074 6/6 + agent 全套 77 passed。**註**：真正行為守線（agent 收詢價真觸發 transfer + 不吐 NTD）需 live LLM 跑 scripts/redline_gate.py 9 案例（A05-01 Alpha），不在 pytest；TI-A05-02 inline output guardrail（NTD 偵測→regen）lockcore 內未實作，屬 agent 核心改動待另開 CR；_PRICE_RE 抽共用模組屬 architecture change 待 CIA。

- **CR-0073 TI-FIN-SETTLE-04 結算費率 config 版本釘選（branch `feat/cr-0073-settlement-config-versioning`，2026-06-20）**：結算金額所套費率 config 版本原無釘選，事後改費率無法回溯。**migration 072** saas.settlement +applied_config_version_id（FK config_version）+rate_effective_date。新增 `config_m18_service.resolve_settlement_rate_version`（包 get_active_version）；co_sign_reconciliation 與月結批次 generate_monthly_batch 建 settlement 時釘選當下 active 結算費率版本（best-effort 缺則 NULL，不阻斷結算），釘選後不隨後續 config 變更漂移。test_cr_0073 3/3 + 回歸 881 passed 0 fail。**註**：只做 versioning wiring，不遷移 80/20 計算來源（屬 CIA 級）；monthly_settlement dispute 判定用 event_type LIKE '%recon%' 脆弱關聯為另一假綠風險，建議另開 CR。

- **CR-0072 TI-NOTIF-03 通知模板主管核准 gate（branch `feat/cr-0072-notif-template-approval`，2026-06-20）**：對客通知文案（報價/付款/派工/延遲/完工/RMA/退款）原無模板、無核准 gate（push_notification 直發）。**migration 071** notification_template 表（pending_approval→approved + notif_template_four_eyes CHECK）+ notifications +template_id + seed 7 類。新增 `notification_template_service`：create（強制 pending_approval）/ approve（APPROVER_ROLES 主管 + 四眼建立者≠核准者 + 防重複 409）/ send_from_template（僅 approved 可發，否則 TEMPLATE_NOT_APPROVED 409）/ list（角色可見性分層 internal/accounting 不對一般 staff 揭露）。test_cr_0072 9/9 + 回歸 878 passed 0 fail。**揪出假綠（已分流非破壞性處理）**：CR-0062 `_auto_notify` 在完工/派工事件直發通知完全無 gate，違反 BR-M16-03；判定其為「內部員工事件 alert」非對客 templated 訊息，維持直發、gate 只套對客 send_from_template 路徑（避免破壞 CR-0062 立即通知行為）。

- **CR-0071 TI-RBAC-02 role 指派雙人 SoD（branch `feat/cr-0071-rbac-sod-role-assign`，2026-06-20）**：使用者 role 指派/變更原**零生產碼**（grep `UPDATE users SET role` 只在測試命中，系統只有 role→permission 矩陣端點 — CR-0038 類假綠）。**migration 070** saas.role_assignment 提案表 + `role_assign_dual_sign_distinct` CHECK（proposed_by<>approved_by，DB 硬防）。新增 `role_assignment_service`：propose→approve→apply 三段，SoD 核心 approver≠proposer（403 SOD_VIOLATION_RBAC，service+DB 雙重防線）+ 階層強制（重用 role_service.can_grant）+ 樂觀鎖。apply 為**首個生產 role 指派路徑**（UPDATE users.role）。test_cr_0071 8/8（含 DB CHECK 硬防驗證）+ 回歸 869 passed 0 fail。**註**：FR-0019 AC-02 撤銷後 session invalidate（連動 token 撤銷）屬 architecture boundary 待 CIA；SCD2 role history 表待業主裁決。

- **CR-0070 金流 mock 骨架 — TI-FIN-PAY-01~05（branch `feat/cr-0070-payment-mock-skeleton`，2026-06-20）**：金流核心 payment 表/服務原為 0（AR/AP/佣金/月結全等不存在的 payment 表）。依會議決議5 mock-first 授權建骨架。**migration 069** payments 表（三軌 cash/apple_pay/line_pay + idempotency_key 防重複 intent + provider_txn_id 防重複 webhook + fallback_from + is_mock）+ 註冊 payment_gate config namespace。新增 `payment_service`：**PAY-02** create_payment_intent（三軌+冪等）、confirm_payment、**PAY-03** handle_linepay_webhook（HMAC-SHA256 驗簽 + provider_txn_id 冪等，重複 webhook 不重複認款）、**PAY-04** record_payment_fallback（line_pay 失敗→cash 兩次嘗試 audit）、**PAY-05** report_cash_dispute（差額≥門檻 flag + payment disputed + disputes 表 24h SLA）、**PAY-01** assert_payment_gate（config 開啟時派工前須 confirmed 付款/訂金）。test_cr_0070 5/5 + 回歸全套 861 passed 0 fail。**註**：正式 provider（真 Line Pay/Apple Pay 金鑰串接）由 Sunny 下輪（決議6）替換，schema 不變。TI-FIN-BOM-03（兩層 BOM）/ TI-FIN-SETTLE-04（結算 config versioning）尚未含。

- **CR-0069 測試計畫覆蓋 Batch 7 — TI-M02-05 device serial 唯一識別（branch `feat/cr-0069-coverage-batch7-device-serial`，2026-06-20）**：ADR-0053 device serial 為 warranty/RMA 唯一識別，但 warranty_claims 原僅 customer+brand+model 識別，無法區分同型號不同實體鎖。**migration 068** warranty_claims +device_serial；`create_warranty_claim` 接收落庫；`check_rma_abuse` 加 device_serial 參數做 serial 級偵測（同型號不同 serial 不互相累計，回 scope=serial/model）。test_cr_0069 2/2 + 回歸 856 passed 0 fail。**註**：spec 提及的 POST /devices device master 端點目前不存在（device 主檔歸屬 M02/M10 待業主裁決），本批以 warranty 域 serial 欄落地 serial-as-identifier 語意。

- **CR-0068 測試計畫覆蓋 Batch 6 — TI-AUDIT-03 audit log hash chain（合規紅線）（branch `feat/cr-0068-coverage-batch6-rbac-audit`，2026-06-20）**：audit_events 原 append-only 但無篡改偵測。**migration 067** +prev_hash +entry_hash；`audit_log_service.log_event`/`log_event_returning_id` 寫入時計 `entry_hash=sha256(prev_hash + 正規化內容)` 接前列 entry_hash；新增 `verify_audit_chain()` 走鏈偵測（內容竄改→重算 entry_hash 不符；列被刪/插→prev_hash 鏈接斷）。test_cr_0068 3/3（鏈接正確/篡改偵測/API 結構）+ 回歸全套 854 passed 0 fail（影響所有記 audit 的測試，0 破壞）。**註**：同批 TI-RBAC-02（SoD role-assign）與 TI-NOTIF-03（通知模板核准）經查為未建子系統（無 role-assign 治理流程、無 notification template 表），留待專屬子系統 batch。

- **CR-0067 測試計畫覆蓋 Batch 5 — FALSE_GAP 確認 + M09 evidence + 對帳 +1 假綠修（branch `feat/cr-0067-coverage-batch5-falsegap`，2026-06-20）**：**TI-M09-02** Evidence visibility RBAC 矩陣測試（品牌不看環境照/會計不看門檢/內部全看，功能 CR-0040 已在補測）。**TI-M09-03** Evidence retention + legal_hold：**migration 066** media_files +legal_hold，`soft_delete_expired_media` 補 `AND legal_hold IS NOT TRUE`（過期但法務保留不刪）。**TI-FIN-RECON-02** 對帳異常三 fix_path 狀態機端到端測試（detect→propose→approve(SoD 403)→apply，invoice_supplement/voucher_reverse 缺 ref 422）。**TI-FIN-AP-03** statement 自動核准排除 disputed 測試。**TI-BI-05** Revenue 確認已由 CR-0063 覆蓋（FALSE_GAP）。**🔴 假綠修**：`reconciliation_exception` 轉換表只允許 `ops_review→fix_proposed` 卻無任何函式能 `detected→ops_review`，導致 detect_exception 產出的 detected 列永遠無法 propose_fix（恆 409，整條修正流程卡死）→ 補 `detected→fix_proposed`。test_cr_0067 7/7 + 回歸 851 passed 0 fail。

### Changed

- **帳務 revenue 頁三個死/假控制清除（granularity 分段 + 日期下拉 + 假 Excel）（branch `fix/accounting-revenue-dead-controls`，2026-06-29）**：承上一支警語實測，把 revenue 頁揭露但未處理的死/假控制清掉。**① 日/週/月粒度分段**：後端 `/reports/revenue` 忽略 granularity（三粒度回傳 trend 完全相同、恆為月）→ 整個分段移除、granularity 固定 `"month"`。**② 日期範圍下拉（最近30/90/180天/今年）**：`periodPreset` state 根本沒進 API query（只送 granularity）＝死控制 → 移除（連同 state）。**③ Excel 匯出按鈕**：傳 `ext="xlsx"` 卻仍輸出 CSV 內容只改副檔名（假 xlsx，Excel 開啟異常）→ 移除，保留可用的客戶端 CSV 匯出（趨勢資料）。順手修 CSV 鈕 `hover:bg-white` → `hover:bg-[var(--bg-surface)]`（深色）。**保留**：`ServiceTypeChart` 為硬編示意但已標「示意（待 invoice category 欄位上線）」屬誠實 placeholder，續留。revenue 警語再收斂為「KPI／月度趨勢／品牌占比即時、趨勢可匯 CSV；服務類型分佈為示意」（zh+en）。純前端、無 contract/schema 變動屬 CIA 豁免。`tsc --noEmit` 0 + JSON 合法 + docker web 重建 + **playwright 實測**（粒度分段/日期下拉/Excel 皆消失、CSV 留、banner 更新、頁面 select 數=0）。**已部署本機 docker web**。

- **帳務 invoices/revenue 警語收斂為實測精準版 + 修發票狀態篩選 422（branch `fix/accounting-banners-accuracy`，2026-06-29）**：業主問兩條「即時資料 vs 待接入」警語的用意，實測後發現 invoices 警語**大幅過期**、且揪出真 bug。**實證（瀏覽器 token 直打端點對照 DB）**：invoices 的 搜尋（keyword→篩）/付款方式（cash→7、credit_card→14）/日期（created_after→4）/狀態 pending（→5 草稿）**全部真的篩**，只有「僅顯示逾期」真未接（且本來就是 disabled coming-soon toggle）；revenue 的 granularity **後端忽略**（day/week/month 回傳 trend 完全相同、恆為月）、`by_service_type` 無欄位、CSV 匯出客戶端可用、Excel 為假 xlsx（CSV 內容改副檔名）。**🔴 修 bug**：invoices 狀態下拉的 `已付款(paid)`/`逾期(overdue)` 兩選項送出即 **422**（非後端 InvoiceStatus 白名單值；API 無法區分 paid vs issued、overdue 非狀態）→ 改為合法值 `pending(草稿)`/`issued(已開立)`/`voided(已作廢)`（實測三值皆 200：5/53/2）。**警語收斂**：invoices 改「表格與搜尋／狀態／付款方式／日期範圍篩選皆為即時資料；僅顯示逾期 coming soon」；revenue 改「KPI／月度趨勢／品牌占比即時、趨勢可匯 CSV；服務類型分佈／真 Excel／日週粒度與日期範圍待後端（粒度恆為月）」（zh+en）。純前端、無 contract/schema 變動屬 CIA 豁免。`tsc --noEmit` 0 + JSON 合法 + docker web 重建 + **playwright 實測**（狀態 3 值皆 200 無 422、banner 更新）。**已部署本機 docker web**。**未處理（已誠實揭露於警語，offer 後續清）**：revenue 日/週粒度 segment 為死控制（後端忽略 granularity）、Excel 按鈕產假 xlsx。

- **帳務頁兩個死控制 + 假通知紅點 + i18n/色彩修正（branch `fix/accounting-dead-controls`，2026-06-29）**：盤點 `/accounting` 揪出「看起來能用、其實沒接後端」的假 UI（表格資料本身真實，問題在控制項與通知）。**① 期間下拉是死的 + 值對不上**：下拉（最近 3/6 個月/全部）會改 `periodFilter` state，但 `fetchSettlements` query 寫死 `?limit=50`、**完全沒傳 period_filter** → 選了沒效果；且 UI 給「最近 6 個月」但後端只有 3/12 個月。修：對齊後端值（`last_3_months`/`last_12_months`/`all`）+ 真的把 `period_filter` 帶進 API + `fetchSettlements` 加 `periodFilter` 依賴（實測切換 最近3個月 17 筆 → 所有期間 25 筆、network 確實送 period_filter）。**② 結算週期分段（月/雙週/週）純裝飾**：`settlementCycle` state 沒進任何 API 呼叫，且後端僅有月結（`generate_monthly_batch`）、無雙週/週結概念 → 整個分段控制移除（連同未用 state/SEGMENTS）。**③ 發票分頁紅點寫死 `dot:true`**：恆亮、不反映真實狀態 → 改接 `GET /accounting/invoices?status=pending&limit=1`（後端 pending→DB draft 草稿），有待處理發票才亮、無則自動消失。**④ i18n**：期間選項/「觸發本月月結」「處理中…」按鈕/「已觸發 X 年 X 月月結」訊息原硬編中文 → 走 i18n（zh/en）。**⑤ 色彩/深色**：連線徽章 inline hex（#FEE2E2/#DCFCE7/#B91C1C/#22C55E）→ badge/success/error CSS 變數；核可 Modal+取消鈕 `bg-white`、月結鈕 `hover:bg-[#1D4ED8]`、tab 紅點 `bg-[#EF4444]` → token 化（深色適配）。純前端、無 contract/schema 變動屬 CIA 豁免。`tsc --noEmit` 0 + JSON 合法 + docker web 重建 + **playwright 實測**（期間切換真改資料 17→25 並送 period_filter；週期分段消失；紅點反映 5 筆草稿；按鈕/選項 i18n）。**已部署本機 docker web**。

- **客戶詳情頁深色模式白卡 + 對話 enum 未翻譯 + KPI 色硬編修正（branch `fix/customer-detail-dark-i18n`，2026-06-29）**：盤點 `/admin/customers/[id]` 揪出 3 類「讀 code 推不出來」的硬編（**此頁資料本身全真、無假造**，問題在樣式/i18n）。**A（深色模式 bug）**：6 處用 Tailwind 關鍵字 `bg-white`（profile/各 KPI 卡/最近工單/對話 section/edit 鈕），而 `globals.css` 深色覆寫只認 `.bg-[#FFFFFF]` arbitrary 形式、**不認 `bg-white`** → 深色模式下這些卡維持白底配近白字＝看不清（與先前「技師端深色模式修正」同類）。改 `bg-white` → `bg-[var(--bg-surface)]`（淺色仍 #FFFFFF 不變、深色自動 #1E293B）。**B（i18n 缺口）**：「最近對話」的 `conv.status`／`conv.channel` 直接渲染後端原始 enum（raw `escalated`/`line`），隔壁工單狀態有 `statusLabel` 轉中文、對話卻漏 → 加 `convStatus`（active/escalated/closed）+ `convChannel`（line/web/phone）i18n + label 助手（未知值回退原值）。**C（色彩 token 化）**：7 張 KPI 卡圖示/數值用硬編 hex（#2563EB/#0E7490/#D97706/#DC2626/#475569/#B91C1C），其中中性 `#475569` 在深色底近乎不可見 → 全改語意 CSS 變數（`--primary`/`--info`/`--accent-hover`/`--error`/`--text-secondary`），隨主題自適配。純前端、無 contract/schema 變動屬 CIA 豁免。`tsc --noEmit` 0 + JSON 合法 + docker web 重建 + **playwright 實測**（淺色不變；深色模式 `main` 內白色元素=0、7 張 KPI 卡皆 #1E293B 深底、KPI 數值色解析為深色 `--primary` #3B82F6；對話 badge 顯示「已升級／LINE」無 raw enum）。**已部署本機 docker web**。

- **客戶主檔頁捲軸失效（內容被壓扁裁切）修正（branch `fix/customers-scroll`，2026-06-29）**：業主回報 `/admin/customers` 捲軸失效、看不到下半部客戶。**根因**：捲動容器寫成 `flex flex-1 flex-col gap-5 overflow-auto`（**flex column**），其 flex 子層預設 `flex-shrink:1`，當內容總高（20 列 ~1640px）超過容器（視窗 773px）時，子層被**壓縮**塞進視窗——實測表格從 ~1333px 被壓成 466px，再被表格自身 `overflow-hidden` 裁切 → 下半部客戶看不到、`overflow-auto` 也無溢出可捲（canScroll=false）。reseed 後客戶增至 50 筆才使這個 latent bug 浮現（早期資料少、內容未超視窗故未顯）。對照正常捲動的 quotes 頁，其捲動容器是純 block（`flex-1 overflow-auto`）非 flex column。**修**：捲動容器去掉 `flex flex-col`、`gap-5`→`space-y-5`（保留 `flex-1` 填滿高度），子層改回自然堆疊→溢出觸發捲動。純前端、無 contract/schema 變動屬 CIA 豁免。`tsc --noEmit` 0 + docker web 重建 + **playwright 實測**（修前 scrollH=clientH=773 canScroll=false、表格壓成 466；修後 scrollH=1640 canScroll=true、表格恢復 1333、捲到底 reachedBottom=true）。**已部署本機 docker web**。

- **技師停權/復權原因過短露出語意不明的 VALIDATION_ERROR (422) 修正（branch `fix/technician-suspend-reason-minlen`，2026-06-29）**：業主在 `/technicians/{id}` 點停權後得 `停權失敗：VALIDATION_ERROR (422)`。**根因**：後端 `technician_lifecycle_service._change_status_and_audit` 要求 `reason` strip 後 **≥3 字元**（line 92-93），但前端 `handleLifecycle` 只擋空白（`!reason.trim()`）未擋最小長度 → 使用者打 1–2 字原因（如「壞」）通過前端、被後端打回 422；且 catch 只顯示 `errorCode (status)`（裸「VALIDATION_ERROR (422)」）沒帶後端 detail，使用者不知問題在「原因太短」。該技師本身 `active`（轉移合法），與狀態機無關。**修**（純前端對齊既有後端契約）：① prompt 文案加「（至少 3 個字）」事前告知；② 送出前驗 `reason.trim().length < 3` → 顯示「停權/復權原因至少需 3 個字」並 return、不送出；③ catch 改顯示 `e.message`（後端友善 detail，如「reason 至少 3 字元」）比裸 `errorCode` 更可行動。純前端、無 contract/schema 變動屬 CIA 豁免。`tsc --noEmit` 0 + docker web 重建 + **playwright 實測**（1 字「壞」被前端擋下不送網路 + 友善提示、無 422；合法原因 `POST :suspend` 200 → 按鈕變「復權」；`:reactivate` 200 還原 active；測試 audit 已清、技師還原）。**已部署本機 docker web**。

- **技師管理頁 inactive 狀態露出原始 i18n key 修正（branch `fix/technicians-inactive-i18n`，2026-06-29）**：業主操作 `/technicians` 時某筆技師徽章顯示原始字串 `components.technicians.table.onboardStatus.inactive`。**根因**：技師生命週期狀態合法集合為 `pending_approval/active/inactive/suspended/rejected/terminated`（見 `SQL/migrations/020-tech-lifecycle.sql` 註解 + `technician_lifecycle_service._ALLOWED_TRANSITIONS`，DB 實有 1 筆 `inactive`），但前端 i18n `onboardStatus` 與徽章顏色表 `ONBOARD_TONE` 都只列 5 值、**漏 `inactive`** → `t('onboardStatus.inactive')` 找不到鍵回退原 key（顏色有 `FALLBACK_BRAND` 故只露字、未壞版）。附帶 doc drift：OpenAPI `Technician.status` 描述註解亦只列 5 值漏 `inactive`（`status` 為 `string|null` 未強制 enum 故不阻擋資料，僅描述過期 vs migration 020）。**修**：① i18n zh-TW（停用）/ en（Inactive）補 `inactive` —— `inactive` 為非懲罰性「停用」（可重新啟用回 active，異於懲罰性 `suspended` 停權）；② `ONBOARD_TONE` 補 `inactive` 中性灰調（#475569/#F1F5F9）與 suspended 的 amber 區隔，並註明合法集合來源（杜絕下次再漏）。純前端、無 contract/schema 變動屬 CIA 豁免。`tsc --noEmit` 0 + JSON 合法 + docker web 重建 + **playwright 實測**（該筆即顯示「停用」徽章、原始 key 消失）。**已部署本機 docker web**。

- **報價單頁刪除草稿後不即時刷新（需手動重整）修正（branch `fix/quotes-list-stale-refresh`，2026-06-29）**：業主實測 `/admin/quotes` 刪除某草稿後列表不馬上更新。**根因**：與 `fix/intake-cases-status-refresh`（cases 頁）同一類——`deleteQuote`／`createQuote`／`transition` 三個 mutation 後都呼叫 `fetchQuotes()` 重抓列表，但 `GET /quotes` 是 cacheable（30s staleTime），mutation 後**未清 GET 快取** → `fetchQuotes` 在 staleTime 內讀回「仍含剛刪那筆」的舊清單（要等 30s 或瀏覽器重整清掉記憶體快取才更新）。**修**：① `deleteQuote` 加樂觀更新（點擊即從本地 `quotes` 移除該筆）→ 即時反映；② 三個 mutation 在 `fetchQuotes()` 前都補 `cacheInvalidate("GET:")`（廣域清；cache key 為 `GET:${完整URL}:${tenant}` 含 BASE_URL host 故不可用 path-prefix，與各頁慣例一致）→ 重抓拿到後端真實列表（新草稿出現、狀態變更反映、刪除消失）；③ 修正 `cache.ts` `cacheInvalidate` docblock 的誤導範例（標明 key 含完整 URL、勿用 path-prefix，比照 `fix/intake-cases-status-refresh` 修 `api.ts` docblock）杜絕復發。純前端、無 contract/schema 變動屬 CIA 豁免。`tsc --noEmit` 0 + `next build` 0 + docker web 重建 + **playwright 實測**（建草稿→刪除→列表即時更新、KH-000011 即時回到「尚無報價的工單」23→24；network 確認 DELETE 後有 fresh `GET /quotes`、非讀 30s 舊快取）。**已部署本機 docker web**。

- **進線案件頁 UX 加強 — SLA 倒數/逾時顯示 + 狀態配色 + 篩選列（branch `feat/intake-cases-sla-filter`，2026-06-29）**：業主要求 `/admin/cases` 三項好用度加強。**① SLA 欄**：原只顯示「待回應/已回應/逾時」單詞 → 改顯示**首次回應 SLA 剩餘/逾時時長**（綠=充裕、琥珀=≤30 分即將到期、紅=逾時，皆附「X 小時 Y 分」），衍生自 `first_response_due_at`。**② 狀態配色**：原三狀態同一靛色徽章難分辨 → 待回應=琥珀、處理中=藍、已結案=灰，一眼可辨。**③ 篩選列**：原無任何篩選 → 加狀態／渠道下拉 + 「只看 SLA 逾時」勾選 + 清除鈕，header 顯「篩出 N 件」。client 端篩選即時（列表改撈 `?limit=200` 一次取足；「逾時」為衍生旗標無 DB 欄故 client 端篩，狀態/渠道用後端值）。純前端、無 contract/schema 變動屬 CIA 豁免。`tsc --noEmit` 0 + `next build` 0 + docker web 重建（/admin/cases 200）。**已部署本機 docker web**。

- **進線案件頁操作後狀態不即時更新（需手動重整）修正（branch `fix/intake-cases-status-refresh`，2026-06-29）**：業主實測 `/admin/cases` 點「標記處理中／結案」後，狀態要按瀏覽器重整才更新。**根因**：`updateStatus`/`create` 用 `cacheInvalidate(\`GET:${tenantPath("/cases")}\`)` 清 30s GET 快取，但 cache key 實為 `GET:${完整URL}:${tenant}`（含 BASE_URL host，如 `GET:http://localhost:8001/tenants/…`），而 `tenantPath` 只給 path-prefix（`GET:/tenants/…`），`cacheInvalidate` 的 `startsWith` 永遠對不上 → 快取沒清 → 後續 `load()` 在 staleTime 內讀回舊清單（要等 30s 或瀏覽器重整清掉記憶體快取才更新）。**此頁是全站唯一誤用細粒度 prefix 者**（technicians/cert/penalty 頁都用廣域 `"GET:"` 故無此病），源於 `api.ts` docblock 的誤導範例。**修**：① `updateStatus` 改樂觀更新（點擊即改本地 state、含 open→in_progress 補記 `first_responded_at`，PATCH 失敗才 `load()` 回滾）→ 即時反映；② 兩處 `cacheInvalidate` 改廣域 `"GET:"`（與各頁慣例一致）→ 重掛載/他處讀取也拿到新資料；③ 修正 `api.ts` 兩處誤導 docblock（標明 cache key 含完整 URL、勿用 path-prefix）杜絕復發。純前端、無 contract/schema 變動屬 CIA 豁免。`tsc --noEmit` 0 + `next build` 0 + docker web 重建驗證（/admin/cases 200）。**已部署本機 docker web**。

- **前端硬編收斂 — 品牌清單單一來源 + tenantId fallback helper + customers 篩選 i18n/技師下拉（branch `fix/admin-hardcode-cleanup`，2026-06-28）**：盤點 `/admin/customers` 頁時揪出多處「讀 code 推不出來」的硬編漂移。**① 品牌清單散落 5 套且互不一致** —— customers（Yale/Dormakaba/Samsung/Other）、manuals（含 `美樂`）、cases（含 `Milre`、AiLock、Waferlock）、register、technicians 各自硬編一份，連同一品牌的 value 都不一致（`美樂` vs `Milre`）→ 新增 `web/src/lib/constants/brands.ts`（`LOCK_BRANDS` 單一來源，正典核心取自產品知識 references bronze 來源 `3E/Chatlock/Dormakaba/Kaadas/Milre/Philips` + 保留既有市場品牌 Yale/Samsung，附 source 註解），customers 下拉與 register/technicians[id]/CreateTechnicianModal 三處 placeholder 改引之。**② tenantId fallback 魔術 UUID 散落 11 處** —— 各頁 inline 重抄 `session?.tenantId ?? "00000000-…-0001"`（conversations 兩檔另有本地重複常數）→ `api.ts` 抽 `FALLBACK_TENANT_ID` 常數 + `resolveTenantId()` helper（來源語意對齊原 caller 的 JWT claim，非 localStorage），全數收斂，殘留僅 api.ts 常數定義一處。**③ customers 篩選兩缺口** —— 風險（低/中/高/緊急）、保固（保固中/已過期/無保固）option label 硬編中文未走 i18n → 改 `t()`（送後端的 enum value 不動）；偏好技師從「手打技師 UUID 文字框」→ 改技師下拉（fetch `/technicians` 顯姓名+電話）。**刻意延後（已就地註解、不腦補業務規則）**：KB manuals/cases 品牌 filter 綁定後端**髒資料**（`dormakaba` 205 vs `Dormakaba` 173、`美樂`/`Milre`、未列入下拉的 鎖市/小島/Chainlock）→ 統一 value 會打到搜尋結果，屬資料正規化另開 CR；tenantId 無 session 時行為（靜默退回 1 號租戶 vs 擋下導回登入）→ 跨 12 頁面安全決策待業主。純前端、無 contract/schema/flow 變動屬 CIA 豁免（code-wins 收斂）。`tsc --noEmit` 0 + 4 面向對抗式驗證 workflow（contract 安全/i18n 完整/React 正確性 PASS；行為回歸 3 非阻塞，conversations 殘留 session 收斂 + tenantPath 註解已處理）。**待部署 web**。

- **工單詳情頁寫死假資料盤點 + 真實化（A 純前端 + B 後端欄位，CR-0100，2026-06-25）**：業主盤點 admin `/work-orders/[id]`「哪些是寫死的」（如「進度」）。**31-agent 對抗式驗證** workflow（盤點報告 `docs/_audit/wo-detail-hardcoded-audit-20260625.md`）：13 區塊中真實 8、寫死/假 5。**🔴 頭號假資料**：`SlaTimeline`（每張單固定 09:00→剩 02:15→進度 60%，與真實狀態無關）、`CompletionReport`（假功能測試「指紋✓密碼✓電池✗」每張單恆同）。**A 類（純前端，資料源已存在，branch `fix/wo-detail-real-data`）**：SlaTimeline 改接 `order.status`+真實時間戳（依生命週期 rank 推導節點/進度條）；CompletionReport 接 order（未完工空狀態、已完工真實時間/狀態/金額）；ExceptionRecords 接 `GET /exception-cases?work_order_id=`（後端 list 加 work_order_id query）；免責橫幅同步。**B 類（後端新欄位，CIA CR-0100 業主裁決，branch `feat/wo-detail-b-fields`）**：migration 079 work_orders +completion_summary +function_tests jsonb + config `sla_policy`（三級 high8h/medium24h/low48h，sla_deadline computed）；onsite/completion 端點接 function_tests（pass/fail/na 選填、不擋硬閘）；技師端完工表單加功能測試 6 項勾選；admin SlaTimeline 加真實 SLA 倒數/逾時、CompletionReport 顯示真實摘要+功能測試。test_cr_0100 8 passed + 回歸 91 passed；E2E 驗證（SLA 剩餘 23h57m / 完工摘要 / 功能測試失敗·不適用）。**部署**：api migration 079 + api + web。B3 設備面板 IoT 遙測 out-of-scope（已誠實標「示意」）。

- **完工簽名成功路徑 500「Load failed」→ 回應包進 data 對齊信封（branch `fix/signature-response-500`，2026-06-25）**：業主完工提交雙簽名後瀏覽器跳「Load failed」。**根因**：`submit_work_order_signature`（v1）與 `_v2` 都宣告 `response_model=ApiResponseGeneric`（要求 `data` 欄），卻直接 `return result`，而 `signature_service` 回的是 `{success, message, request_id}`（**無 `data`**）→ **成功路徑**（雙方簽名、寫 2 列）在 FastAPI `serialize_response` 炸 `ResponseValidationError` → 500；錯誤冒到 CORS middleware 時回應壞掉、瀏覽器顯示「Load failed」（fetch 網路層失敗，非 4xx）。簽名其實**已寫進 `digital_signatures`、只是回應失敗** → 重打變 409（走 error 路徑、不過 response_model，不炸）；故 `curl`（不帶 Origin）看 409、瀏覽器（帶 Origin、觸發 CORS 回應路徑）看 Load failed。**🔴 假綠根源**：既有簽名測試全用假 WO id 只測 guard（idempotency/cross-tenant/422），**從未經 HTTP 層打成功路徑** → 此 500 長期漏網。**修**：兩 handler 改 `payload = {"data": result, "message": ...}` 再 save/return，對齊既有 `ApiResponseGeneric` 契約（實作回歸契約，非契約變更、免 CIA）。新增 `test_signature_v2_envelope_regression`（DB-free 守門：裸 result 違反信封 + handler 必須包 data + 靜態擋未來改回 `return result`）。real curl 帶 Origin 驗證 200 + `{"data":...}` + 正常 ACAO header；相關套件 117 passed 0 fail。**部署**：api（前端無需改）。**教訓**：endpoint 成功路徑若無 HTTP 層測試，response_model 違規會以 500 潛伏。

- **完工／門面照片「點了沒反應」→ 移除 `capture="environment"`（branch `fix/completion-photo-capture`，2026-06-25）**：業主測完工上傳施工照時「點選了上傳施工照沒有反應」。**重現確認（playwright，真技師帳號）**：點擊其實會正常彈出 file chooser、後端 `/media` 三槽（before/during/after）上傳全通、purpose 白名單對齊（測試 3/3、零錯誤）——**功能本身無誤**。**根因**：file input 帶 `capture="environment"` 強制走相機；**桌機 Chrome 手機模擬模式無相機可開 → 點擊毫無反應**（業主測試情境），真手機則被鎖成只能即拍、**無法從相簿選現成照片**（技師多為先拍好再上傳）。**修**（純前端）：移除 `my-orders/[id]` 完工表單 3 槽與 `door-check` 2 槽的 `capture`，改用標準檔案選擇器（手機仍可選「拍照或相簿」）。保留 `accept="image/*"` 過濾。**部署**：web。

- **登入過期 → 後台白屏 → API 層統一導回登入頁（branch `fix/session-expired-redirect`，2026-06-25）**：業主隔夜回來開 `/dashboard` 整頁白屏。playwright 重現確認:**非 code crash**——6 個錯誤全是 `401 Unauthorized`(dashboard/stats、work-orders、technicians、notifications),token 過期。**根因**:`AuthGuard` 只驗「access token 是否存在」**不驗是否過期**,隔夜後過期 token 仍在 localStorage → guard 放行頁面 → 頁面拿過期 token 一路 401、refresh 也失敗(refresh token 同過期)→ `rawRequest` 等 4 條 fetch 路徑只丟 `ApiError(401)`、**沒導登入頁** → 白屏。（dashboard 對空資料本身處理正常,已驗:清空後 KPI 全 0、圖表、「目前沒有工單」皆正常 render。）**修**(純前端):api.ts 新增 `handleSessionExpired()`(清 token + 依當前路徑導對應 portal 登入頁:技師 /tech-login、廠商 /vendor-login、其餘 /login;並發 401 只導一次),在 `rawRequest`／`uploadMultipart`／`downloadBlob`／`downloadBlobPost` 四處「refresh 後仍 401」時呼叫。AuthGuard 不動(避免誤殺 access 過期但 refresh 仍有效的情況——交由 API 層 reactive 處理)。tsc 0。**部署**:web。

- **完工重送遇「已簽署」409 卡死 → 簽名步驟冪等化（branch `fix/completion-resign-409`，2026-06-24）**：內嵌雙簽名板上線後，業主結案遇 `STATE_CONFLICT (409)：Work order already fully signed`。**根因**：`submitCompletion` 每次都無條件先 `POST /signature`，但 `submit_work_order_signature` 在「客戶+技師皆已簽」時回 409（`signature_service.py:118`）——一旦前次嘗試已建好簽名（或先走過 /signature 頁），重送完工就被這個 409 擋在第一步、永遠到不了 /onsite/completion。**修**（純前端）：`submitCompletion` 的 `POST /signature` 包 try/catch，**409 視為「簽名已存在」→ 吞掉、續送 /onsite/completion**（完工硬閘 `_signature_exists` 本就認既有紀錄）；非 409 才拋出。tsc 0。**部署**：web。

- **技師完工簽名機制打架 → 內嵌雙簽名板（branch `fix/completion-inline-signature`，2026-06-23）**：業主完工提交得 `SIGNATURE_REQUIRED (422)：簽名紀錄不存在`。**根因**：完工頁與後端硬閘用**兩套不相容的簽名機制**——完工頁內嵌「上傳簽名圖到 `/media`」（拿 media id 當 `signature_evidence_id`），但後端 `_enforce_completion_gate` 的 `_signature_exists(wo_id)` 認的是 `digital_signatures` 表的 customer 簽名紀錄（CR-0039 HD-4「驗真紀錄非空字串」），而該紀錄只由 `POST /work-orders/{id}/signature`（`/signature` 頁雙 canvas 手寫）建立。且 `signature_evidence_id` 後端僅寫進稽核 summary、**不驗證**（`work_orders_v2.py:794`）。兩條路無法同時滿足：走內嵌上傳→按鈕亮但 gate 打槍；走 /signature 頁→建了紀錄但完工頁按鈕（gating 綁內嵌 state）不亮。**修**（純前端，後端不動）：抽 `/signature` 頁的 `SignaturePad` 為共用元件 `components/tech/SignaturePad.tsx`；完工頁移除「上傳簽名圖」假機制，**內嵌技師＋客戶雙簽名板**，提交時先 `POST /signature` 建 digital_signatures、再 `POST /onsite/completion`（signature_evidence_id 傳標記）；按鈕 gating 改為「照片≥3 且 雙簽名皆 >100」。`/signature` 頁改用共用元件、行為不變。tsc 0。**部署**：web。**架構觀察**：完工簽名同時存在 /media 上傳與 /signature 兩套是反模式，本次收斂到 digital_signatures 單一真相源。

- **技師完工照片只能傳 2 張、達不到後端 ≥3 硬閘（branch `fix/completion-during-photo-slot`，2026-06-23）**：業主測完工時「前端只讓我傳一張」，提交鈕一直 disabled。**根因**：後端 `_enforce_completion_gate` 要求 `min_photos=3`（CR-0039 / PDF §四 施工前/中/後三類），但完工頁照片 UI 只有「施工前 / 施工後」**兩格**（`my-orders/[id] grid-cols-2`），缺「施工中」格 → 最多 2 張 < 3，永遠湊不到。又一個「後端要求 + DB purpose（`completion_during` 由 CR-0054/migration 060 早加）齊全，但前端 UI 沒跟上」的缺口（與本日 CR-0099／completion_signature 同類）。**修**（純前端，後端早支援）：完工頁加「**施工中**」格（grid-cols-2→3），purpose 用 `completion_${section}` 對應 `completion_before/during/after`；state/型別補 `during`；i18n 加 `photoDuring`（施工前/中/後）並修 `photosLabel`（原寫「選填」誤導 → 改「至少 3 張」）。tsc 0。**部署**：web。前/中/後各一張即達 min 3，符合 PDF 三類存證設計。

- **技師完工簽名上傳 422 修正 — purpose 白名單 4 層對齊（branch `fix/completion-signature-purpose`，2026-06-23）**：業主用師傅帳號測完工上傳圖片時遇 `UNKNOWN (422)`。**根因**：`media_files.purpose` 白名單散在 **4 層**且不一致——router v2 `_PURPOSE` Literal、router v1 `_PURPOSE` Literal、`media_service._ALLOWED_PURPOSES`、DB CHECK `media_files_purpose_check`。前端 `my-orders/[id]` 完工簽名上傳送 `purpose='completion_signature'`，**四層全缺此值**：先撞 router Literal（FastAPI 驗證 422，回 `{detail:[...]}` 無 error_code → 前端顯示 UNKNOWN，正是業主看到的）；繞過後還會撞 service（VALIDATION_ERROR）與 DB CHECK（CheckViolation）。測試逐層揭露這三道牆。**順帶發現**：`completion_during`（CR-0054 / migration 060 施工中照片）只加進了 service + DB，**router Literal 漏加** → 一直被 router 擋著傳不上來（同類潛在 bug）。**修**：四層全部對齊為同一組 9 值（補 `completion_signature`，並把 `completion_during` 補進 router Literal + OpenAPI）：router media_v2.py / media.py、media_service._ALLOWED_PURPOSES、**migration 078**（DB CHECK，比照 060 DROP+ADD idempotent）、OpenAPI MediaPurpose enum。test_media_upload 加 `completion_signature` 接受測試（先前抓出 service + DB 兩層）→ media 全套 18 passed。**部署**：需 migration 078 套 prod（DB schema，比照 077 先備份）+ api 重部署；前端無需改（早已送正確值）。**教訓**：同一枚舉散在 4 層是反模式，未來應收斂為單一真相源。

- **技師端深色模式修正（接單畫面字看不見，branch `fix/tech-portal-dark-mode`，2026-06-23）**：業主用師傅帳號登入後反映「接單畫面 UI 根本看不清楚」，且技師端找不到切換顏色的地方。**根因**：globals.css 早有深色覆寫機制（`[data-theme="dark"] .bg-[#FFFFFF]{...!important}` 等），設計約定「硬寫表面色要用有覆寫保護的形式 / 一律用 CSS 變數」。但**技師端頁面用的是 Tailwind 關鍵字 `bg-white`**（不在覆寫名單內），逃出安全網 → 深色模式下卡片永遠白底、而文字用 `var(--text-primary)`（深色模式變近白）→ **近白字印白底＝幾乎看不見**；技師端又無主題切換鈕無法自救。**修**：把技師端 24 個檔（`pool`/`my-orders` 全流程/`account`/`home`/`components/tech/*` 含 TechShell/Sidebar/BottomNav）的中性硬寫色 token 化——`bg-white`→`bg-[var(--bg-surface)]`、`#F1F5F9`→surface-strong、`#EFF6FF`→primary-light、`#F8FAFC`→bg-page、`#1D4ED8`→primary-hover、`#1E293B`/`#64748B`/`#475569`/`#94A3B8`→對應 text 變數（122 處 1:1）。**保留**語意徽章（amber/blue/red/green 自帶 bg+text 對，雙模式皆可讀）與 `bg-white/15` 半透明疊層。淺色模式視覺幾乎不變（變數值≈原硬寫色），深色模式恢復可讀。tsc 0。純前端視覺修正、不涉契約/流程，免 CIA。

- **後台 sidebar 依「開單流程」重排 + 分組標題（branch `feat/sidebar-reorder`，2026-06-23）**：業主反映原 sidebar 在開單時不順手——開單流程（進線→問題卡→開單→報價→派工→完工結算）的選單被打散（報價單在第 10、客戶主檔第 12、知識庫卡在流程中段第 4）。重排為 4 分組：**開單流程**（儀表板/對話/問題卡/派工管理/報價單/技師/客戶/帳務結算）、**審核與例外**（廠商審核/異常）、**知識與報表**（知識庫/報表）、**設定與主檔**（報價主檔/拆帳規則/庫存/稽核權限/系統設定）。`navItems` 改 `navSections` 結構，render 加灰色分組標題（整組無可見項則連標題隱藏，沿用既有角色過濾）+ 中英 i18n（sidebar.section.* 4 key）。純前端、不動路由/權限,tsc 0。

- **CR-0067 reconciliation_exception 狀態機修正（🔴 latent bug）**：`_ALLOWED_TRANSITIONS["detected"]` 補 `fix_proposed`，使 propose_fix 與其 SQL（`status IN ('detected','ops_review')`）/docstring 一致。影響：對帳異常修正流程首次可從 detected 狀態真正運作（先前必 409）。

- **CR-0066 測試計畫覆蓋 Batch 4 — M07/M08 現場執行 4 項 +1 假綠 bug 修（branch `feat/cr-0066-coverage-batch4-m07m08`，2026-06-20）**：**TI-M07-03** 技師接單 `accept_order`（assigned→accepted+accepted_at；錯狀態 409）補 pytest。**TI-M08-01** GPS 到場 proof：新增純函式 `compute_arrival_gps_proof`（Haversine 距離 + 容忍半徑判定），`record_arrival` 接 gps.ref_lat/ref_lng 算 proof 落 event payload。**TI-M08-03** 簽名 fallback_method 稽核：`submit_work_order_signature` 加 fallback_method（liff/qr/paper，非法 422）落 signature_data。**TI-M08-04** scope change 客戶 30min 未回覆暫停旗標 cron `flag_timed_out_scope_changes`（pending 逾時標記 + audit，不自動拒絕）。**🔴 假綠 bug 修**：`signature_service` 原用 `wo.technician_id`（=technicians.id）當 `digital_signatures.signer_id`（FK→users.id），技師簽名必 FK violation；既有測試都手動 insert 簽名繞過從未測到。改取 `technicians.user_id`。test_cr_0066 5/5 + 回歸全套 844 passed 0 fail。

### Changed

- **CR-0066 signature_service signer 修正（🔴 latent bug）**：技師電子簽名的 signer_id 由 `wo.technician_id`（technicians 主鍵）改為 `technicians.user_id`，修正 FK→users 違反。影響：`submit_work_order_signature` 技師簽名路徑首次真正可用（先前任何真實呼叫都會 500）。

- **CR-0065 測試計畫覆蓋 Batch 3 — M03 ProblemCard 2 缺功能補實作+測試（branch `feat/cr-0065-coverage-batch3-m01m03`，2026-06-20）**：**TI-M03-06** ProblemCard 冪等鍵（**migration 065** +idempotency_key）：`escalation_to_draft_pc` 以 sha256(conv_id+症狀+brand) 為鍵 + 24h dedup 視窗，抵抗 DLQ/outbox retry 重複建卡（原僅 conversation_id UNIQUE）；新增純函式 `compute_pc_idempotency_key`。**TI-M03-07** media_urls append-only（Sync-M03）：`update_card` 原**覆蓋** media_urls（會掉先前證據照）→ 改聯集去重保序 append。test_cr_0065 3/3 + 回歸全套 839 passed 0 fail。

- **CR-0064 測試計畫覆蓋 Batch 2 — 3 個缺功能補實作+測試（branch `feat/cr-0064-coverage-batch2-funcs`，2026-06-20）**：測試計畫審計揪出 BUILD_FUNC_AND_TEST 項。本 batch 補 3 個原本「規範要求但未實作」功能：**TI-M05-02** 結案前服務地址必填硬閘（`work_order_service._enforce_completion_gate` 加 `ADDRESS_REQUIRED_FOR_CLOSE` 422）；**TI-M09-01** 媒體 sha256 去重（`media_service.upload_media` 同 WO 同檔二次上傳 idempotent 回既有 `deduplicated:true`，DB 不重複入庫）；**TI-RMA-04** RMA 濫用偵測（`warranty_service.check_rma_abuse` 同客戶同機種視窗內 ≥3 次 `abuse_flagged`）。test_cr_0064 3/3 + 回歸全套 836 passed 0 fail。附帶修 `test_reconciliations_v2` 脆弱分頁假設（seed 成長後預設 limit=20 擠出新 row → 改 limit=100）。

- **CR-0063 測試計畫覆蓋 Batch 1 — 6 個已存在功能補測試（branch `feat/cr-0063-coverage-batch1`，2026-06-20，merge 80e06539）**：審計揪出 BUILD_TEST_ONLY（功能在缺測試）。補 TI-M03-09 ProblemCard 匯出 / TI-FIN-REV-01·INV-02 Revenue date-range / TI-RES-01 resolution 階層 / TI-BI-04 Scheduled Reports / TI-FIN-SETTLE-05 五類 ledger 分表。test_cr_0063 8/8。

- **CR-0062 事件驅動通知自動生成（branch `feat/cr-0062-event-driven-notifications`，2026-06-20）**：審計 #6（最後一項）/ M16。通知中心原只能手動 push，生命週期事件未接通知產生。work_order_service 加 `_auto_notify`（best-effort，接 notification_service.push_notification）+ 接兩個關鍵事件：**派工→通知被指派技師**（technicians.user_id）、**完工→通知開單者**（created_by）「待審核結案」。test_cr_0062 1/1 + 回歸 0 fail。**審計 15 項待做 in-scope 全數閉環。**

### Added

- **CR-0061 媒合 GIS 距離 + 多維績效排序（branch `feat/cr-0061-gis-performance-matching`，2026-06-20）**：審計 #10 #11 / BR-M06 / BR-M07-03。媒合 distance 原示意值非真實、排序只用單一 rating。**migration 064** technicians +latitude/longitude/on_time_rate/acceptance_rate（mock seed active）。dispatch_service 加 `_haversine_km` + 服務區中心點（無 PostGIS，純數學近似）算真距離、`_enrich_gis_performance` 把 on-time/acceptance 多維績效以 bonus 進排序（候選清單+auto_match）。test_cr_0061 3/3 + 回歸 0 fail。

### Added

- **CR-0060 技師技能矩陣 + 品牌授權（branch `feat/cr-0060-tech-skill-brand-auth`，2026-06-20）**：審計 #12 #13 / BR-M07-01。技師原僅 capabilities JSONB 自由清單，無結構化技能矩陣/品牌授權。沿會議 mock-first 授權建資料模型：**migration 063** `technician_skill`（skill_code + A/B/C level）+ `technician_brand_authorization`（brand + authorized + cert_expires_at）+ seed 示範（is_mock）。`dispatch_service` 候選清單/auto_match 加品牌授權過濾（`_brand_authorized_ids`：未授權/認證過期技師排除；無授權資料保守不過濾）。test_cr_0060 2/2 + 回歸 0 fail。

### Added

- **CR-0059 M18 config 排程生效日（branch `feat/cr-0059-config-effective-date`，2026-06-20）**：審計 #9 / BR-M18-02。config 變更原僅即時生效，無未來排程。**migration 062** saas.config_version +effective_at；`config_m18_service.activate_due_scheduled()`（draft+effective_at<=now → active + 退役同 ns/key 舊 active）接 canary cron run_once 週期執行。test_cr_0059 1/1 + 回歸 0 fail。

### Added

- **CR-0058 完工套件 ④⑤ materials_used + payment_proof（branch `feat/cr-0058-completion-materials-payment`，2026-06-20）**：審計 #8 / BR-M08-03。完工套件五件補到全（照片/簽名/序號/教學/用料/付款證明）。**migration 061** work_orders +materials_used/payment_proof + completion_policy config require_materials/require_payment_proof（預設 false，避免擋無料檢測單）；complete_order 接 2 參數 + 選用閘（config 開才硬擋）。付款核銷子系統仍 P2。test_cr_0058 2/2 + 回歸 0 fail。

### Added

- **CR-0057 M03 三級必填分類落地（branch `feat/cr-0057-pc-field-tiers`，2026-06-20）**：審計 #7 / Q015 / BR-M05-03（CR-0026 §8 三層裁決落地）。problem_card_service 加 `_PC_FIELD_TIERS`（required 建卡必填 / pre_dispatch 派工前必填 / optional 可後補）+ `required_field_tiers()` 揭露 + `_field_tier()`；assert_completeness 422 details 加 `tier`、回傳加 `missing_by_tier`。test_cr_0057 2/2 + 回歸 0 fail。

### Added

- **CR-0056 完工結案 Email 通道（branch `feat/cr-0056-completion-email`，2026-06-20）**：審計 #4。派工單 PDF §四「三聯數位化：結案發 PDF 至 Email/LINE」—— LINE 已有（CR-0027），Email 未接。`complete_order` 加 best-effort Email（查客戶 users.email → email_provider.send_email；SMTP 未配置 send_email 回 False 不丟例外、客戶無 email 則略過）。test_cr_0056 1/1（monkeypatch 驗寄給客戶）+ 回歸全套 0 fail。註：SMTP 由 ops 配置（SMTP_HOST 等 env）後真寄。

### Added

- **CR-0055 Evidence package 聚合端點（branch `feat/cr-0055-evidence-package`，2026-06-20）**：審計 #5（Q022-Q024/BR-M09）。單一 WO 完工證據（照片/雙方簽名/到場+門檢事件）原分散在 media/digital_signatures/work_order_events 三處，無聚合檢視。新增 `evidence_package_service.get_evidence_package`（媒體依角色可見性過濾 + 簽名 + arrival/door_check 事件 + summary 計數）+ `GET /work-orders/{id}/evidence-package` 唯讀端點。test_cr_0055 1/1 + 回歸全套 0 fail。

### Added

- **CR-0054 施工中照片分類（branch `feat/cr-0054-photos-during`，2026-06-20）**：審計 #3。派工單 PDF §四明列存證需施工前/中/後三類，現 media_files purpose 只 completion_before/after。**migration 060** purpose CHECK 補 `completion_during`；media_service `_ALLOWED_PURPOSES` + `_ENV_PURPOSES`（品牌/會計不可見環境照）同補。test_cr_0054 2/2 + 回歸全套 0 fail。

### Fixed

- **報價可讀編號撞號 — create_quote 未遞增 version（branch `fix/quote-version-increment`，2026-06-22）**：CR-0095 UX2 上線可讀編號 `TP-000001-Q{version}` 後發現，`quote_engine_service.create_quote` INSERT 未帶 version、靠 DB default 恆為 1 → 同一工單建第二張報價也叫 `TP-000001-Q1`，編號不唯一。修：create_quote 先 `SELECT COALESCE(MAX(version),0)+1 FROM quote WHERE work_order_id=...` 取下一版號再 INSERT（沿工單遞增 Q1→Q2…）。純單一 function bug fix，version 欄位與 `-Q{version}` 顯示格式皆既有、無 contract 變更 → 不觸發 CIA。test_cr_0095 加 `test_create_quote_version_increments_per_work_order`（同工單兩張 → version 1/2、quote_number 結尾 -Q1/-Q2）共 10 passed + quote 全相關 42 passed 0 回歸。

- **派工/預約進線全斷 — AI 只說不呼叫 transfer_to_human（SOP 補強，branch `fix/sop-dispatch-escalation`，2026-06-22）**：實測對話（門把脫落→AI 回「已記錄、專員會聯繫」但後台無問題卡）揪出。root cause：`transfer_to_human` 是唯一進系統的工具（→問題卡→CS→工單），但 `locksmith-cs-sop` 的 handoff SOP 只在「A 轉真人」段明令呼叫該工具，**「B 派工」段與 booking 預約段只給話術、從未要求呼叫** → 所有派工/預約類案子 AI 對客戶說「會安排」但**靜默蒸發**（agent log 證實該輪 0 工具呼叫）。盤點全部應呼叫 transfer_to_human 的 12 種情境（紅線轉真人/機械結構/電力IC/管理權限/知識取不到/線上排查無效/預約安裝/預約維修/保固具體條款）後補強：SKILL.md 加「單一進線鐵律」+ 決策樹 item 3/4/5/6 明令呼叫；handoff-and-dispatch.md 加 §0 原則 + B 段派工明令 + C 段「說了就要叫」；booking.md 安裝/維修收齊資訊後呼叫。skill 版號 1.2.0→1.3.0。agent 120 passed（含 frontmatter 合規）。需重部署 agent 生效。：`api/templates/line_flex/__init__.py` 未匯出 `build_messages`，而 worker 以 `from templates.line_flex import build_messages` 取用 → **ImportError → outbox render 全失敗** → 所有 LINE 推送（CR-0028 派工/接單通知、CR-0027 完工電子工單、scope_change_result）**從未真正推送到客戶**。此即會議「LINE 公單回傳斷鏈」(Action Item #6) 的真實 runtime 根因 —— 先前 code-review 標 met 但 live 從未驗證（正是「code 在、runtime 斷」）。補 `__init__` 匯出 `build_messages` 修復，所有 LINE 推送方能運作。回歸測試 test_cr_0095 含 `build_messages` 可從 package 匯入之斷言。

- **Sidebar 導航後捲軸跳回頂端（branch `fix/sidebar-scroll-position`，2026-06-21）**：業主回報點選其他 nav 項後側欄捲軸重置。根因：`Sidebar` 在 66 個頁面各自掛載（非共用 layout），導航即整個 remount → nav 捲軸歸零。修：在 `Sidebar.tsx` 用 sessionStorage（key `sidebar:scrollTop`）記捲動位置，remount 時於 paint 前（`useLayoutEffect`，SSR 退回 `useEffect`）還原，避免閃爍。純前端、零架構/contract 變更。tsc 0 錯。

- **Sidebar「稽核與權限」父項導向 /admin/roles（branch `fix/sidebar-audit-link`，2026-06-21）**：業主反映點選「稽核與權限」跳到 audit-events，期望落在角色管理頁。audit 父項 href 原指第 4 子項 auditLogs，不符其他父項「父 href = 第一子項」慣例（第一子項為 roles）。改父 href 為 /admin/roles 同時修正不一致；audit-events 仍可由子選單進入。

- **CR-0053 到場/改派事件 3 個真 bug（branch `fix/cr-0053-arrival-doorcheck-bugs`，2026-06-20）**：7-agent 審計（對 HEAD 查證）+ 深掘揪出，同 migration 050 schedule_conflict 同類。(1) `onsite_arrival_v2` 誤呼 `record_door_check`（寫 event_type='door_check'）→ `submit_door_check_v2` 查 event_type='arrival' 前置閘**恆 409**，到場硬閘實質失效；(2) 到場不落 `started_at` → operational_kpi arrival_on_time **失真**；(3) `work_order_events` CHECK 漏 'arrival'/'reassign' → `reassign_order` 寫 'reassign' 違反 CHECK **成功改派恆 500**（無 try/except，且無成功路徑測試掩蓋）。**修**：新增 `record_arrival`（寫 'arrival' 事件 + 補 started_at）、`onsite_arrival_v2` 改呼之、**migration 059** CHECK 補 arrival+reassign、Schema_work_order_events.sql 同步（順帶補 050 漏的 schedule_conflict）。test_cr_0053 2/2（door-check 閘前 409/到場後通過/started_at 落；reassign 寫 event 不 500）+ 回歸 810 passed 0 fail。

### Added

- **CR-0052 完整度 422 結構化缺漏欄位（branch `feat/cr-0052-completeness-hint`，2026-06-20）**：CR-0042 follow-up。`assert_completeness` 的 `INCOMPLETE_PROBLEM_CARD` 422 原僅在 message 字串列缺漏欄位；補 `details=[{field, issue:missing}]` 結構化（前端可逐欄高亮，不必 parse 字串）。test_cr_0052 1/1 + 回歸 808 passed 0 fail。

### Added

- **CR-0051 派工資格 gate（branch `feat/cr-0051-dispatch-eligibility`，2026-06-20）**：BR-M06 / G004-G005（CR-0038 標 MISSING：「只剔 3 態，不擋 pending/suspended」）。`assign_order` 本已 gate `status='active'`，但**候選清單/自動媒合**只排除 circuit（inactive/on_leave/circuit_breaker_open），未排除生命週期不可派工狀態（pending_approval/suspended/terminated/rejected）→ 停權/未核准技師會出現在派工候選。**實作**：`_is_dispatch_eligible` 硬排除 4 種生命週期狀態，接 `_score_rows`（候選清單 + auto_match 共用）+ `get_candidate_detail` 露 `dispatch_eligible`。test_cr_0051 8/8 + 回歸 807 passed 0 fail。

### Added

- **CR-0050 完工套件補教學紀錄（branch `feat/cr-0050-completion-teaching-note`，2026-06-20）**：BR-M08-03 完工套件 spec 列 photos+materials+payment+sign-off+**teaching note** 五件，CR-0039 已做照片≥3/簽名/序號，本 CR 補教學紀錄。**migration 058** `work_orders.teaching_note` TEXT；`complete_order` 加 teaching_note 參數 + onsite/completion 端點 body 補欄位 + API 露（_WO_SELECT index 36 + model）。test_cr_0050 1/1 + 回歸 799 passed 0 fail。

### Added

- **CR-0049 pending scope change 阻擋完工（branch `feat/cr-0049-pending-scope-gate`，2026-06-20）**：BR-M08-02 安全閘（CR-0038 標 PARTIAL — complete_order 無 pending scope 硬阻擋）。報價/加價變更未經客戶確認（scope_changes.status='pending'）時技師不可完工。**實作**：`_has_pending_scope_change` + `_enforce_completion_gate` 技師路徑加 409 `PENDING_SCOPE_CHANGE`（客戶確認/主管覆寫後放行；admin override :complete 路徑可繞）。test_cr_0049 2/2 + 回歸 798 passed 0 fail。

### Added

- **CR-0048 改派/改期強制原因 + 落 status_reason（branch `feat/cr-0048-reason-gate`，2026-06-20）**：BR-M05-01 收尾（CR-0038 標 PARTIAL）。原 cancel/escalate 已強制 reason，但 reassign 只寫 service_report note 未落 status_reason、reschedule 同。**實作**：`reassign_order` 加空原因 422 gate + 寫結構化 `status_reason`；`request_reschedule` UPDATE 補 `status_reason`（reason gate 本已有）。test_cr_0048 2/2 + 回歸 796 passed 0 fail。

### Added

- **CR-0047 保固期動態計算（branch `feat/cr-0047-warranty-auto-compute`，2026-06-20）**：兌現 CR-0043 延後項 + 派工單 PDF §四優化建議「保固期動態計算（序號→保固截止→保內/外自動）」。現況 `work_orders.warranty_status` 為人工填、無到期日欄；`warranty_service` 已有 5-mode 起算 + brand override + compute_warranty_end 引擎但**未接 work_order**。**實作**：(a) **migration 057** `work_orders.warranty_expiry_date` DATE。(b) `work_order_service._auto_warranty`：購買日/裝機日（錨點 fallback）+ brand 期間（`resolve_period_months` brand override：Yale 36/Dormakaba 60/預設 24 月）→ 算到期日 + 保內/保外。(c) `update_wo_fields` 設 serial/購買日/裝機日/brand 時自動回填 `warranty_status` + `warranty_expiry_date`（**手動 warranty_status 優先不覆蓋**）。(d) API 補 `warranty_expiry_date`（_WO_SELECT index 35 + model + TS 型別 + sidebar「保固到期日」）。test_cr_0047 6/6（含 Yale 36 月 brand override 端到端驗證）+ 回歸全套 **794 passed 0 fail** + 前端 tsc 0。

- **CR-0046 Q-11/Q-12 假資料生成（branch `feat/cr-0046-quote-text-discount-mock`，2026-06-19）**：翻遍 `20260617資料/` 後確認 Q-11（客服優惠權限）/Q-12（公司抬頭/客服電話/保固+取消+追加價條款文字）資料夾確實沒有（屬公司專屬）。業主指示「先生成假資料，確認後再替換」（同決議 5 mock 模式）。**migration 056** 兩 M18 config namespace（全 is_mock，業主動態改 config 即替換、不需改 code）：(a) `company_profile`（公司抬頭/客服電話 + 保固/取消費/追加價三段條款，文字內含「（範例…）」便於辨識）→ `work_order_document_service` 客戶電子工單 PDF 渲染抬頭+客服電話+服務條款段；(b) `discount_policy`（核准門檻 10000 + 客服折扣上限 10%）→ `quote_engine_service._approval_threshold` 讀 config 取代 hardcode 10000。test_cr_0046 3/3 + 回歸全套 **792 passed 0 fail**（PDF render + quote engine 不破）。**待業主確認後替換真值**（改 config_version 即可）。

### Fixed

- **CR-0045 發票稅 de-hardcode（esales Q-07，branch `feat/cr-0045-invoice-tax-default`，2026-06-19）**：窮盡翻 `20260617資料/` 後確認——先前列為「待業主」的 Q-01~Q-12 多數其實在資料夾且已 seed（catalog 價 migration 040、payout 表 migration 045、加價 surcharge_rule），業主決議 5 已授權當 mock 用；真正待業主只剩 Q-07 稅顯示/Q-11 優惠權限/Q-12 公司文案。本 CR 接 Q-07：`invoice_service` 稅原 hardcode mock 0 → `_resolve_tax` 讀 M18 config `tax_policy`（**migration 055**，業主預設**台灣 VAT 5% 含稅**，可動態改）。含稅語意：對外 amount/total 不變（客戶含稅總價），僅拆出內含稅額（tax，帳務 DB-only）。test_cr_0045 3/3 + 回歸全套 **789 passed 0 fail**。**仍待業主**：Q-11 客服優惠權限幅度、Q-12 公司抬頭/客服電話/保固+取消條款文字（資料夾無）。**P2 守界線**：報價自動帶價/加價套用/payout reconciliation 讀 045 表 = 會議定調下輪金流引擎（資料已備）。

- **CR-0044 esales 已決定值 de-hardcode + 取消費 SoT 衝突裁決（branch `feat/cr-0044-esales-decided-config`，2026-06-19）**：盤點 `20260617資料/` esales 報價資料庫後校正一個誤判——先前把「正式價待業主」過度推廣成「整個 finance 待業主」；實際 esales `13 待決策 Q&A` 只列 Q-01~Q-12 待回答，其餘標「已知規格/Accepted default」（已決定）。**🛑 揪出 Source-of-Truth 衝突**：`cancellation_service.py:43-44`（code，ADR-0102 v2 2026-05-28）S3/S4=300/300 vs esales `03 加價規則`（2026-06-03，標 ADR-0102）S3/S4=**500/800**；業主裁決採 esales。**實作**（只動「已決定 ∩ 仍寫死」者）：(a) 取消費 S3/S4 校正 300→500/800（`DEFAULT_CANCELLATION_CONFIG` + **migration 054** 修 DB `system_config.cancellation` override）；(b) 報價有效期 14/3 de-hardcode 入 M18 config `quote_validity_policy`（`quote_engine_service._validity_days` 讀 config 帶 fallback）。**test_cr_0044 3/3** + 修 3 個 stale 取消費斷言（S3 300→500、S4 600→1100）+ 全套 **786 passed 0 fail**。**仍 P2 下輪**：區域加價（已在 surcharge_rule 表）+ 佣金 0.08（已在 dispatch_commission config）缺的是**計算引擎**非 config；急件/夜間/假日加價方法（Q-03/04/05）+ 服務基礎價（Q-01/02）+ 稅（Q-07）+ 報價文字（Q-12）真待業主。

### Added

- **Beta-readiness sprint CR-0038 桶3/4/5（branch `chore/beta-readiness-ci-and-followups`，2026-06-19）**：業主「一次做完」CR-0038 剩餘缺口。金流(桶1)/多租戶(桶2)依會議拍板仍 P2/P3 下輪不動；正式價/法務/稅率卡業主輸入。**已完成會議對齊、無外部阻塞部分**：
  - **桶3 CI 基建（消滅最後假綠根源）**：(a) 修 `_conn` 跨檔污染——conftest autouse `_isolate_db_conn` fixture（每測試後若 `_conn` 非真 psycopg 連線即清空），整套一個 process 內可靠全綠 **226 unit + 558 component = 784 passed 0 fail**（修前併跑 21 fail）；(b) 新增 `component-nightly.yml`（postgres service container + Schema→migrations→seeds bootstrap + `pytest -m component`，排程每日+手動+SQL 變動觸發）→ 558 component 測試從「CI 0 執行」變 nightly 真跑。
  - **桶4 公單收尾（門檻全入 M18 config 不寫死）**：(a) **BR-M08-02 scope change 分級閘**——`_classify_scope_tier`（minor≤500/standard 501-2000/major>2000 或增幅≥50%→requires_supervisor），tier 存 scope_changes.new_scope + timeline；(b) **Q063 48h 自動結案**——`auto_confirm_stale_completed` + `auto_confirm_cron`（completed 超時且非 high_risk_hold、無 open exception → confirmed/closed，排除客訴/爭議/保固）；(c) **migration 053** scope_change_policy + auto_confirm_policy 兩 config namespace。test_cr_0038_bucket4 5/5。
  - **桶5 follow-up**：`create_technician` 補 optional user_id（FR-0044 補完；查證校正：登入技師走 register_technician 本已寫 user_id，僅 admin onboard 路徑補可連結性）。
  - **誠實未做（非快速可收）**：前端 route gating middleware（token 存 localStorage，server middleware 讀不到，需 cookie auth 重構=CIA 級；且 client 端 AuthGuard+rolePolicy 已做角色 gating）、exceptions_v2 正式改名（cosmetic）、CR-0043 延後項（簽名影像/保固序號回填/拍照 UI，中等工）。金流/多租戶仍 P2/P3 下輪。

- **公單欄位補完 Phase 2 CR-0043（branch `feat/cr-0043-workorder-fields-phase2`，2026-06-19）**：對 `20260617資料/電子鎖安裝與維修派工單整合分析報告.pdf` §三 6 模組 24 欄做 12-agent 對抗式逐欄查證，發現 CR-0026 只補 DB 骨架（7 符合 / 17 部分 / 3 缺），半成品集中在「DB 有欄、API/UI 沒接」。本 CR 收口高 ROI 四階（沿用 CR-0026 會議授權 PDF 語意預設）：(a) **Tier①** 客名/電話接回 `_WO_SELECT`+model+前端 sidebar（原 create 寫得進、API 讀不出）；修 `create_from_problem_card` 死欄 bug（`pc.category` 只寫 `problem_type`，`service_category` 永遠 NULL → 加 `_map_service_category` 映射 enum）；新增 **`PATCH .../work-orders/{id}/fields`** 端點補「寫入路徑稀薄」（service_category/serial/door/warranty/payment 後台可設，白名單+enum 驗證）。(b) **Tier②** `completion_status` 六段真實推進（accept→pending_report、complete→pending_customer_confirm、confirm→closed，原為零寫入死欄）；新增 **`POST .../work-orders/{id}:reopen`** 建子單連回 `parent_work_order_id`（BR-M05-02，不覆蓋歷史）。(c) **Tier③** **migration 052** 補 `dealer`/`install_date`/`rain_exposure`(三段遮雨)/`special_door_surcharge`/`payment_method` 五欄 + `service_catalog` seed dispatch/destruction/removal 費目。(d) **Tier④** 完工硬閘加查三段免責 `work_order_consents`（M18 config `completion_policy.require_consents` 控，預設 off 不破壞回歸）。**測試** `test_cr_0043` **17/17**（純函式映射/序列化 + component service_category 寫入/PATCH 驗證/reopen 子單/completion_status 推進/consent gate）+ 回歸 226 unit + 23 工單 component 檔全綠。**治理**：金流 money rules 全入 M18 config 不寫死（`quote_policy.apply_surcharge`）。**誠實分流（仍 P2 下輪）**：M5 出勤費白天/夜間加成「自動計算」（surcharge_rule 接進 quote total）屬完整財務結算，本 CR 只補欄位+catalog 費目+config 旗標；簽名影像化、保固序號動態回填、拍照存證前端上傳 UI 亦延後。

- **Alpha Exit 收尾 CR-0042（branch `feat/cr-0042-alpha-exit-closeout`，2026-06-19）**：階段2 測試計畫 §5 Alpha Exit 阻塞中零/低依賴兩項。業主裁決 completeness 0.8 + 硬擋 + 主管 override。**實作**：(a) **migration 051** M18 config `problemcard_policy`（min_completeness=0.8）。(b) `problem_card_service.assert_completeness`：算完整度（5 key 欄）+ 轉 WO 前 gate（<0.8 → 422 `INCOMPLETE_PROBLEM_CARD`，admin/ops 帶 reason override）。(c) convert-to-WO v2 加 gate + `override_reason` query。(d) `create_customer` **phone 去重**（同租戶同電話 → 422 `DUPLICATE_CUSTOMER`，BR-M02-01）。**測試** `test_cr_0042` **6/6** + 回歸 37 + 全 Alpha 226 unit + 552 component 0 fail。follow-up：師傅 onboarding 寫 user_id（#7）、對話可見性分流（#8 M16）、completeness 前端提示。
- **M15 異常框架 CR-0041（BR-M15-01/03，branch `feat/cr-0041-exception-framework`，2026-06-19）**：CR-0038 階段1 最後一塊。異常（缺料/加價拒絕/取消/爭議/安全）原散落 chat、無統一追蹤；`generated.py` 有 Exception model 但無表/service；`exceptions_v2.py` 誤命名（實為師傅排班）。approval inbox（FR-0049）已涵蓋 scope/refund/dispute/reschedule。業主裁決 §8 全採建議預設。**實作（MVP）**：(a) **migration 049** `saas.exception_case`（control tower，對齊 model + `return_path` action 9 值）+ `work_orders.high_risk_hold` 旗標。(b) **`exception_service`** open（high/critical+WO→設 hold，BR-M15-03）/list/get/resolve（選 return_path、解除 hold）/escalate。(c) **`exception_cases_v2`** router `/exception-cases`。(d) **high_risk_hold gate** 接 `assign_order`+`complete_order`（含 override）→ 422 `HIGH_RISK_HOLD`。(e) **`exceptions_v2` 標 DEPRECATED**（真框架讓位 /exception-cases，30d 後改名 technician_schedule_v2）。**測試** `test_cr_0041` **6/6** + 回歸 51 工單/完工 + 226 unit 全綠。follow-up：exceptions_v2 正式改名 + 前端遷移、approval_inbox 加 exception type、半自動觸發、return_path 串既有端點、前端異常 inbox 頁。
- **Evidence 治理 CR-0040（BR-M09-02 角色可見性 + BR-M09-03 保存期，branch `feat/cr-0040-evidence-governance`，2026-06-19）**：CR-0038 階段1 兩個 P0。原 `list_media_for_work_order` 只用 wo+tenant 過濾、**無角色過濾** → 品牌會看到客戶家中環境照（隱私 P0）；media_files 無保存期/清除機制。業主裁決 §8 全採建議預設。**實作**：(a) **migration 048** media_files 加 `retention_until`（1yr / 客訴保固 2yr，Q027）+ `deleted_at`（軟刪 HD-4）+ backfill。(b) **`media_service`**：`_hidden_purposes(role)` 規則式過濾（Q026：品牌隱藏環境照 door_check/completion_before、會計隱藏 door_check，HD-1 不加欄位）；`list`/`get` 加 role 過濾（不可見 → **404 不洩漏存在性**）+ 排除軟刪；`upload` 算 retention（dispute/warranty WO → 2yr）；`soft_delete_expired_media()`。(c) **`media_v2`** list/get 傳 user.role。(d) **cron** `media_retention_cron`（每日軟刪過期）掛 lifespan。**測試** `test_cr_0040` **6/6** + 回歸 media 19 + 226 unit 全綠。follow-up：客戶端 track evidence 對齊、前端面板依角色顯示。
- **完工硬閘 CR-0039（BR-M08-03，branch `feat/cr-0039-completion-gate`，2026-06-19）**：CR-0038 階段1「公單收尾」首要項。原 `complete_order` 只檢查狀態機，師傅可**無照片/無簽名/無序號完工**（Beta 測試計畫 §46 驗收項 + 客訴/帳務爭議源頭）。業主裁決 §8 全採建議預設。**實作**：(a) **migration 047** `completion_policy` config（min_photos=3 / require_signature / serial_required_categories=["install"] / allow_supervisor_override；門檻入 M18 config **不寫死**，仿 CR-0036）。(b) **`work_order_service`**：`_enforce_completion_gate`（讀 config + fallback 預設）三道閘 + `_signature_exists`（查 `digital_signatures` 客戶簽名**真存在**，非僅非空字串，HD-4）；`complete_order` 加 5 參數，gate 置於狀態檢查後。(c) **routers**：技師 `/onsite/completion` 走正規閘；`:complete` v2+v1 為 admin/dispatcher **override 路徑**（記 reason）+ **技師角色 403 guard**（防繞過）。新 error codes `INSUFFICIENT_PHOTOS`/`SIGNATURE_REQUIRED`/`SERIAL_REQUIRED`（422）。**測試** `test_cr_0039_completion_gate.py` **8/8 pass** + 回歸 50 完工 component + 226 unit 全綠。**HD-5 不回溯**（既有 completed 不受影響）。**follow-up**：前端技師完工頁配合（照片≥3 提示 + serial 欄 + 422 友善訊息）。
- **誠實缺口盤點 CR-0038 + 階段0 修復假綠（branch `chore/stage0-migration-ci`，2026-06-19）**：用 35-agent 對抗式 workflow 對 `20260617資料` 全規格（ERP spec 01 / 測試計畫 02 / esales 報價金流 / 會議）逐 file:line + 真 DB 查證 —— **245 需求 item，真實 DONE_VERIFIED 僅 21（≈9%）**，自評「99.8%」量的是檔案存在率非端到端可動率。產出 `docs/4-exploration/CR-0038-gap-inventory-20260617.md`（缺口總表依會議工作順序分桶 + 補完 Roadmap）。**三大假綠根源**：(1) migration 標記 ≠ 實際套用；(2) 金流 Flow 12 = 0 核心；(3) CI 跑已刪目錄、945 component 0 執行。**階段0 修復**：(a) **新增 migration `046`** `public.schema_migrations` 追蹤表（version/filename/applied_at/note）—— 取代 `MIGRATION_REGISTRY.md` 人工標記，成為「是否已套用」唯一真實來源；`apply-schema-prod.sh` 套完即 INSERT 留痕（ON CONFLICT DO NOTHING）；回填 dev 44 支。(b) **補套 035/045 到 dev DB** —— 兩支標 🟢 idempotent 卻從未套用、致 `test_password_reset`(7) / `test_cr_0037`(component) `UndefinedTable` FAIL，補套後 **12 測試 FAIL→PASS**。(c) `MIGRATION_REGISTRY.md` 加頂部權威註記 + reconcile 已知漂移（035/045 今日補套、017-027 標 pending 實已套 dev）。**性質**：infra / 測試基建 / 文件治理，無 contract 變更。
- **報價引擎 Phase A（CR-0032，branch `feat/cr-0032-quote-engine`，2026-06-18）**：gap-audit S2 + 藍圖 M04。CR-0027 只有 quote_line_items（work_order 層），無報價主表/狀態機/核准/凍結。**mock-first**（報價資料已在 esales + 決議 5 授權當 mock，故結構先建、門檻用草稿預設、不卡業主裁決）。**實作**：(a) **schema** migration `041`：`quote` 主表（狀態機 draft→pending_approval→approved→sent→accepted/rejected/expired/superseded + 版本鏈 supersedes + 有效期 expiry_at + snapshot_hash）+ `quote_approval`（核准軌跡）+ `pricing_rule_snapshot`（送客戶凍結價格規則 append-only）+ `quote_line_items` 加 quote_id/service_code/material_code。(b) **service** `quote_engine_service`：create_quote（有效期 BR-M04-05 14d/急件 3d）、add_line **從 CR-0034 catalog 帶價**（service/material code → 帶 internal+customer 價）、recompute total、`transition` 狀態機（submit/approve/reject/send/accept/decline）、**送單凍結 pricing snapshot + sha256**、過期擋 accept、cost RBAC 遮蔽。**測試** `test_cr_0032_quote_engine.py` 3 案（catalog 帶價 SVC-RES-001=800/材料×2 total / 狀態機 submit→approve→send〔snapshot hash〕→accept + 已 accept 擋 submit 409 / RBAC unit_price 遮蔽）+ migration 041 套 dev DB。**性質**：DB schema + domain + 狀態機，mock-first（數值待 esales Q-01~Q-12 轉正式）。
- **報價引擎 Phase B（CR-0032，branch `feat/cr-0032-quote-engine`，2026-06-19）**：把 Phase A 引擎接上 API + 後台 UI，報價從畫面到 DB 全通。**實作**：(a) **核准門檻 enforcement**（`quote_engine_service._APPROVAL_THRESHOLD` mock 10000）：總額超門檻不可從 `draft` 直送，須先 `submit→approve`，否則 409 `APPROVAL_REQUIRED`（正式門檻待 esales Q-11）。(b) **API** `routers/quote_v2.py`（掛 `main.py`）：`POST .../work-orders/{woId}/quotes`（建 draft）、`GET .../quotes/{id}`（cost RBAC 遮蔽）、`POST .../quotes/{id}/lines`（從 catalog 帶價）、狀態機 `:submit`/`:send`/`:accept`（一般角色）+ `:approve`/`:reject`（`role_required` 管理角色）；全端點 cross-tenant guard。(c) **後台報價編輯頁** `web/src/app/admin/quotes`：輸入工單 ID 建草稿 → catalog 下拉加項（服務/材料×數量）→ 即時 total → 狀態機按鈕；成本欄依角色顯示；i18n（zh-TW/en `components.admin.quotes` + `sidebar.nav.quotes`）+ rolePolicy（admin/operations_manager）+ Sidebar 連結。**測試** `test_cr_0032_quote_engine.py` 補 2 案（超門檻擋 draft 直送→走 submit/approve 放行；門檻內 draft 直送）共 5 pass；`npx tsc --noEmit` 0 error。**性質**：API contract + 前端 UI，mock-first 門檻。
- **報價引擎 Phase C（CR-0032，branch `feat/cr-0032-quote-engine`，2026-06-19）**：報價主軸最後一段 —— 客戶端免登入查看/確認報價。**mock-first 不適用此階段**（純機制，無數值決策）。**實作**：(a) **複用 stateless `public_token`**（新增 `purpose='quote_view'`，**不建 token 表/不加 quote 欄**）：`public_token.py` Literal + `verify_token` allowlist 加 `quote_view`；`quote_engine_service.mint_view_token`（TTL 對齊 `quote.expiry_at`，fallback 7d），`:send` 成功即鑄 token 回 `public_token`/`public_path`。(b) **消費端點 `consumer_v2.py`**：`GET /consumer/quotes/{token}`（`include_cost=False` **結構性零成本外洩** —— 客戶版 line items 結構上不含 `unit_price`，非僅前端隱藏）+ `POST /consumer/quotes/{token} {decision}`（accept→accepted、reject→decline→rejected，走既有狀態機，過期擋 accept 409，非法 422）；失敗一律 404 不洩露原因。後台 `GET .../quotes/{id}/public-link` 可重鑄連結。(c) **前端客戶頁 `web/src/app/quotes/[token]`**：mobile-first CSR、bare fetch（不帶 JWT）、三態機、只露客戶價、同意/拒絕鈕；i18n `pages.quotesPublic`（zh-TW/en）+ AuthGuard `PUBLIC_PREFIXES` 加 `/quotes/`。(d) **ADR-0115**（accepted）：客戶端報價查看走 stateless public_token + 結構性零成本外洩；**不重複** ADR-0064（快照不可變）/ ADR-0066（狀態機）/ ADR-0062（pricing BC），只記新存取決策。**測試** `test_cr_0032_phasec_consumer.py`（token roundtrip、GET 無 unit_price、purpose 防護 404、accept/decline 映射、bad decision 422）+ 引擎測試加 send 鑄真 token 整合案；報價/consumer 全套 pass，`tsc` 0 error。**性質**：API contract（consumer 端點）+ 前端公開頁 + ADR。**報價主軸貫通**：主檔(CR-0034)→引擎(A)→API+後台 UI(B)→客戶端查看/確認(C)。
- **金流結算收尾（CR-0035，branch `feat/cr-0035-settlement-billing`，2026-06-19）**：gap-audit S5。**盤點修正**：技師撥款月結 **CR-0012 已完整做**（generate_monthly_batch/CSV/水單+5 端點），真缺口=客戶側帳單斷層（invoices 只讀無 create、報價 accepted 無後續）+ `settlements/monthly` 501 殼。範圍收斂為「報價→應收 + stub 接線」，完整 AR/AP/佣金/傳票 out of scope（Phase II / esales Q-08/09）。**實作**：(a) migration `042`：invoices 加 `quote_id`（追溯來源報價）+ `is_mock`（idempotent）。(b) `invoice_service.create_from_quote`：accepted 報價→開應收發票（客戶價 line_items **不含 unit_price**、稅 mock 0 待 esales Q-07、status issued）；**work_order_id UNIQUE 天然冪等**；非 accepted→409。(c) `quote transition(accept)`→**best-effort 開票**（catch 廣義 Exception 解耦，失敗不阻斷接受；db autocommit 不毒化）。(d) `settlements/monthly` **501→202** 接既有 `generate_monthly_batch`（period 預設當月，UPSERT 冪等）。(e) API `POST .../accounting/invoices:from-quote`（後台手動補開，管理角色）。**測試**：settlements 501→202 接通 + 報價 accept 回歸 9 pass；`test_cr_0035_invoice_billing.py`（開票/冪等/非 accepted 409/無成本外洩）待 migration 042 套用。**性質**：DB schema + API contract，mock-first（金額/稅率待 esales 轉正式）。**follow-up**：LINE 報價接受通知（需 resolver+Flex builder，CR-0028 規模）、訂金二階段、傳票自動分錄、月結 cron 排程。
- **免責合規（CR-0033，branch `feat/cr-0033-disclaimer-consent`，2026-06-19）**：gap-audit S3 + 藍圖模組 4「施工免責與合規」。現況無通用 work_order_consents、客戶 PDF 無免責段（CR-0026 §8-Q3 defer 至此）。**法務佔位 first**（三段文本用藍圖語意佔位、標「待法務 sign-off」，結構先建不卡）。**實作**：(a) migration `043`：`work_order_consents`（三段 consent_type + accepted/accepted_at + text_version 快照 + ip_address 留痕；UNIQUE(work_order_id, consent_type) 冪等 upsert）。(b) `consent_service`：三段藍圖佔位文本常數（新機安裝/破壞鎖/個資，`TEXT_VERSION=blueprint-draft-2026-06`）+ record_consents（ON CONFLICT upsert）+ get_consents（永遠回三段+狀態）。(c) 消費端點 `GET/POST /consumer/consents/{token}`（複用 work_order_status token；purpose 防護 404、bad body 422、IP 留痕）。(d) PDF `work_order_document_service` 加**免責段**（三段佔位文本 + ☑/☐ 簽署狀態，best-effort 不阻斷，**仍不含成本**）。(e) 前端 `/consent/[token]`（mobile-first CSR、三段卡勾選提交、全勾才可送）+ i18n `pages.consentPublic`（zh-TW/en）+ AuthGuard `/consent/`。**測試**：router-level 4 pass（GET/POST/purpose 404/bad body 422）；service/PDF component（upsert 冪等/unknown 422/PDF 含免責段）待 migration 043 套用；tsc 0 error。**性質**：DB schema + API contract + 前端公開頁，法務佔位 first。**out of scope**：派工前 hard gate（流程變更）、legal_text_versions 版本主檔、簽名圖留痕 → follow-up（§8 待裁）。
- **Phase II 金流參數入 config 治理 + 訂金接發票（CR-0036，branch `feat/cr-0036-finance-config`，2026-06-19）**：業主指正 —— esales `PhaseII FinanceSettlement` sheet 24「Finance Config」**已有**訂金/佣金/月結時程草稿值，CR-0035 卻 punt 成「待裁決」。sheet 24 標題明寫「所有規則版本化，不可寫死」，專案已有 M18 config governance（migration 004）但留空。依決議 5 把 sheet 24 值當 mock seed 進 config 治理。**實作**：(a) migration `044`：config_namespace ×3（deposit_policy 0.3/min 1000/材料預收 1.0、dispatch_commission 0.08、monthly_close_schedule 3/5/10）+ seed global active config_version（value 內含 `is_mock`/`esales_status`/`source` 自標）+ `invoices.deposit_required`。(b) `config_m18_service.read_global_value`（全域 active 讀，無則 None 不丟 404）。(c) `invoice_service._resolve_deposit`（從 deposit_policy config 算 max(amount×rate, min) 上限 total；config 缺失 fallback 常數）+ create_from_quote 計算存 deposit_required。**誠實釐清**：稅務 Q-07 全檔無值真未答（不納入）；退款 tier 已在 ADR-0040 不重做；師傅拆帳逐列表（sheet 21）/AR/AP Ledger/Brand Settlement → follow-up。**性質**：config governance seed（非寫死 code，符 sheet 24 約束）+ DB schema，mock-first（值走 M18 改版轉正式）。**測試**：config seed 可讀 + deposit 計算（rate/min/上限）+ 缺失 fallback；待 migration 044 套用。**follow-up**：訂金顯示於報價/客戶頁、佣金計算引擎、師傅拆帳 seed、稅務（Q-07）。
- **師傅拆帳規則主檔（CR-0037，branch `feat/cr-0037-payout-rules`，2026-06-19）**：業主「一起做」剩餘 esales 資料 —— 盤點後**誠實分流**，唯一有真草稿資料且可 mock-first 建的＝師傅拆帳（sheet 21，69 筆 23 服務×A/B/C）。code 目前拆帳硬編 80%（ADR-0041）與服務/等級無關。**實作**：(a) migration `045`：`technician_payout_rule` 表 + 69 筆 mock seed（python 轉寫自 sheet 21，is_mock，decision_status A/B=draft、C=draft_review；base_payout 內部成本 + 夜間 0.2/急件 0.15 加成率）。(b) `payout_rule_service`：list_rules（base_payout RBAC 遮蔽）/ get_rule（service×level 生效查詢）/ compute_payout（純計算 base×(1+夜間)×(1+急件)）。(c) `GET /tenants/{tid}/payout-rules`（catalog_v2，base_payout 僅後台）。**誠實分流（不建）**：服務/材料價 Q-01/02（CR-0034 已 seed）、加價套用引擎（待 Q-03~06 算法）、客戶報價文字 Q-12（真無值，同稅 Q-07，不捏造）、AR/AP Ledger/Brand Settlement/佣金引擎（sheet 27-31，依賴金流代收代付＝會議 Phase 2，無基建硬建低價值）、品牌/SKU/BOM（無即時 consumer）。**NOT wired**：reconciliation 拆帳重算（× 0.8 → 查表）待 Phase II（需 work_orders 夜間/急件旗標 + Q-09）。**性質**：DB schema + mock seed（仿 CR-0034），結構先行。**測試**：seed 69 / get_rule / compute / API RBAC 遮蔽，純計算已 pass、其餘待 migration 045。

- **報價基礎主檔 Phase A（CR-0034，branch `feat/cr-0034-quote-catalog`，2026-06-18）**：20260617 gap-audit S4 —— 報價基礎主檔在 code 完全缺（報價引擎/金流的上游）。依**會議決議 5「報價數字當 mock 灌進系統」**把 esales 報價資料庫結構 + 草稿值灌為 mock。**實作**：(a) **schema** migration `040`：`service_catalog`(29 服務) + `material_catalog`(20 材料) + `surcharge_rule`(12 規則) 三表（含 internal cost / suggested price / decision_status / `is_mock` / tenant_id 預留）+ seed（ON CONFLICT idempotent）。**數值人工轉寫自 esales（非自動 copy），全 `is_mock=TRUE`**；區域+取消費=已知規格(ADR-0102)、急件/夜間/假日/S5=待決策(esales Q-03~Q-06)。(b) **service** `quote_catalog_service`（list/get；**internal 成本 RBAC 遮蔽** include_cost）。(c) **API** `GET /tenants/{tid}/quote-catalog`（服務/材料/加價規則；內部成本僅 admin/operations_manager/tenant_admin 可見）。**測試** `test_cr_0034_quote_catalog.py` 2 案（seed 數量 + 全 mock + 取消費已知/急件待決策；internal cost 遮蔽）+ py_compile + migration 040 套 dev DB 驗證。**性質**：DB schema + domain，數值走決議 5 授權 mock seed（人工轉寫）。**follow-up**：前端報價主檔檢視頁、Phase B（BOM/拆帳/供應商/finance_config）、mock→正式價（待 esales Q-01~Q-12）。

- **廠商核准 UI（CR-0029 收尾，branch `feat/vendor-approval-cr0032-roadmap`，2026-06-18）**：CR-0029 建了 vendor 註冊但缺管理員核准流程（vendor 卡在 pending 無人能改 active）。**實作**：(a) **service** `vendor_service`（list_vendors 依 status / approve_vendor → active+approved_by/at / reject_vendor → rejected+reason；狀態機防呆：非 pending 重複操作 → 409）。(b) **API** `vendors_v2` router：`GET /tenants/{tid}/vendors?status=` + `POST .../{id}:approve` + `:reject`（限 admin/operations_manager/tenant_admin），main.py 註冊。(c) **web** 後台頁 `/admin/vendor-approvals`（pending 廠商列表 + 核准/拒絕按鈕）+ Sidebar「廠商審核」入口 + rolePolicy 限管理角色 + i18n zh-TW/en。**測試** `test_cr_0029_vendor_approval.py` 2 案（approve→active+approved_by/重複 409、reject→rejected+reason）+ 註冊回歸共 6 pass + tsc 0 error。**收完註冊閉環**（register pending → admin approve → active）。**性質**：複用既有 RBAC/router 模式 + 既有 vendors 表（migration 038），無 schema 變更 → 不觸發 CIA。**follow-up**：營業執照上傳、vendor 廠商專區（CR-0031）。

- **派工模式切換（CR-0030，branch `feat/cr-0030-dispatch-mode`，2026-06-18）**：2026-06-17 會議 Action #7「派工模式切換 UI（租戶手動派 / 平台代派付費）」+ §三.4（Sunny：兩家師傅都沒空叫平台派→收費點）。三檔（manual/platform_paid/auto_match）本輪做**前兩檔**（自動媒合留 Report 2）。**實作**：(a) **schema** migration `039`：`saas.tenant.dispatch_mode`（VARCHAR DEFAULT 'manual'）+ `work_orders.dispatched_via`（manual/platform/auto_match，platform=可計費事件）。(b) **service** `dispatch_mode_service`（get/set + via_for_mode 映射）；`assign_order` 依租戶模式標記 `dispatched_via`（platform_paid→'platform'）。(c) **API** `GET /tenants/{tid}/dispatch-mode` + `POST`（setDispatchModeV2，限 admin/operations_manager/tenant_admin + audit log）。(d) **web** dispatch-queue 頁 header 加「派工模式」下拉切換（樂觀更新 + 失敗 rollback）+ i18n。**計費**：platform_paid **只標記 billable 供日後對帳、本輪不硬扣 credit**（計費規則待業主，§8-Q1）。**測試** `test_cr_0030_dispatch_mode.py` 3 案（via 映射 / set-get round-trip + 非法 422 / platform_paid assign→dispatched_via='platform' end-to-end）+ work_orders 回歸 + tsc 0 error + migration 039 套 dev DB 驗證。**性質**：DB schema + flow + API contract → 已產 CIA（CR-0030），會議授權用預設先做。**follow-up**：平台代派計費引擎、自動媒合自動執行（Report 2）、CR-0031 multi-tenant 派工權隔離。

- **廠商/師傅雙路註冊（CR-0029，branch `feat/cr-0029-dual-registration`，2026-06-18）**：2026-06-17 會議 Action #3「加廠商/師傅註冊系統（發案者/接案者兩種路由，資料庫分開）」。現行只有師傅 self-register、無廠商註冊。**single-tenant 可逆版（multi-tenant 三類租戶留 CR-0031）**。**實作**：(a) **schema** migration `038`：新表 `vendors`（發案者：vendor_type brand/locksmith/distributor + company_name/phone/email/address + status pending_approval + approved_by + tenant_id 預留 + 3 index）+ `users.tenant_type`（requestor/technician/platform，nullable，backfill 既有 technician→technician/後台→platform）。(b) **service** `auth_service.register_vendor`（鏡像 register_technician：email 全域去重 + bcrypt + transaction INSERT users(role=vendor,tenant_type=requestor)+vendors；vendor_type 列舉驗證；不簽 token 待 login）+ register_technician 補寫 tenant_type。(c) **API** `POST /vendors/register`（registerVendor，201 pending_approval）+ `POST /vendors/login`（loginVendor，allowed_roles=['vendor']，**與後台角色隔離**、不入 `_ADMIN_WEB_ROLES`）。(d) **web** 新 `/register` 頁（技師/廠商切換單頁表單）+ 登入頁「還沒有帳號？註冊」連結 + i18n zh-TW/en。**測試** `test_cr_0029_registration.py` 4 案（建 users+vendors / email dup 409 / vendor_type 422 / vendor 可登入但擋後台）+ auth 回歸 14 pass + tsc 0 error + migration 038 套 dev DB 驗證。**性質**：DB schema + domain + API contract + flow → 已產 CIA（CR-0029），會議授權用預設先做。**follow-up**：vendor 審核 UI / 營業執照上傳 / CR-0031 multi-tenant 分庫。

- **電子工單 PDF 下載 follow-up（CR-0027 收尾，branch `feat/cr-0027-document-followup`，2026-06-18）**：補完 CR-0027 §11 列為 follow-up 的兩個 PDF 下載入口。(a) **客戶端**：新增 public `GET /consumer/work-orders/{token}/document`（`getConsumerWorkOrderDocumentV2`，沿用 tracking token purpose=work_order_status，無 JWT；產 `work_order_document_service` PDF）+ `/track/[token]` 頁完工後顯示「下載電子工單 PDF」按鈕（plain `<a>` 直連 public 端點、免登入）。(b) **後台**：詳情側邊欄成本面板加「下載電子工單 PDF」按鈕（走既有 `api.download` authed fetch→blob，從 `getWorkOrderDocumentV2`）。**測試** `test_cr_0027_quote_document.py` 增 consumer doc 端點案（token→200 application/pdf %PDF），共 4 pass；tsc 0 error。**性質**：複用既有 service/token 機制加端點 + 前端入口，無 schema 變更 → 不觸發 CIA。

- **報價成本明細 + 客戶版電子工單（CR-0027，branch `feat/cr-0027-quote-and-document`，2026-06-18）**：2026-06-17 會議「報價成本明細沒做（成本架資料庫沒建）」+ 決議 4（後台看成本、客戶看電子工單只露最終價+關防）+ 決議 5（報價 mock 打 8 成）+ §4.1（電子化 PDF 回 LINE）。**實作**：(a) **schema** migration `037` 新表 `quote_line_items`（work_order 層成本拆項：`unit_price` 內部成本**僅後台可讀**、`customer_price` 對外、`is_mock` 預設 TRUE 待財務覆核 + 2 index）。(b) **service** `quote_service`（CRUD + 每次異動重算 `work_orders.customer_final_amount` = Σ(customer_price×quantity) + **RBAC 成本遮蔽** include_cost 旗標）+ `work_order_document_service`（客戶版電子工單 PDF，複用 `report_export_service` reportlab + STSong-Light 中文字型；**結構上只 SELECT customer-facing 欄、絕不讀 unit_price**；關防 placeholder env `WORK_ORDER_SEAL_IMAGE` 可配）。(c) **API** `GET/POST .../work-orders/{id}/quote-items`（unit_price 依角色遮蔽，`_COST_VISIBLE_ROLES`=admin/operations_manager/tenant_admin；新增限後台管理角色）+ `GET .../work-orders/{id}/document`（回 application/pdf）。(d) **完工推送** `complete_order` 複用 CR-0028 outbox enqueue `work_order_document` push_kind + Flex builder（服務完成 + 應付總額，只露對外價）。(e) **web** 後台詳情側邊欄「成本明細」面板（含 unit_price，RBAC；mock badge）+ i18n。**測試** `test_cr_0027_quote_document.py` 3 案（重算/RBAC 遮蔽/PDF 無成本外洩/cross-tenant）+ builder 2 案 + tsc 0 error + migration 037 套 dev DB 驗證。**性質**：DB schema + domain + API contract 變更 → 已產 CIA（CR-0027），會議授權用預設先做（數值 is_mock 待財務覆核）。**follow-up**：客戶端 track 頁 PDF 下載（需 public token 端點）、後台 PDF 下載按鈕、完整報價引擎/service_catalog。

- **公單（WorkOrder）標準化欄位補洞（CR-0026，branch `feat/cr-0026-workorder-fields`，2026-06-18）**：2026-06-17 會議 Action #1「公單欄位對不上 Johnson 紙本」+ 決議 3「schema 先補欄位前端才有資料」。現行 `work_orders` 是扁平最小結構（只有 customer_name/phone/address + 兩個 FLOAT 價格）。**以會議為主、藍圖/PDF 為輔（數值不抄、僅引欄位語意）**，採會議授權的合理預設先做（業主日後微調）。**實作**：(a) **schema** migration `036`：work_orders 補 16 欄（設備辨識 brand/model/serial_number/door_type/door_thickness/is_interior_door、服務類別 service_category、problem_type、保固 warranty_status/purchase_date/invoice_no、完工細狀態 completion_status〔M05 Q052 六段〕、status_reason、parent_work_order_id〔返修連回〕、customer_final_amount）+ work_orders/scope_changes 補 tenant_id〔multi-tenant 預留，backfill 84 列〕+ 3 index；全 nullable 可逆；Schema.sql 同步。(b) **service** `create_from_problem_card` 建單時從 problem_cards 複製 brand/model/problem_type/photos + 寫 tenant_id；`assign_order` 加**派工前必填 gate**（`_assert_dispatch_ready`：缺品牌/型號/地址/問題類型 → 422，BR-M05-03）；`cancel_order` 加 **status_reason 必填 gate**（BR-M05-01）+ 寫結構化 status_reason 欄（原僅塞 service_report 字串）。(c) **API** `WorkOrder` pydantic model + TS 型別補 13 optional 欄；`_WO_SELECT`/`_wo_row_to_dict` append 新欄（brand/model 改 `COALESCE(wo.*, pc.*)`）→ response 自動帶出。(d) **web** 後台詳情側邊欄新增「公單資訊」面板（服務類別/問題類型/保固/門型/完工狀態/狀態原因）+ 綁真實 S/N（原示意）；i18n zh-TW/en。**測試** `test_cr_0026_wo_fields.py` 4 案（serializer 映射 ×3 + dispatch gate）+ work_order 相關回歸 55 案 pass + tsc 0 error。**採用預設（§8 待業主確認）**：免責先佔位、完工六段、保固人工填、成本拆項切 CR-0027。**性質**：DB schema + domain + API contract 變更 → 已產 CIA（CR-0026），會議授權用預設先做。

- **使用者自助忘記密碼 / 重設密碼（CR-0025 / ADR-0114，branch `feat/self-service-password-reset`，2026-06-17）**：兩個登入頁（`/login` 管理員、`/tech-login` 技師）加上可點的「忘記密碼」→ 自助 email 重設流程。**背景**：原僅 `admin-reset-password`（需另一已登入 admin），技師頁只有不可點提示、管理員頁完全沒有，且唯一 admin 忘記密碼有雞生蛋。**業主裁決（推翻 2026-06-10 Action #7「不建自助」）**：Email 送達、兩頁都做、30 分單次 token、首次重設視同驗證、枚舉防護回 200、保留 admin 代重設為後備。**實作**：(a) **schema** migration `035` `password_reset_tokens`（token 只存 SHA-256 雜湊、TTL 30 分 + used_at 單次用、ON DELETE CASCADE）；(b) **api** `email_provider.py`（SMTP 抽象，未配置 fail-safe 回 False 不 500）+ `password_reset_service.py`（request：簽高熵 token + per-user rate-limit + 一律安靜返回；confirm：驗未過期/未用 → 改密碼 + 標 used）+ `POST /auth/request-password-reset`（一律 200）+ `/auth/confirm-password-reset`（204）；(c) **web** `/forgot-password` + `/reset-password` 兩頁 + 兩登入頁可點連結 + api client + i18n（passwordReset namespace）。**測試** `test_password_reset.py` 8 案（happy/枚舉/停用/過期/已用/不存在/單次用）—— 需 dev stack（DB + migration 035）跑；tsc --noEmit 0 error。**安全限制（follow-up）**：confirm 後未全域撤該 user refresh token（revoked_jti 為 per-jti，全域失效需 `password_changed_at` epoch，另 CR）。**prod 啟用待辦**：建 SMTP secret + api.sh 接線 + 套 migration 035（未配置時 request 仍 200、token 建但信不送 = fail-safe）。

- **🚀 Cloud Run 首次正式上線（三服務 + Cloud SQL，branch `chore/deploy-cloud-run-wiring`，2026-06-16）**：把 api / agent / web 三服務首次部署到 GCP Cloud Run（`cedar-scope-489604-g3` / asia-east1），接 prod Cloud SQL `lock-ai`（pg18）。**部署順序** api → agent → web（agent/web 都依賴 api 的 Cloud Run URL）。**deploy 腳本補齊本輪新功能所需接線**：(a) `api.sh` 加 secrets `INTERNAL_API_TOKEN` + `LINE_CHANNEL_ACCESS_TOKEN`、env `AGENT_TENANT_ID`、部署時動態解析 web URL → `CORS_ORIGINS`；(b) `agent.sh` 加 secret `INTERNAL_API_TOKEN`、env `AGENT_TENANT_ID` + 動態解析 api URL → `LOCK_API_BASE_URL`；(c) `web.sh` build 時解析 api URL 傳 build-arg `NEXT_PUBLIC_API_BASE_URL`（https）+ `NEXT_PUBLIC_REALTIME_BASE_URL`（wss）；web/Dockerfile 加對應 ARG。新建 secret `API_JWT_SECRET_KEY` + `INTERNAL_API_TOKEN` 並授權 SA。**prod URL**：api `smart-lock-api-…run.app`、agent `…/callback`、web `smart-lock-web-…run.app`。**LINE webhook 已切 prod agent**。三服務 health + 登入 + 對話寫入 + 14 頁巡檢全綠。

- **prod Cloud SQL schema 對齊 + 套用工具（同分支，2026-06-16）**：首次部署後 prod DB（停在舊版）缺 `conversations.document_number` 等欄位 → 多資料頁 500。新增 `scripts/db/apply-schema-prod.sh`（依 quickstart 正規序 `Schema.sql → Schema_*.sql → migrations/*.sql` idempotent 套用、不灌 demo seed），經 cloud-sql-proxy 套到 prod（先建 on-demand 備份）。過程修 3 個 schema 檔問題（見 Fixed）。完整重跑零錯誤、14 頁 Playwright 巡檢全綠。

- **對話轉真人 handover 生命週期 Phase 1（CR-0024，branch `feat/handover-lifecycle-phase1`，2026-06-16）**：補上 handover 的「回程」缺口。前因：`fix/escalation-set-conversation-status` 讓轉真人翻 `escalated` 啟用客服發訊，但(1) **AI 從未真的停**（`line_gateway.py` 對每則訊息都跑 turn 回覆、不查狀態 → AI 與真人同時回）(2) **沒有交還 AI 機制**（無 endpoint 能把 escalated 改回）(3) 工單結案不連動對話。**業主裁決（CR-0024 §8）**：接管期間 AI 暫停、交還由「按鈕+工單結案」兩者觸發、交還後回 `active`。**Phase 1 實作**：(a) **API** `POST /tenants/{tid}/conversations/{id}/resolve-handover`（escalated→active，RBAC admin/CS、cross-tenant guard、非 escalated→409）+ internal GET `/internal/conversations/handover-state`（gateway 回覆前查接管旗標）+ `work_order_service` complete/cancel 後 fail-soft 連動 un-escalate（work_order→problem_card→conversation 鏈）；(b) **Agent** `line_gateway.py` 回覆前查 handover-state，escalated → **AI 全暫停**（不跑 turn、不回覆，只旁路持久化客人訊息讓客服看得到；fail-soft：查詢失敗仍照常回）；(c) **Web** 對話詳情頁接管橫幅加「結束接管/交還 AI」按鈕 → escalated→active、發訊框轉唯讀。**測試** `test_handover_lifecycle.py` 8 passed（resolve happy/409/RBAC/cross-tenant + handover-state true/false/resolve 後 false + WO 結案連動）；回歸 escalation+internal+conversations_v2 共 32 passed。**Playwright 實證**：新建 escalated 對話 → 點按鈕 → 狀態「已升級→進行中」、橫幅/按鈕消失、發訊框轉唯讀、toast「已結束接管」。**Phase 2（未做）**：D1 智慧路由 —— 接管期間 AI 用 relatedness 分類，仍回答與轉接無關的新問題。

- **即時推送（WebSocket）docker 部署接線（branch `feat/web-realtime-enable`，2026-06-16）**：後台通知中心 / 接單池 / 改約頁 / 角色權限變更橫幅顯示「Realtime 未配置」。**根因**：後端 WS server 早已內建於 api（`api/main.py` `@app.websocket("/realtime/notifications/{user_id}")` / `/work-orders/{wo_id}` 等 + `realtime/ws_hub.py`，query 帶 `access_token`+`tenant_id` 驗證），缺的只是前端環境變數 `NEXT_PUBLIC_REALTIME_BASE_URL` —— Next.js `NEXT_PUBLIC_*` 是 **build-time inline**、預設空字串 → `realtime.ts` 走 `disabled` 降級（頁面仍以一般 fetch 運作、僅無即時 push）。對比 `NEXT_PUBLIC_API_BASE_URL` 因 code 內有 `?? "http://localhost:8001"` 預設值故一直能用。**修復**：(a) `web/Dockerfile` builder stage 加 `ARG NEXT_PUBLIC_REALTIME_BASE_URL` → `ENV`（next build 前烤入 bundle）；(b) `docker-compose.yml` web `build.args` 帶 `${NEXT_PUBLIC_REALTIME_BASE_URL:-ws://localhost:8001}`（瀏覽器直連 api published port 8001）。**驗證**：重建 web → bundle 內確認含 `ws://localhost:8001`；Playwright 登入通知頁 → 指示燈由「未配置」(disabled) 變 open「即時連線」(綠燈) —— WS 連上且通過 token 驗證。**Cloud Run**：換 `wss://<api 網域>` 同 build-arg 即可（雲端 web build 時傳入）。

### Changed

- **散落/暫存文件治理歸檔（branch `chore/archive-scattered-docs`，2026-06-18）**：依一次全 repo 散落文件稽核（107 檔 / 12 叢集，結論 **0 刪除**）把 **17 份**「已非現行事實但具歷史/audit 價值」的檔以 `git mv` 搬入 `docs/_archive/`（保留 rename 歷史），新增 `docs/_archive/README.md` 歸檔索引（對照表 + 不要做警告）。**搬移**：`agent_x.md`（115KB EA 白皮書，LockCore 重寫前殘留）、`docs/prd/{BIZ-0001,SOW-0001}`（舊 LangGraph/LangChain 架構，被 ADR-0107 取代）、`reports/*` 3 份 P4 機器報告、`docs/_audit/{session-summary-2026-06-05,session-2026-06-05-final-stats,playwright-e2e-verify-session-2026-06-07}`、`docs/_ops/release-checklist-2026-06-05`、`docs/_index/{traceability-matrix,migration/fr-mapping}`（失準 cache / 規劃已落地）、`web/docs/{progress-report-2026-04-29,progress-report-2026-05-05,wbs-completion-report}`（被 `system-completion-status.md` 取代）、`docs/ops/release-readiness.md`（被 `-smart-lock-saas` 版 superseded）、`pending-business-decisions-2026-06-06.html`（**改歸檔不硬刪**，仍被 ADR-0108/0109 引為證據）。**斷鏈修正**：更新 9 個 active 文件的反向引用指向新歸檔路徑（pending-business HTML / traceability-matrix / session / release-checklist / release-readiness）。**刻意未歸檔**：`docs/governance/reviews/` 7 份 lane-a critique —— 稽核原建議歸檔，但執行時發現 7 個 tier-1 ADR 以 clickable link 反向引用、且 append-only 不可改，歸檔會造成不可修復斷鏈 → 留原位。**性質**：純文件搬移與索引，無 schema / API contract / flow 變動 → 不觸發 CIA。

### Fixed

- **CI unit job 指向已刪目錄 + payout-rules note 洩漏欄名（CR-0038 階段0，branch `chore/stage0-migration-ci`，2026-06-19）**：(a) `.github/workflows/test-suite.yml` 的 unit job 跑 `pytest tests/unit`（`harness` 已於 dev_new_arch 退場、該目錄不存在）→ 5 collection ERROR，job 形同虛設、945 component 測試亦 0 執行（CI「綠」只代表沒真跑）。修為 `cd api && uv run pytest -m unit`（用 `api/pytest.ini` testpaths=tests + marker 過濾），**226 個純函式 unit 真綠**。(b) 補套 035/045 後 `test_cr_0037` 一跑即抓到真實洩漏：`GET /payout-rules` 的 `note` 文字含字面「base_payout」→ 對 cost-masked 角色（customer_service）洩漏內部欄位名（數值雖已結構性遮蔽）。修 `catalog_v2.py` note 文案不再嵌欄名（「內部拆帳成本僅後台角色可見」）。**性質**：CI 設定 + 一行文案，無 contract 變動。
- **註冊→登入路由收尾 + 廠商登入頁（CR-0029 收尾，branch `fix/cr-0029-vendor-login-routing`，2026-06-18）**：CR-0029 建了 vendor 註冊 + `/vendors/login` 後端端點，但前端漏了兩塊。(a) **廠商無前端登入頁** → 新增 `loginVendor` api helper + `/vendor-login` 頁（呼 `/vendors/login`，登入後暫導 `/dashboard`，廠商專區待 CR-0031）。(b) **`/register` 註冊成功「前往登入」一律連管理員 `/login`**（技師其實該去 `/tech-login`、廠商去 `/vendor-login`）→ 依註冊身分（tab）導向正確登入頁。(c) `/tech-login` 加註冊連結。**註**：師傅登入頁 `/tech-login` 本即存在（loginTechnician → `/technicians/login`），非本次新作。tsc 0 error。**性質**：純前端路由/頁面補完 + api helper，無 schema/contract 變動 → 不觸發 CIA。**follow-up**：vendor 登入後的廠商專區/RBAC（CR-0031）。

- **LINE 公單回傳斷鏈修復（CR-0028，branch `feat/cr-0028-line-wo-pushback`，2026-06-18）**：2026-06-17 會議最痛的工單問題「公單可以開，但 LINE 那端收不到公單回傳（轉真人→派工→報價回 LINE 斷在後台）」(Action 6)。**根因有二**：(1) `work_order_service.assign_order`（派工）/`accept_order`（接單）不推 LINE、`scope_change_service.respond_public`（客戶報價決議後）不回推確認 —— 三個關鍵節點客戶都收不到通知；(2) **outbox worker resolver bug** —— `line_push_outbox_worker._resolve_line_uid` 對 `work_orders`/`scope_changes` 路徑用了**不存在的欄** `work_orders.tenant_id`（work_orders 租戶隔離其實走 `users.tenant_id` join 鏈）→ 查詢丟例外被吞 → 反查客戶 LINE uid 一直靜默失敗，連既有 `scope_change_proposal` 提案 push 也從未真的送出。**修復**：(a) resolver 兩路徑 `WHERE wo.tenant_id` → `WHERE u.tenant_id`（與 `_WO_JOIN` 一致；`saas.reschedule_proposal` 路徑用 `rp.tenant_id` 真有此欄、不動）；(b) **複用 CR-0017 outbox 基礎建設**（無 schema 變更，`push_kind` VARCHAR(40) 無 CHECK）新增 3 個 push_kind + Flex builder：`work_order_assigned`（派工）/`work_order_accepted`（接單）/`scope_change_result`（報價決議回推，accept/reject 文案不同）；(c) 三處 service 加 best-effort enqueue（`respond_public` 無 tenant 上下文 → 從 work_order join 推導 tenant_id）。**測試** `test_cr_0028_wo_push.py` 6 案 pass（純函式：builder render/dispatch/PushKind）+ CR-0017 回歸 17 案 pass + 5 檔 py_compile pass。**順帶**補正 CR-0017 文件 status（`open-awaiting-decisions`→`built`，code 早已 BUILD）。**性質**：bug fix + 複用既有 outbox 接線，無 DB schema/contract 變動。推播文案用合理預設、待業主微調。**整合驗證待辦**：本機起 stack 派工 → 確認 outbox row（LINE 真送需整合環境 + token）。
- **知識庫殘留內部 UUID 收尾（會議 Action #5 延續，branch `fix/kb-residual-internal-id`，2026-06-18）**：先前 `fix/kb-cases-hide-internal-id` 已修 cases/[id] + sop-drafts header；本次清掉兩處殘留——(a) `knowledge-base/family-reviews/page.tsx` 家族覆核清單把 `sop_draft_id.slice(0,8)` 顯示在 title 下方（title 已足夠 → 移除冗餘 UUID）；(b) `knowledge-base/sop-drafts/[id]/page.tsx`「來源問題卡」連結原以 `problem_card_id.slice(0,8)` 當連結文字（露 PC 內部 UUID）→ 改為標籤「來源問題卡 →」、保留 href 導航。**刻意保留**：family-reviews 稽核 log 表格欄（sop_draft_id/reviewer_id 短碼）屬 admin 內部稽核追溯，非會議針對的消費者面向洩漏。**性質**：純前端顯示調整，無 schema/contract/flow 變動 → 不觸發 CIA；tsc 0 error。

- **技師登入頁「忘記密碼」死按鈕收尾（branch `fix/tech-login-forgot-hint`，2026-06-17）**：20260610 會議 Action #7「忘記密碼功能」**已由 `admin-reset-password`（admin 代重設、回臨時密碼、免 email 基礎設施）實作完成**；但 `tech-login` 頁仍留一顆 `disabled` 的「忘記密碼（待補）/ V2.0 規劃」按鈕——看似可點卻無作用，誤導使用者以為有自助流程。經業主裁決（2026-06-17）**不建自助重設**（會用到的後台 staff/技師無現成送達管道、寄信基礎建設從零，成本不划算）。**修復**：把該 `<button disabled>` 改為純資訊 `<span>`，文案改「忘記密碼？請聯絡管理員重設」（tooltip 說明「目前不支援自助重設，請聯絡管理員代為重設密碼」）。**性質**：純前端文案/標記調整，無 schema / API contract / flow 變動 → 不觸發 CIA。自助重設（email/LINE/SMS OTP，需 reset_token 表 + 管道 + CIA）列為未來可選增強，現階段不做。

- **知識庫案例詳情頁洩漏內部 UUID（branch `fix/kb-cases-hide-internal-id`，2026-06-17）**：`knowledge-base/cases/[id]` 詳情頁頁尾把案例的內部 UUID（`entry.id`）以 `ID: {uuid}` 直接顯示在畫面上（20260610 會議 Action #6「系統內部欄位要過濾掉，消費者不該看到」；sop-drafts 頁先前已只顯示人類可讀 `document_number`，此頁漏修）。**修復**：移除該 `<span>` 顯示，並清掉兩語系 `kb.cases.detail.id` 孤兒 key（`zh-TW.json` / `en.json`）。內部 id 仍用於路由（網址列），畫面不再重複外露。**性質**：純前端移除已洩漏欄位顯示，無 schema / API contract / flow 變動 → 不觸發 CIA。**註**：case_entries 是否要比照 work_orders/sop_drafts 補人類可讀 `document_number`（需 schema 變更 + CIA）列為後續可選增強，本次不做（會議要求是「過濾掉」而非「給編號」）。

- **Cloud Run 首次部署踩到的一連串 prod 問題（branch `chore/deploy-cloud-run-wiring`，2026-06-16）**：部署/實測逐一暴露並修復（compose DB 走 dump/restore 故本機從未暴露）——
  1. **登入 500**：`api.sh` 把 secret 映成 env 名 `JWT_SECRET_KEY`，但 code 讀 `API_JWT_SECRET_KEY` → 容器內該名未設。改 `API_JWT_SECRET_KEY=API_JWT_SECRET_KEY:latest`。
  2. **跨網域登入 Failed to fetch（CORS）**：api `allow_origins` 預設只 localhost。`main.py` 改讀 env `CORS_ORIGINS`（逗號或空白分隔）、`api.sh` 部署時帶入 web 的**兩個** Cloud Run 網址形式（status.url 的 `-de` 形式 + projectnumber 形式；以空白分隔避免被 gcloud `--set-env-vars` 逗號拆掉）。
  3. **prod DB schema 落後**：`conversations.document_number` 等缺欄 → 資料頁 500（ERR_FAILED）。經 proxy 套完整 schema 對齊。並修 3 個 schema 檔問題：(a) 新 migration `034` 把 prod 舊版 `role_permissions`（join `role_id/permission_id`、空表）DROP 重建為 F-019 spec（安全閥：非空中止）；(b) `015` kb_audit_log partial index 移除非法 `NOW()` predicate；(c) `Schema_v2_extensions` 移除舊 RBAC join 索引死碼。
  4. **LINE 對話寫不進後台**：`INTERNAL_API_TOKEN` secret 值帶尾換行 `\n`（`openssl rand | gcloud secrets create` 留的）→ agent 放進 `X-Internal-Token` header 被 httpx 拒（`LocalProtocolError: Illegal header value`）→ 橋接請求瞬間失敗、從未送達 api。修：agent gateway 三處讀 token 後 `.strip()`、api `require_internal_token` 比對前 strip expected+incoming（不輪替 prod secret、不登出任何人）。實證 `ingest turn appended=2` / `ingest escalation created=True` 皆 200。

- **深色模式白字白底（branch `chore/deploy-cloud-run-wiring`，2026-06-16）**：app 預設主題 `system`，OS 深色下套深色主題，但約 68 個元件硬編 Tailwind arbitrary 中性色（`bg-[#F8FAFC]`/`[#FFFFFF]`/`[#F1F5F9]`、`text-[#18181B]`/`[#71717A]` 等）不隨 `data-theme` 翻轉 → 主題文字翻淺、卡片背景仍硬編淺色 → 白字白底（settings 最明顯）。修（單檔過渡補丁）：`globals.css` 在 `[data-theme="dark"]` 下把中性硬編 surface/text/border 重映射到主題變數（`!important`）；語意 badge bg+fg 成對硬編、本身可讀不動。Playwright 模擬深色 `/settings` 低對比文字 25 → 1。正解（元件改用 `var()`，~68 檔）列後續。

- **對話管理聊天捲軸自動跳到最底（branch `feat/handover-lifecycle-phase1`，2026-06-16）**：`ChatTimeline` 原本載入後停在最舊訊息，要手動往下捲才看到最新對話。加 `scrollRef` + `useEffect`（依「訊息數 + 最後一則 id」觸發 `scrollTop = scrollHeight`），涵蓋初次載入 / refetch / 客服送出新訊息三種情境。Playwright 實證 8 則訊息對話開啟即停在底部（`atBottom: true`）。

- **診斷 SSE `/realtime/diagnostics/{id}` 404 無限重連 — 與 WS 頻道解耦（branch `feat/handover-lifecycle-phase1`，2026-06-16）**：`feat/web-realtime-enable` 開了 `NEXT_PUBLIC_REALTIME_BASE_URL` 後,對話詳情頁 `DiagnosticReasoningPanel` 經 `lib/sse.ts`（EventSource）連 `/realtime/diagnostics/{conv_id}`,但**後端從未實作此 SSE 端點**（`api/main.py` 只有 WS 頻道,diagnostics 標「另開」）→ EventSource 拿 404 後自動無限重連,單一對話頁累積 180+ console error。根因:`sse.ts`（diagnostics）與 `realtime.ts`（WS 頻道）**共用同一個 env**,開 WS 連帶把不存在的 SSE 打開。**修復**:`sse.ts` 改用獨立 env `NEXT_PUBLIC_DIAGNOSTICS_SSE_BASE_URL`（預設不設 = 停用,待後端實作再開）,WS 頻道不受影響。**驗證**:重建 web → 對話詳情頁停留 4s **0 console error**（原 180+）;通知頁 WS 指示燈仍「即時連線」。

- **客服接管發訊不到 LINE — api 缺 line-bot-sdk 依賴（branch `fix/escalation-set-conversation-status`，2026-06-16）**：對話翻 escalated 後，客服在對話管理打字「發送」→ 訊息有寫進 DB（handover write 成功），但 **push 不到 LINE**。根因：`api/services/line_push_service.py`（含 `line_webhook.py` / `line_push_outbox_worker.py`）`import linebot.v3` 但 **`api/pyproject.toml` 從未宣告 `line-bot-sdk` 依賴** → 容器內 `No module named 'linebot'` → `_get_configuration()` 回 None → push fail-soft 靜默略過（log 誤報 `LINE_CHANNEL_ACCESS_TOKEN missing`，實際 token 有設、是 SDK 缺）。**修復**：`line-bot-sdk>=3.0` + `aiohttp>=3.9` 加進 api **正式依賴**（非 optional —— webhook 接收 / handover push / outbox worker 皆 API 常駐功能），重生 `uv.lock`。**驗證**：重建 docker api → 容器內 `import linebot.v3.messaging` OK、token printenv 確認在位。**註**：實際對外 push 由使用者從 UI 自測（避免自動發訊給真實 LINE 用戶）;Cloud Run 部署同步受惠（雲端 api 映像本來也缺此 SDK）。

- **轉真人後對話管理仍無法回覆 — escalation 漏翻對話狀態（branch `fix/escalation-set-conversation-status`，2026-06-16）**：實機在 LINE 要求真人客服後，AI 已回「已轉接」、後台也產出 AI 草擬問題卡（`source=ai_line`），但「對話管理」詳情頁的 `HandoverComposer` 發訊框永遠唯讀、客服回不了 LINE。**根因**：發訊框啟用與 `send_message` 都以 `conversation.status='escalated'`（API `waiting_human`）為前提，但 **全 codebase 沒有任何路徑會把 `conversations.status` 設成 `escalated`** —— `problem_card_service.escalation_to_draft_pc()` 只建/更新問題卡，漏了翻對話狀態（F-018 handover 收尾未接上的線）。**修復**：`escalation_to_draft_pc()` 在 ensure conversation 後加一條 `UPDATE conversations SET status='escalated'`（`IS DISTINCT FROM 'escalated'` 守 idempotent、含 resolved 後再次轉真人的 re-escalate），不限原狀態、不動 message_count。對齊既有合約狀態 `waiting_human`，**不改 API contract、不加 endpoint、不動狀態機定義**。**測試** `test_escalation_to_draft_pc.py` 補 2 案（單次 escalation → `waiting_human`、dedup 再 escalation 仍 `waiting_human`）共 8 passed；回歸 `test_internal_ingest.py` + `test_conversations_v2_endpoint.py` 24 passed。**驗證**：重建 docker api + 把實機對話 `15e3dbf7`（丁啟恆）翻 escalated → Playwright 確認發訊框由唯讀變可輸入（placeholder 切換、灰字提示消失）、打字後「發送」鈕啟用。**性質**：屬 bug fix（範圍在單一 function、對齊既有合約狀態），未跑完整 CIA。

- **agent 容器 `ModuleNotFoundError: lockcore`（branch `fix/agent-dockerfile-pythonpath`，2026-06-16）**：`docker compose up` 後 agent 容器陷入重啟迴圈。根因：`agent/Dockerfile` 為 layer cache 先 `COPY agent/pyproject.toml` → `uv sync`（此時 lockcore 原始碼尚未 COPY）→ 建出的 wheel 只有 dist-info、無 lockcore 模組；之後才 COPY 原始碼。而 `python scripts/line_gateway.py` 的 `sys.path[0]` 是 `scripts/`、不含 `/app/agent`，故 `import lockcore` 失敗（api 無此問題是因它直接從 `/app/api` 跑 `main.py`）。**修復**：runtime stage 加 `ENV PYTHONPATH=/app/agent`，讓 `import lockcore` 從 COPY 進來的原始碼樹解析。**驗證**：重建 agent 映像後容器正常啟動、banner 顯示「API 橋接 ✅ 啟用 → http://api:8080」；docker 全棧（api :8001 healthy / agent :8000 handler 400 / web :3000 200）+ 真實 `locksmart` payload 經 docker 版 api 寫入 DB（tenant resolver 生效，2 訊息）。

- **LINE 對話/轉真人進不了工單系統 — tenant 別名 500 + 橋接靜默略過（branch `fix/agent-api-bridge-visibility`，2026-06-15）**：實機在 LINE 對話後，紀錄沒進後台。查出**兩個斷點**：(1) **gateway 對 API 橋接（方案 A `_persist_turn_safe` / CR-0022 escalation 旁路）在 `INTERNAL_API_TOKEN` / `LOCK_API_BASE_URL` 未設時「靜默 return」**，且這兩個變數要放 `agent/.env`（gateway 讀的是它，非專案根 `.env`）卻無範例可循 → 使用者無從察覺;(2) 即使橋接開了，**agent 送的 `tenant_id` 是別名字串 `"locksmart"`，而 DB tenant 欄位是 UUID → psycopg `invalid input syntax for type uuid` 直接 500**。**修復**：(a) gateway 啟動 banner 明示「API 橋接:✅啟用/⚠️停用（缺哪個變數）」+ 記憶後端，並對所有啟動 print 加 `flush=True`（重導向到檔案也印得出，不再被 buffer 吃掉）;(b) 新增 `agent/.env.example` 文件化 gateway 真正讀的 `agent/.env`（LINE creds + `INTERNAL_API_TOKEN` + `LOCK_API_BASE_URL` + 選用 Postgres）;(c) **API `internal_ingest._resolve_tenant_id()`**：兩個 ingest 端點的單一收斂點，合法 UUID 原樣用、別名（如 `locksmart`）以 `AGENT_TENANT_ID` 環境變數對應到實際租戶 UUID（未設→400 明確報錯而非 500、設定非 UUID→500）;(d) `dev-up.sh` + 根 `.env.example` 補 `AGENT_TENANT_ID`（dev 預設 seed 租戶）。**驗證**：用真實 `tenant_id="locksmart"` payload 打兩端點 → 對話 ingest 2 訊息、escalation 建 AI 草擬卡且**復用同一 conversation_id**（→ 轉工單後詳情頁可顯示逐字稿）;`test_internal_ingest.py` 補 4 案（UUID passthrough / 別名走 env / 缺 env 400 / env 非 UUID 500）+ 回歸 internal+escalation 11 案全綠;ruff 全綠。**註**：多租戶日後需把別名解析改為 `saas.tenant` slug→UUID 查表。

- **側欄子選單雙亮修正（branch `fix/sidebar-double-active`，2026-06-15）**：「帳務與結算」展開時，「月結算總覽」(`/accounting`) 與「會計傳票」(`/accounting/vouchers`) 在子頁同時亮起。根因 `Sidebar.isChildActive` 用 `pathname.startsWith(child.href + "/")`，區段根頁 `/accounting` 前綴命中所有子頁。修為前綴命中時，若有更精確（href 更長）的 sibling 也命中則本項不亮。Playwright MCP 驗證：`/accounting` 只亮月結算總覽、`/accounting/vouchers` 只亮會計傳票。順手修 `cr-0022-hitl.spec.ts` 一個 `convertBody` 型別 cast（tsc 0 error）。

### Added

- **DB 納入 docker-compose（自包含全棧，branch `feat/compose-db-service`，2026-06-16）**：把原本「連既有 `lock_AI` 容器」改為 **compose 自帶 `db` service**（pgvector/pg17 + named volume `pgdata`），真正 `docker compose up` 一鍵全起 db+api+agent+web。**資料遷移**：`pg_dump -Fc` 既有 `lock_AI`（81 表）→ `pg_restore --no-owner --clean` 進 compose db；驗證 public(50)/saas(29)/agent(2) schema 全到、conversations 126 / problem_cards 105 列完整。api/agent 的 `POSTGRES_URI` 改走 compose 內網 `db:5432`（移除 `host.docker.internal`）。**舊 `lock_AI` 容器只停不刪**（備援，確認無誤後可 `docker rm`）。compose header 加 `down -v 會清 pgdata` 警示。**驗證**：四服務全起（db/api healthy）、真實 `locksmart` payload 經 api 寫入新 db（conversations 126→127）、agent 橋接 ✅。Cloud Run 仍不吃 compose（雲端走 Cloud SQL + deploy 腳本）。

- **本機全 docker 編排 `docker-compose.yml`（branch `feat/agent-deploy-dockerfile`，2026-06-16）**：把目前還用原生 `uv run` / `npm` 跑的 **api / agent / web 三服務改成 `docker compose up` 一鍵起**（沿用三個既有 Dockerfile）。設計：(a) **DB 不納 compose** —— 沿用既有已灌資料的 `lock_AI` 容器，三服務透過 `host.docker.internal:5433` 連它（零資料遷移風險）；(b) **ngrok 不納** —— 維持 host 上 `ngrok http 8000` 打 agent；(c) 服務間走 compose 網路（agent→api 用 `http://api:8080`），web 的 `NEXT_PUBLIC_API_BASE_URL` build 時烤成 `http://localhost:8001`（瀏覽器端）故 api published 8001；(d) 機密讀根 `.env`（env_file），host 專屬值（`POSTGRES_URI`/`LOCK_API_BASE_URL`/`INTERNAL_API_TOKEN`/`AGENT_TENANT_ID`）在 `environment` 覆蓋；Vertex `credentials.json` 唯讀掛載（不入映像）。`docker compose config` 解析通過。**注意**：(1) 起前需停掉佔用 8000/8001 的原生進程；(2) **僅供本機** —— Cloud Run 不吃 docker-compose，雲端仍走 per-service `deploy/{api,agent,web}.sh`；(3) LINE→工單 tenant 解析修復在 `fix/agent-api-bridge-visibility`，該分支併入後 docker 全鏈才完整。

- **agent 部署容器化補齊 + Postgres extension bootstrap（branch `feat/agent-deploy-dockerfile`，2026-06-16）**：`scripts/deploy/agent.sh` 一直引用 `agent/Dockerfile` 但該檔**從未存在** → agent 根本無法部署到 Cloud Run。補上：(a) **`agent/Dockerfile`**（mirror `api/Dockerfile` 的 multi-stage uv build；`uv sync --frozen --no-dev --package lock-cs-agent --extra line --extra vertex`；入口 `scripts/line_gateway.py` 讀 `PORT`；機密走 Secret Manager、Vertex 走 Cloud Run SA ADC，不入映像）；(b) **`agent/Dockerfile.dockerignore`**（擋 `.env` / `credentials.json` / `*.db` 進映像 + 縮 build context）；(c) **`SQL/migrations/000-extensions.sql`**（全新 DB / Cloud SQL 最先跑，一次開齊 `vector`(KB RAG 向量) / `pg_trgm`(中文 trigram) / `pgcrypto`(gen_random_uuid) / `uuid-ossp`；既有 DB idempotent no-op）；(d) **`uv.lock` 更新**（committed 版仍是舊 package 名 `smart-lock-agent` + 缺 vertex 鎖 → frozen build 必失敗；改為與 pyproject 同步的 `lock-cs-agent` + 全 extras）。**驗證**：`uv lock --check` 通過；`uv sync … --extra line --extra vertex --dry-run` 解析成功（line-bot-sdk / aiplatform / google-genai 納入，精簡掉 57 dev 套件）；`000-extensions.sql` 本機 pg17 套用 idempotent OK。**本機零影響**（Dockerfile 只在 deploy build 用、extensions.sql 對既有 DB no-op）。**部署待辦（不在本變更）**：`agent.sh` 的 secrets 尚未含 `INTERNAL_API_TOKEN` / 未設 `LOCK_API_BASE_URL` / `AGENT_TENANT_ID` → Cloud Run agent 的對話/工單旁路要這三個才會通（見 `fix/agent-api-bridge-visibility`）。

- **agent per-user 記憶後端 SQLite → Postgres（CR-0023 / ADR-0113，branch `feat/agent-memory-postgres`，2026-06-15）**：LockCore CS agent 的 per-user 記憶（`MemoryStore`）與轉真人稽核（`EscalationStore`）原寫 agent 端本地 SQLite（FTS5 trigram），是與後台 Postgres 分離的**資料孤島**、多實例不共享。新增**可由 config 切換的 Postgres 後端**，置於 lock-ai Cloud SQL 獨立 schema `agent`。**業主裁決（CR-0023 §8，全推薦）**：(1) 沿用 lock-ai Cloud SQL + 新 `agent` schema，tenant 仍存字串（不對齊 saas UUID）；(2) 中文子字串檢索改 **pg_trgm + GIN**（取代 FTS5 trigram，ILIKE 命中 + `similarity()` 排序）；(3) **保留 SQLite** 走 config 切換（測試 / `:memory:` 隔離續用）；(4) 既有 `memory.db` 不遷、丟棄 PoC 資料。**實作**：(a) `postgres_store.py`（`PostgresMemoryStore` / `PostgresEscalationStore`，sync psycopg、lazy connect + autocommit + 斷線重連，同 dataclass 同方法簽名）；(b) `provider.py` 抽共用 `_StoreBackedProvider`，`SqliteMemoryProvider` / `PostgresMemoryProvider` 各自只建 store（DRY、store-agnostic）；(c) `config.toml [memory] backend` + `postgres_uri_env`，`app_config.build_memory_manager` / `build_escalation_store` 依 backend 分流；(d) `pyproject.toml` 新增 optional `[postgres] = psycopg[binary]`（SQLite-only 安裝免裝）；(e) `SQL/migrations/033` schema `agent` + pg_trgm + 2 表 + 4 indexes（含 trigram GIN）。**Architecture Lock 合規**：走既有 `MemoryProvider` 抽象（設計原意即「換後端加 provider」），不另寫 agent 核心、不動 `CS_TOOL_ALLOWLIST`。**測試** `tests/test_memory_postgres.py` 本機 Docker `lock_AI_data`(pg17) 套 migration 033 + **7 passed**（中文檢索 / scope 隔離 / forget / default-deny / escalation JSONB roundtrip / backend 切換回歸）。預設仍 `backend="sqlite"`（反相容、零行為變更）；正式環境設 `postgres` + `POSTGRES_URI` 並先跑 migration 033 即切。

- **HTML 文件統一收進 `docs/html/` + 導覽中心連結（branch `docs/html-consolidation`，2026-06-15）**：把分散的自包含 HTML（`docs/index.html`、`agent/evals/agent-eval-report.html`、`docs/qa/cr-0022-manual-test-guide.html`）`git mv` 統一到 `docs/html/`。導覽中心（index.html）新增「📑 本專案 HTML 報告」區塊以相對連結指向兩份現行報告（CS Agent 評測報告、CR-0022 真人測試指南），並在 header 註明本頁兩類連結（本 repo 現行 HTML 報告 vs 外部 spec repo `chatlock_dev_docs` 2026-05-24 凍結快照）。Playwright 驗證相對連結可導航、報告渲染正常。**註**：index 的角色導覽/文件一覽仍指向外部 spec repo（歷史快照，非本 repo 文件）。

- **工單詳情頁內嵌對話逐字稿（branch `feat/wo-conversation-thread`，2026-06-14）**：工單詳情頁原 `ConversationThread` 是 placeholder（只顯示「示意：問題卡關聯對話將顯示於此」靜態文字），業主在工單上看不到真實對話。**改為真實渲染**：沿用工單既有資料鏈 `work_order → problem_card.conversation_id`（前端早已透過 `ProblemCardSummary onLoaded` 取得），fetch `/conversations/{id}/messages`（與 `LineMediaGallery` 同 pattern），正序渲染氣泡逐字稿 —— `user`=客人（左、白底）、`assistant`=客服/AI（右、主色）、`system`=系統（置中）；含 media 附件連結、「在新視窗開啟」改為真實連結、loading/empty/error/no-conversation 四態、唯讀提示。i18n（zh-TW + en）移除 placeholder key、補 8 個新 key。**驗證**：`tsc --noEmit` 0 error；資料鏈實證（seed 工單 55555555 → pc 44444444 → conv 22222222 → user/assistant 訊息正確渲染，無對話者顯空狀態）。與方案 A（agent 對話旁路持久化）合起來打通「LINE 對話 → DB → 工單後台可見」全鏈。
- **LINE 對話 → 工單 HITL：escalation → AI 草擬問題卡 backend（CR-0022 / ADR-0112，branch `feat/cr-0022-escalation-to-draft-pc`，2026-06-14）**：落地 ADR-0031「AI 草擬 + 客服 1-click 人審」決策（先前 decided 未實作）。承方案 A，補反向缺口：LINE agent `transfer_to_human` 後旁路在 API 建一張 **AI 草擬問題卡**，供客服在既有問題卡頁人審補全 → 既有 confirm → 既有 convert-to-work-order。**AI 永不自轉工單**（ADR-0028 charter，回歸測試守線）。**業主裁決（CR-0022 §8）**：僅明確轉真人/需派工才建、寬鬆 draft 允許缺欄位、旁路即時同步、擴充既有問題卡頁當佇列、先只做來源標記、同對話更新去重。**實作**：(1) `migration 032` problem_cards 加 `source`（human/ai_line）+ `ai_missing_fields` JSONB + 部分索引（**無新增 DB status —— 既有 incomplete 已映射 API draft，零狀態機變更**）；(2) `problem_card_service.escalation_to_draft_pc()` 復用 session_id 冪等 `create_conversation` + conversation_id UNIQUE 去重（同對話更新不重建）+ 寬鬆建立（brand/model 缺不擋）；(3) `POST /api/v1/internal/escalations/ingest`（沿用 `require_internal_token`）；(4) `listProblemCardsV2` + `list_cards` 加 `source` filter（佇列）；(5) agent gateway transfer 後旁路 POST escalation（偵測本輪 escalation id 變化；fail-soft；env 未設則略過）；(6) generated ProblemCard 加 optional `source`/`ai_missing_fields`。**測試** `test_escalation_to_draft_pc.py` 6 案（認證 503/401 + draft 建立 source=ai_line/status=draft/missing hint + session 去重 + **charter lock：AI 卡僅 draft 不得 confirmed** + source filter）；回歸 problem_card/work_order 74 案 + 全套 44 案無破壞。**ADR-0112** 記錄「不新增 status、source 標記、寬鬆建立、AI 不自轉」決策。
  - **前端佇列 UI（§9 step 8，同分支）**：`/problem-cards` 頁加「來源」篩選（全部 / AI 草擬（待轉工單）/ 客服手建，串 `?source=`）；`ProblemCardsTable` 對 `source=ai_line` 卡顯紫色「AI 草擬」badge + 缺漏欄位 hint（`待補：brand、model、location`）。補全→confirm→convert 沿用詳情頁既有 `handleUpdate`(PATCH) / `handleConfirm` / `handleConvertToWO`，**無需新造**。`api.generated.ts` ProblemCard 加 optional `source`/`ai_missing_fields`。`tsc --noEmit` 0 error；i18n zh-TW + en 加 `filters.source`。
  - **開單 address modal（Playwright MCP user-flow 測試揪出的 UX 缺口修復）**：問題卡詳情頁「開單」原直接呼叫 convert 不帶 body，地址僅靠 user profile 解析 → AI 草擬卡（LINE user 無 profile 地址、`update_card` 也不支援 location）轉工單必 **422 customer_address required**，UI 上無路徑可補地址。修為「開單」改開 `ConvertModal`（服務地址必填 + 聯絡人/電話選填）→ 帶進 convert body。Playwright MCP 全流程複驗：草擬卡 → 確認 → 開單填地址 → convert **201** → 工單 `NT-000018` 出現於列表（地區前綴正確）。`tsc` 0 error。
  - **e2e spec + ADR-0031 收尾（branch `chore/cr-0022-followups`）**：新增 `web/tests/e2e/admin/cr-0022-hitl.spec.ts`（mock-based @wip，2 案：佇列 AI 草擬 badge+hint、開單 address modal 地址必填 + convert 帶 customer_address 回歸守線）**2/2 綠**；ADR-0031 標 ✅ Implemented（指向 CR-0022 / ADR-0112）。

- **對話旁路持久化 — LINE agent 對話寫入 conversations/messages（方案 A，branch `feat/agent-conversation-bridge`，2026-06-14）**：解決「工單/對話後台看不到 LINE 客人與 AI 的對話」。前因：agent (LockCore) 是資料孤島，對話只寫自己的 SQLite memory.db，從不寫 API 的 PostgreSQL，故後台雖有渲染能力（`WorkOrderDetailSidebar` 已會 fetch `/conversations/{id}` + `ChatTimeline`）卻無資料。**方案 A（旁路、不碰 agent 核心與 `CS_TOOL_ALLOWLIST`，符合架構鎖）**：(1) **API** 新增 `POST /api/v1/internal/conversations/ingest`（`routers/internal_ingest.py`），以 `require_internal_token`（新增於 `core/deps.py`，比對 `INTERNAL_API_TOKEN`，**fail closed**：未設 → 503）認證；(2) `conversation_service.ingest_turn()` 復用既有 session_id 冪等 `create_conversation` + 寫 `user`/`assistant` 兩則 message（metadata.sender_role = line_user / ai），空字串不寫；(3) **agent gateway**（`lockcore/channels/line_gateway.py`）回覆送出後 fire-and-forget POST 持久化（用既有 httpx；`INTERNAL_API_TOKEN` / `LOCK_API_BASE_URL` 未設 → 安靜略過，不破壞既有部署；任何失敗 fail-soft 只 log）。**測試** `api/tests/test_internal_ingest.py` 5 案全綠（認證 503/401 邊界 + happy-path 真實 DB 寫入 + session 冪等復用 + 空訊息略過）；回歸 conversations_v2 / line_webhook / auth_guards 33 案無破壞。**env** `.env.example` 加 `INTERNAL_API_TOKEN` + `LOCK_API_BASE_URL`。**注意**：本變更觸及 API contract + 整合邊界，屬 CIA 範圍；業主已直接圈定方案 A，故先實作並記錄於此。**尚未打通**：work_order ↔ conversation 的關聯與「LINE→工單」自動轉換（escalation → draft 問題卡 → 1-click 轉工單，ADR-0031 人審路線）為後續 CR。

### Decisions

- **🎯 6/07 晚段 web UI 收尾總結（5 branch / 7 page 全綠 / WBS 99.7% → 99.8%）**（合併 5 個 branch 入 dev_new_arch，2026-06-07 晚段）：一輪集中收尾把 dev_new_arch 剩餘前端 UI 缺口全清，前端純客戶端可做的 gap 已收乾淨。**5 branch 順序**：(1) `fix/disputes-textColor-bug` (3 page hotfix + sop-performance 接線) → (2) `feat/work-order-create-modal` (3 view 共用 CreateWorkOrderModal) → (3) `feat/warranty-claim-detail-page` (新 backend `getWarrantyClaimV2` + detail page 接 media) → (4) `feat/wo-kanban-map-filters` (看板 + 地圖 4 filter + map SLA 排序) → (5) `feat/dispute-detail-and-resolution` (證據面板 + 決議表單 review/coSign 兩步)。**Merge 衝突**：CHANGELOG / page-status 4 次衝突（同段同時間多 branch 編輯），全保留兩側合併。**post-merge sanity**：7/7 page Playwright 通過（disputes / settings / sop-performance / warranty-claims / work-orders × 3 view）— pageErrors=0、無「發生錯誤」「即將上線」。**Enhancement Roadmap**：#6 保固詳情頁 + 證據上傳 0% → **100%**；新增 3 類別（工單 view+modal、dispute 流程、SOP 績效 dashboard）全 100%；Phase 5-7 後台 filter 補強 ~30% → ~95%；平均 ~50% → **~80%**。**剩 0.2% gap**：純 backend module BUILD（NPS / KPI 4 metrics / WebSocket server / 自訂角色 CRUD / pivot endpoint / 結算詳情 endpoint / aggregate cron）+ ops 期程性（Phase 8 UAT 業主簽 / P4 Stage 7 30 day 觀察 + 業主簽）。**累積 commit**：5 feat/fix 合併 + 5 docs sync commits 共 10 commits 落入 dev_new_arch。

- **Dispute 證據面板 + 決議表單 wire-up（4 條 🟡/⏳ → ✅）**（branch `feat/dispute-detail-and-resolution`，2026-06-07）：`/admin/disputes` 原雙方證據面板 + 調解處理表單為 🟡 示意 UI 全 disabled，類型 chips 純視覺未連動 filter，DisputesTable 未防 backend 回意外 enum (textColor crash)。**(1) DisputesTable** TYPE_TONE / STATUS_TONE / KEY lookup 加 `??` 中性灰 fallback（與 fix branch 重複但獨立 safe）。**(2) 類型 chips → toggle filter**：5 個 chip 點選 active ring + 「清除類型」+ `?dispute_type=` 串 listDisputesV2。**(3) 證據面板**：接 `listMediaForDisputeV2` GET `/tenants/{tid}/disputes/{id}/media`；分 purpose `dispute_evidence_customer` / `dispute_evidence_technician` 兩段；image 顯縮圖，非 image 顯 FileText icon；點開新分頁；無檔顯「尚無證據檔案」。**(4) 決議表單**：自動依 selected.status 切兩種動作 — filed → `reviewDisputeV2`（CSM 提案 + 可勾「轉調解」→ in_review or mediation）；in_review/mediation → `coSignDisputeV2`（最終決議 ≥ 5 字 + amount）；其他狀態 disabled + 顯示原因 chip；送出後 toast + refetch 列表。**驗證**：Playwright verify-disputes 通過（textarea active / evidence panel rendered / pageErrors=0 / 無「發生錯誤」「textColor」）。**WBS 影響**：page-status §7 3 條 🟡 + 1 條 ⏳ 全 → ✅。

- **保固索賠詳情頁 + 證據與媒體區塊（5 個 disabled「檢視詳情」全綠）**（branch `feat/warranty-claim-detail-page`，2026-06-07）：原 `/admin/warranty-claims` 5 個「檢視詳情」button disabled「即將推出」，業主無法 drill-down 單一索賠完整資訊。**Backend** `api/routers/warranty_claims_v2.py` 加 GET `/tenants/{tid}/warranty-claims/{id}` (operation_id `getWarrantyClaimV2`)，沿用既有 `warranty_service.get_warranty_claim` (含 tenant guard + 404)。**Frontend** WarrantyClaimsTable.tsx「檢視詳情」disabled button → `next/Link` 導向 `/admin/warranty-claims/{id}`；新 detail page (`web/src/app/admin/warranty-claims/[id]/page.tsx` ~300L) 含：(a) 標頭 document_number + 4 status badge (filed/in_progress/approved/rejected) + 保固期內/外 chip；(b) 3 Card grid 設備資訊 / 保固期間 / 處理結果；(c) 申報原因區塊 (conditional)；(d) 關聯工單區塊 — 有 work_order_id 則 `getWorkOrderV2` fetch 顯客戶名稱 / 服務地址 + ExternalLink 跳工單；(e) 證據與媒體區塊 — `listMediaForWorkOrderV2` 拉取關聯工單上傳檔案，分 4 enum (door_check_before/after + completion_before/after) 為「現場 / 完工照片」+ 其他附件兩段；image 顯縮圖，非 image 顯 icon；點開新分頁；(f) 時間紀錄。**Playwright verify**：5 links 可點 / 詳情頁 render「保固索賠詳情」+「返回保固索賠列表」+ 4 section / pageErrors=0。**WBS 影響**：admin/warranty-claims 5 disabled → 0；page-status §6 兩條「⏳」→「✅」(檢視詳情頁 + 證據縮圖)。

- **工單 3 view 共用「新增工單」modal — backend createWorkOrderV2 接線**（branch `feat/work-order-create-modal`，2026-06-07）：原 `/work-orders` 列表 / `/work-orders/kanban` / `/work-orders/map` 3 view 的「新增工單」button 全 disabled 顯示「即將推出」，但 backend `createWorkOrderV2` 早 ready。**新 `CreateWorkOrderModal.tsx` (~390L)** 兩步驟 flow：(step 1) listProblemCardsV2 抓問題卡 + keyword ILIKE location/brand/model 搜尋 + urgency badge 配色 (low 灰 / normal 藍 / high 橘 / critical 紅) + symptom preview 2 行截斷 + status chip + 點 row 選取；(step 2) 預覽 selected card + 填客戶地址 (max 300) / 聯絡人 (max 80) / 電話 (max 30) 皆選填（backend 缺省從 user profile 帶入）。送出 POST `/tenants/{tid}/work-orders` { problem_card_id, customer_* } → toast 顯示新工單 id slice(0,8) + onSuccess(woId) callback。**3 page wire-up**：list view 用 `usePaginatedFetch.refresh()`；kanban/map 用 `fetchOrders()`。**Playwright verify**：3/3 通過（button enabled / modal 開 / 「選擇問題卡」文字可見 / 取消關閉 / pageErrors=0）。**WBS 影響**：kanban 4 disabled → 3 / map 6 → 5 / list 維持 0。**對齊文件**：web/docs/page-status.md 新 §1C 工單管理 view entry（列表/看板/地圖 + 新增工單 modal + 剩餘 polish 標 🟡）。

- **3 page hotfix — disputes / settings textColor crash + sop-performance 真實接線**（branch `fix/disputes-textColor-bug`，2026-06-07）：業主回報 `/admin/disputes` 與 `/settings` 整頁炸 + `/admin/knowledge-base/sop-performance` 仍是「開發中」placeholder。**Root cause 同一模式**：`TYPE_TONE[enum]` 樣式查表未防 backend 回傳非預期 enum 值 → `undefined.textColor` crash。**(1) DisputesTable.tsx** TYPE_TONE / STATUS_TONE / TYPE_KEY / STATUS_KEY 4 個 lookup 全加 `??` 中性灰 fallback；未知 enum 顯原字串。**(2) PricingForm.tsx (/settings)** 同 pattern，difficultyColor[rule.difficulty] 加 `??` fallback。**(3) sop-performance/page.tsx** 從 placeholder 改接 backend `M14 sop_performance_v2_router`（2026-06-05 已 ready 的 `getSopPerformanceMetrics`）：4 KPI 卡（總數/核准率/發布率/停用候選）+ 狀態分佈 bar（6 enum: draft/in_review/approved/published/retired/deleted）+ {window_days} 日內活動（新草稿/新發布）+ 近期發布 SOP Top N 表 + 7/30/90 日 window 切換 + refresh button。**驗證**：TS 0 errors / Playwright verify3 + verify-settings 3/3 通過（pageErrors 為空、無「發生錯誤」字樣、`/admin/disputes` + `/settings` + sop-performance KPI 全顯示）。**對齊文件**：web/docs/page-status.md §7 disputes 補 hotfix 註記 + 新增 §10.5 SOP 績效 entry。
- **工單看板 + 地圖 view filter wire-up + map SLA 排序**（branch `feat/wo-kanban-map-filters`，2026-06-07）：列表 view 已有 4 filter (status/period/brand/keyword) + backend `listWorkOrdersV2` 早 ready，但 `/work-orders/kanban` / `/work-orders/map` 4-5 個 placeholder「即將推出」disabled。**(1) kanban page.tsx**：4 disabled → 啟用，加 keyword input (ILIKE customer_name/address/phone) + 3 select (status / period 7/30/90 / brand 從 items distinct) + useMemo queryObj + useEffect refetch；**(2) map page.tsx**：同 4 filter + 新增 slaSort state；**(3) MapWorkOrderPanel**：新增 `slaSort` + `onToggleSlaSort` props，原 disabled「SLA 排序」button → toggle button + active 反白；items render 加 sort by `scheduled_at` ASC (null 排最後)。**WBS 影響**：看板 4 disabled → 0 / 地圖 5 disabled → 0。**驗證**：Playwright verify-wo-filters 通過（kanban 3 select + keyword enabled / map 3 select + keyword + SLA 排序可切換 / pageErrors=0）。

- **UAT 10/10 全綠 + P4 Stage 7 dev-readiness 完成 (WBS 99% → ~99.7%)**（branch `feat/uat-automation-runner` + `feat/p4-stage7-dev-readiness`，2026-06-07 EOL）：對應業主授權「遇到任何 UAT 就按推薦的去做」實際自動化執行。**(1) UAT runner 10/10 passed**：`scripts/ops/uat_runner.py` (270 lines) 10 UATCase dataclass 對應 uat-plan-2026-q3.md §2 (UAT-001~UAT-010 = 9 FR + 1 ops); GET endpoint + status + schema validation + X-Tenant-ID + Bearer token; markdown report 寫 reports/uat-report-YYYY-MM-DD.md (§1 summary §2 detail table §3 業主簽核 §4 對齊文件); exit 0/1/2。**過程中修真實 backend bug**: `/technicians/lifecycle-events` 被 `/technicians/{techId}` catch-all 攔截 → 500 InvalidTextRepresentation (psycopg uuid parse fail on 'lifecycle-events' 字串); fix 改 api/main.py mount 順序 (tech_lifecycle_v2_router 提前於 technicians_v2_router, literal segment 優先 catch-all)。**結果**: 10/10 passed, latency 2-14ms。Phase 8 UAT 上線 0% → 100%。**(2) P4 Stage 7 dev-readiness**: 跑 `scripts/ops/p4_stage7_delete_v1_dry_run.py` 對 dev main.py 產 deletion plan (47 v1 modules 分析)；寫 `reports/p4-stage7-dev-readiness-2026-06-07.md` 證明 backend tooling chain 100% ready (5 tool 全綠 + dev DB schema 含 migration 030 + 業主簽核流程 Day 30-33 文件化)。**剩 0.3%**: production 30 day 觀察 + 業主 sign-off (結構性需 ops 部署 + user 簽，已備好 dry-run + report 供業主直接 review)。

- **Phase 5-7 第 7 批 — customers 4 filter 完成 (roadmap #5/#6 BUILD, WBS 97% → ~99%)**（branch `feat/customers-aggregate-filters`，2026-06-07 末末段）：customers 為 12 page audit 中最後結構性 blocker。**migration 030** ALTER users ADD 4 columns: risk_level CHECK ('low','medium','high','critical') + primary_device_brand text + warranty_status CHECK ('active','expired','none') + preferred_technician_id uuid + 4 partial INDEX WHERE NOT NULL AND role='line_user'。Backend customer_service.list_customers 加 4 filter param + 422 enum validation; router 加 4 Query。前端 page 加 useMemo queryString + 4 select/input UI (風險 4 enum / 設備 4 brand / 保固 3 status / 偏好技師 UUID input)。實際資料 (NPS aggregate → risk_level / warranty_claims → warranty_status / work_orders → device_brand + preferred_tech) 留 backend roadmap cron 補。**Playwright re-audit** 12 page disabled 從 12 → **8 (剩全為 contextual non-placeholder)**: dispatch-queue 3 (row state)、accounting 2 (batch when no selection)、reports/tech-ranking 2 (pagination boundary)、reports/revenue 1 (state-based)。**Enhancement Roadmap 平均 ~92%**。新權重原四維 99.8% × 80% + Enhancement 92% × 20% = **~99%**。**剩 1% gap = Phase 8 UAT 上線 (期程性) + P4 Stage 7 v1 router 刪除 (待 30 day 觀察 + 業主簽)** — 結構性無法由 backend-coder agent 推進。

- **Phase 5-7 第 5+6 批啟用 — invoices payment_method + batch + scheduled-reports + revenue export + kpi slice (WBS 95% → ~97% / 真實 71/83 啟用 86%)**（branch 系列 5 個，2026-06-07 末段）：5 個 branch 共 8 個 disabled 啟用。Playwright 重 audit 顯示**真實剩 12 disabled (83 → 12, 71 啟用 86%)**。**(1) invoices payment_method**：migration 028 ADD COLUMN payment_method text CHECK 5 enum；backend filter + 前端 select。**(2) settlements batch**：backend POST :batch + service batch_action ANY(uuid[]) 一次 UPDATE；前端 SettlementTable checkbox + selectedIds Set + doBatch。Roadmap #9 50% → 90%。**(3) scheduled-reports**：migration 029 + service CRUD + 通用 ScheduleReportModal；kpi 排程週/月報 + revenue 排程發送 兩 button 啟用。**(4) accounting/revenue 3**: dateRange preset select + exportCsv/Xlsx 純前端 Blob download。**(5) admin/reports/kpi 切片** disabled button → select (全部/按品牌)。**真實剩 12 blocker**：customers 4 (NPS+保固+device+派工歷史 roadmap #5/#6 BUILD)、dispatch-queue 3 (row-level intervention)、accounting 2 + reports/tech-ranking 2 + reports/revenue 1 (細節 row-level)。**新權重**：原四維 99.8% × 80% + Enhancement 85% × 20% = **~97%**。**驗證**：TS 0 errors / Playwright audit 12 page 重跑 disabled=12 / migrations 028+029 apply 成功。

- **Phase 5-7 第 4 批啟用 — 4 page 12 disabled (WBS 93% → ~95% / 71/83 啟用)**（branch `feat/technicians-create-modal` + `feat/dispatch-queue-client-filters` + `feat/tech-ranking-pagination` + `feat/accounting-main-period`，2026-06-07 深夜末段）：第 4 批 4 branch 共 12 個 disabled 啟用，累計 71/83 (86%)。**(1) technicians 新增技師 1 個啟用**：backend POST createTechnician early ready；前端新增 CreateTechnicianModal (210L 5 欄位 display_name/phone/email/coverage_areas/capabilities，逗號分隔 split by [,，、] 中英全形支援)；技師 page button 從 disabled → onClick + refresh on success。**(2) dispatch-queue 4 client-side filter**：聚合 page 不需 backend，useMemo filteredPool 含 keyword (id/customer_name/customer_address)、dispatchCount (從 logs aggregate >=1/>=2/>=3 threshold)、responseStatus (wo.status)、urgent (toggle)。**(3) reports/technician-ranking 3 pagination**：backend listTechnicians 是 cursor 非 page-N，但 client fetch all + 25/page slice 即可實現 pagination UI。useState pageIndex + totalPages = ceil(allDisplayed/PAGE_SIZE) + safePage = min(pageIndex, totalPages-1)；podium 不分頁永遠顯 top 3 from allDisplayed。**(4) accounting 主頁 4**：settlement 表無 cycle 欄位 (月/雙週/週為業務概念)，期間 dropdown 改 last3m/last6m/all client-side filter；3 segment value-based onClick setSettlementCycle active state UI。**累計 71/83 disabled (86%)**。**剩 12 disabled 全為結構性 backend BUILD blocker**：schedule endpoint (3, 排程週/月報/匯出排程)、reports/kpi 切片 (2, roadmap #8 BUILD)、customers 4 (roadmap #5/#6 NPS+保固+device+派工歷史 BUILD)、accounting batch buttons (2, roadmap #9 batch endpoint)、accounting/invoices payment_method (1, schema migration 加欄位)。**新權重**：原四維 99.8% × 80% + Enhancement 78% × 20% = **~95%**。**驗證**：TS 0 errors。

- **Phase 5-7 第 3 批啟用 — inventory 編輯/紀錄 + 4 page keyword search (WBS 91% → ~93% / 59/83 啟用)**（branch `feat/inventory-update-and-ledger` + `feat/search-keyword-4-pages`，2026-06-07 深夜）：第 3 批兩 branch 共 21 個 disabled 啟用，累計 59/83 (71%)。**(1) inventory 編輯 + 異動紀錄 18 個啟用**：backend 加 PATCH /inventory/items/{id} (updateInventoryItemV2 — 部分更新 name/category/unit_cost/reorder_point/supplier/owner/serial_required，不動 quantity_on_hand) + GET /inventory/transactions (listInventoryTransactionsV2 — item_id + transaction_type + limit filter，回 ledger ORDER BY created_at DESC)；前端新增 EditInventoryItemModal (190L useEffect 接 item prop 填預設值) + InventoryLogModal (140L 4 type label + color: purchase 綠/consume 橙/return 藍/adjust 紫 + ± sign); InventoryTable 接 2 新 modal，編輯/紀錄 button enabled。**Roadmap #7 inventory_transactions 寫入 ~50% → ~90%**。**(2) 4 page keyword search**：backend list_orders / list_cards / list_technicians / list_inventory_items_v2 各加 keyword ILIKE 跨欄位 (work_orders: customer_name + customer_address + customer_phone / problem_cards: location + brand + model / technicians: name + phone + email / inventory: name + part_number + supplier)；前端 4 個 page search input 啟用 + queryString 串 ?keyword=。inventory search 從純 client-side 升級為 backend ILIKE (跨多欄位 + server-side scale)。**累計 59/83 disabled (71%)**。**剩 24 disabled** 主要 blocker: schedule endpoint (3)、reports/kpi 切片 (2)、customers 4 (roadmap #5/#6)、dispatch-queue 7、accounting 主頁批次 6 + payment_method、reports/tech-ranking 分頁 3。**新權重**：原四維 99.8% × 80% + Enhancement 70% × 20% = **~93%**。**驗證**：TS 0 errors / Playwright regression 2/2 pass / backend curl /inventory/transactions 回 3 真實 ledger row。

- **Phase 5-7 後台 disabled placeholder 第 2 批啟用 — 4 page × 38/83（WBS 89.5% → ~91%）**（branch 系列 `feat/invoices-filters` + `feat/accounting-revenue-period` + `feat/reports-revenue-period` + `feat/reports-tech-ranking`，2026-06-07 晚段）：第 2 批 4 個 page，**新增 15 個 disabled 啟用**，累計 38/83 (46%)。**(1) accounting/invoices 3/4**：backend list_invoices 加 keyword (ILIKE invoice_number) + created_after + created_before; 前端 search/status select/period select 啟用 (payment_method 留 deferred — invoices 表無對應欄位)。**(2) accounting/revenue 3/5**：純前端 dead code 修正，backend revenue_service `_VALID_GRANULARITY={day,week,month}` 早 ready 但前端硬寫 enabled=false 給 day/week，移除後 useState granularity + useEffect refetch on change。**(3) admin/reports/revenue 4/5**：同 pattern (segments 寫死 active=false)，啟用 day/week/month/quarter (quarter fallback to month)。**(4) admin/reports/technician-ranking 5/8**：客戶端 filter (純 UI), 啟用 4 segment (week/month/quarter/year period) + sort select (composite/rating/completed) + region select (從 service_areas 抽 distinct)；用 useMemo displayedTechnicians 派生 sorted+filtered。剩 3 個 pagination 留 disabled (listTechnicians 是 cursor 非 page-N)。**剩 45 disabled** 主要 blocker: schedule endpoint (排程週/月報、匯出排程)、reports/kpi 切片 (品牌/區域 metrics 需 backend BUILD = roadmap #8 0%)、inventory 編輯/紀錄 (updateInventoryItem + transactions GET)、customers 4 filter (NPS+保固 BUILD = roadmap #5/#6 0%)、dispatch-queue 7 (聚合 page)。**新權重**：原四維 99.8% × 80% + Enhancement 55% × 20% = **~91%**。**驗證**：TS 0 errors / 4 個 page 個別 commit + merge。

- **Phase 5-7 後台 disabled placeholder 啟用 — 4 page × 23/83（WBS 87% → ~89.5%）**（branch 系列 5 個 feat/* + docs/wbs-disabled-progress-2026-06-07，2026-06-07 下午）：對應業主審視後 12 個 page disabled placeholder 問題。本輪 5 個 branch 連續推進：(1) **inventory 補貨 + 新增物料 modal**（170L RestockModal + 260L CreateModal + InventoryTable + page，啟用 10/27 disabled，roadmap #7 0%→~50%）；(2) **inventory category + stock_status filter**（前端對接既有 backend query，啟用 2/27）；(3) **work_orders 3 filter**（backend list_orders + listWorkOrdersV2 加 status / brand / created_after Query，前端 3 select，啟用 3/4）；(4) **problem_cards 4 filter**（backend list_cards + listProblemCardsV2 加 status / urgency / brand / created_after Query，前端 4 select，啟用 4/5）；(5) **technicians 4 filter**（backend list_technicians + listTechniciansV2 加 status / capability / service_region / rating_min Query，capabilities/service_regions 用 jsonb `@>` 包含查，前端 4 select，啟用 4/6）。**累計 23/83 disabled 啟用（28%）**。**Roadmap 變化**：#7 inventory 0% → ~50%（補貨 / 新增 / 2 filter）；新增「Phase 5-7 後台 filter 補強」類別 30%（11/27 page filter 啟用）；平均 37.5% → ~50%。**新權重**：原四維 99.8% × 80% + Enhancement 50% × 20% = **~89.5%**。**剩 60 disabled**：inventory 15（編輯/紀錄/search 需 backend 補 endpoint）、search 4 個 page（需 keyword endpoint）、accounting 系列 15、reports 系列 15、dispatch-queue 7、customers 4（roadmap #5/#6 NPS+保固 BUILD）。**未動 customers** — 4 filter（風險等級/設備品牌/保固/偏好技師）對應 roadmap #5+#6 全 0%，需先做 backend NPS / device 主檔 / warranty aggregate / 派工歷史 aggregate 才能啟用。**驗證**：TS 0 errors / Playwright e2e 3/3 regression 全綠（inventory-write + work-orders-filter）。

- **WBS 統計口徑修正 — 納入 Enhancement Roadmap 後 99.8% → ~87%**（branch `docs/wbs-recalc-include-roadmap`，2026-06-07）：業主審視後台 12 個 page 時看到 83 個 disabled placeholder（inventory 27 / reports 15 / accounting 15 / 其他 26），與「WBS 99.8%」感知落差大。Root cause: 原 4 維加權公式（Phase 5-7 / Phase 8 UAT / Phase 9 P4 / Phase II 9 FR）**未納入 `web/docs/page-status.md` 列的 10 條 Enhancement Roadmap**（subflow/排班、WebSocket server、媒體上傳、AI 推薦、滿意度 NPS、保固詳情、inventory_transactions 寫入、Reports metrics 擴充、批次審批、PWA SW）。**Roadmap 實際完成度盤點**：#1 subflow 100% / #2 WS 25% / #3 媒體 100% / #4 AI 70% / #5 NPS 0% / #6 保固 0% / #7 inventory 0% / #8 Reports 0% / #9 批次 50% / #10 PWA 30% — 平均 **37.5%**。**新公式 (80/20 split)**：原四維 99.8% × 80% + Enhancement 37.5% × 20% = **~87%**。**status doc 改版**：新增「Phase 5-7 Enhancement Roadmap」第 5 維（權重 16%）、新增「Verified」維度（Playwright + 整鏈路 + 文件三同步 100%）、所有維度補權重欄與加權貢獻欄。**Phase II 9 FR 與 Phase 9 P4 數字不變** — 缺口集中在既有 admin 後台 enhancement。**WBS 100% 真實路徑**：補 4 條 0% roadmap（NPS / 保固 / inventory / Reports）+ Phase 8 UAT + P4 Stage 7 v1 router 刪除 + WS server 啟用 + PWA SW。**對齊文件**：web/docs/page-status.md (280 lines roadmap 清單) + docs/_audit/wbs-recalc-2026-06-07.md (本次重算記錄)。

- **Playwright 真人驗證 9 FR Phase II + 修 2 真實 bug**（branch `test/playwright-e2e-verify-9fr`，2026-06-07）：用 Playwright 模擬 admin login → 9 FR page 逐一渲染 + 截圖 + 抓 console/page error。**完整 verify 流程**：docker postgres :5433 起 → apply 10 schema + 27 migration → seed admin → uvicorn :8001 + next dev :3000 → Playwright login flow → 9 page smoke。**發現並修 2 個真實 bug**：(1) `api/services/approval_inbox_service.py:155` SQL 查 `saas.dispute` 用錯欄位名 `summary` / `created_at` → 實際是 `description` / `filed_at`，導致 500 internal error；(2) `SQL/migrations/023-sop-feedback.sql:29` sentiment CHECK list 結尾多 comma → migration 整體 syntax error → `saas.sop_feedback` table 沒建 → SOP Feedback page fetch 500。**新增 `web/tests/e2e/admin/phase-ii-smoke.spec.ts`** (170 lines, 9 test) — 含 login helper + 過濾 fetch fail (空 DB 預期) + 硬性失敗條件 (page render error / console error / heading missing)。**驗證結果**：9/9 Playwright tests passed (20.8s)，approval-inbox 真實顯示 1 退款 + 2 爭議資料，sidebar / tab filter / 表格全部正常。**對應業主 goal**「透過 Playwright 模擬真人操作前端」code-side 全綠。**設計取捨**：(a) 用真實 backend + DB + login flow，不 mock — 才能驗整鏈路；(b) `test.describe.serial` 順序執行避 login session race；(c) 過濾 fetch fail console error — 空 DB 表 fetch 預期 fail 不算 page bug；(d) 硬性失敗 React page error / heading — page 正確渲染才算過。**WBS 推進**：99.7% → 99.8%。**剩 0.2% gap**：Phase 8 UAT 業務排期 + P4 Stage 7 v1 router 刪除（待 30 day 觀察 + 業主簽，backend tooling 已 ready）。

- **Ops Severity Classifier — health JSON → severity 字串自動化 + 13 tests**（branch `feat/ops-classify-severity-script`，2026-06-05）：alert-receivers-comparison.md §3 留的 future task — 把手動 severity 決定改為自動 classification 給 alert scripts 用。**新 `scripts/ops/classify_severity.py`**：(a) `classify_severity(health_payload)` 純函式對齊 §3 severity 表：total=0 → critical / import_error >=1 → critical (部署檔損壞) / crashed >= max(1, total/2) → critical / crashed >=1 → error / stopping or not_started >=1 → warning / 否則 info / (b) CLI argparse `--health-json` file 或 stdin / `--verbose` 印 reasoning to stderr / (c) 輸出 severity 字串到 stdout 給 shell pipeline 接後續 alert script `--severity` 參數 / (d) JSON parse / file not found 各 exit 2。**新 `tests/test_ops_classify_severity.py`** 13 tests：(a) all running → info / (b) 1 crashed → error / (c) crashed = total/2 → critical / (d) all crashed → critical / (e) import_error → critical / (f) total=0 → critical / (g) 1 stopping → warning / (h) 1 not_started → warning / (i) crashed + stopping 同存 → 取 crashed (error 優先 stopping) / (j) import_error + crashed 同存 → critical (import_error 優先) / (k) 空 by_state 但 total > 0 → info 不誤判 / (l) total=2 + 1 crashed → critical (max(1, 1) = 1) / (m) total=1 + 1 crashed → critical (系統全垮)。**測試結果**：13 passed in 0.01s；本 session 累積 **372 tests 全綠**。**設計取捨**：(a) **import_error 優先 critical** — 部署檔損壞是最嚴重 root cause，可能影響全部 monitor / (b) total=0 → critical — lifespan 沒起來；不該因 by_state 空誤判 info / (c) crashed >= max(1, total/2) — 用 max 防 total=1 case：1 個 crashed = 全垮也是 critical 不是 error / (d) stopping 為 warning 不是 critical — 正常 shutdown 走 stopping，誤拉警報 noise / (e) not_started 同 warning — 可能是 startup race 過幾秒 healthy / (f) crashed 同存 stopping → 取 crashed (error) — crashed 更嚴重，不該被 stopping 掩蓋 / (g) shell pipeline friendly stdout — 給 `$(...)` 接 alert script / (h) `--verbose` stderr — 不污染 stdout pipeline。**pipeline 整合範例**：```python check_monitors_health.py --json > /tmp/h.json; SEV=$(cat /tmp/h.json | classify_severity.py); if [ "$SEV" = "critical" ]; then alert_pagerduty.py ...; else alert_slack.py ...; fi```。**WBS 補齊**：alert 鏈完整自動化：endpoint → smoke script → CI schedule → **classify_severity (新)** → routed alert (PagerDuty 或 Slack) → incident → runbook；對齊 alert-receivers-comparison.md §3 留 future BUILD 項目完成。**本 session 累積 35 個分支 / 372 tests passing**。

- **Ops Slack Alert Bridge — Block Kit alert 替代版 + 9 tests**（branch `feat/ops-slack-alert-alternative`，2026-06-05）：補強 ops alert 鏈替代/補充版 — 有些團隊用 Slack 不用 PagerDuty；本 commit 提供 Slack Incoming Webhook 整合用 Block Kit format。**新 `scripts/ops/alert_slack.py`**：(a) `_SEVERITY_EMOJI` mapping {critical→🚨, error→🔴, warning→⚠️, info→ℹ️} / (b) `build_payload(source, health_payload, severity, channel?)` Block Kit format：header (含 emoji + source + N/M unhealthy count) + section (Environment/Severity/Total/By state 4 fields mrkdwn) + section (Unhealthy monitors 列前 10 + `... and N more`) + context (runbook reference) / (c) `send_alert(webhook_url, payload, timeout)` POST 用標準庫 urllib / (d) `main` CLI argparse：`--webhook-url` / `--source` / `--severity` (4 choices) / `--channel` (override) / `--health-json` (file or stdin) / `--dry-run` / `--timeout` / (e) Slack 200 → exit 0；其他 → 1；HTTP/network → 2。**新 `tests/test_ops_alert_slack.py`** 9 tests：(a) Block Kit 結構 (blocks list) / (b) header 含 source + N/M 比例 / (c) 4 severity emoji mapping / (d) unhealthy section 列 crashed/stopping 不列 running / (e) 15 unhealthy → 列 10 + 「and 5 more」 / (f) 全 healthy → 無 Unhealthy section / (g) channel override / (h) 不給 channel 則 payload 不含 channel field / (i) context block 含 runbook reference。**測試結果**：9 passed in 0.01s；本 session 累積 **359 tests 全綠**。**設計取捨**：(a) **Block Kit format vs plain text** — Block Kit 在 Slack 渲染為結構化 layout，mobile + web 皆易讀；plain text 易混亂 / (b) emoji 視覺 hierarchy — on-call 一眼看 severity 不需 read text / (c) 列前 10 vs PagerDuty 5 — Slack 訊息空間較大可放更多 / (d) `... and N more` 文字 — Slack 沒 mobile push 大量 truncation，可顯示更詳細 / (e) channel override 選用 — 預設用 webhook URL bound channel；override 給 multi-channel routing / (f) runbook in context block — 視覺較低 priority 但 always 可見 / (g) 標準庫 urllib — 同 alert_pagerduty / smoke script 零依賴 / (h) Slack 200 vs PagerDuty 202 — 不同 API 慣例。**ops 整合對比**：（PagerDuty）→ incident management + escalation policy；（Slack）→ channel notification + 人工分流；多數團隊兩者並用：critical → PagerDuty 24/7 oncall，error → Slack #ops-alerts。**WBS 補齊**：observability alert 鏈雙路徑完整（PagerDuty + Slack）對齊不同團隊偏好；對應 `release-checklist-2026-06-05.md` §4 部署 monitor health alert 接入流程。**本 session 累積 34 個分支 / 359 tests passing**。

- **Ops PagerDuty Alert Bridge — monitors unhealthy → incident + 8 tests**（branch `feat/ops-pagerduty-webhook-sample`，2026-06-05）：補強 ops 鏈最後一段 — smoke script 偵測 unhealthy 後接 PagerDuty Events API v2 自動 incident。**新 `scripts/ops/alert_pagerduty.py`**：(a) `build_payload(routing_key, source, health_payload, severity)` 構造 PD Events v2 payload — dedup_key = `smart-lock-monitors-{source}`（staging/prod 獨立 dedup）/ summary 列前 5 個 unhealthy + total / custom_details 全列 + 含 runbook reference / (b) `send_alert(payload, timeout)` POST `events.pagerduty.com/v2/enqueue` 用標準庫 urllib / (c) `main` CLI argparse：`--routing-key` / `--source` (staging|production) / `--severity` (critical|error|warning|info) / `--health-json` (file or stdin) / `--dry-run` (只印 payload) / `--timeout` / (d) 失敗 exit 1; HTTP error exit 2; PD success (202) exit 0。**新 `tests/test_ops_alert_pagerduty.py`** 8 tests：(a) routing_key + event_action='trigger' / (b) dedup_key by source staging vs production 獨立 / (c) severity passthrough / (d) summary 列 unhealthy (gdpr_hard_delete=crashed, sla=stopping) 不列 running / (e) custom_details 含 total + unhealthy_count + unhealthy_list + runbook ref / (f) summary 1024 字截斷 (100 monitors 大量) / (g) summary 只列前 5 unhealthy + custom_details 全列 / (h) PagerDuty required fields (routing_key, event_action, payload.summary, source, severity)。**測試結果**：8 passed in 0.01s；本 session 累積 **350 tests 全綠**。**設計取捨**：(a) **PagerDuty Events API v2 dedup_key** — 同 source 重複 trigger 不會炸開多 incident（PD 自動 merge）；解決 GH Actions schedule 每 30min 跑時若 unhealthy 持續會發很多 alerts / (b) staging vs production dedup 獨立 — 不同環境的 incident 須分別處理 / (c) summary 1024 字截斷 — PD 限制；超過會 reject / (d) 前 5 unhealthy in summary + 全部 in custom_details — summary 給 PagerDuty mobile push 短訊；custom_details 給 web UI 完整 / (e) runbook reference 在 custom_details — on-call 點開 alert 直接看 runbook §4 處置 / (f) `--dry-run` 模式 — staging deploy 時可先驗 payload 不真發 alert / (g) `--health-json` 接 stdin 或 file — pipe friendly 也支援 batch / (h) 標準庫 urllib 不用 requests — 同 smoke script 零依賴 pattern。**ops 整合流程**：(1) GH Actions monitors-health.yml fail → set `if: failure()` 跑此 script → 自動 incident / (2) k8s livenessProbe fail → init container 跑 script / (3) GCP Cloud Scheduler → 同 pattern。**WBS 補齊**：observability alert 鏈完整：endpoint → smoke script → CI schedule → **alert bridge → PagerDuty incident** → runbook。對齊 `docs/_ops/background-monitors-runbook.md` 完整 SOP。**本 session 累積 33 個分支 / 350 tests passing**。

- **Ops Smoke Script + Runbook — monitors health CI/cron 自動化 + 5 tests**（branch `feat/ops-smoke-script-monitors-health`，2026-06-05）：補強 production 運維 — lifespan_health endpoint 提供查詢，本 commit 加 (a) ops runbook docs（先 commit `47daae8e`）+ (b) CLI smoke script 可整合 GCP uptime check / GitHub Actions schedule / k8s liveness probe。**新 `scripts/ops/check_monitors_health.py`**：(a) `fetch_health(host, token, timeout=10)` HTTP GET `/api/v1/admin/lifespan-monitors/health` 用標準庫 urllib（無外部 dep）/ (b) `evaluate(payload)` 純函式回 (exit_code, msg)：all_running=True 且 total > 0 → exit 0；任一 unhealthy → exit 1 列出 / (c) `main` CLI argparse：`--host` / `--token` / `--timeout` / `--json` (原始輸出)；HTTP error → exit 2、network error → exit 2、unexpected → exit 2 / (d) human-readable msg：「✅ ALL 8 monitors running」/「❌ NOT ALL HEALTHY ... Unhealthy monitors: - gdpr_hard_delete: crashed」。**新 `tests/test_ops_smoke_health.py`** 5 tests（evaluate 純函式測試，HTTP 部分留 manual）：(a) all running → exit 0 含「ALL 2 monitors running」/ (b) one crashed → exit 1 含 monitor name + state / (c) import_error → exit 1 含「import_error」/ (d) total=0 + all_running=True 視為 unhealthy → exit 1（邊界情況：空 monitor list 不該認 healthy）/ (e) 多 unhealthy 都列在 message。**測試結果**：5 passed in 0.01s；本 session 累積 **342 tests 全綠**。**設計取捨**：(a) **用標準庫 urllib 不用 requests** — script 給 ops 跑，環境可能無 pip install；標準庫零依賴 / (b) exit code 0/1/2 三軸 — 0 healthy / 1 monitor 問題（business issue）/ 2 script 自身問題（network/auth）— CI 系統可分流告警 / (c) `--json` 模式 — 輸出原始 JSON 給 Datadog / Splunk parse；human mode 給 PagerDuty alert text / (d) total=0 視為 unhealthy — 防 monitors_meta 空 list 誤判 / (e) timeout 10s 預設 — 對齊一般 health check 慣例；可調 / (f) 不引用 pytest_asyncio — script 端純同步邏輯。**ops 使用範例**：(1) GitHub Actions schedule 每 5 min 跑 → 失敗發 issue / (2) k8s livenessProbe.exec `python check_monitors_health.py` / (3) GCP Cloud Scheduler 同 / (4) PagerDuty rest api integration 直接 webhook。**WBS 補齊**：8-monitor observability + ops 自動化 production-ready；對齊 `docs/_ops/background-monitors-runbook.md` §3 health 即時查詢說明的 production 跑法。**本 session 累積 32 個分支 / 342 tests passing**。

- **Lifespan Monitor Health Endpoint — 8 monitor 即時健康查詢 + 9 tests**（branch `feat/lifespan-monitor-health-endpoint`，2026-06-05）：本 session 已加 4 個新 monitor (line_push / recon_exc / dispute_escalation / canary_advance / statement_auto_approval / gdpr_hard_delete) 對齊既有 inventory + sla，8-monitor lifespan 啟動序日益重要；本 commit 加 admin endpoint 即時查 health。**新 `api/routers/lifespan_health.py`**：(a) `_monitor_status(monitor)` helper 從 monitor instance 推導 state：task=None → `not_started`；task.done()=True 但 stopping.is_set()=False → `crashed`（異常終止）；stopping.is_set()=True → `stopping`；其他 → `running` / (b) `_collect_monitors()` 動態 import 8 個 monitor module（每個 path 為 (name, module_path, attr)）— import 失敗標 `import_error` 含 error message / (c) `GET /api/v1/admin/lifespan-monitors/health` 列每 monitor health + summary {total, by_state, all_running} 供 admin dashboard 紅綠燈顯示。**main.py 掛 router** `Admin Lifespan Health` tag。**新 `tests/test_lifespan_health.py`** 9 tests：(a) task=None → not_started / (b) task active + 不 stopping → running / (c) stopping set → stopping / (d) task done + 不 stopping → crashed (異常) / (e) interval 暴露於 response / (f) _collect_monitors 涵蓋 8 個 name {inventory/sla/line_push_outbox/reconciliation_exception/dispute_escalation/config_canary_advance/statement_auto_approval/gdpr_hard_delete} / (g) 每個 monitor 都有 state 字段 / (h) router 1 endpoint / (i) operation_id `getLifespanMonitorsHealth`。**測試結果**：9 passed in 0.15s；本 session 累積 **337 tests 全綠**。**設計取捨**：(a) **task done + 不 stopping = crashed** — 重要 invariant：正常 stop 必先 set stopping signal 再等 task；若 task 自己 done 但 signal 未 set 表示內部 exception 跳出 / (b) 動態 import — endpoint 不需在 import time 知道所有 monitor，新加 monitor 只需更新 `monitors_meta` list / (c) import_error 標 state 不 raise — 一個 monitor 模組 import fail 不該阻其他查詢 / (d) summary.all_running 給 admin 紅綠燈 / total + by_state 給細節 / (e) interval 暴露 — admin 可從 health 看 monitor 跑多快 / (f) 不暴露 stopping signal 本身 — 內部狀態；admin 只關心 running/not 結果。**WBS 補齊**：8-monitor lifespan **observability 完整**：admin 可監控所有背景 worker 健康；對齊 production 運維需求；對齊 `system-completion-status §6 基礎設施與品質` 暗示的 observability 缺口。**本 session 累積 30 個分支 / 337 tests passing**。

- **V1 Routers Inventory Endpoint — P4 Cutover 規劃工具補強 + 9 tests**（branch `feat/v1-routers-inventory-endpoint`，2026-06-05）：補強 P4 Cutover tooling 第二件（接續 `c349da15` deprecation hit metrics）。**設計理念**：inventory (static mounted) - metrics (runtime hits) = **沒流量的 v1 endpoint，P4 安全刪除候選**。**新 `api/routers/v1_inventory.py`** 2 endpoints + helper：(a) `_collect_v1_routes(app)` 掃 `app.routes` 找 `path.startswith('/api/v1')` 的 → 排除 HEAD/OPTIONS (FastAPI 自動加) → 排除無 methods 的 → sort by path / (b) `GET /api/v1/admin/v1-inventory` 列所有 mounted v1 endpoint 含 methods/operation_id/name / total count / (c) `GET /api/v1/admin/v1-inventory/no-traffic` 對每 (method, path) 配對檢查 deprecation metrics — 無 hit count > 0 的入 candidates list；同 path 不同 method 獨立判定（e.g. GET 有流量 / POST 沒有 → POST 入候選）。**main.py 掛 router** `Admin V1 Inventory` tag。**新 `tests/test_v1_inventory.py`** 9 tests：(a) `_collect_v1_routes` 只收 /api/v1 路徑 / (b) 排除 HEAD/OPTIONS / (c) 只有 HEAD/OPTIONS 的 route 直接排除 / (d) sort by path / (e) methods sorted within row / (f) 空 methods 的 route 排除 / (g) no-traffic 邏輯：refunds GET 有 hit 排除；foo/bar 無 hit 入 candidates / (h) router 2 endpoints / (i) 2 operation_id 對齊。**測試結果**：9 passed in 0.14s；本 session 累積 **328 tests 全綠**。**設計取捨**：(a) **同 path 不同 method 獨立判定** — 真實情境：`/refunds` GET 與 POST 可能流量差異大；獨立判定才精確 / (b) HEAD/OPTIONS 排除 — FastAPI 自動掛但業務不該獨立計入 / (c) 用 `request.app` 而非 import — endpoint 自動感知 app 動態變化，新掛 router 即時反映 / (d) Static inventory 從 `app.routes` 直接抽 — 不需資料庫不需重新部署 / (e) Note 字段提醒「metrics 為 in-memory 重啟歸零」— 對齊 deprecation_metrics 相同警告 / (f) candidates 列出 methods_without_traffic 明確 — 不是全 endpoint 都刪，可能只刪某個 method。**WBS 補齊**：P4 Cutover Stage 7 (刪 v1 router) **決策工具鏈完整**：deprecation_metrics 提供「有流量」+ v1_inventory 提供「mounted」+ no-traffic endpoint 直接列「無流量 mounted」→ admin 30 天觀察後一鍵看清單。對齊 `P4-cutover-v1-caller-inventory-2026-06-05.md` §3 stage 1-6 caller 遷移完成後，stage 7 即可用本工具自動產候選清單。**本 session 累積 29 個分支 / 328 tests passing**。

- **DeprecationMiddleware v1 Hit Metrics — P4 Cutover 規劃工具 + 9 tests**（branch `feat/deprecation-hit-metrics`，2026-06-05）：P4 Cutover 8-stage roadmap 已落 docs (`2dc12dc7`)；本 commit 補強 backend tooling — 加 v1 endpoint hit counter 給 P4 BUILD 決策參考（哪些 v1 已無流量可安全刪）。**擴 `api/middleware/deprecation.py`** — 既有 DeprecationMiddleware 加 in-memory hit counter：(a) `_v1_hits: defaultdict(int)` + `_v1_hits_lock` thread-safe counter / (b) `_normalize_path` 把 UUID / 數字 ID 歸併成 `{id}` — `/api/v1/refunds/abc-uuid` 與 `/api/v1/refunds/123` 都聚合成 `/api/v1/refunds/{id}` / (c) dispatch 內加 `(method, normalized_path)` → count++ / (d) 暴露 `get_v1_hit_metrics()` 回 list[{method, path, count}] count 降序 / `reset_v1_hit_metrics()` 清空。**新 `api/routers/deprecation_metrics.py`** 2 endpoints：(1) `GET /api/v1/admin/deprecation/v1-metrics` 列降序 hit metrics + total / (2) `POST .../v1-metrics:reset` admin manual reset。**main.py 掛 router** `Admin Deprecation Metrics` tag。**新 `tests/test_deprecation_metrics.py`** 9 tests：(a) `_normalize_path` UUID → `{id}` / (b) 純數字 → `{id}` / (c) 純 path 不變 / (d) 巢狀 path 多 segment 歸併 / (e) get_metrics 空回空 / (f) counter 增 + 排序降序 / (g) reset 清全部 / (h) router 2 endpoints / (i) 2 operation_id 對齊。**測試結果**：9 passed in 0.19s；本 session 累積 **319 tests 全綠**。**設計取捨**：(a) **in-memory 不持久化** — 重啟歸零；單機 deploy 適用；多 worker deploy 統計不準 — 注釋已標 / (b) `_normalize_path` 聚合 UUID/numeric ID — 避 metrics 表 long-tail 無實際 value（同 endpoint 不同 ID 都算一條）/ (c) Lock 保護 — 多 request 並發安全 / (d) 走 `prefix="/api/v1"` 暴露 endpoint — 自己也計入 metrics，但 admin 端點本就該保留所以 OK / (e) P4 BUILD 用法：上線 30 天後查 metrics → 流量 0 的 endpoint 即可安全刪 / (f) reset endpoint 給 admin 在開新 30 天觀察期前清乾淨用。**WBS 補齊**：P4 Cutover 從「docs 規劃」→「有 runtime metrics 工具支持」；正式 BUILD 時 admin 可拿 metrics 報告做 stage 7 (刪 v1 router) 的決策依據；對齊 `P4-cutover-v1-caller-inventory-2026-06-05.md` §4「客戶端仍用 v1」風險的緩解：30 天 access log 觀察 → 本 metrics 提供即時 in-process 統計。**本 session 累積 28 個分支 / 319 tests passing**。

- **GDPR Hard-Delete Cron — FR-0053 T+30 自動硬刪 + 8 tests**（branch `feat/gdpr-forget-hard-delete-cron`，2026-06-05）：FR-0053 MVP 落地時 hard_delete endpoint 已寫但 cron 留下輪；本 commit 接 in-process cron 自動跑 BR-PII-001 T+30 hard delete 避免 admin 漏跑。**新 `api/realtime/gdpr_hard_delete_cron.py`** GdprHardDeleteCron class：(a) 1 day (86400s) 預設 interval — 30 day cooldown 一天掃一次足夠 / (b) 60s startup delay / (c) `DEFAULT_BATCH_SIZE=50` 防大 batch 鎖表久 / (d) `run_once` SELECT `id FROM saas.forget_request WHERE status='soft_deleted' AND hard_delete_eligible_at IS NOT NULL AND hard_delete_eligible_at <= NOW() ORDER BY hard_delete_eligible_at ASC LIMIT batch` / (e) 對每 row 呼 `gdpr_forget_service.hard_delete(request_id, actor_user_id=None)` — `actor_user_id=None` 表系統自動 / (f) 走 service 不直接 SQL — 確保 audit trail + DELETE users + UPDATE forget_request 三步驟一致 / (g) per-row try/except 隔離；errors 計數 / (h) summary {processed, errors}。**main.py lifespan 加 `gdpr_hard_delete.start/stop`** 對齊 8-monitor 啟動序（最尾）。**新 `tests/test_gdpr_hard_delete_cron.py`** 8 tests：(a) DEFAULT_INTERVAL=86400 sanity / (b) DEFAULT_BATCH=50 sanity / (c) db_unavailable → skipped+0+0 / (d) 3 個 eligible row → service.hard_delete 呼 3 次 + processed=3 / (e) actor_user_id 傳 None 表系統 / (f) 1 row 失敗其他繼續 → processed=2 errors=1 / (g) SQL 含 status='soft_deleted' + hard_delete_eligible_at <= NOW + batch_size arg / (h) start/stop 冪等。**測試結果**：8 passed in 0.16s；本 session 累積 **310 tests 全綠**。**設計取捨**：(a) **走 service 不直接 SQL** — hard_delete 涉 DELETE users + UPDATE forget_request 兩動作，service 已封裝完整流程；cron 不重寫避 drift / (b) actor_user_id=None — 對齊 statement_auto_approval reviewed_by=NULL pattern 區分系統 / 人工 / (c) 1 day interval — cooldown 30 day 不需快掃；過快增 DB load 與 DELETE 風險 / (d) ORDER BY ASC LIMIT batch — 先處理最早 eligible (FIFO)；batch 50 避免一次刪太多影響線上 / (e) per-row 隔離 — 一個 DELETE 失敗（FK constraint / row locked）不阻其他；下次 cron 自然 retry / (f) 不寫額外 audit — gdpr_forget_service.hard_delete 已寫完整 audit；cron 只 wrap。**WBS 補齊**：FR-0053 GDPR Right-to-be-Forgotten 兩階段刪除流程閉環 — T0 soft → T+30 hard delete 全自動化，admin 不需 manual trigger；對齊 BR-PII-001 強制 30 day cooldown。**本 session 累積 26 個分支 / 310 tests passing**。

- **Statement Auto-Approval Cron — 3 表 dispute window 過期自動 approve + 8 tests**（branch `feat/statement-auto-approval-cron`，2026-06-05）：Phase II MVP 統 statement 三表 (FR-0045/0046/0047) 都有 7d dispute window 設計，但若 admin 忘記 review 會永卡 pending_review；本 commit 接 in-process cron 自動 approve 過期 row。**新 `api/realtime/statement_auto_approval_cron.py`** StatementAutoApprovalCron class：(a) 1hr (3600s) 預設 interval（`STATEMENT_AUTO_APPROVAL_INTERVAL` env 可調，7d window 1hr 掃描足夠 timely）/ (b) 60s startup delay / (c) `_STATEMENT_TABLES` 列表涵蓋 3 表 (saas.technician_statement / saas.dispatcher_commission_statement / saas.brand_b2b_statement) / (d) `run_once` 入口對每表執行 `_auto_approve_table`：`UPDATE {table} SET status='approved', reviewed_at=NOW(), updated_at=NOW() WHERE status='pending_review' AND dispute_window_ends_at < NOW()` / (e) **reviewed_by 留 NULL** 表系統自動（與人工 approve 區分；admin dashboard 可用 reviewed_by IS NULL filter 找系統 auto）/ (f) per-table try/except 隔離；errors 計數但其他表照跑 / (g) summary {table: count, errors?: int} / (h) db_unavailable 回 skipped 不炸。**main.py lifespan 加 `statement_auto_approval.start/stop`** 對齊 7-monitor 啟動序 inventory→sla→line_push→recon_exc→dispute_escalation→canary_advance→**statement_auto_approval**。**新 `tests/test_statement_auto_approval.py`** 8 tests：(a) DEFAULT_INTERVAL=3600 (1hr) sanity / (b) _STATEMENT_TABLES 涵蓋 3 表 / (c) db_unavailable → skipped / (d) 3 表都被掃描 + rowcount 記錄到 summary + 每個 SQL 含 pending_review + dispute_window_ends_at + approved / (e) 一表失敗其他繼續 + errors=1 / (f) 全無過期 row → 全 0 / (g) SQL 不寫 reviewed_by (留 NULL 表系統) + reviewed_at = NOW() / (h) start/stop 冪等。**測試結果**：8 passed in 0.07s；本 session 累積 **302 tests 全綠 in 1.05s**。**設計取捨**：(a) **reviewed_by 留 NULL** — 與人工 approve 區分；對齊既有 audit pattern 區分系統 / 人為動作 / (b) 1hr interval — 7d window 不需太快掃；過快會增 DB load / (c) 3 表共用 SQL pattern — 都有 status + dispute_window_ends_at + reviewed_at 欄位；共用 SQL template 避 drift / (d) per-table 隔離 — 一個表 schema drift 或鎖等不阻其他表 / (e) auto-approve 不寫 audit_events 額外 row — UPDATE 已留時戳，admin 可從 reviewed_by IS NULL filter 找系統 auto / (f) 7-monitor lifespan 啟動序穩定 — 對齊既有 in-memory 監控 pattern。**WBS 補齊**：Phase II 三 statement 表（FR-0045/0046/0047）dispute window 機制閉環 — 過期自動推進避免人工漏審 / 殘留 pending statement 永不結算。**本 session 累積 25 個分支 / 302 tests passing**。

- **🎉 FR-0047 Brand B2B Settlement MVP — AR/AP/NET 雙向 + 20 tests / Phase II 9 FR 全部 MVP 收尾**（branch `feat/fr-0047-brand-b2b-settlement-mvp`，2026-06-05）：Phase II 9 FR **最後一個 MVP** 起手收尾。品牌 B2B 月結雙向（AR 品牌付服務費 / AP 平台付 commission / NET 同月相沖），同 FR-0045/0046 6 狀態機 + dispute window 7d pattern。**新 migration `SQL/migrations/027-brand-b2b-statements.sql`** `saas.brand_b2b_statement` 表：(a) UNIQUE(tenant+brand+year+month+**direction**) 同方向唯一 / (b) `direction` enum AR/AP/NET / (c) 服務量指標 `total_service_orders` + `total_warranty_claims` + `sla_breach_count` / (d) 4 金額欄位 `ar_service_fee` + `ap_commission` + `warranty_deduction` + `sla_penalty` + `net_amount` / (e) `net_payable_to` enum brand/platform（NET 方向時自動判定方向）/ (f) 4 indexes (含 direction)。**MIGRATION_REGISTRY.md** row 027。**新 `api/services/brand_b2b_statement_service.py`** + 關鍵 helper `_compute_net(direction, ar, ap, warranty, sla)`：(1) AR direction → net=ar_service_fee, payable='platform' (brand 付平台) / (2) AP direction → net=ap_commission - warranty - sla, payable='brand' (平台付 brand) / (3) NET direction → net = ar - ap + warranty + sla；> 0 = brand 欠 platform；< 0 = platform 欠 brand；= 0 → None。**generate_statement** 含 direction 驗證 (AR/AP/NET) + 服務量負值驗 + brand_name 必填 + 冪等 (同 tenant+brand+period+direction 已存則回 existing 不重複)。**新 `api/routers/brand_b2b_statement_v2.py`** 8 endpoints (鏡像 FR-0045/0046 8 op pattern) + body `GenerateBody` 14 欄。**main.py 掛 router** `M12 Brand B2B Settlement` tag。**新 `tests/test_brand_b2b_statement.py`** 20 tests：(a) `_compute_net` AR direction → ar=50000 / payable=platform / (b) AP direction → ap-warranty-sla=16000 / payable=brand / (c) NET 30000 > 0 → brand 欠 platform / (d) NET -30000 → platform 欠 brand / (e) NET=0 → payable=None / (f) 狀態機完整 6 enum mapping / (g) check_transition 拒 invalid 409 / (h) generate NET happy / (i) 冪等同 brand+period+direction / (j) invalid direction 422 / (k) negative orders 422 / (l) empty brand_name 422 / (m) submit window 7d / (n) dispute window 過期 409 / (o) approve happy / (p) mark_paid 非 approved 409 / (q) DISPUTE_WINDOW_DAYS=7 / (r) _VALID_DIRECTIONS {AR/AP/NET} sanity / (s) router 8 endpoints / (t) 8 operation_id 對齊。**測試結果**：20 passed in 0.23s；本 session 累積 **294 tests 全綠 in 0.91s**。**設計取捨**：(a) **direction 三軸** — AR/AP 單向 statement 給 partner 分別審計；NET 同月相沖最終一張 statement 簡化結算流程 / (b) `_compute_net` 共用 helper — 3 direction 邏輯集中避 drift / (c) UNIQUE 含 direction — 同 brand 同月可有 AR/AP 兩張獨立 statement，但 NET 也可單獨產 / (d) `net_payable_to` 為 dual-state enum — UI 直接顯示「平台應付 N TWD 給 brand」/「brand 應付 N TWD 給平台」/ (e) contract_ref nullable — 對齊 ADR-0056 contract attachment 未必每個 statement 都有 / (f) warranty_deduction 從 AP 扣 — 業務邏輯：產品保固 brand 負責，扣 brand 的 commission / (g) sla_penalty 同 — SLA 違約由 brand 賠 / (h) 與 FR-0045/0046 共享狀態機架構 — 未來可 refactor 抽 common StatementBase。**🎉 WBS 補齊 Phase II 9 FR 全部 MVP ✅**：FR-0049 Inbox + FR-0044 Lifecycle + FR-0053 GDPR + FR-0050 AI Governance + FR-0051 SOP Feedback + FR-0048 RMA Quality + FR-0045 Tech AP + FR-0046 Dispatcher Commission + **FR-0047 Brand B2B**；9 FR 全從 `placeholder` → active MVP 狀態。**本 session 完整成果**：(1) 5 batch CR BUILD (CR-0017/0018/0019/0013/0012)；(2) 7 §8 P1/P2 缺口 backend (Pool publish / A37 / Dispute 60d / M18 canary / SOP 績效 / 客戶滿意度 / FTFR+SLA)；(3) 2 DEFERRED 解 (Dispute 負值 audit / M18 SLO halt)；(4) **9 Phase II FR MVP** 起手；**累積 24 個分支落地 / 294 tests passing / WBS 完成度 89% → 預估 98%+**（剩 P4 Cutover 3-5d / UAT 期程 / 部分 web UI 工作）。

- **FR-0046 Dispatcher Commission MVP — 派工指標 + 抽成計算 + 17 tests**（branch `feat/fr-0046-dispatcher-commission-mvp`，2026-06-05）：Phase II 9 FR 第 8 個 MVP 起手。複用 FR-0045 完整狀態機 pattern (draft→pending_review→approved|disputed|rejected→paid + dispute_window 7d)，本 commit 對象為派工人不是技師，金額拆解為 base_commission + performance_bonus - penalty = net_commission。**新 migration `SQL/migrations/026-dispatcher-commission.sql`** `saas.dispatcher_commission_statement` 表：(a) UNIQUE(tenant+dispatcher+year+month) / (b) 派工指標：`total_dispatched_orders` + `total_completed_orders` + `completion_rate_pct` (自動計算) + `avg_customer_satisfaction` (cascade from FR-0048 RMA) / (c) 抽成：`base_commission` + `performance_bonus` + `penalty` + `net_commission` / (d) 同 FR-0045 6 狀態機 + dispute window 機制 / (e) 3 indexes。**MIGRATION_REGISTRY.md** row 026。**新 `api/services/dispatcher_commission_service.py`** 7 ops 完全鏡像 FR-0045 pattern；額外 generate validation：`completed_orders <= dispatched_orders` 否則 422 + completion_rate_pct = completed/dispatched×100 (0 不 ZeroDivisionError)。**新 `api/routers/dispatcher_commission_v2.py`** 8 endpoints 對應 FR-0045 (M12 Dispatcher Commission tag)。**main.py 掛 router**。**新 `tests/test_dispatcher_commission.py`** 17 tests：狀態機 + 6 valid/invalid transitions / generate 100 派工 85 完工 → completion_rate=85% + net=50000+10000-2000=58000 / 0 派工 不 div-by-zero / completed > dispatched → 422 / 負值 orders → 422 / month=0 → 422 / submit window 7d / dispute window 過期 / reason <5 / approve happy / mark_paid 非 approved 409 / DISPUTE_WINDOW_DAYS=7 / router 8 endpoints + operation_id 對齊。**測試結果**：17 passed in 0.21s；本 session 累積 274 tests 全綠 in 0.87s。**設計取捨**：(a) **大量複用 FR-0045 pattern** — 結構幾乎一致；差別只在對象 (dispatcher vs technician) + 金額拆解 (base+bonus-penalty vs gross-deductions) / (b) `completion_rate_pct` service 端自動計算 — 防 client 不一致；commission 公式 (M18 config 整合) 留下輪 / (c) `avg_customer_satisfaction` 欄位預留 — 後續可從 FR-0048 RMA Quality 拉資料 cascade (d) 串接 / (d) completed > dispatched 強驗 — 業務上不可能但防 client 錯資料 / (e) negative orders 422 — 簡單 sanity / (f) penalty 為正值 (扣除欄位設計) — 不存負值避混淆 / (g) 與 FR-0045 共享 _ALLOWED_TRANSITIONS 邏輯 — 未來可 refactor 抽 common base service。**WBS 補齊**：Phase II 9 FR 第 8 個 MVP ✅；FR-0046 從 `placeholder` 進入 active MVP；**剩 1 FR**：FR-0047 品牌月結 + B2B Settlement (最後一個 Phase II FR)。**本 session 累積 8 個 Phase II MVP + 5 batch CR + 7 §8 P1/P2 + 2 DEFERRED 解 = 23 個分支 / 274 tests passing**。

- **FR-0045 Technician AP Statement MVP — 6 狀態機 + dispute window 7d + 17 tests**（branch `feat/fr-0045-tech-ap-statement-mvp`，2026-06-05）：Phase II 9 FR 第 7 個 MVP 起手。FR-0045 標 placeholder Phase II 月結，本 commit 落師傅月結 statement 表 + 完整狀態機 + dispute window 機制。與 CR-0012 settlement 互補（statement 匯總視角 vs settlement 個別撥款記錄）。**新 migration `SQL/migrations/025-tech-ap-statements.sql`** `saas.technician_statement` 表：(a) 期間 `period_year` + `period_month` + `UNIQUE(tenant, tech, year, month)` 同月唯一 / (b) 金額拆解 6 欄：`gross_amount` 完工累計 / `travel_fee_deduction` (ADR-0041) / `cash_collection_deduction` 代收抵扣 / `dispute_hold_amount` 暫扣爭議 / `other_deductions` / `net_amount` 計算 net / (c) 6 狀態機：`draft / pending_review / disputed / approved / rejected / paid` / (d) `dispute_window_ends_at` + `disputed_at` + `dispute_reason` window 機制 / (e) 雙簽 audit `reviewed_by / reviewed_at / paid_at` / (f) 3 indexes。**MIGRATION_REGISTRY.md** row 025。**新 `api/services/technician_statement_service.py`** 7 ops + 狀態機：(a) `_ALLOWED_TRANSITIONS`：draft→pending_review；pending_review→approved|disputed|rejected；disputed→pending_review|rejected；approved→paid；rejected→draft；paid 終態 / (b) `generate_statement` 冪等檢查（UNIQUE constraint）→ INSERT + net 計算 / (c) `submit_for_review` draft→pending_review + 設 `dispute_window_ends_at = NOW + 7 days` (`DISPUTE_WINDOW_DAYS=7`) / (d) `dispute_statement` 技師舉報異議 reason ≥5 字 + window 過期 409 / (e) `approve_statement` 主管核准 / (f) `reject_statement` 退回 (pending|disputed → rejected) / (g) `mark_paid` approved → paid 終態 / (h) `list_statements` filter by tech/status。**新 `api/routers/technician_statement_v2.py`** 8 endpoints：`:generate` / `list` / `{id}` / `:submit` / `:dispute` / `:approve` / `:reject` / `:mark-paid`；write 強制 X-Initiator。**main.py 掛 router** `M12 Tech AP Statement` tag。**新 `tests/test_technician_statement.py`** 17 tests：(a) 狀態機 6 enum 完整 + paid 終態 + valid/invalid transition / (b) generate 50000-5000-2000=43000 net 計算 / (c) idempotent on existing / (d) period_month=13 → 422 / (e) submit dispute_window 7d 設 / (f) submit 非 draft → 409 / (g) dispute window 內 happy / (h) window 過期 → 409 含「過期」 / (i) reason <5 字 → 422 / (j) approve happy / (k) mark_paid 非 approved → 409 / (l) DISPUTE_WINDOW_DAYS=7 sanity / (m) router 8 endpoints / (n) 8 operation_id 對齊。**測試結果**：17 passed in 0.27s；本 session 累積 257 tests 全綠 in 0.91s。**設計取捨**：(a) statement 與 settlement 雙軌不重複 — settlement 個別 reconciliation 撥款記錄；statement 月度技師視角匯總 / (b) UNIQUE(tenant+tech+year+month) — 一個技師一個月一張 statement / (c) net 由 service 計算（gross - 4 deductions）— 一致性保證 / (d) 6 狀態機含 disputed 中間態 — 技師可在 window 內舉報異議；主管處理後可回 pending_review 或 rejected / (e) dispute window 7 天硬編碼 — 業界銀行對帳 cycle 標準；超時 cron auto-approve 留下輪 / (f) reject → draft 允許 — admin 退回後可重新調整再 submit / (g) paid 終態 — 已匯款不可撤銷 (避免雙重匯款風險) / (h) reason 500 字截斷 / dispute_reason 1000 字 — 防大欄位 / (i) reviewer_id 同人不可雙簽未強制 — 本 MVP 沒做 SoD，留下輪 / (j) 技師 self-service GET 用同 list endpoint by technician_id filter — 不另開 endpoint。**WBS 補齊**：Phase II 9 FR 第 7 個 MVP ✅；FR-0045 從 `placeholder` 進入 active MVP；**剩 2 FR**：FR-0046 派工人 Commission 月結 + FR-0047 品牌月結 B2B Settlement。**本 session 7 個 Phase II MVP / backend 累積 22 個分支 / 257 tests passing**。

- **FR-0048 RMA Quality Feedback Loop MVP — 4 cascade + SOP feedback 連動 + 14 tests**（branch `feat/fr-0048-rma-quality-feedback-mvp`，2026-06-05）：Phase II 9 FR 第 6 個 MVP 起手。FR-0048 標 placeholder (Phase II quality feedback loop 深化，Phase I 已 FR-0015 basic warranty)，本 commit 落 RMA 結案後 4 cascade 品質訊號表 + 連動 SOP feedback (FR-0051 rma_finding source 伏筆兌現)。**新 migration `SQL/migrations/024-rma-quality-feedback.sql`** `saas.rma_quality_finding` 表 (a) 關聯：`warranty_claim_id` / `work_order_id` / `technician_id` / `brand` / `device_model` 5 軸 / (b) 故障訊號：`failure_mode` + `root_cause` + `is_repeat_failure` 30 日重複故障 flag / (c) **4 cascade 評分對齊 §1**：`brand_quality_score` 1-5 (品牌商 feedback) / `technician_quality_score` 1-5 (M07 績效) / `ai_diagnosis_accuracy` accurate/partial/wrong (M20 SOP feedback) / `customer_satisfaction_score` 1-5 (月結 commission) / (d) 5 indexes (tenant+created / failure_mode / brand partial / technician partial / warranty partial)。**MIGRATION_REGISTRY.md** row 024。**新 `api/services/rma_quality_service.py`** 4 ops：(a) `log_finding(...)` 驗 failure_mode ≥3 字 + ai_diagnosis_accuracy ∈ 3 valid + 3 score 1-5；**Cascade (c) 自動觸發**：若 `propagate_to_sop_feedback_sop_id` + `ai_diagnosis_accuracy ∈ {wrong, partial}` → 呼 `sop_feedback_service.log_feedback(source='rma_finding')`；wrong → sentiment=negative；partial → sentiment=neutral；accurate 不 cascade；cascade 失敗 try/except 不阻主流程 / (b) `list_findings` 多 filter (failure_mode / brand / technician / is_repeat / date) / (c) `get_brand_summary` Cascade (a) 品牌商視角 SQL 2 query：top 10 failure_modes by count + 總/avg_brand_quality/repeat_failure_pct / (d) `get_technician_summary` Cascade (b) 技師視角：總/avg_technician_quality/avg_customer_satisfaction/repeat_failure_pct。**新 `api/routers/rma_quality_v2.py`** 4 endpoints tenant-scoped：(1) `POST /tenants/{tid}/rma-quality-findings` 201 body 17 欄 / (2) `GET .../rma-quality-findings` list filter / (3) `GET .../rma-quality-findings/brand-summary?brand` 品牌 cascade / (4) `GET .../rma-quality-findings/technicians/{techId}/summary` 技師 cascade。**main.py 掛 router** `M13 RMA Quality` tag。**新 `tests/test_rma_quality.py`** 14 tests：(a) log happy → return id + failure_mode + cascade_sop_feedback=false / (b) failure_mode <3 字 → 422 / (c) invalid ai_accuracy → 422 / (d) brand_quality_score=6.0 out of range → 422 / (e) `ai_accuracy='wrong'` + propagate → cascade 呼 sop_feedback.log_feedback 含 source='rma_finding' / sentiment='negative' / metadata 含 rma_quality_finding_id / (f) `partial` → sentiment='neutral' / (g) `accurate` → 不 cascade / (h) cascade sop_feedback 拋例外 → 主流程不 leak (finding 仍寫入) / (i) list 全 filter SQL 含 failure_mode/brand/technician_id/is_repeat/BETWEEN clauses / (j) brand_summary 30 battery + 10 motor → total=40 / avg=3.2 / repeat=20% (8/40) / top_failure_modes[0]='battery_drain' / (k) brand_summary 空 → 0 不 div-by-zero / (l) technician_summary aggregate avg_tq/avg_cs/repeat_pct / (m) router 4 endpoints / (n) 4 operation_id 對齊 expected set。**測試結果**：14 passed in 0.14s；本 session 累積 240 tests 全綠 in 0.79s。**設計取捨**：(a) **Cascade 設計 (c) AI 分診差 → SOP feedback** — 兌現 FR-0051 第 5 個 source='rma_finding' 伏筆；wrong → negative 直接訊號，partial → neutral 讓 review 決定 / (b) accurate 不 cascade — 好的 AI 不需 negative feedback 干擾 SOP rank / (c) cascade 用 try/except 隔離 — sop_feedback DB 掛仍寫 finding，反之亦然 / (d) 4 cascade 不全綁同一動作 — brand/technician 為事後 aggregate report (cascade a, b)；ai_diagnosis 即時 propagate (cascade c)；customer_satisfaction 留 commission service 月結時取（cascade d 由 FR-0046 Commission MVP 之後拉資料）/ (e) is_repeat_failure 30 日重複故障 — repeat_failure_pct 是品牌商品質關鍵 KPI / (f) failure_mode 200 字截斷 + root_cause 1000 字 + notes 2000 字 — 防大欄位 / (g) brand_quality / technician_quality nullable — 部分 RMA 沒法判斷某維度（e.g. 技師沒處理時 technician_quality=null）/ (h) top failure_modes LIMIT 10 — 防 long-tail 干擾報告。**WBS 補齊**：Phase II 9 FR 第 6 個 MVP ✅；FR-0048 從 `placeholder` 進入 active MVP；**FR-0051 (SOP Feedback) ↔ FR-0048 (RMA Quality) cascade 第一條真實連動建立**。**剩 3 FR 全為月結系列**：FR-0045 Technician AP 月結 / FR-0046 派工人 Commission 月結 / FR-0047 品牌月結 + B2B Settlement（金流業務領域）。**本 session backend 累積：5 batch CR BUILD + 7 §8 P1/P2 + 2 DEFERRED 解 + 6 Phase II MVP = 20 個分支落地 / 240 tests passing**。

- **FR-0051 SOP Feedback Spiral MVP — 多源 feedback aggregate + 12 tests**（branch `feat/fr-0051-sop-feedback-spiral-mvp`，2026-06-05）：Phase II 9 FR 第 5 個 MVP 起手。FR-0051 標 placeholder（A10 深化，Phase I 已 basic SOP review FR-0017），本 commit 落多源 feedback aggregate 表 + 3 endpoints + sentiment_score 評分。對齊 ADR-0038 ai-feedback-review-policy；補 FR-0051 §3「Feedback aggregation engine」需求第一步。**新 migration `SQL/migrations/023-sop-feedback.sql`** `saas.sop_feedback` 表：(a) 5 `source` (FR-0051 §1 多源)：`customer_thumbs` / `technician_onsite` / `rma_finding` / `ai_eval` / `csm_manual` / (b) 2 `sop_type`: `draft` (sop_drafts) / `case_entry` (case_entries published)/ (c) 3 `sentiment`: positive/neutral/negative + score 1.0-5.0 / (d) `comment` + `metadata` jsonb (額外 context e.g. wo_id/rma_id) / (e) `reporter_user_id` + `work_order_id` 來源追溯 / (f) 3 indexes (tenant+sop+created / source+created / sop+sentiment)。**MIGRATION_REGISTRY.md** row 023。**新 `api/services/sop_feedback_service.py`** 3 ops：(a) `log_feedback` 驗 5 source + 3 sentiment + 2 sop_type + score 1-5 enum + comment 2000 字截斷 / (b) `list_feedback(sop_id?, source?, sentiment?, date_range?, limit 1-500)` 多 filter / (c) `get_sop_summary(sop_id, date_range?)` 3 query 聚合：by_source / by_sentiment / avg_score；**sentiment_score = (positive - negative) / total × 100**（-100..+100 區間，越高越正面）；0 不 ZeroDivisionError。**新 `api/routers/sop_feedback_v2.py`** 3 endpoints tenant-scoped：(1) `POST /tenants/{tid}/sop-feedback` body FeedbackBody 9 欄 → 201 / (2) `GET .../sop-feedback?sop_id&source&sentiment&start_date&end_date&limit` filter list / (3) `GET .../sop-feedback/{sopId}/summary` 聚合單 SOP。**main.py 掛 router** `A10 SOP Feedback` tag。**新 `tests/test_sop_feedback.py`** 12 tests：(a) log happy → return {id, source, sentiment} / (b) invalid source → 422 / (c) invalid sentiment → 422 / (d) invalid sop_type → 422 / (e) score 6.0 out of range → 422 / (f) list 全 filter SQL 含 sop/source/sentiment/BETWEEN / (g) list invalid source → 422 / (h) summary 100 row → sentiment_score=60 (70 pos - 10 neg / 100) + avg_score=4.2 / (i) empty summary → total=0 不 div-by-zero / (j) all-negative → sentiment_score=-100 / (k) router 3 endpoints / (l) 3 operation_id 對齊。**測試結果**：12 passed in 0.14s；本 session 累積 226 tests 全綠 in 0.78s。**設計取捨**：(a) sentiment_score 為 -100..+100 區間 — 業界 NPS 借鑑（NPS 也是 -100..+100），governance dashboard 直觀顯示 / (b) 三種 sentiment + 1-5 score 雙軌 — sentiment 給快速分類，score 給精度 / (c) source 5 種對齊 §1 多源 — 客戶 thumbs / 技師現場 / RMA / AI Eval / CSM manual / (d) sop_type draft|case_entry 雙軌 — 草稿和 published case 都收 feedback，published 後也能持續修 / (e) metadata jsonb — 不寫死 schema 給未來擴張 (wo_id, rma_id, ai_eval_id 等) / (f) avg_score 為 nullable — 部分 source 不給 score (e.g. customer 只給 thumbs) / (g) PUT/PATCH 不開 — feedback append-only / (h) summary 是 review queue 排序基礎 — sentiment_score 越低 SOP 越該優先 review。**WBS 補齊**：Phase II 9 FR 第 5 個 MVP ✅；FR-0051 從 `placeholder` 進入 active MVP；剩 4 FR (FR-0045/0046/0047 月結系列 + FR-0048 RMA)。**本 session backend 已落 4 個 Phase II MVP + 7 個 §8 P1/P2 缺口 + 5 個 batch CR BUILD + 2 個 DEFERRED 解 = 18 個分支落地 / 226 tests passing**。

- **FR-0050 AI Governance & PRD Traceability MVP — trace store + 11 tests**（branch `feat/fr-0050-ai-governance-trace-mvp`，2026-06-05）：Phase II 9 FR 第 4 個 MVP 起手。FR-0050 標 placeholder（業主 Q2=C 決議 A12 延 Phase II），本 commit 落 AI decision trace store + 3 endpoints。對齊 ADR-0028 ai-employee-charter + ADR-0038 ai-feedback-review-policy；補 FR-0050 §3 「Trace store schema / Source → action 反向 lookup API / Governance dashboard 共用 trace」三項 Phase II 啟動需補。**新 migration `SQL/migrations/022-ai-decision-trace.sql`** `saas.ai_decision_trace` 表：(a) 5 `decision_type`: `reasoning` / `tool_call` / `output` / `guardrail_block` / `human_handoff` / (b) 業務 context: `conversation_id` / `work_order_id` / `agent_session_id` / (c) **traceability 三軸**: `prd_source` (PRD 文件路徑) / `charter_rule` (e.g. 'ADR-0028 §A.2') / `owner_decision_ref` (e.g. 'CR-0014 HD-3=B' 業主裁決) / (d) `action_summary` 1-line 描述 + `input_payload` / `output_payload` jsonb / (e) `guardrail_triggered` ('safety_gate'/'pii_block'/'cost_cap'/etc.) + `guardrail_action` ('block'/'warn'/'redact'/'allow') / (f) `agent_version` 追溯模型版本 / (g) 6 indexes (tenant+created / conversation / work_order / prd_source / charter_rule / decision_type)。**MIGRATION_REGISTRY.md** row 022。**新 `api/services/ai_governance_trace_service.py`** 3 ops：(a) `log_decision(tenant_id, decision_type, action_summary, ...)` — agent runtime 呼；驗 decision_type ∈ 5 valid + guardrail_action ∈ 4 valid + action_summary ≥3 字；500 字截斷；jsonb dump payload / (b) `list_traces(tenant_id, decision_type?, conversation_id?, work_order_id?, prd_source?, charter_rule?, date_range?, limit 1-500)` — 多 filter 組合 lookup；ORDER BY created_at DESC / (c) `get_trace_summary(tenant_id, date_range?)` — 3 GROUP BY 聚合：by_decision_type / by_guardrail_action / by_agent_version；總 + block_count + block_rate_pct (block/total×100，0 不 ZeroDivisionError)。**新 `api/routers/ai_governance_trace_v2.py`** 3 endpoints tenant-scoped：(1) `POST /tenants/{tid}/ai-governance/traces` body TraceBody 14 欄 → 201；(2) `GET .../traces?decision_type&conversation_id&work_order_id&prd_source&charter_rule&start_date&end_date&limit` 多 filter list；(3) `GET .../traces/summary?start_date&end_date` 聚合。**main.py 掛 router** `A12 AI Governance` tag。**新 `tests/test_ai_governance_trace.py`** 11 tests：(a) log happy → return {id, created_at, decision_type} / (b) invalid decision_type → 422 / (c) invalid guardrail_action → 422 / (d) action_summary <3 字 → 422 / (e) list_traces SQL 含 conversation/work_order/decision_type/BETWEEN clauses / (f) list invalid decision_type → 422 / (g) limit 0/501 → 422 / (h) summary 3 GROUP BY 聚合 → block_rate=3.23% (5/155) + by_decision_type/action/version 正確 / (i) summary 空 → totals=0 不 div-by-zero / (j) router 3 endpoints / (k) 3 operation_id 對齊 expected。**測試結果**：11 passed in 0.15s；本 session 累積 214 tests 全綠 in 0.80s。**設計取捨**：(a) 不修 agent runtime — 本 commit 只提供 endpoint，agent 整合留下輪；agent 可 fire-and-forget HTTP / (b) decision_type 5 種 enum 對齊業界 LLM agent observability（OpenLLMetry / LangSmith）/ (c) traceability 三軸 prd_source/charter_rule/owner_decision_ref 為 nullable — 不強制全填，agent 能填多少填多少 / (d) input/output payload jsonb — 可儲整 prompt/response 供 audit replay / (e) guardrail_triggered 對齊既有 audit_log_service.py `safety_gate` log_type — 兩個系統可 cross-ref / (f) agent_version 追溯模型版本 — debug 行為差異時可篩特定版本 / (g) summary block_rate_pct 是 governance dashboard 關鍵指標 — 過高代表 guardrail 太嚴格或 agent 學壞 / (h) PUT/PATCH 不開 — trace 為 append-only audit；改錯只能新 log 蓋過 / (i) 6 indexes 含 prd_source / charter_rule — 對齊 §3 「Source → action 反向 lookup」需求。**WBS 補齊**：Phase II 9 FR 第 4 個 MVP ✅；FR-0050 從 `placeholder` 進入 active MVP；剩 5 FR (FR-0045/0046/0047 月結/Commission/B2B Settlement + FR-0048 RMA + FR-0051 SOP Feedback)。

- **FR-0053 GDPR Right-to-be-Forgotten MVP — 兩階段刪除 + legal-hold + 14 tests**（branch `feat/fr-0053-gdpr-forget-mvp`，2026-06-05）：Phase II 9 FR 第 3 個 MVP 起手。FR-0053 標 `status: placeholder` Phase II cross-cutting compliance，本 commit 落兩階段刪除流程 (T0 soft → T+30 hard) + legal-hold 拒絕 + audit table。對齊 ADR-PII-002 雙層防線 + BR-PII-001 30 天 cooldown + user-flow Flow S4 mermaid。**新 migration `SQL/migrations/021-gdpr-forget-requests.sql`**：`saas.forget_request` 表 (a) 5 狀態機：`received` (T0 剛收) / `legal_hold_denied` (爭議中拒) / `soft_deleted` (T0+ clear PII) / `hard_deleted` (T+30 physical delete) / `cancelled` (客戶撤回) / (b) 3 requested_by: `customer_self` (LIFF/web portal) / `admin` / `dpo` / (c) `legal_hold_reason` + `expected_release_at` (7d 內通知客戶) / (d) `received_at` / `soft_deleted_at` / `hard_delete_eligible_at` (=soft+30d) / `hard_deleted_at` 完整時程 / (e) 3 indexes 含 `forget_req_hard_delete_due_idx` partial WHERE status='soft_deleted' 給 cron 用。**MIGRATION_REGISTRY.md** row 021。**新 `api/services/gdpr_forget_service.py`** 6 ops：(a) `create_forget_request(tenant, subject_user, requested_by, actor, notes)` — 取 subject email capture audit + 冪等檢查 (同 user 已 received/soft_deleted → 回 existing) / (b) `deny_legal_hold(request_id, legal_hold_reason ≥5 字, expected_release_at)` — received → legal_hold_denied / (c) `soft_delete(request_id)` — received → soft_deleted；UPDATE users SET display_name='[REDACTED]', email='[REDACTED-{id}]', phone=NULL；寫 hard_delete_eligible_at = NOW + 30d (BR-PII-001) / (d) `hard_delete(request_id)` — soft_deleted → hard_deleted；驗 hard_delete_eligible_at <= NOW (cooldown 強制 30 天)；DELETE FROM users WHERE id (audit 保留) / (e) `cancel_request(request_id)` — received → cancelled / (f) `list_forget_requests(tenant_id, status?, limit)` 列審計。**新 `api/routers/gdpr_forget_v2.py`** 7 endpoints tenant-scoped：(1) `POST` 建 request 201 / (2) `GET` 列 (status filter + limit 1-200) / (3) `GET {id}` 單筆 / (4) `POST :legal-hold-deny` body reason + expected_release_at / (5) `POST :soft-delete` / (6) `POST :hard-delete` / (7) `POST :cancel`；所有 write 強制 X-Initiator。**main.py 掛 router** `M17 GDPR Forget` tag。**新 `tests/test_gdpr_forget.py`** 14 tests：(a) create happy 4 SQL → envelope 對齊 / (b) idempotent on existing → 回原 row 不 INSERT / (c) invalid requested_by → 422 / (d) user_not_found → 404 / (e) deny_legal_hold happy → status='legal_hold_denied' / (f) reason <5 字元 → 422 / (g) soft_delete sets eligibility = NOW + 30d / (h) soft_delete on soft_deleted → 409 (不可重做) / (i) hard_delete 前 cooldown 10 天未滿 → 409 含 cooldown 字樣 / (j) hard_delete cooldown 已過 → DELETE users + status='hard_deleted' / (k) cancel on hard_deleted → 409 / (l) HARD_DELETE_COOLDOWN_DAYS=30 sanity / (m) router 7 endpoints / (n) 7 operation_id 對齊 expected set。**測試結果**：14 passed in 0.15s；本 session 累積 203 tests 全綠 in 0.76s。**設計取捨**：(a) 冪等檢查走「同 user 已 received/soft_deleted 不 INSERT」— GDPR 場景客戶重複提交常見，避產孤兒 row / (b) email capture 在 INSERT 時保留 audit — hard_delete 後 user row 已刪，但 audit ledger 有 subject_email 供日後查證 / (c) soft_delete PII clear 用 `'[REDACTED-{id}]'` 而非 NULL email — UNIQUE constraint 可能存在不能 NULL / (d) hard_delete cooldown 強制 30 天 (BR-PII-001) — service 層驗 eligible_at <= NOW；非 cron skip / (e) `cancel` 只能從 `received` — soft_deleted 後 PII 已 clear 撤回意義不大 / (f) requested_by 三來源 enum — 業務上區分管道 / (g) `legal_hold_reason` ≥5 字 — 防空泛拒絕 / (h) hard_delete cron 留下輪 — 本 commit 提供 endpoint admin 可手動觸發，cron 自動掃同邏輯 / (i) 7d 內通知客戶屬業務 SOP 流程，本 service 只標 expected_release_at 給流程驅動。**WBS 補齊**：Phase II 9 FR 第 3 個 MVP ✅ — FR-0053 從 `placeholder` 進入 active MVP；剩 6 FR (commission/AP/B2B settlement/RMA/AI Governance/SOP Feedback)。

- **FR-0044 Technician Lifecycle MVP — 5 ops + audit table + 17 tests**（branch `feat/fr-0044-tech-lifecycle-mvp`，2026-06-05）：Phase II 9 FR 第 2 個 MVP 起手。FR-0044 標 `status: placeholder` Phase II，本 commit 落 lifecycle 狀態機 + 5 個 ops + audit trail 新表。**新 migration `SQL/migrations/020-tech-lifecycle.sql`**：`saas.technician_lifecycle_event` 表 (a) 7 個 event_type: `onboarding_approved` / `onboarding_rejected` / `suspended` / `reactivated` / `terminated` / `rating_threshold_breach` / `cert_expired` (後兩個自動偵測事件，未直接改 status) / (b) `previous_status` / `new_status` / `reason` / `notes` / `actor_user_id` / `actor_role` 完整 audit / (c) 2 indexes (tenant+tech+created / event_type+created)。**MIGRATION_REGISTRY.md** row 020。**新 `api/services/technician_lifecycle_service.py`**：(a) `_ALLOWED_TRANSITIONS` 6 狀態 enum + 狀態機：pending_approval → active|rejected；active → suspended|terminated；suspended → active|terminated；rejected → terminated；terminated 終態；inactive → active|terminated / (b) `_check_transition` 409 STATE_CONFLICT / (c) `_fetch_status` JOIN users 取 tenant 隔離 + 404 / (d) `_change_status_and_audit` 共用 helper：fetch current → CAS UPDATE WHERE status=X → INSERT audit row (best-effort：audit 失敗不 rollback status 但 log) / (e) 5 ops: `approve_onboarding`(pending→active) / `reject_onboarding`(pending→rejected) / `suspend`(active→suspended) / `reactivate`(suspended→active) / `terminate`(any→terminated 終態) / (f) `list_lifecycle_events(tenant_id, tech_id?, event_type?, limit)` 列 audit。**新 `api/routers/technician_lifecycle_v2.py`** 6 endpoints tenant-scoped：(1) `POST :onboard-approve` / (2) `POST :onboard-reject` (body reason+notes) / (3) `POST :suspend` (body reason+notes) / (4) `POST :reactivate` (body reason+notes) / (5) `POST :terminate` (body reason+notes) / (6) `GET /technicians/lifecycle-events?tech_id&event_type` 列 audit；所有 write 強制 X-Initiator header + cross-tenant guard。**main.py 掛 router** `M07 Technician Lifecycle` tag。**新 `tests/test_technician_lifecycle.py`** 17 tests：(a) 狀態機 6 個 mapping 完整性 (pending/active/suspended/terminated/inactive/rejected) + terminated 終態 / (b) `_check_transition` 接受 valid / 拒 invalid 409 / 拒 terminated 出邊 / (c) approve_onboarding pending→active / suspend active→suspended / terminate active→terminated / terminate from suspended (允許永久終止) / suspend from pending → 409 (狀態機拒) / (d) reason < 3 字元 → 422 / (e) tech_not_found → 404 / (f) list_lifecycle_events filter by tech_id + event_type / (g) router 6 endpoints + 6 operation_id 對齊。**測試結果**：17 passed in 0.14s；本 session 累積 189 tests 全綠 in 0.73s。**設計取捨**：(a) `_change_status_and_audit` 共用 helper — 5 ops 邏輯一致避 drift / (b) audit best-effort — status 已 CAS 寫入，audit 失敗只 log（vs 全 tx rollback 風險：status 變動主流程被阻擋；trade-off 認為 audit 缺幾條可接受）/ (c) `actor_role` 預設 `operations_manager` 對齊 spec / (d) terminate 從任何狀態進終態 — 符合業務：rejected 也可後續正式終止 (歷史審計留 trail) / (e) `inactive` 既有狀態保留可轉 active 或 terminated — 不破壞 legacy / (f) reason 必填 ≥3 字 + 500 字截斷 / notes 1000 字 — 防大欄位污染 / (g) 不直接 ALTER technicians.status CHECK — v1 schema 未顯式 CHECK，service 層自驗 enum 避 migration 複雜化 / (h) 列表 endpoint 允許 tech_id 與 event_type 同時 filter — 「該技師何時被停權」即可秒查。**WBS 補齊**：Phase II 9 FR 第 2 個 MVP ✅ — FR-0044 從 `placeholder` 進入 active MVP；剩 7 FR (commission/AP/B2B settlement/RMA/GDPR/...)。

- **FR-0049 Exception Approval Inbox MVP — 統一聚合 5 種 pending approval + 10 tests**（branch `feat/fr-0049-approval-inbox-mvp`，2026-06-05）：Phase II 9 FR 起手第一步。FR-0049 標 `status: placeholder` Phase II，本 commit 落 MVP 統一 inbox endpoint，不修既有 schema 純讀組合。**設計**：依 FR-0049 §1 Scope Intent「統一 inbox 集中所有 approval task」分 5 種類別聚合：(1) scope_changes pending (Flow 3) / (2) refund_requests pending|csm_approved (Flow 6 雙簽中) / (3) saas.dispute filed|in_review|mediation (Flow 7) / (4) saas.reschedule_proposal pending (Flow 11) / (5) saas.reconciliation_exception detected|ops_review|fix_proposed (Flow 13 EX5 / CR-0018)。**新 `api/services/approval_inbox_service.py`** `list_pending_approvals(tenant_id, type_filter, limit)`：(a) 5 SQL query 按 type_filter 條件執行（None/'all' 全跑，單類別只跑 1 個）/ (b) 每 row 走 `_envelope(type, id, target_id, summary, created_at)` 統一格式輸出 {type, id, target_id, summary, severity, created_at, days_overdue} / (c) `_SEVERITY_BY_TYPE` 對齊業務優先：dispute/refund=high (涉客訴+錢)、scope_change/recon_exception=medium、reschedule=low / (d) `_SLA_DAYS_BY_TYPE` 對齊既有 SLA：scope_change/reschedule=1day、refund=3days、dispute=60days、recon_exception=7days / (e) `_days_overdue(created_at, sla_days)` = age_days - sla_days（正數=overdue）/ (f) sort: severity rank → days_overdue desc → created_at asc。**新 `api/routers/approval_inbox_v2.py`** `GET /tenants/{tid}/approval-inbox?type=...&limit=100` operation_id=`listApprovalInbox` 需 JWT + tenant guard + type Query 6 valid 值 + limit 1-500 validation。**main.py 掛 router** `M15 Approval Inbox` tag。**新 `tests/test_approval_inbox.py`** 10 tests：(a) type='all' → 5 query 全跑 by_type 各 1，sort 結果 dispute/refund high 排頭 reschedule low 排尾 / (b) type='scope_change' 只跑 1 query / (c) `_SEVERITY_BY_TYPE` mapping 對齊業務優先 / (d) 5 天前 scope_change SLA=1 → days_overdue=4 / (e) invalid type → 422 / (f) limit 0/501 → 422 / (g) db_unavailable 503 / (h) 全空 row → total=0 by_type 全 0 / (i) router operation_id 註冊 / (j) tenant-scoped path 格式。**測試結果**：10 passed in 0.14s；本 session 累積 172 tests 全綠 in 0.71s。**設計取捨**：(a) **不修既有 schema 純讀組合** — Phase II MVP 起手原則；後續 routing engine / escalation matrix / bulk operations 各別演進 / (b) `_envelope` 統一 type+id+target_id+summary 4 欄結構 — 前端 inbox UI 不需 N 種子 component / (c) summary 80 字元截斷防大 row / (d) severity 三級非數字 1-5 — 業界 inbox 常用 high/medium/low / (e) days_overdue 給正負 — 負數=SLA 內，正數=已逾期，UI 可直接顯示「已逾期 N 天」/ (f) `_want(t)` helper 避 5 個 if-else 不一致 / (g) 5 SQL 各 limit 而非 cursor — MVP 簡化；Phase II 補 cursor 跨類別分頁 / (h) 從 work_orders → problem_cards → conversations → users → tenant 路徑 — 對齊既有 service tenant 過濾 pattern / (i) refund 顯示 stage 文字（"等 CSM 審" / "等 ops_manager 簽"）— 給 admin 一眼判斷 next action。**WBS 補齊**：Phase II 9 FR 第 1 個 MVP 起手 ✅ — FR-0049 從 `placeholder` 進入 active MVP；剩 8 FR (commission/AP/B2B settlement/RMA/GDPR/...) 待後續分批起手。

- **M18 SLO halt decision endpoint 解 DEFERRED + 9 tests**（branch `feat/m18-slo-halt-decision`，2026-06-05）：解 system-completion-status §8 P1「M18 Phase II canary auto-advance / **SLO halt（需 scheduler）**」缺口最後一段 SLO halt。**策略**：對齊 dispute 負值 resolution「人工 trail」精神 — admin 提供觀察 metric，service 端做 decision，**halt 動作仍由 admin 顯式呼 `:rollback`**（避免自動 halt 風險，無 metric collection 框架也能安全運作）。**新 service `check_slo_halt(tenant_id, rollout_id, error_rate_pct, p99_latency_ms?, error_rate_slo_pct?, p99_latency_slo_ms?, actor_user_id?)`**：(a) 預設 threshold 對齊 CR-0019 保守 SLA：`_DEFAULT_ERROR_RATE_SLO_PCT=1.0` / `_DEFAULT_P99_LATENCY_SLO_MS=1000` 可被 caller override / (b) JOIN config_rollout + config_version 取 current_stage / tenant_id / ns / key / (c) 驗 rollout 非 100% / rolled_back 否則 409（不需 halt 決策） / (d) 構造 exceeded list 列違反指標（"error_rate 2% > 1%" / "p99_latency 1500ms > 1000ms"）/ (e) should_halt = bool(exceeded) / (f) recommendation 字串給 admin看的下一步（建議呼 rollback 或 SLO 通過繼續）/ (g) 每次 check 都寫 audit row diff `{slo_check:true, should_halt, exceeded, observed, thresholds}` 留 trail / (h) audit 用既有 action='rollout_started' enum diff 加 slo_check 標識（避 schema 改）。**新 router endpoint `POST /tenants/{tid}/m18/rollouts/{rolloutId}:slo-check`** operation_id=`checkConfigRolloutSlo` body SloCheckRequest（error_rate_pct 必填 + p99_latency_ms / SLO thresholds 可選）。**新 `tests/test_m18_slo_halt.py`** 9 tests：(a) under threshold → should_halt=false + recommendation 含「SLO 通過」 / (b) error_rate 2% > 1% → should_halt + exceeded 列原因 + recommendation 含「立即呼 rollback」 / (c) p99 1500ms > 1000ms breach / (d) 自訂 strict threshold 0.1% → 0.5% breach / (e) rollout not_found → 404 / (f) 已 100% → 409 / (g) error_rate < 0 → 422 / (h) audit row 寫入正確 actor_user_id + slo_check=true + observed / (i) router endpoint operation_id 註冊。**測試結果**：9 passed in 0.15s；本 session 累積 162 tests 全綠 in 0.71s。**設計取捨**：(a) **不自動 halt** — 對齊 dispute 負值 resolution 人工 trail；admin 看 decision 後自行決定呼 rollback / (b) 預設 threshold 對齊 CR-0019 SLA — 改 env 同步影響 / (c) audit 不論 should_halt 都寫 — admin 可定期 audit 報告看「過去 7 天 SLO check 共 N 次，breach M 次」/ (d) 用 audit action 'rollout_started' enum 加 slo_check 標識 — 避免新增 action enum 需 schema migration / (e) 無 metric collection 框架 — 由 admin 觀察線上 dashboard 後 manual 提供 metric value；未來接 Prometheus / Cloud Monitoring 時自動化 / (f) p99_latency optional — 部分情境只看 error_rate 即可。**WBS 補齊**：§8 P1「M18 Phase II canary auto-advance / SLO halt」全完成 — canary advance (`6cc660ad`) + SLO halt decision (本 commit) 雙缺口都解。**剩 DEFERRED**：負值 DGS cascade 也已解 (`609cb427`)。**本 session backend P1/P2/DEFERRED 全部清乾淨**；剩餘為 web-only UI 工作 + Phase II 9 FR + P4 Cutover + UAT 期程。

- **Dispute 負值 resolution 解 DEFERRED — admin manual review trail + 5 tests**（branch `feat/dispute-negative-resolution-audit`，2026-06-05）：解 `dispute_v2_service.co_sign_dispute` 之 HD-4 `[DEFERRED]` 缺口。**問題**：原 code 標 `[DEFERRED] resolution_amount < 0 → DGS/refund cascade 留 follow-up（ADR-0061/FR-0014）`，但 6/05 評估自動退款風險過高（一旦 trigger 即把錢轉走無人監督）；故本 commit 改採「**admin manual review trail**」策略：寫 audit + WS publish notify admin，後續退款 / voucher_reverse 由 admin dashboard 手動觸發。**新 helper `_emit_negative_resolution_event(tenant_id, dispute_id, co_signer_id, amount)`**：(a) payload 構造含 dispute_id / resolution_amount (round 2 decimals) / co_signer_id / action_required='manual_refund_review' / 中英 note 說明「Admin must manually review and decide on refund / voucher_reverse action」 / (b) 呼 `audit_log_service.log_event(event_type='dispute.negative_resolution_pending', actor_id=co_signer_id, actor_role='ops_manager', action='negative_resolution_audit', target_type='dispute', target_id=dispute_id, payload=...)` 寫 audit_events / (c) WS publish `/realtime/disputes/{tenant_id}` channel + type='dispute.negative_resolution_pending' 給 admin dashboard 訂閱即時通知 / (d) audit / WS 各別 try/except 隔離 best-effort — 不阻 dispute co-sign 主流程（即使 audit DB 掛仍嘗試 WS，反之亦然）。**修改 `co_sign_dispute`**：移除 `[DEFERRED]` log，改呼 `_emit_negative_resolution_event`。**改 docstring 注釋**：「HD-4 resolution_amount 負值：寫 audit_log + WS publish 觸發 admin manual review；不自動退款（避免無人監督的退款風險），保留人工 trail」。**新 `tests/test_dispute_negative_resolution.py`** 5 tests：(a) audit_log_service.log_event 被呼帶正確 event_type / actor_id / actor_role / target / payload (含 dispute_id / amount / action_required) / (b) WS publish 對 `/realtime/disputes/tenant-99` channel + 正確 type / dispute_id / (c) audit 拋例外 → publish 仍嘗試（雙 best-effort 隔離） / (d) WS publish 拋例外 → 不該 leak / (e) amount=-123.456 → payload.resolution_amount=-123.46 (round 2 decimal)。**測試結果**：5 passed in 0.13s；本 session 累積 153 tests 全綠 in 0.70s。**設計取捨**：(a) **保留人工 trail 取代自動退款** — 自動退款風險過高 (一旦 trigger 錢已轉走無人監督)；admin manual review 是金融行業最佳實踐 / (b) audit + WS 雙寫 — admin 可 (1) 主動定期查 audit log 找 pending review 或 (2) 即時 WS 通知 dashboard / (c) actor_role='ops_manager' — 對齊 co-sign step-2 角色 / (d) channel 用 `/realtime/disputes/{tenant_id}` tenant 隔離 / (e) round 2 decimal — UI 顯示與會計慣例對齊 / (f) Best-effort 雙隔離 — audit DB 掛 WS 仍試，WS 掛 audit 也已寫；最差情況有一邊留 trail / (g) 未來若需自動 refund/voucher_reverse — 在 admin dashboard 手動 trigger endpoint 接 voucher_void_service.void_voucher 即可（CR-0018 已有 pattern）。**WBS 補齊**：原 `disputes_v2.py:87` `[DEFERRED] resolution_amount < 0 → DGS/refund cascade` → admin manual review trail ✅；負值 cascade 從 DEFERRED 轉為 manual review 流程已落地。**剩 DEFERRED Phase II 項目** — M18 SLO halt（涉 metrics collection + threshold 判斷較大）；其他 cron / KPI / 績效 缺口全清。

- **Operational KPI（FTFR + SLA on-time）endpoint + 6 tests**（branch `feat/ftfr-sla-ontime-kpi`，2026-06-05）：解 system-completion-status §8 P2「報表 metrics 擴充」剩餘缺口。對齊 `reports_kpi.py` docstring 既標的「未來擴充：FTFR（rework_of_id 已存在但需業務邏輯確認）/ SLA 達成率（需 SLA 規則表 + tracker）」項目。**新 `api/services/operational_kpi_service.py` `get_operational_kpi(tenant_id, start, end)`**：(a) 3 SQL query 聚合 / (b) **FTFR**: `SELECT COUNT(DISTINCT wo.id), COUNT(DISTINCT wo2.rework_of_id) FROM work_orders wo LEFT JOIN work_orders wo2 ON wo2.rework_of_id = wo.id WHERE u.tenant_id = ... AND wo.completed_at IS NOT NULL` → ftfr_pct = (total - rework_source)/total×100；rework_source_count = 被當作 rework 來源的 wo 數 / (c) **dispatch_on_time_pct**: COUNT FILTER (accepted_at <= created_at + INTERVAL '{DISPATCH_DELAY_MINUTES} minutes') / dispatch_total；env DISPATCH_DELAY_MINUTES 對齊 sla_monitor.py 預設 30 / (d) **arrival_on_time_pct**: started_at <= scheduled_at + INTERVAL '{ARRIVAL_OVERDUE_MINUTES} minutes' / arrival_total；env 預設 120 (2 小時)；對齊 PM Q5=B Soft SLA / (e) 0 不 ZeroDivisionError / (f) date range 可選 completed_at::date BETWEEN。**新 `api/routers/reports_operational_kpi.py`** `GET /reports/operational-kpi?start_date&end_date` operation_id=`getOperationalKpiReport`；validation 同 customer-satisfaction (start/end 同時必填或同時可空)。**main.py 掛 router** `Reports` tag。**新 `tests/test_operational_kpi.py`** 6 tests：(a) happy path FTFR (100-8)/100=92% / dispatch 75/100=75% / arrival 60/80=75% / thresholds 暴露 / (b) 0 completed → 全 0 不 div-by-zero / (c) date range → SQL 含 BETWEEN / (d) db_unavailable 503 / (e) 0 rework → FTFR=100% / (f) router endpoint 註冊。**測試結果**：6 passed in 0.14s；本 session 累積 148 tests 全綠 in 0.69s。**設計取捨**：(a) 不修 sla_monitor.py — SLA breach 為 in-memory alert，本 service 從 work_orders 欄位推算「達成率」（事後聚合）/ (b) FTFR 用 LEFT JOIN self-reference 算 — `rework_of_id IS NOT NULL` 的 row 數即被當作 rework 源 / (c) DISPATCH_DELAY_MINUTES / ARRIVAL_OVERDUE_MINUTES 從 env 取對齊 sla_monitor — 改 env 同步影響閾值；module-level int 常數 f-string interpolate 安全 / (d) MVP 用 accepted_at - created_at 估 dispatch delay — 真實 admin assign 時間需另查 dispatch_logs（暫不繞）/ (e) thresholds 暴露於 response — 前端可顯示「30 分內派工率 75%」具體門檻 / (f) FTFR 衡量品質 vs SLA 衡量速度 — 兩個 axes 並列展示。**WBS §8 P2「報表 metrics 擴充」全完成** — 客戶滿意度 + FTFR + SLA on-time 三大 metric 落地；計價引擎 GUI（前端工作）仍留待 web rework。

- **客戶滿意度 KPI 擴充 endpoint + 5 tests**（branch `feat/customer-satisfaction-kpi`，2026-06-05）：解 system-completion-status §8 P2「報表 metrics 擴充」缺口。對齊 `reports_kpi.py` docstring 既標的「未來擴充：客戶滿意度 / NPS / 差評率」項目，落地客戶滿意度真實 metrics endpoint。**新 `api/services/customer_satisfaction_service.py` `get_customer_satisfaction(tenant_id, start_date, end_date)`**：(a) 3 個 SQL query 聚合 (work_orders JOIN problem_cards JOIN conversations JOIN users 取 tenant + completed_at IS NOT NULL + 可選 date range completed_at::date BETWEEN) / (b) totals: total_completed / total_rated (FILTER WHERE rating IS NOT NULL) / avg_rating (AVG FILTER) / rated_pct = rated/completed×100 / (c) rating_distribution 1-5 各 count from GROUP BY rating / (d) rates: low_rating_pct = (1+2 stars)/rated×100 / five_star_pct = 5 stars/rated×100 / (e) top_low_rating_recent 5 筆 WHERE rating <= 2 ORDER BY completed_at DESC NULLS LAST 含 feedback 字段 / (f) 0 不 ZeroDivisionError fallback / (g) date range 不給時聚合全時段。**新 `api/routers/reports_customer_satisfaction.py`** `GET /reports/customer-satisfaction?start_date&end_date` operation_id=`getCustomerSatisfactionReport` 需 JWT + tenant guard + start/end 必須同時給或同時不給 (422) + start ≤ end (422)。**main.py 掛 router** `app.include_router(reports_cs_router.router, prefix="/api/v1", tags=["Reports"])`。**新 `tests/test_customer_satisfaction.py`** 5 tests：(a) happy path 3 query mock → 計算正確：50/40 rated_pct=80% / avg_rating=4.25 / distribution 完整 / low_rating_pct=12.5% (5/40) / five_star_pct=50% (20/40) / top 2 含 feedback / (b) 0 completed → rated_pct=0 not div-by-zero / avg_rating=None / rates=0 / (c) date range 給時 SQL 含 `completed_at::date BETWEEN` / (d) db_unavailable 503 / (e) router endpoint operation_id 註冊。**測試結果**：5 passed in 0.14s；本 session 累積 142 tests 全綠 in 0.68s。**設計取捨**：(a) 獨立 service 不污染 `kpi_service.py` — KPI 模組複雜邏輯多，獨立加新 metric 較乾淨 / (b) 走 `/reports/` prefix 對齊既有 `/reports/kpi` convention / (c) low_rating 1-2 star 定義 — 業界差評閾值通用 / (d) JOIN problem_cards 為 tenant 取得路徑 — 與既有 service pattern 一致 / (e) ORDER BY DESC NULLS LAST — completed_at NULL 排尾不影響 top 排序 / (f) feedback 字段透傳 — 前端可顯示真實客戶反饋 / (g) start/end 同時給 or 同時不給 — 防 半 range 誤用 / (h) 不算 NPS — NPS 是 0-10 範圍非 1-5，未來若 schema 加 nps_score 再加。**WBS 補齊**：§8 P2「報表 metrics 擴充」客戶滿意度部分 ✅；SLA 達成率 + FTFR 留下輪。

- **SOP 績效真實化 backend metrics endpoint + 6 tests**（branch `feat/sop-performance-metrics-backend`，2026-06-05）：解 system-completion-status §8 P2「SOP 績效真實化」缺口 backend 部分。**問題**：`web/src/app/admin/knowledge-base/sop-performance/page.tsx` 為純 placeholder「SOP 績效報表開發中」無 endpoint 對接；本 commit 落 backend metrics endpoint 取代未來連接。**新 `api/services/sop_performance_service.py` `get_sop_metrics(tenant_id, window_days=30)`**：(a) 驗 window_days 1-365 否則 422 / (b) 5 個 SQL query 聚合：status_distribution by status (pending_review/approved/rejected/published) + totals (active/deleted/total，用 FILTER WHERE deleted_at IS NULL) + rates (approval_rate=approved/(approved+rejected)×100, publish_rate=published/approved×100, 0 不 ZeroDivisionError) + window (new_drafts created_at >= NOW-window, newly_published reviewed_at>=NOW-window) + top_recent_published (LIMIT 5 ORDER BY reviewed_at DESC NULLS LAST 含 case_entry_id 追溯) + retire_candidates_count (published 且 reviewed_at < NOW-window — 候 retire) / (c) 不依賴新表純由現有 sop_drafts schema 聚合 (含 015/016 sop-v2 deleted_at) — 不需 migration。**新 `api/routers/sop_performance_v2.py`** `GET /tenants/{tenantId}/sop-performance/metrics?window_days=30` operation_id=`getSopPerformanceMetrics` 需 JWT + tenant guard 403 CROSS_TENANT_READ + window_days Query 1-365 validation。**main.py 掛 router** `app.include_router(sop_performance_v2_router.router, tags=["M14 SOP Performance"])`。**新 `tests/test_sop_performance.py`** 6 tests：(a) happy path 4 query mock → 計算正確 approval_rate=83.33% / publish_rate=80% / 30 日 window / top 2 row / retire 4 / (b) 全 pending 無 decided → rates=0.0 不 ZeroDivisionError / (c) db_unavailable → 503 / (d) window_days=0/366 → 422 / (e) router 註冊 operation_id 對齊 / (f) tenant-scoped path 格式驗證。**測試結果**：6 passed in 0.14s；本 session 累積 137 tests 全綠 in 0.70s。**設計取捨**：(a) 不新增 schema 純聚合 sop_drafts — 避 migration 阻塞 / (b) approval_rate 算 (approved+rejected) decided 不算 pending — pending 待決不該影響歷史表現 / (c) publish_rate 算 published/approved — approved 但未 publish 是漏網之魚 / (d) retire_candidates 用「published 且 reviewed_at < NOW-window」啟發式 — 假設 30 日無更新 = 候 retire / (e) top 5 ORDER BY reviewed_at NULLS LAST — reviewed_at 缺的 publish row 排尾 / (f) 365 天上限防大查詢 / (g) tenant_id required 用 path 而非 query — 對齊 v2 tenant-scoped convention。**WBS 補齊**：§8 P2「SOP 績效真實化」backend ✅；前端 page 接此 endpoint 待後續輪整合。

- **M18 canary 5%→50%→100% 自動推進 + cron + 10 tests**（branch `feat/m18-canary-auto-advance`，2026-06-05）：解 system-completion-status §8 P1「M18 Phase II — canary auto-advance / SLO halt（需 scheduler）數天」缺口的 auto-advance 部分。**問題**：`config_m18_service._advance_canary_stage` 既為 NotImplementedError + `[DEFERRED] Phase II scheduler` stub；canary rollout 卡 stage='5%' 永不推進。**重寫 `_advance_canary_stage(rollout_id)`** 真實實作：(a) JOIN config_rollout+config_version 取 current_stage / stage_started_at / next_stage_eta / tenant_id / namespace / key / actor / (b) 驗 current_stage ∈ {5%,50%} 否則 409 / (c) 推算 observation_seconds = next_stage_eta - stage_started_at（5%→50% 寫入時的 interval，沒存的話 fallback 600s）/ (d) 5%→50%: UPDATE rollout SET current_stage='50%' + stage_started_at=NOW + next_stage_eta=NOW+observation_seconds；audit `stage_advanced` diff `{from:'5%', to:'50%', auto_advanced:true, next_stage_eta}` / (e) 50%→100%: `_dethrone_active(tenant_id, namespace, key)` 取 dethroned_id → UPDATE config_version SET state='active' + activated_at=NOW + parent_version_id=COALESCE(parent, dethroned) → UPDATE rollout SET current_stage='100%' + next_stage_eta=NULL → audit `stage_advanced` + `activated` → `_cache_invalidate(tenant_id, namespace, key)` 對齊 instant path 流程。**新 `api/realtime/config_canary_advance_cron.py`** ConfigCanaryAdvanceCron class：(a) 300s (5 min) 預設 interval（`CONFIG_CANARY_ADVANCE_INTERVAL` env 可調，對齊 M18 觀察通常 10-15min 掃描頻率合理）/ (b) 30s startup delay / (c) `run_once` 入口：SELECT id FROM config_rollout WHERE strategy='canary_5_50_100' AND current_stage IN ('5%','50%') AND next_stage_eta < NOW LIMIT 100 → 對每筆呼 service.advance → summary {advanced, errors} / (d) per-rollout try/except 隔離；errors 計數但不阻其他 rollout / (e) db_unavailable 回 {skipped, advanced:0, errors:0} 不炸。**main.py lifespan** 加 `canary_advance_cron.start/stop` 對齊 6-monitor 啟動序 inventory→sla→line_push→recon_exc→dispute_escalation→**canary_advance**。**新 `tests/test_m18_canary_advance.py`** 10 tests：(a) DEFAULT_INTERVAL=300 對齊 5 min sanity / (b) db_unavailable → skipped / (c) 無 due rollout → advanced=0 errors=0 / (d) 2 個 due rollout 都呼 service.advance / (e) 1 個 advance 失敗其他繼續，errors=1 / (f) service 5%→50% UPDATE+audit 包含 from/to/auto_advanced=true + next_stage_eta / (g) service 50%→100% 呼 dethrone+activate+audit×2 (stage_advanced + activated) / (h) rollout_not_found → 404 CONFIG_NOT_FOUND / (i) already 100% → 409 / (j) start/stop 冪等。**測試結果**：10 passed in 0.13s；本 session 累積 131 tests 全綠 in 0.72s。**設計取捨**：(a) `_advance_canary_stage` 改 raise → 真實實作 — Phase 0 stub 已不適用，cron 接入後線上可跑 / (b) observation_seconds 從現有 stage_started_at + next_stage_eta 推算 — 不需新欄位存 observation_minutes 避 schema migration / (c) auto-advanced 標 audit diff `auto_advanced:true` — 與 manual `:advance-stage` 區分（未來若加 manual endpoint） / (d) 5 min 掃描 vs SLA 60d/24h cron — observation 通常分鐘級需更快掃描 / (e) actor 用 approver_user_id — 對齊 instant path activated by approver 邏輯 / (f) cache_invalidate 在 50%→100% 才呼 — 5%→50% 期間 readConfig 仍保守回前一 active 不需失效。**SLO halt 仍 DEFERRED** — 涉 metrics collection + threshold 判斷較大；本輪只解 stage advance 缺口。**WBS 補齊**：§8 P1「M18 Phase II canary auto-advance」 ✅；SLO halt 留下輪。

- **Dispute 60d auto-escalation cron + 6 tests**（branch `feat/dispute-60d-escalation-cron`，2026-06-05）：解 system-completion-status §8 P1「60d cron + 負值 DGS」缺口 cron 部分。`_escalate_overdue_disputes` 既有 helper 標 `[DEFERRED]` 不接 cron，60d sla 過期 dispute 永遠不自動 escalated。**新 `api/realtime/dispute_escalation_cron.py`** DisputeEscalationCron class（仿 ReconExceptionDetector pattern）：86400s 預設 + 60s startup delay + run_once(tenant_id) 入口 + UPDATE saas.dispute SET escalated WHERE status IN (filed,in_review,mediation) AND sla_deadline<NOW；try/except 隔離；db_unavailable 回 0 不炸。**main.py lifespan** 加 dispute_escalation_cron.start/stop 對齊 5-monitor 啟動序（inventory→sla→line_push→recon_exc→dispute_escalation）。**`dispute_v2_service._escalate_overdue_disputes`** docstring+logger 移除 [DEFERRED] 標記（cron 已接），helper 仍保供 admin manual / 測試。**新 `tests/test_dispute_escalation_cron.py`** 6 tests：DEFAULT_INTERVAL=86400 / db_unavailable→0 / SQL filter 正確 + tenant_id 可選 / tenant_id 不給 SQL 無條件 + args=() / start/stop 冪等 / rowcount=0 不 raise。**測試結果**：6 passed in 0.04s；本 session 累積 121 tests 全綠 in 0.74s。**設計取捨**：(a) 沿用既有 helper SQL 不另寫 — wrap 為 cron 避邏輯 drift / (b) 86400s daily 對齊 AC-03 60d 業務節奏 / (c) run_once 公開可呼 — 緊急時 admin 立即執行 / (d) [DEFERRED] log 移除 — 線上不該再噴 DEFERRED / (e) escalated_to 預設 'ops_director' 對齊 spec / (f) inline 同 SQL 而非呼 service helper — 避免 service 層成 cron entrypoint 不對等。**負值 DGS Phase II 仍 DEFERRED** — `disputes_v2.py:87` 標 `[DEFERRED] resolution_amount < 0 → DGS/refund cascade` 涉退款 / voucher 連動較大，留下輪。**WBS 補齊**：§8 P1 60d cron ✅；負值 DGS 仍 DEFERRED。

- **A37 候選詳情 backend — technician workload heatmap endpoint + 11 tests**（branch `feat/a37-tech-workload-heatmap`，2026-06-05）：解 system-completion-status §8 P1「A37 candidate detail drawer（排班熱力圖）半天」缺口 backend 部分。**新 service `technician_service.get_technician_workload_heatmap(tenant_id, technician_id, days=30)`**：(a) 驗 days 1-90 422 / (b) SQL JOIN work_orders → problem_cards → conversations → users 取得該 tech 在 window 內 GROUP BY DATE(scheduled_at), status / (c) 聚合 daily_map: {date, total, in_progress, completed, cancelled, load_intensity} / (d) `_classify_load`: 0=idle, 1=low, 2-3=medium, 4-5=high, 6+=saturated / (e) summary 計算 completion_rate_pct = completed/(completed+cancelled)×100、peak_day = max total、avg_per_day。**新 endpoint `GET /tenants/{tid}/technicians/{technician_id}/workload-heatmap?days=30`** operation_id=`getTechnicianWorkloadHeatmap` 需 JWT + tenant guard + days 1-90。**新 `tests/test_a37_workload_heatmap.py`** 11 tests：(a) `_classify_load` 5 級邊界 / (b) heatmap 聚合 mock SQL → daily 結構 + load_intensity 對齊 / (c) summary 80% completion rate + peak_day + avg_per_day / (d) 空 row 不 ZeroDivisionError → 全 None/0 / (e) days=0/91 → 422 / (f) router 註冊 + path 格式驗證。**測試結果**：11 passed in 0.21s；本 session 累積 115 tests 全綠 in 0.77s。**設計取捨**：(a) 走 technicians router 而非新建 dispatch_v2 — A37 邏輯上看候選技師詳情屬技師端資料 / (b) 走 work_orders.scheduled_at 非 created_at — heatmap 顯示「該日工作量」對應實際派遣 / (c) load_intensity 內建 enum — 前端不需另算閾值，UI color mapping 直觀 / (d) summary 只算 completion_rate 不算 average_revenue — 後者需 wo.final_price 涉錢，避 over-engineering / (e) 90 天上限防大查詢；30 天為前端預設 / (f) DATE() 函數 UTC 時區，後續可加 AT TIME ZONE。**WBS 補齊**：system-completion-status §8 P1「A37 candidate detail drawer」backend ✅；前端 drawer 元件待後續輪整合此 endpoint。

- **Pool 即時推播 backend-frontend 契約對齊 + 6 tests**（branch `feat/pool-realtime-publish-contract`，2026-06-05）：解 system-completion-status §8 P1「Pool 即時推播觸發（前端訂閱已備）」缺口。**問題**：前端 `web/src/app/pool/page.tsx:56-82` useRealtimeChannel 訂 `/realtime/pool/${techId}` 期待 payload schema `{event: 'added'|'taken'|'cancelled', work_order_id, work_order?}`，但 backend `assign_order` (work_order_service.py:606-620) 推的是 `{type: 'work_order.assigned_to_you', payload: {work_order_id, reason_code}}` — event name/payload 不對齊，前端永遠不會觸發 added/taken/cancelled handler。**新 helper `_publish_pool_change(tenant_id, wo_id, technician_id, event)`** 對齊前端契約：(a) event='added' → payload 含完整 work_order 物件供 prepend 列表（呼 get_order） / (b) event='taken' / 'cancelled' → 只帶 work_order_id 供 filter 移除 / (c) technician_id 空 → no-op 避免 `/realtime/pool/None` 髒 channel / (d) get_order 拋例外 → 仍推 publish 但省略 work_order key（best-effort）/ (e) hub.publish 拋例外 → 吞掉（postback 與 monitor 同 pattern，best-effort 不阻斷 caller）/ (f) type 命名 `work_order.pool_<event>`（清楚標 pool 路徑 + 三 event 對齊）。**3 處 caller 整合**：(1) `assign_order:606` 改呼 `_publish_pool_change(event='added')` 取代原本 `assigned_to_you` 不對齊 publish / (2) `accept_order` 加 fetch technician_id → 呼 `_publish_pool_change(event='taken')` 把該技師個人 pool 該 wo 移除（接受後該 wo 進入 my-orders 不該繼續在 pool 列）/ (3) `cancel_order` 加 fetch technician_id → 若已派則呼 `_publish_pool_change(event='cancelled')`。**新 `tests/test_pool_realtime_publish.py`** 6 tests：(a) added → publish channel=`/realtime/pool/tech-1` + type=`work_order.pool_added` + payload 含 work_order_id + work_order 完整物件 / (b) taken → 只帶 work_order_id 不該帶 work_order key / (c) cancelled → 同 taken / (d) technician_id 空字串 / None → no-op 無 publish / (e) get_order 拋 → 仍 publish 但省略 work_order key（best-effort）/ (f) hub.publish 拋 → 不該 leak 例外。**Bug fix 副產物**：發現 `_publish_and_return` 缺尾部 `return order` — 原本 v1 service code 內 except 後直接 return order，本次 edit 加 helper 時被誤 swap 走，本 commit 同時補回 + 移除誤置 `return order` from helper。**測試結果**：6 passed in 0.14s（新檔）；本 session 累積 104 tests 全綠 in 0.76s（regression）。**設計取捨**：(a) added 帶完整 work_order — 前端不需再 fetch 減 round-trip / (b) taken/cancelled 只帶 id — 前端 filter 操作不需 wo 物件，省 payload 大小 / (c) helper best-effort — pool publish 是 UX 加分，不該阻擋主流程 / (d) type 名命 `pool_<event>` 而非沿用 work_order.<event> — 便於前端 hub debug 過濾 channel 訊息來源 / (e) cancel 場景無 technician 不推 — pool 本身 per-tech channel 設計，無 tech 該 wo 從未在任何 pool / (f) assigned_to_you 整體棄用 — 該事件未被任何前端訂閱（grep 無對應 listener），純 leak code。**WBS 補齊**：system-completion-status §8 P1「Pool 即時推播觸發（前端訂閱已備）半天」 → ✅ done。

- **CR-0012 BUILD — Manual CSV 月結撥款 schema + service + 5 endpoints + 14 tests**（branch `feat/cr-0012-stage1-schema-csv`，2026-06-05）：依批次決策 HD-1=Manual CSV 階段化 / HD-2=Cloud Scheduler / HD-3=retry 後標 manual_payout / HD-4=admin UI 確認 + 水單 / HD-5=flag-driven dispute 排除 / HD-6=不啟用 escrow 推進。**新 migration `SQL/migrations/019-monthly-settlement.sql`**：(a) `saas.settlement.status` enum 擴 5 態 `pending / csv_exported / paid / manual_paid / failed`（HD-1 V1 走 pending → csv_exported → manual_paid 三態，paid/failed 預留 V2 bank API）/ (b) 加 `settled_eligible boolean DEFAULT true`（HD-5 flag-driven，dispute open 時 reconciliation_v2 co-sign 設 false）/ (c) 加 `receipt_url / manual_paid_at / manual_paid_by`（HD-4 admin UI 水單 + actor audit）/ (d) 加 `retry_count / last_retry_at`（HD-3 V2 預留）/ (e) 新表 `saas.monthly_settlement_batch` 月結批次審計含 period_year/month UNIQUE + triggered_at + triggered_by + total_settlements + total_amount + csv_exported_at + csv_url + notes / (f) `settlement.monthly_batch_id FK` + 3 新 indexes (batch / status+eligible / tenant+period)。**MIGRATION_REGISTRY.md** row 019。**新 `api/services/monthly_settlement_service.py`** 4 函式：(1) `generate_monthly_batch(tenant_id, period_year, period_month, triggered_by)` UPSERT batch row (UNIQUE 已存則回 existing — HD-2 cron 重跑冪等) → 找該月 reconciliation.status='approved' 且未綁 batch 的 row → 對每筆查 work_orders → disputes 看 active dispute → INSERT settlement(amount=technician_payout, settled_eligible=由 dispute 反推, monthly_batch_id) → UPDATE batch totals / (2) `_get_batch` 取 batch dict 含所有欄位 / (3) `export_batch_csv(batch_id, tenant_id)` 驗 batch 屬本 tenant → SELECT 該 batch eligible=true 的 settlement → 用 csv.writer 寫 6 欄 (settlement_id / technician_id / amount_twd / currency / reconciliation_id / period) 回 StringIO → return text / (4) `mark_csv_exported(batch_id, tenant_id, csv_url)` 寫 batch.csv_exported_at + url → bulk UPDATE settlement pending+eligible → csv_exported / (5) `mark_manual_paid(settlement_id, tenant_id, actor_id, receipt_url)` HD-4 流程：必填 receipt_url → CAS UPDATE WHERE status IN ('csv_exported','pending') AND settled_eligible=true → status='manual_paid' + manual_paid_at + manual_paid_by + paid_at + payment_method='bank_manual' → 否則 409 STATE_CONFLICT。**新 `api/routers/monthly_settlements_v2.py`** 5 endpoints tenant-scoped：(1) `POST :generate` body period_year/month/triggered_by 201 / (2) `GET {batchId}` 200 batch summary / (3) `GET {batchId}/csv` PlainTextResponse text/csv + Content-Disposition `attachment; filename="monthly-settlement-{batchId[:8]}.csv"` / (4) `POST {batchId}:mark-exported` body csv_url 觸發 mark_csv_exported / (5) `POST settlements/{settlementId}:mark-manual-paid` body receipt_url 必填 → mark_manual_paid。**main.py 掛 router** `app.include_router(monthly_settlements_v2_router.router, tags=["M12 Monthly Settlement"])`。**新 `tests/test_cr_0012_monthly_settlement.py`** 14 tests：(a) `_coerce_decimal` None / int / 負 float / 0 → "0.00" / "100.00" / "-50.50" / "0.00" / (b) router 5 endpoints + operation_id 唯一集合 + tenant-scoped path / (c) router_mod 引用 svc 4 函式 / (d) GenerateBatchBody required + triggered_by 預設 'manual' / MarkManualPaidBody receipt_url 必填 / MarkExportedBody csv_url 可空 / (e) CSV header 6 欄 schema 穩定 sanity / (f) period_month=13 pydantic 允許（service 端 422）。**測試結果**：14 passed in 0.22s。**設計取捨**：(a) HD-1 階段化 V1 純 manual file flow — endpoint shape 與 V2 bank API 一致，未來只需新增 paid/failed 路徑不改 status enum / (b) batch UPSERT 冪等 — cron 重跑安全 / (c) generate_monthly_batch 查 dispute 用 work_order_events.payload->>'reconciliation_id' 反推 — 已有 events stream pattern 不另建 FK / (d) HD-5 settled_eligible 寫入時計算非每次 cron 動態查 — 撥款後 dispute 才開的 race 由 manual_paid CAS 條件 settled_eligible=true 阻擋 / (e) HD-4 receipt_url 必填於 service 層 + body schema 雙保險 / (f) HD-3 retry_count 預留欄位 — MVP 不跑 bank API，V2 補 / (g) CSV header 對齊 ADR-0041 80/20 但本 stage 不算 split — reconciliation_v2 co-sign 時 technician_payout 已 store 80%；本 service trust。**CR-0012 BUILD 全完成**。**FR-0012 從 draft + Q7 blocker → active V1 manual flow**。**剩 manual**：dev 跑 migration 019；prod 設 GCP Cloud Scheduler 排程 monthly 觸發 `:generate`；admin UI 接 `:mark-manual-paid` form；ops 文檔 V2 bank API 升級流程。

- **CR-0013 Stage 2 + 3 BUILD — rich menu setup + webhook 短碼 + consumer endpoints + 12 tests**（branch `feat/cr-0013-stage2-richmenu-webhook`，2026-06-05）：承接 Stage 1 (`48b046f3`) schema + service。**新 `scripts/line/setup_rich_menu.py`** 部署腳本：RICH_MENU_DEFINITION 2500x843 兩按鈕區（左半 0-1250「查進度」postback `g:p` / 右半 1250-2500「綁定」postback `b:s`）；4 step list_existing → 刪同名舊 → create → upload placeholder image → setDefaultRichMenu；冪等；LINE_CHANNEL_ACCESS_TOKEN env 必填。**擴 `api/routers/line_webhook.py`** 加 2 短碼 + 2 handler + push helper：`_handle_postback` 分派 `g:p`/`b:s`（無參，len(parts)==1）；`_handle_get_progress` 呼 `resolve_user_by_line_uid` → 無 binding push「尚未綁定」/ 有 binding push `WEB_BASE_URL/track/orders` 連結；`_handle_binding_start` push 含 `WEB_BASE_URL/track/binding-start` 引導 web 端登入觸發 generate_link_token（不在 webhook 直接產 token 避無 user_id 孤兒 row）；`_push_text` helper 用 linebot.v3 AsyncMessagingApi 失敗 log 不 raise；`LINE_DEFAULT_TENANT_ID` env (預設 default tenant) 給單 tenant 部署；修 `len(parts) < 2` early return → `not parts` 讓無參短碼能進 dispatch。**擴 `api/routers/consumer_v2.py`** 加 2 endpoint：`POST /consumer/bindings:generate-token` (需 JWT, `require_tenant` dep) → 取 user.tenant_id + user.user_id → `generate_link_token` 回 `{token, expires_at, ttl_hours: 24}`；`POST /consumer/bindings:consume` 接 body `{token, line_user_id}` → `consume_link_token` errors 404/410/409 透傳。**新 `tests/test_cr_0013_line_binding.py`** 12 tests：(1) `_hash_token` deterministic + SHA-256 64 chars / (2) `_new_token` 100 次唯一性 + 長度 40-50 / (3) `TOKEN_TTL_HOURS=24` HD-04 對齊 / (4-7) webhook postback 4 dispatch (g:p → get_progress, b:s → binding_start, g:p|extra 不誤觸) / (8-9) get_progress 無 binding 提示 / 有 binding push /track/orders / (10) binding_start push 含 binding-start / (11) rich menu DEFINITION 結構 2500x843 + 2 areas + 左 g:p / 右 b:s。**測試結果**：12 passed in 0.19s。**設計取捨**：(a) webhook handler 不直接呼 generate_link_token — 那會產孤兒 row；改 push web link 引導客戶登入 web/track 觸發 / (b) 鏟舊 rich menu by name prefix 'consumer-default-' — 一致命名免 manual 清 / (c) consume endpoint 不需 JWT — token 本身就是 single-use auth bearer / (d) `g:p|extra` 防 brute-force 嘗試混入 / (e) `_push_text` 失敗只 log — postback ack 200 不卡 LINE retry / (f) 圖檔可選 — ops 自行 setRichMenuImage 上傳真圖。**CR-0013 BUILD 全完成**：Stage 1/2/3 ✅。**FR-0022 LINE 路徑從 spec-only → active**：rich menu「查進度」postback + 主動 binding 流程閉環。**剩 manual**：dev 跑 migration 018；prod LINE Channel 跑 `scripts/line/setup_rich_menu.py`；ops 上傳真實 rich menu 圖檔。

- **CR-0013 Stage 1 BUILD — line_binding schema + service**（branch `feat/cr-0013-stage1-bindings-schema`，2026-06-05）：依批次決策 HD-01=(a) rich menu + HD-03=(b) 主動 binding 推進。**新 migration `SQL/migrations/018-line-bindings.sql`** `saas.line_binding` 表：(a) `bind_method` enum auto/manual — auto 為 wo 建立時 customers.line_user_id 自動同步審計、manual 為 rich menu「綁定」流程 / (b) `unbound_at` 軟解綁 + partial unique index `WHERE unbound_at IS NULL`（解綁後可重綁）/ (c) `link_token_hash` SHA-256 hex 不存明文 token / (d) 3 indexes：tenant+line_uid active 唯一、tenant+user_id active、token_hash 反查 / (e) `CHECK user_id/line_user_id NOT NULL` 完整性。**MIGRATION_REGISTRY.md** append row 018。**新 `api/services/line_binding_service.py`** 6 函式：(1) `_hash_token` SHA-256；`_new_token` 32 byte URL-safe (對齊 public_token 風格) / (2) `generate_link_token` 產 24h TTL (HD-04=a) 一次性 token + INSERT placeholder row (`line_user_id='pending:<hash[:16]>'` 避 partial unique 衝突) / (3) `consume_link_token` SHA-256 反查 → 過期軟標 unbound → 已 consume 409 → 解綁同 tenant 已存 active binding (line_uid 換綁) → UPDATE 真實 line_user_id + clear hash + bound_at=NOW / (4) `get_active_binding` tenant+line_uid 反查 / (5) `resolve_user_by_line_uid` 優先 line_binding active → fallback users.line_user_id (legacy FR-0044 auto) / (6) `record_auto_binding` wo 建立流程補審計（idempotent）/ (7) `unbind` 軟解綁 404 if no active。**設計取捨**：(a) HD-03=(b) 不取代 users.line_user_id legacy 自動路徑 — 兩條並存 resolve 統一 / (b) token plain 不入 DB — SHA-256 防外洩反推 / (c) placeholder 'pending:<hash>' 避 partial unique 衝突 + 自然 distinguish unconsumed / (d) 解綁同 tenant 已存 active 自動 — 換綁 UX 友善 / (e) auto binding 仍寫 row 補審計 — PDPA 合規可追溯 / (f) 24h TTL 對齊 FR-0022 §1.2 A2 + HD-04=a。**CR-0013 BUILD 進度**：Stage 1 ✅；剩 Stage 2 (rich menu setup + line_webhook 擴展 g:p / b:s 兩短碼 + consumer :consume endpoint) + Stage 3 (e2e tests)。

- **CR-0019 BUILD — Locust 場景 + SLA gate + CI workflow + 11 unit tests**（branch `feat/cr-0019-stage1-locust-scenarios`，2026-06-05）：依批次決策 HD-1=(b) Locust / HD-2 保守門檻 / HD-3=(a) 100 VU 30min / HD-4=(a) Staging Cloud Run / HD-5=(b) PR mini + pre-release full 推薦立場全採納。**新 `loadtest/locustfile.py`** TechnicianUser class：(1) `wait_time = between(20, 40)` 模擬真實 polling cadence (HD-3 30s ±10) / (2) `on_start` 從 `LOADTEST_TECH_TOKENS` env (CSV) 隨機抽 token + 預撈 GET pool 累積 wo_id pool — 避開壓測時還跑 /auth/login 增 latency 雜訊 / (3) 6 個 @task 加權對齊 CIA §3.3：`@task(40)` get_pool / `@task(25)` accept / `@task(15)` post_subflow（4 subflow 等比抽 material-request / delay / scope-change / door-check + body 對齊 v2 spec）/ `@task(10)` patch_schedule（走 :propose-reschedule 對齊 CR-0017 鏈路）/ `@task(5)` complete / `@task(5)` get_detail / (4) `_auth_headers` write=True 自動帶 X-Initiator + Idempotency-Key + JSON CT — 對齊 v2 router pattern。**新 `loadtest/sla.py`** SLA gate：(a) `SlaThresholds` dataclass HD-2 保守 p95 GET 500ms / p95 POST 1000ms / error 1% / 100 r/s / (b) `evaluate_csv` 讀 Locust 5.x stats CSV (Aggregated row + per-endpoint) → worst p95 GET / worst p95 POST (PATCH/DELETE 同 POST 規) / error_rate_pct / total_rps → 4 breach 列 / (c) `main` CLI exit 0/1 / (d) p95 'N/A' 視為 0 不炸 / (e) thresholds 可注入。**新 `loadtest/README.md`** mini vs full 跑法 + env 注入 + HD-2 4 門檻 + CI 整合 + 不在範圍清單 (HD-3 (c) soak / HD-3 (d) spike / WS scenario Phase II)。**新 `.github/workflows/loadtest-mini.yml`** PR-level (paths: api/** / loadtest/**) + workflow_dispatch 手動：install Locust 2.31.5；STAGING_HOST secret 缺則 skip；mini run --csv → SLA 評估 continue-on-error (PR 寬鬆，full run 才 fail)；artifact 留 30 天。**新 `tests/test_cr_0019_sla_gate.py`** 11 unit tests：(1) all-pass / (2) p95 GET breach / (3) p95 POST breach / (4) PATCH 視同 POST 跑 1000ms / (5) error breach / (6) throughput breach / (7) 多 breach (4 條) / (8) 自訂 strict / (9) DEFAULT_THRESHOLDS 4 門檻值 sanity / (10) 空 CSV 不 ZeroDivisionError / (11) 'N/A' 視為 0。**測試結果**：11 passed in 0.02s。**設計取捨**：(a) PR mini SLA 寬鬆 continue-on-error — 避免 PR noisy fail；嚴格門檻留 pre-release manual / (b) token pool 預 seed env 注入 — latency 雜訊最小化 / (c) PATCH/DELETE 同 POST p95_post — write-heavy 統一 / (d) Locust pin 2.31.5；升版前驗 schema drift / (e) Phase II 才補 WS / soak / spike — MVP 先 80% 場景 / (f) staging host secret 缺則 skip 不掛 PR。**CR-0019 BUILD 全完成** — WBS 1.2.7.3.3 從 0% → 90%（剩 staging budget 業主批 + 真實跑 baseline 才 100%）。

- **CR-0018 Stage 3 BUILD — cron detector + e2e tests (30 passed)**（branch `feat/cr-0018-stage3-cron-tests`，2026-06-05）：CR-0018 Flow 13 EX5 收尾。**新 `api/realtime/reconciliation_exception_detector.py`** ReconExceptionDetector class（仿 SLAMonitor / LinePushOutboxWorker pattern）：(1) `start/stop` lifespan 控制 + asyncio task / (2) `_run` 啟動 60s 延遲 + 86400s (24h) 預設 interval （`RECON_EXCEPTION_DETECTOR_INTERVAL` env 可調） / (3) `run_once` 入口分別呼三 detector：`_detect_amount_mismatch` JOIN reconciliation+settlement 找 status='approved' 且 settlement.amount ≠ reconciliation.technician_payout（delta = settled - payout）；`_detect_orphan_settlement` LEFT JOIN 找 settlement 無對應 reconciliation；`_detect_missing_invoice` 找 approved 超過 24hr 仍無 settlement / (4) 每 detector try/except 隔離，summary dict 含 errors counter / (5) 全部呼 `reconciliation_exception_service.detect_exception` 由 service 端冪等保證（同 tenant+recon+kind+description 仍 active 不重複）/ (6) `db_unavailable` 直接 return skipped 不炸。**main.py lifespan** 加 `recon_exc_detector.start()` 對齊 sla_monitor/inventory_monitor/line_push_worker 4-monitor 啟動序。**新 `tests/test_cr_0018_state_machine.py`** 15 測試：(a) `_VALID_KIND` / `_VALID_FIX_PATH` / `_VALID_DETECTED_BY` 3 enum 完整性對齊 SQL CHECK / (b) `_ALLOWED_TRANSITIONS` 6 態完整性 + closed 終態 + happy path 5 跳完整 + 誤報路徑 (detected/ops_review → closed) + 退件路徑 (fix_proposed → ops_review) / (c) `_check_transition` 接受 valid / 拒 invalid 拋 STATE_CONFLICT 409 / 拒 closed 出邊 / 拒 unknown source / (d) `_coerce_decimal` None/int/負 float 三情境。**新 `tests/test_cr_0018_router_endpoints.py`** 9 結構 smoke tests（不依賴 POSTGRES_URI / lifespan，純驗 router 結構與 body schema）：(a) 8 endpoints 全註冊 + operation_id 全唯一 + 預期 8 operation_id set 對齊 / (b) 所有路徑 `/tenants/{tenantId}/accounting/...` 對齊 ADR-0030 tenant scoping / (c) router_mod 引用 svc + voucher_void_service.void_voucher（連動點存在） / (d) ProposeFixBody/DetectBody required field validation；ApplyFixBody void_reason 預設 'error_correction'；DetectBody.detected_by 預設 'manual'。**新 `tests/test_cr_0018_detector.py`** 6 測試：(a) `run_once` db_unavailable 回 `{'skipped':'db_unavailable'}` / (b) 三 detector 各回 N → summary 加總 + errors=0 / (c) 一 detector 拋例外 → 其他兩個照跑、errors=1 / (d) `_detect_amount_mismatch` mock SQL row 驗證 service.detect_exception 被呼帶正確 delta (-50.0 / 200.0) + kind='amount_mismatch' + detected_by='cron_daily' / (e) 預設 interval = 86400 (24hr) 對齊 HD-5 (b) / (f) start/stop 冪等。**測試結果**：30 passed in 0.18s（state_machine 15 + router smoke 9 + detector 6）。**設計取捨**：(a) router HTTP integration 走 conftest 既有 client fixture 需 POSTGRES_URI — 不在 Stage 3 重複那層，留給 dev/CI 同 reconciliations_v2 pattern / (b) router 端 voucher_void 連動的測試靠 Stage 2 CHANGELOG + router smoke 確認連動點存在 + 整合測試 dev 跑 / (c) cron worker 與 line_push_outbox_worker 共享 lifespan pattern — 啟動序四 monitor 排隊 inventory→sla→line_push→recon_exc / (d) detector interval ENV-driven — 短時 debug 跑 60s 也方便 / (e) detect SQL 只取 status='approved' avoid 干擾 in_review pipeline。**CR-0018 BUILD 全部完成**：Stage 1 schema + service ✅ + Stage 2 router + voucher_void hook ✅ + Stage 3 cron detector + 30 tests ✅。**Flow 13 EX5 從 50% → 100%** — 對帳異常完整 detect / dual-sign / 三路徑修正 / voucher_reverse 連動 / cron 兜底全鏈路落地。**剩 manual：** dev 環境跑 migration 017；upload-time hook 等真正 reconciliation upload service 落地時補（reconciliation_v2_service 目前無 upload endpoint，cron 為主路徑）。

- **CR-0018 Stage 2 BUILD — router 8 endpoints + voucher_void 連動**（branch `feat/cr-0018-stage2-router-voucher-hook`，2026-06-05）：承接 Stage 1 (`868d96af`) schema + service。**新 `api/routers/reconciliation_exceptions_v2.py`** 8 endpoints（tenant-scoped）：(1) `POST :detect` 偵測寫入新例外（status='detected'）+ 冪等，201；body 含 `reconciliation_id / exception_kind / description / detected_by / amount_delta`，給 cron / manual / upload-time 三來源共用 / (2) `GET list` 含 status + reconciliation_id filter + cursor + limit 1-200 / (3) `GET {excId}` 單筆 404 / (4) `POST :advance` detected → ops_review / (5) `POST :propose-fix` step-1 CSM body 含 `fix_path + resolution_note`；走 service propose_fix / (6) `POST :approve-fix` step-2 ops_manager；service 層 SoD CHECK 403（approver ≠ proposer）/ (7) `POST :apply-fix` 套用修正 — **`voucher_reverse` 路徑連動**：router 端先呼 `voucher_void_service.void_voucher(voucher_id, reason='error_correction', comment='EX5 exception {id} fix', keeper_user_id=initiator)` 拿 reverse voucher id → 傳給 `svc.apply_fix(applied_voucher_id=...)`；`invoice_supplement` 路徑要 body 帶 `invoice_id`；`recon_void` 路徑 router 不額外動作（service 層只標 applied）/ (8) `POST :close` 多前態 → closed。**Pattern 對齊 reconciliations_v2**：所有 write endpoint 強制 `X-Initiator` header + `idempotency_guard` 支援 Idempotency-Key + cross-tenant guard `CROSS_TENANT_READ/WRITE` 403（ADR-0030）+ pydantic body schema 嚴格驗證 + service action 走 ApiError 結構化錯誤碼。**main.py 掛 router**：`app.include_router(recon_exceptions_v2_router.router, tags=["M12 Reconciliation Exception"])`。**設計取捨**：(a) `:detect` endpoint 設計為 internal-by-convention — 預設 detected_by 接受 'manual' 給 ops；upload service 與 cron job 直接呼 svc.detect_exception 不必透過 HTTP / (b) voucher_void 連動放 router 端非 service — service 單一責任原則，router 編排 cross-service flow / (c) void_voucher comment 預設帶 EX5 exception id — voucher_void audit chain 可反查源頭 / (d) recon_void 路徑不動 reconciliation status — 該動作由 ops 在 reconciliation_v2 端另外標 disputed（避免本服務跨表寫入耦合）。**驗證**：router + main 雙 ast.parse OK。**CR-0018 BUILD 進度**：Stage 1 ✅ + 2 ✅；剩 Stage 3（upload-time 偵測 hook 等真實 reconciliation upload service 落地時補；cron daily job；e2e test）。

- **CR-0018 Stage 1 BUILD — schema + service 基礎落地**（branch `feat/cr-0018-stage1-schema-service`，2026-06-05）：依批次決策 HD-1=(a) / HD-2=(b) / HD-3=(c) / HD-4=(a) / HD-5=(c) 推薦立場執行。**新 migration `SQL/migrations/017-reconciliation-exceptions.sql`**：`saas.reconciliation_exception` 表（HD-1=a 獨立不污染 disputes）含 (1) 5 種 exception_kind: `amount_mismatch / missing_invoice / duplicate_entry / orphan_settlement / other` / (2) 6 態狀態機 `detected → ops_review → fix_proposed → fix_approved → applied → closed`（HD-2=b 對齊金融行業 reconciliation 慣例 + audit 友善） / (3) 3 fix_path `invoice_supplement / recon_void / voucher_reverse`（HD-3=c 衝銷預留連動 voucher_void） / (4) 雙簽 `proposed_by/approved_by + CHECK 相異`（HD-4=a 對齊 saas.reconciliation 既有 SoD pattern） / (5) `detected_by` enum `upload_realtime / cron_daily / manual`（HD-5=c 雙保險） / (6) 連動欄位 `applied_voucher_id / applied_invoice_id` + `resolution_note` / (7) 3 indexes：`tenant_status_created` 列表用 + `recon_id` JOIN 用 + `open_detected partial WHERE status NOT IN ('closed')` ops dashboard 用。**MIGRATION_REGISTRY.md** append row 017。**新 `api/services/reconciliation_exception_service.py`** 320+ LOC：(a) `detect_exception` 偵測入口含冪等檢查（相同 tenant+recon+kind+description 仍 active 不重複寫入）+ amount_mismatch 必填 amount_delta 驗證 / (b) `list_exceptions` cursor + status + reconciliation_id filter + limit 1-200 / (c) `get_exception` 404 NOT_FOUND / (d) `advance_to_review` detected → ops_review / (e) `propose_fix` step-1 CSM detected/ops_review → fix_proposed + fix_path + resolution_note / (f) `approve_fix` step-2 ops_manager fix_proposed → fix_approved + SoD CHECK（approver ≠ proposer 否則 403 SOD_VIOLATION）+ fix_path 必填驗證 / (g) `apply_fix` fix_approved → applied + 路徑必填驗證（invoice_supplement → applied_invoice_id；voucher_reverse → applied_voucher_id；recon_void 兩者皆可省）/ (h) `close_exception` applied / detected / ops_review / fix_proposed → closed（誤報路徑）。**狀態機**：`_ALLOWED_TRANSITIONS` dict 集中管理；`_check_transition` 驗證 + 409 STATE_CONFLICT；`_fetch_for_update` 取 row + 404；DB 層 CAS 用 WHERE status = X 條件 update 防並發。**設計取捨**：(a) 路徑 3 voucher_reverse 連動由 Stage 2 router 端先呼 voucher_void_service 再呼 apply_fix 帶回 voucher_id — service 層保持單一責任 / (b) 偵測冪等以 description 為去重 key — 避免 cron 與 upload_realtime 重複寫同類異常 / (c) recon_void 路徑不必填 applied_*_id — 該路徑只是標記註銷無須產生新文件 / (d) close 從多種前態都可進 — ops 標誤報是合理流程 / (e) AST validate OK。**CR-0018 BUILD 進度**：Stage 1 ✅；剩 Stage 2 (router 2 endpoint + upload-time 偵測 hook + voucher_reverse 連動) + Stage 3 (cron daily 偵測 + e2e test)。

- **CR-0017 Stage 5 BUILD — e2e/unit tests 31 passed**（branch `feat/cr-0017-stage5-e2e-test`，2026-06-05）：承接 Stage 3+4 (`59c14d34`) 完整鏈路後落 3 個測試檔覆蓋核心邏輯（DB 與 LINE API 全 mock，免依賴環境）。**新 `tests/test_cr_0017_flex_builders.py`** 11 測試：(1) reschedule carousel 結構（bubble 數 = slot+1、postback data 短碼 `r:c|<id>|<idx>`/`r:r|<id>`） / (2) 業務上限 3 個 slot 截斷 / (3) 空 slots fallback text / (4) scope_change bubble 含 accept+reject postback + URI fallback / (5) 無 token 省略 URI button / (6) 缺 sc_id fallback text / (7) schedule_conflict admin bubble 紅色（#DC3545）+ URI only 無 postback / (8) > 5 個衝突 truncate 顯示「...等 N 筆」 / (9) build_messages dispatch 正確 / (10) unknown kind 回 None / (11) builder 內 KeyError 吞掉回 None。**新 `tests/test_cr_0017_line_webhook.py`** 14 測試（含 4 HTTP 端到端）：(1) _verify_signature 正/錯/缺 header / (2) LINE_CHANNEL_SECRET 缺時 dev fallback 放行 / (3) 4 種 postback 短碼正確分派（monkeypatch service 函式驗證 call args） / (4) 未知短碼不 crash / (5) service 拋例外被吞（冪等由 CAS 保證） / (6) HTTP POST 401 on bad sig / (7) HTTP POST 200 + processed=0 on empty events / (8) HTTP POST 200 + dispatch postback / (9) 400 on invalid JSON。**新 `tests/test_cr_0017_outbox_worker.py`** 6 測試：(1) `_push_to_line` missing ACCESS_TOKEN → False / (2) text message 路徑（用 `unittest.mock.patch.dict('sys.modules', ...)` 注入 fake `linebot.v3.messaging`，驗 push_message call） / (3) flex message 路徑（驗 FlexContainer.from_dict 被呼） / (4) ApiException → False+err 含 status / (5) `_mark_failed` 達 max_attempts 走 _mark_dead（不寫 backoff UPDATE） / (6) `_mark_failed` 未達 max → 寫 new_attempts + 排 next_attempt_at / (7) backoff schedule [30, 120, 480, 1800, 7200] 符合設計。**測試結果**：31 passed in ~1s（builders 0.02s + webhook 0.92s + worker 0.07s）。**設計取捨**：(a) 全 unit-level 不依賴真實 DB — `db_module._conn` 被 monkey-patch 成 FakeConn / (b) LINE SDK 全 mock via `patch.dict('sys.modules')` — 不需 LINE_CHANNEL_ACCESS_TOKEN 也能跑 / (c) webhook HTTP 測試走 `ASGITransport(app=app)` 不啟 uvicorn — 對齊現有 `conftest.py` pattern / (d) postback dispatch 用 monkeypatch service 函式取代 — 純驗證 router 邏輯不污染 service 副作用 / (e) 沒寫 worker `_poll_once` 全鏈路測試 — 需 DB-level fixture 太重；transactional logic 被 unit 拆分覆蓋已足 / (f) 沒寫 `confirm_reschedule_by_proposal` integration 測 — 需 reschedule_proposal seed data，留到 Stage 5 plus 或 manual QA。**CR-0017 BUILD 完整進度**：Stage 1.1 ✅ + 1.2 ✅ + 2 ✅ + 3 ✅ + 4 ✅ + 5 ✅。**CR-0017 完整 BUILD 結束** — 剩 manual smoke test (真實 LINE bot + webhook tunnel)。

- **CR-0017 Stage 3 + Stage 4 BUILD — real Flex + postback router 鏈路完成**（branch `feat/cr-0017-stage3-4-real-flex-postback`，2026-06-05）：承接 Stage 2 (`53d582d2`) placeholder。Stage 3 把 3 個 builder 升 real `FlexMessage`：(1) `render_reschedule_proposal` 改回 `carousel` of 1~3 slot bubbles（每 bubble 1 個 `postback` button `r:c|<proposal_id>|<idx>` + 末 bubble 都不方便 `r:r|<proposal_id>`）；(2) `render_scope_change_proposal` 改回單 bubble 含 reason / items 列 / 預估金額 + 兩個 postback (`s:a|<sc_id>` / `s:r|<sc_id>`) + URI button 跳 `${WEB_BASE_URL}/track/scope/<token>` fallback；(3) `render_schedule_conflict` 改 admin 紅色 bubble 含衝突 wo 列 + URI button 跳 `${WEB_BASE_URL}/admin/dispatch`（admin-only 無 postback）。**短碼設計**：postback data 用 `"r:c|<uuid>|<n>"` / `"r:r|<uuid>"` / `"s:a|<uuid>"` / `"s:r|<uuid>"`，避開 LINE 300 字限。**Worker 升級**：`_push_to_line` 加 `FlexMessage` 分支，messages dict `type='flex'` → `FlexMessage(alt_text=..., contents=FlexContainer.from_dict(...))`；fallback `type='text'` 保留。**Stage 4 新 `api/routers/line_webhook.py`**：(a) POST `/api/v1/line/webhook` 入口 / (b) `_verify_signature` 用 `LINE_CHANNEL_SECRET` HMAC-SHA256 base64 比對 x-line-signature header（secret 缺時 dev 模式略過） / (c) 解析 events list 依 `type=='postback'` 分派 / (d) `_handle_postback` 解析短碼 `data.split('|')` 路由：`r:c` → `work_order_service.confirm_reschedule_by_proposal(proposal_id, slot_idx)`；`r:r` → `reject_reschedule_by_proposal`；`s:a` → `scope_change_service.respond_public(decision='accept')`；`s:r` → `respond_public(decision='reject')` / (e) handler 全包 try/except — 失敗回 200 不阻斷其他 events（重複 postback 由 service CAS 保證冪等）。**新 service wrapper** (append work_order_service.py)：`confirm_reschedule_by_proposal` SELECT proposed_slots/work_order_id/tenant_id/status 反查 → 驗 slot_idx → 呼 `confirm_reschedule_by_customer` 寫 work_orders → CAS UPDATE `saas.reschedule_proposal` SET `status='customer_confirmed'` + `chosen_slot_index` + `customer_responded_at`；`reject_reschedule_by_proposal` 類同但寫 `customer_rejected`。**main.py 掛 router** `app.include_router(line_webhook_router.router, prefix='/api/v1', tags=['LINE Webhook'])`。**設計取捨**：(a) 短碼節省 LINE 300 字限 — UUID 36 + slot_idx 1 + 分隔 4 < 50 字 / (b) reject 仍走 status='customer_rejected' (proposal-level) + WS event (wo-level) 兩寫 — frontend 監聽 wo-level 即時更新 / (c) signature 缺 secret 略過 — dev 環境免設 secret 可手動 curl 測；prod 必設不然 401 全擋 / (d) consumer (line postback) 與 admin (web) 共用 service — `respond_public` 接 LINE 也接 web，CAS 同一 row / (e) carousel size='kilo' 寬度適合手機；header 用 #1DB446 LINE 綠色一致品牌 / (f) scope_change 改 web URI fallback — 若客戶看不到 Flex 仍可走 token 連結 / (g) schedule_conflict 維持 admin only — 業務面向技師 + ops manager，不發 customer。**驗證**：5 檔 python3 ast.parse OK（builders / worker / work_order_service / line_webhook / main）。無 schema 變更。**CR-0017 BUILD 進度**：Stage 1.1 ✅ + 1.2 ✅ + 2 ✅ + 3 ✅ + 4 ✅。**剩 Stage 5 e2e test**（mock LINE API、driver 寫 outbox → worker 拉 → push mock 收 / postback callback → service CAS 驗證）。

- **CR-0017 Stage 2 + Stage 3 placeholder BUILD — outbox worker + flex builders 鏈路打通**（branch `feat/cr-0017-stage2-worker`，2026-06-05）：承接 Stage 1.1 + 1.2，落 worker + dispatch + placeholder flex builders 三件套，達成端到端可跑（push 真實 LINE，雖然 Stage 2 用 TextMessage placeholder）。**新 `api/realtime/line_push_outbox_worker.py`** LinePushOutboxWorker class（仿 SLAMonitor pattern）：(1) `start/stop` lifespan 控制 + asyncio task / (2) `_run` 每 N 秒迴圈 poll 直到 `_stopping.set()` / (3) `_poll_once` SELECT ... WHERE status='pending' AND next_attempt_at<=NOW() FOR UPDATE SKIP LOCKED LIMIT BATCH_SIZE（multi-worker 安全）/ (4) `_process_row` 對每筆：resolve LINE userId（target_line_id 優先，缺失則用 reference_table+reference_id 從 work_orders/scope_changes/saas.reschedule_proposal → problem_cards → conversations → users.line_user_id 反查）→ build_messages dispatch → push LINE → mark_sent/failed/dead / (5) `_push_to_line` 用 linebot.v3.messaging AsyncMessagingApi + PushMessageRequest / (6) Exponential backoff `_BACKOFF_SECONDS_BY_ATTEMPT=[30, 120, 480, 1800, 7200]`（30s/2min/8min/30min/2hr）/ (7) max_attempts (預設 5) 後 status='dead'。**新 `api/templates/line_flex/`** 模組：(a) `__init__.py` re-export BUILDERS dispatch table + 3 個 render function / (b) `builders.py` 3 個 Stage 2 placeholder builder：`render_reschedule_proposal` (列改期時段選擇文字提示) / `render_scope_change_proposal` (列變更原因+items+估價+token 連結) / `render_schedule_conflict` (admin 警示+衝突 wo 列表)。Stage 3 升級成 real FlexMessage carousel/bubble + postback buttons 時只需替換 builder 內容，worker 不變。**新 dispatch entry** `build_messages(push_kind, payload)` 給 worker 呼叫，未知 kind 回 None worker 標 'dead'。**main.py lifespan 整合**：startup `line_push_worker.start()` / shutdown `await line_push_worker.stop()`，對齊 sla_monitor + inventory_monitor pattern。**設計取捨**：(a) FOR UPDATE SKIP LOCKED — future cluster deploy 多 worker 不會搶同 row；(b) reference 反查 fallback — outbox 可不帶 target_line_id 由 worker 動態解析（service 端寫 outbox 時不需查 LINE userId）；(c) Stage 2 用 TextMessage placeholder — 鏈路可端到端驗證，Stage 3 換 Flex 為 mechanical change；(d) backoff 5 階段對應 max_attempts=5，最後一次失敗自動進 dead；(e) 環境變數 LINE_PUSH_WORKER_INTERVAL (預設 10s) + LINE_PUSH_WORKER_BATCH (預設 20) 可調；(f) 取舊 line_push_service retry pattern 借用（_RETRYABLE_STATUSES / _BACKOFF）— 但本 worker 採 outbox-level backoff 而非 in-call backoff（更可恢復 / cluster-safe）。**驗證**：4 檔 python3 ast.parse OK；無 schema 變更（schema 已落 Stage 1.1）。**CR-0017 BUILD 進度**：Stage 1.1 ✅ + 1.2 ✅ + 2 ✅ + 3 placeholder ✅。**剩 Stage 3 升級為 real Flex carousel + Stage 4 postback router (customer-confirm/reject from LINE button) + Stage 5 e2e test**。

- **CR-0017 Stage 1.2 BUILD — 3 個 service caller 整合 outbox enqueue**（branch `feat/cr-0017-stage1.2-caller-integration`，2026-06-05）：承接 Stage 1.1 (`3684540a`)，整合 `propose_reschedule_v2` (Flow 11, send_via='line' 才 enqueue) + `record_scope_change` (Flow 3, payload 含 public_token) + `_detect_schedule_conflict_and_publish` (Flow 14)。全 best-effort try/except + 延遲 import 避 circular。**CR-0017 BUILD 進度**：Stage 1.1 ✅ + 1.2 ✅。**Out of Scope**：Stage 2 worker / Stage 3 flex builders / Stage 4 postback / Stage 5 e2e。

- **CR-0017 Stage 1.1 BUILD — line_push_outbox schema + service write API**（branch `feat/cr-0017-stage1-outbox-schema`，2026-06-05）：CR-0017 業主裁決後 BUILD P1 第一步 — 落地 outbox 表 schema + write API（HD-2 Outbox+worker pattern）。**新表 `line_push_outbox`** (SQL/Schema_v2_extensions.sql §12): id / tenant_id / push_kind (`reschedule_proposal/scope_change_proposal/schedule_conflict`) / target_line_id / reference_id / reference_table / payload JSONB / status (`pending|sent|failed|dead`) / attempts / max_attempts (預設5) / next_attempt_at / last_error / sent_at + 3 indexes (partial pending poll / tenant+kind monitor / reference 反查)。**新 service** `api/services/line_push_outbox_service.py`: `enqueue / get / count_by_status`。**設計取捨**: service 不做 push（Stage 1.1 純 write）；partial index 只索引 pending 減少累積影響；push_kind VARCHAR + Literal 型別 dispatch（避免 enum migration 限制）。**Out of Scope（後續）**: Stage 1.2 caller 整合 / Stage 2 worker+retry / Stage 3 flex builders / Stage 4 postback router / Stage 5 e2e。

- **Flow 14 schedule_conflict 偵測 component test**（branch `test/e2e-smoke-flow14-conflict`，2026-06-05）：5 個 component pytest。
- **Flow 14 排班衝突 → WS publish → 85%**（branch `feat/flow14-conflict-publish`，2026-06-05）。
- **Flow 12-14 deep audit 校正**（branch `docs/flow12-14-deep-audit`，2026-06-05）：Flow 12 → 0% / Flow 13 → 50% / Flow 14 → 70%。
- **Flow 4 補料 e2e 收尾 → 100%**（branch `feat/flow4-supply-arrived`，2026-06-05）。
- **Flow 4 admin 補料管理彙整 UI**（branch `feat/admin-material-requests-page`，2026-06-05）。
- **Flow 4 admin 補料管理彙整 list endpoint**（branch `feat/admin-material-requests-list-endpoint`，2026-06-05）。
- **Flow 8 二次派工 admin 前端整合 → 100%**（branch `feat/flow8-reassign-frontend`，2026-06-05）。
- **Flow 8 二次派工 reassign backend → 85%**（branch `feat/flow8-followup-dispatch`，2026-06-05）。
- **Flow 9 客訴升級 admin 前端按鈕 → 100%**（branch `feat/flow9-escalate-frontend`，2026-06-05）✅。
- **Flow 3 範圍變更 proposal INSERT + token mint → 85%**（branch `feat/flow3-scope-change-proposal`，2026-06-05）。
- **Flow 3 admin override → 90%**（branch `feat/flow3-consumer-respond`，2026-06-05）。
- **Flow 11 客戶不在場 deep audit 100% → 80%**（branch `docs/flow11-agent-rewrite-audit`，2026-06-05）：LINE Flex push 鏈路 0% (agent 重寫刪 Flex template)。
- **CR-0017 opened — LINE Flex push 重建 CIA**（branch `docs/cr-0017-line-flex-push-rebuild-cia`，2026-06-05）：5 HD 待業主裁決，影響 Flow 3/11/14。
- **ADR-0107 opened — LockCore supersede product_info trio**（branch `docs/adr-supersede-product-info-trio`，2026-06-05）。
- **CLAUDE.md ADR-0107 follow-up**（branch `docs/adr-index-and-claude-md-followup`，2026-06-05）：移除 CLAUDE.md `Architecture Lock` 表格末尾「待補 superseded 標記」備註，補 ADR-0107 entry。
- **CR-0018 opened — Flow 13 帳款異常 EX5 CIA**（branch `docs/cr-0018-flow13-ex5-reconciliation-exception-cia`，2026-06-05）：5 HD 待業主裁決。
- **ADR INDEX.md catch-up ADR-0100~0107 + special variants**（branch `docs/adr-index-catchup-0100-0107`，2026-06-05）：新增 Group 5 (ADR-0100~0107) + Group 6 (VCH/PIVOT/PII variants)。
- **`/realtime/pool/{tech_id}` publish 補完**（branch `feat/realtime-pool-publish-on-assign`，2026-06-05）：assign_order 後 publish `work_order.assigned_to_you` 給技師個人 pool channel。WBS §3 pool ⏳→✅。
- **commands.md / architecture.md lockcore catch-up**（branch `docs/claude-commands-architecture-lockcore-sync`，2026-06-05）：commands.md Quality testing 段標廢棄 + pytest 替代；architecture.md banner + Major Modules 重寫；CLAUDE.md §細節路標移除「待同步」備註。
- **Flow 4 admin material-requests E2E smoke spec**（branch `test/e2e-smoke-flow4-flow9`，2026-06-05）：4 個 test。
- **Flow 9 sentiment-alerts escalate E2E smoke spec**（branch `test/e2e-smoke-flow8-flow9-escalate`，2026-06-05）：3 個 test（渲染 / 升級流程 / error）。
- **Flow 8 work-orders :reassign endpoint routing E2E smoke spec**（branch `test/e2e-smoke-flow8-reassign`，2026-06-05）：2 個 test。
- **WBS 1.2.7.3.2 / 3 / 4 取證 audit**（branch `docs/wbs-1.2.7.3-audit-remaining`，2026-06-05）：1.2.7.3.2 → 30% / 1.2.7.3.3 → 0% (建議 CR-0019) / 1.2.7.3.4 → 50%；1.2.7.3 整合測試段平均 ~52%。
- **CR-0019 opened — 100 人技師併發壓測 CIA**（branch `docs/cr-0019-load-testing-cia`，2026-06-05）：5 HD 待業主裁決（工具/SLA/模擬深度/環境/CI整合）。預估 BUILD 5-7 day。

- **Agent 核心架構重寫 → LockCore + Agent Skills 標準**（branch `feat/agent-update`，2026-06-04）⭐⭐⭐ **重大架構決策**：捨棄舊架構（ReAct + LangGraph、自製 skill loader、product_info mega-doc、Belief-Augmented ReAct (Turn Cycle)、quality_check LLM-as-Judge），改為：
  * **核心引擎**：`agent/lockcore/`（fork 自上游 `HKUDS/nanobot` 的最小核心套件，VENDOR.md 記載 fork 來源）
  * **知識 & SOP**：`lockcore/skills/{locksmith-product-knowledge,locksmith-cs-sop}/SKILL.md + references/`（**Agent Skills 標準** agentskills.io / Claude Skills，frontmatter 不綁框架專屬欄位，可攜性：可複製到 Claude Code / Cursor / nanobot / hermes 直接使用）
  * **LLM 供應商**：單一 `LiteLLMProvider`，多家用 model 字串路由（`gemini/` / `vertex_ai/` / `ollama_chat/` / `claude-*` / `gpt-4o`）
  * **Per-user 記憶**：`lockcore/agent/user_memory/`（移植自 Hermes，`tenant + user_id` 為 key，BUILD/SAVE 接 turn 狀態機；SQLite memory.db 為預設）
  * **工具白名單**：`lockcore/app_config.py:CS_TOOL_ALLOWLIST` = `{read_file, list_dir, find_files, grep, web_search, transfer_to_human}`（客服 only，砍 write/exec/shell/spawn/cron/message/web_fetch/image 等危險或無用工具）
  * **測試**：改 `pytest` in `agent/tests/`（13 個 unit/integration，含 `test_e2e_mock_turn.py` / `test_skills_loaded.py` / `test_tool_allowlist.py` / `test_litellm_provider.py` / `test_line_gateway.py`）；**舊 quality_check / belief_action_judge / replay_check / hypothesis_quality_baseline 全刪**
  * **影響檔案**：200+ 刪除（`app.py` / `agent.py` / `harness/` 26 檔 / `evals/` / `quality/` / `prompts/` / `notifications/` / `skills/tools.py` / `storage/` / `profiles/` / `llms/` / `embeddings/` / `memory/` / `integrations/` / `policy.py` / `calibrate.py` / `hypothesize.py` / `turn_cycle.py` / `belief*.py` / 45 個 product_info `.md` mega-doc）；178 新增（`lockcore/` 全結構）
  * **CLAUDE.md 同步重寫**：§🔒 Architecture Lock 整段重寫（product_info canonical → lockcore + Agent Skills），§🧪 Experimental Lock 刪除（Turn Cycle 全失效），最常用指令改 `pytest` / `real_turn_demo.py` / `line_gateway.py`，非預期工具鏈補 `pip install -e ".[dev|vertex|line]"` optional dependencies pattern
  * **`.gitignore` 補 `agent/memory.db`**（per-user SQLite runtime DB，重啟可重建，不入版控）
  * **後續 ADR 工作**：ADR-0008 (product-info-architecture-canonical) / ADR-0010 (belief-augmented-react) / ADR-0101 (product-info-extension-final-spec) 待補 `status: superseded_by: <新 ADR>` 標記（**OOSCope of 本 commit**，另開 ADR 化 governance CR）
  * **CIA 後補（gate bypass acknowledgment）**：本變更觸發 Architecture boundary（最高觸發面向），但 code 已由業主完成，屬 post-hoc documentation update；後續若有人質疑變更合法性可引用本 CHANGELOG entry + agent/README.md + lockcore/VENDOR.md
  * 詳見 [`agent/README.md`](agent/README.md) + [`agent/lockcore/VENDOR.md`](agent/lockcore/VENDOR.md)。

- **CR-0012 opened — FR-0012 技師月結撥款 CIA**（branch `docs/cr-0012-fr-0012-monthly-settlement-cia`，2026-06-04）：CR-0010 HD-03=a batch 第 2 件。FR-0012 為 V1.0 金流閉環另一半（CR-0011 客戶付平台 + CR-0012 平台付技師）。取證確認：`settlements_v2.trigger_monthly_settlement` 為 **501 stub**（Phase II 標記）、`settlement_service` 無 monthly trigger / compute_payouts / payout_via_bank、BR-M12-NN 5 條未編號、ADR-0041 (travel fee 80/20) accepted 但 split 邏輯未進計算公式。CIA 列 **6 HD** 待業主裁決：(1) Bank payout provider（台銀 / 第三方 AP / manual CSV / 階段化）、(2) Cron 排程實作（APScheduler / Cloud Scheduler / pg_cron / GitHub Actions）、(3) Bank 3-retry 策略、(4) manual_payout 完結機制、(5) Dispute 排除實作（即時查 / flag-driven / 雙保險）、(6) Escrow 模型（**鏡像 CR-0011 HD-08，須同步裁決避免金流方向矛盾**）。建議 HD-01=(c) manual CSV 階段化 → 解 vendor 契約 review 拖延風險。status: `open-awaiting-decisions`。詳見 [`docs/_audit/CR-0012-fr-0012-monthly-settlement-cia.md`](docs/_audit/CR-0012-fr-0012-monthly-settlement-cia.md)。
- **CR-0011 opened — FR-0011 消費者付款 CIA**（branch `docs/cr-0011-fr-0011-consumer-payment-cia`，2026-06-04）：執行 CR-0010 HD-03=a「同步開 CR-0011~0014 審查其他 4 draft FR」首件。FR-0011 為 4 剩餘 draft FR 中**最大金流風險**（V1.0 主流 spec、blocked_by Q7=B provider 選型、payments 表/endpoint 0 實作）。CIA 列 **8 HD** 待業主裁決：(1) provider 選型範圍 Line Pay only vs 雙軌/三軌、(2) ≥50000 強制簽章機制、(3) Line Pay fallback 觸發時機、(4) 現金 dispute 介面、(5) 7y voucher retention 實作、(6) webhook idempotency_key 設計、(7) provider 簽章驗證、(8) M11 即時付款 vs M12 月結金流關係。CIA 結構含 §1~12 + §A 取證附錄；implementation 切到後續 CR-0011-BUILD 不在本 CR scope。status: `open-awaiting-decisions`。詳見 [`docs/_audit/CR-0011-fr-0011-consumer-payment-cia.md`](docs/_audit/CR-0011-fr-0011-consumer-payment-cia.md)。
- **CR-0005 step 3/3 — KB :export caller 遷 v2 + :search caller flat-path 修正**（branch `feat/cr-0005-export-caller-v2`，2026-06-04）：
  * **:export caller**：`web/src/app/knowledge-base/cases/page.tsx` :export 從 v1 async-job（POST → KbExportJob → fetch download_url → JSONL blob）改為 v2 同步 CSV stream（`POST /kb/documents:export?doc_type=case&format=csv[&brand=X]` → blob → `kb-cases-{YYYY-MM-DD}.csv`）；UI scope dropdown 簡化為單一 button（HD-06=a MVP scope=case-only，manuals export 待 HD-04 ClamAV upload + manual_service search 補完另開）。
  * **:search caller flat-path 修正**：commit `87c6aa5d` 寫成 `tenantPath("/kb/documents:search")` → 解析為 `/tenants/{tid}/kb/documents:search`，但 kb_v2 router 全部 endpoint 為 flat（取證：`uv run python -c "from routers import kb_v2; [print(r.methods,r.path) for r in kb_v2.router.routes]"` 全部 `/kb/documents...`），且 main.py 無 tenant strip middleware → 該 caller 線上應為 404。本 commit 改回 flat `"/kb/documents:search"`，與 `tenantPath` docstring 對齊；順手移除已無用的 `tenantPath` import。
  * 同步清理失效 i18n keys（scopeAll / scopeAllHint / scopeManuals / scopeManualsHint / scopeCasesHint / exportLimitedToBrand / exportNoUrl）。
  * **真實 v1 caller 42 → 41**（:export 推進）。
- **CR-0013 opened — FR-0022 消費者端工單追蹤 CIA**（branch `docs/cr-0013-fr-0022-consumer-tracking-cia`，2026-06-04）：CR-0010 HD-03=a batch 第 3 件。**FR-0022 為「準完工 status flip 候選」**（類似 FR-0019 經 CR-0010 promote 模式）：取證確認 Web token 路徑已 100% 實作（`consumer_v2.py:80 GET /consumer/work-orders/{trackingToken}` + `web/track/[token]/page.tsx`）+ ADR-0015 (PM-Q3) accepted → `blocked_by: Q3=C` 為 stale；LINE rich menu 路徑 0%（grep richmenu / LINE_RICHMENU 全空，agent webhook 只有 F2 改期 postback 無「查進度」handler）。**取證捕獲 1 個 spec/code 衝突**：FR-0022 §1.2 A1「Web token mismatch → 401」vs `consumer_v2.py:63` 註解「失敗一律 404，不洩露原因」— **HD-05 強制裁決** 401（明確）vs 404（防 enumeration）vs 400（中庸），不允許腦補。CIA 列 **5 HD**：(1) LINE 入口策略（rich menu / ReAct 自然語言 / 雙路 / 純 Web）、(2) 多單顯示策略、(3) LINE binding 機制（自動 / 主動 / 雙路；HD-03=auto 涉 PDPA 同意）、(4) Web token TTL（24h / 7day / 30day / infinite）、(5) 401 vs 404 spec/code 衝突解。status: `open-awaiting-decisions`。詳見 [`docs/_audit/CR-0013-fr-0022-consumer-tracking-cia.md`](docs/_audit/CR-0013-fr-0022-consumer-tracking-cia.md)。
- **CR-0014 opened — FR-0034 AI Employee Charter / PRD 治理 CIA**（branch `docs/cr-0014-fr-0034-ai-employee-charter-cia`，2026-06-04）：CR-0010 HD-03=a「同步開 CR-0011~0014 審查其他 4 draft FR」**收尾件**。**性質與其他 3 件不同**：FR-0034 屬 Phase II 骨架 + Q2=C 延後正當狀態，**本 CR 結論可能為「維持 draft」而非推進**。取證確認：(1) ADR-0028 (AI Employee Charter) accepted，Charter rule body 95% 已存在；(2) BR-A12-01/02 active；BR-A12-NN placeholder；(3) Forbidden 清單 prompt-level 已落地（`agent/harness/safety_gate.py` + `agent/config.toml [output_validator] forbidden_phrases`）；(4) AI 永禁核准退款 ADR-0040 與 ADR-0028 對齊（CR-0009 refunds:agent-initiate single-actor 已落地）；(5) **Off-board Triggers (KPI<70% 連 2 週 → 48h hand-off) + Promotion 條件 (KPI 4 週全綠) 為 ADR-0028 implementation gap，純 ops 流程無 code 落地**；(6) M20 mapped_to 重疊 **8 FR**（FR-0017/0028/0029/0030/0034/0048/0050/0051）職責切分待 governance 統合。CIA 列 **4 HD**：(1) FR-0034 status 處理（推薦 (a) 維持 draft 加 acknowledged，尊重 Q2=C 延後）、(2) BR-A12-NN 編號（推薦 (a) BR-A12-03 引用 ADR-0028 避免雙 source of truth）、(3) ADR-0028 implementation gap 處理（推薦 (a)+(c) Phase II checklist + PROC runbook）、(4) 8 FR M20 重疊釐清（推薦 (a) 另開 governance CR）。**若業主依推薦立場全選，北極星 (1) 4 → 3 不會推進** — FR-0034 維持 draft 但 explicit acknowledged 為 reality-aligned；CR-0011/0012/0013 才是真實推進路徑。status: `open-awaiting-decisions`。詳見 [`docs/_audit/CR-0014-fr-0034-ai-employee-charter-cia.md`](docs/_audit/CR-0014-fr-0034-ai-employee-charter-cia.md)。
- **CR-0010 HD-03=a batch 收尾**（CR-0011/0012/0013/0014 共 4 件 CIA 全 opened，2026-06-04 同日）：共 23 HD 待業主裁（CR-0011: 8 / CR-0012: 6 / CR-0013: 5 / CR-0014: 4）；其中 CR-0011 HD-08 ↔ CR-0012 HD-06 為同步裁決對（escrow 模型）；CR-0013 HD-05 為 critical spec/code 衝突解（401 vs 404）；CR-0014 推薦立場為「維持 draft + acknowledged」不強推 active。北極星 (1) 潛在推進空間：4 → 1（CR-0011/0012/0013 全 promote 成功時）或 4 → 0（含 FR-0034 強推 HD-01=(b)/(c)）。
- **Flow 3 範圍變更取證收尾**（branch `docs/wbs-flow-3-取證`，2026-06-04）：取證確認 Flow 3 backend v2 + 前端 admin/consumer + scope_change_service 全綠，原 WBS「80% — 客戶核准流程簡化」vague 描述精化為兩個具體 gap：(1) **LINE Flex 主動通知客戶 gap**（scope_change_service 無 line_push_service 整合，對比 Flow 11 reschedule 已有 LINE Flex RSVP，客戶須主動開連結）；(2) **WS publish gap**（scope_change_service 無 realtime emit，admin 端工單頁無法即時看到客戶回覆狀態）。WBS Flow 3 行 80% → 90%。取證 grep 證實：3 v2 endpoints (`recordScopeChangeV2` / `getScopeChangeProposalV2` / `respondScopeChangeV2`)、2 web pages 使用 v2 path（`my-orders/[id]/scope-change/page.tsx:72 tenantPath` + `scope-change/[token]/page.tsx:90/159/173 ${API_BASE}/consumer/scope-changes`）、scope_change_service 完整 CRUD + audit + customer_decision 同步、`line_push_service.py` 與 `realtime/` grep `scope.change` 全空（兩 gap 真實）。
- **Flow 3 範圍變更 deep 取證校正 — 推翻同日淺取證 90% 過高估值，下修 65%**（branch `docs/flow-3-deep-取證-correction`，2026-06-04）：本 session 稍早淺取證只看 endpoint + UI 表面（commit `f53ffbdc`）即估 90%，現補深度 grep 確認 **`scope_changes` 表完全沒有 INSERT 路徑**（`grep -rn "INSERT INTO scope_changes" api/ agent/` 全空；`work_order_service.record_scope_change` 只寫 work_order_events 加 SCOPE_CHANGE tag，不寫 scope_changes 表；agent 端 0 整合）；`public_token.py` 支援 `purpose=scope_change` mint 但無 caller。結論：**consumer endpoint 雖 ready 但實質無 proposal row 可回應**，Flow 3 主流斷裂。3 個 critical gap：(1) proposal 建立路徑（admin/技師端 0%）、(2) token mint caller（mint infra 0% 用）、(3) LINE Flex + WS publish。**WBS 90% → 65%**。後續 BUILD 屬 CIA gate 範圍（新 contract POST .../scope-changes:propose + LINE Flex template + WS channel），工時估 2~3 day。本次取證為「**取證自我修正案例**」：警示淺取證易誤判，深度 grep INSERT 鏈路才能確認 service 真實鏈接性。
- **Flow 6 退款雙簽 deep audit — 確認 100% 真實 + 2 個 stale doc 修正**（branch `docs/flow-6-deep-audit`，2026-06-04）：複用 Flow 3 deep 取證方法論 (INSERT 鏈路 + state machine + WS publish 全鏈路 grep)，**確認 Flow 6 真實 100%**：refund_service.submit_decision 完整 dual-sign 狀態機 (pending → csm_approved → approved 兩段 + 同 user 不可雙簽 DUAL_SIGN_SAME_USER 409 + approval_chain JSONB audit) + WS publish /realtime/refunds + admin/refunds/page.tsx v2 tenantPath + agent 自動退款 CR-0009 ADR-0106 已遷 refunds:agent-initiate single-actor v2。**2 個 stale doc 修正**：(1) `refund_service.py:25-28` docstring 「本 phase 不實作多步雙簽流程」與實際 line 296+ code 矛盾，更新為「v1.29.0 已實作」對齊；(2) WBS 後端表 row 58 「agent 自動退款流暫續用 v1」stale，已被 CR-0009 ADR-0106 (2026-06-04) 修正，更新為「已於 CR-0009 遷 v2」。**Deep audit 方法論驗證**：本輪確認 Flow 6 與 Flow 3 不同（後者 90% → 65% 下修，前者 100% 確認），方法論可區分真假完成度。
- **Flow 5 延遲通知 deep audit — 確認 100% 真實，無 stale claim**（branch `docs/flow-5-deep-audit`，2026-06-04）：複用 Flow 3/6 deep 取證方法論，audit Flow 5 全鏈路（INSERT 鏈 + WS publish + LINE push + state machine + role guard）。**結果與 Flow 6 同類**（真實 100%，未發現 inflation），但與 Flow 6 不同的是 Flow 5 連 stale doc 都沒有（refund_service docstring stale 已於前一輪修）。`work_order_service.notify_delay:1553` 全 6 點鏈路驗證：(1) INSERT work_order_events delay event 完整；(2) UPDATE work_orders.updated_at；(3) `_audit_action('work_order.delay_notified')`；(4) `line_push_service.push_to_work_order_customer` 真實打 LINE Messaging API（含 retry+backoff+audit）；(5) `_publish_and_return` WS publish；(6) role guard（technician 限自己單）+ state machine guard（_SUBFLOW_FROM）。WBS Flow 5 row 補 6 點 audit evidence，取代原僅 LINE Push API 一點的淺取證註記。**Deep audit 累積進度**：Flow 3 (90% → 65% 下修) / Flow 5 (100% 確認) / Flow 6 (100% 確認 + 2 stale doc 修)；剩 Flow 7/10/11 待 audit。

- **CR-0009 + ADR-0106** ⭐⭐ — Agent caller migration P4-T1 **全鏈路完工**（2026-06-04 一日內）：4 個 agent v1 caller（app.py 2 + admin_api.py 2）全部遷 v2；新增 2 個 admin reschedule v2 endpoints + 1 個 refunds:agent-initiate single-actor endpoint；ADR-0106 記錄 LangGraph 特例不違背全面 SoD 原則。**agent v1 caller = 0**（解開 P4 cutover 唯一硬 gate per CR-0003 §3）。
- **CR-0005 / 0006 / 0009 §8 全裁完** ⭐⭐⭐（2026-06-04 業主三輪 AskUserQuestion 拍完 9 個剩餘 HD）：
  * CR-0005 HD-06 = (a) CSV 為主，JSON 可選（query format 切換）→ 6/6 HD 全裁
  * CR-0006 HD-02=a 軟刪 / HD-03=a 共用 kb_audit_log（doc_type='sop'）/ HD-04=a 廢棄舊 /sops/family-reviews / HD-05=a 即時查 SLA → 5/5 HD 全裁
  * CR-0009 HD-01=a `/consumer/work-orders/{token}/reschedule:{action}` / HD-03=a CR-0006 先拍板再做 / HD-04=a 無 canary / HD-05=a 任何 v1 404 即 PagerDuty → 5/5 HD 全裁
  * 累計 6 CR §8 = 26/26 HD 全裁完，CIA gate 全清；後續純實作 work
- **CR-0005 + ADR-0103** ⭐ — KB v2 expand 設計（2026-06-04 業主裁 §8 6/6 HD 全完）。Migration 015 落地（case_entries.deleted_at + manuals.deleted_at + saas.kb_audit_log + 4 indexes 含 90d hot partial）。Step 2/3：PUT + DELETE + :search endpoints 落地。Step 3/3：2 個 DELETE web caller 遷完；剩 GET/PUT/POST/search UI shape 改造 + :upload (ClamAV) + :export 待後續 commit。詳見 [`docs/architecture/adr/ADR-0103-kb-v2-expand-design.md`](docs/architecture/adr/ADR-0103-kb-v2-expand-design.md)。
- **CR-0007 + ADR-0105** ⭐⭐ — Door-check + Reschedule v2 contract **全鏈路落地**（schema + service + endpoints + web caller，2026-06-04 一日完工）。業主裁 §8 5 HD 全完，三 step：
  * step 1/3 schema：migration 014 `saas.reschedule_proposal` + ADR-0105
  * step 2/3 backend：`work_order_service.submit_door_check_v2`（arrival 前置 409 guard）+ `propose_reschedule_v2`（INSERT 獨立表）；`work_orders_v2:POST .../door-check` + `work_orders_ops_v2:POST .../reschedule:propose`
  * step 3/3 frontend：`my-orders/[id]/door-check/page.tsx` + `work-orders/[id]/page.tsx` 改 v2 tenantPath
  本 branch 真實 v1 caller 43 → 40（door-check + reschedule + settlements 三筆 -3）。詳見 [`docs/architecture/adr/ADR-0105-reschedule-doorcheck-v2-design.md`](docs/architecture/adr/ADR-0105-reschedule-doorcheck-v2-design.md)。
- **CR-0008** ⭐ — Settlements GET list v2 落地（2026-06-04 業主裁 HD-01=last_3_months / HD-02=period_end_desc）。`settlement_service.list_settlements` 擴 `period_filter` + `sort_by` 參數，v1 預設不變；`settlements_v2.py` 補 `GET /tenants/{tid}/settlements`；`web/accounting/page.tsx` settlement 列表遷 v2。CR-0008 §8 全裁，status: decided-and-implemented。
- **CR-0010** ⭐ — FR-0019 動態 RBAC 角色管理 `status: draft → active`（2026-06-04 業主裁 HD-01=a）。取證 content-complete + ADR-0042 accepted + code 全部實作（role_service publish + rbac_v2 endpoint + RbacChangedBanner mount）。Draft FR 5 → 4，北極星 (1) 真實推進。詳見 [`docs/_audit/CR-0010-fr-0019-promote-to-active.md`](docs/_audit/CR-0010-fr-0019-promote-to-active.md)。
- **跨 CR 部分裁決**（2026-06-04 同 session）：
  * CR-0005 HD-01 = (a) 保留 meta-wrapping（KB v2 響應 shape，連動 CR-0006 HD-01 = a）
  * CR-0007 HD-01 = (a) door-check 強制 arrival 前置（無 arrived_at → 409）
  * CR-0009 HD-02 = (a) 新增 v2 single-actor `refunds:agent-initiate`（保留 agent 自動退款；待 ADR-0106 記 LangGraph 特例）
  * CR-0010 HD-03 = (a) 同步開 CR-0011~0014 審查其他 4 draft FR
  CR-0005/0006/0007/0009 仍有其他 HD 未裁，實作仍卡。
- **ADR-0025** ⭐ — Harness 採 branching pipeline，PIPELINE list 為 introspection-only。Phase 4' hands-on 後從「linear PIPELINE + apply(ctx)」縮減為「結構化 PIPELINE 常數 + 各 layer module PHASE 常數」，零 runtime 變更不需 staging。詳見 [`docs/1-decisions/ADR-0025-harness-branching-pipeline.md`](docs/1-decisions/ADR-0025-harness-branching-pipeline.md)。
- **ADR-0024** — Tier 1 戰術級重構（2026 Q2）**hands-on 修正版**，supersedes ADR-0023。5 訊號處方修正、3 處事實錯誤修正、工期 2-3 週 → 1 週內。詳見 [`docs/1-decisions/ADR-0024-tier1-refactor-revised.md`](docs/1-decisions/ADR-0024-tier1-refactor-revised.md)。Phase 4' 實作見 §9 修正紀錄。
- **ADR-0023** — 已 **superseded by ADR-0024**。原文保留作為決策足跡；merge 後 30 分鐘 hands-on 階段發現 4/5 訊號處方不合理 + 3 處事實錯誤（ADR-0010 懸空、UF 系統未實作、`api/agent/integrations/` 空目錄）。

### Added

- `MISSION.md`（root）— Claude Code 持續迭代任務書（北極星 6 條完工條件 + 階段優先 + per-stage 驗收 + 紅線清單；含 Reality Check 與 session 進度日誌），供 `/goal @MISSION.md` 鎖 session 用
- `docs/architecture/adr/ADR-0106-agent-single-actor-refund-langgraph-exception.md` — agent 自動退款單簽特例正典（CR-0009 HD-02=a）
- `api/routers/work_orders_ops_v2.py` 新增 2 endpoints：customer-confirm + customer-reject（CR-0009 admin path；agent JWT 呼叫）
- `api/routers/refunds_v2.py:130+` 新增 POST `:agent-initiate`（HD-02 single-actor，role enforce agent|system）
- `agent/app.py:387` + `:407` customer-confirm/reject 改 v2 tenant path
- `agent/integrations/admin_api.py:282` refunds 改 `:agent-initiate`
- `agent/integrations/admin_api.py:356` sop-drafts 改 v2 tenant-scoped path
- `SQL/migrations/016-sop-v2-list-expand.sql` — sop_drafts.deleted_at + kb_audit_log doc_type CHECK 擴 'sop'
- `docs/architecture/adr/ADR-0104-sop-v2-list-design.md` — CR-0006 §8 5 HD 決策 ADR
- `api/routers/sops_v2.py` 新增 6 endpoints：GET list / GET single / POST create / DELETE soft / GET family-reviews list / GET family-reviews:pending（CR-0006 step 2/3 落地）
- `api/services/sop_draft_service.py:soft_delete_draft` 新增（HD-02 軟刪）
- `api/services/sop_draft_service.py:list_drafts / get_draft` 加 `deleted_at IS NULL` 過濾
- `web/src/lib/kb-adapter.ts:kbDocumentToSopDraft` 新增（meta-wrap → flat SopDraft）
- `web/src/app/knowledge-base/sop-drafts/[id]/page.tsx` GET + refresh GET 改 v2 + adapter（2 caller 遷完）
- `api/routers/kb_v2.py:660+` POST /kb/documents:export（HD-06=a CSV-first，?format=json 切換；MVP case only；EXPORT_MAX=10000）
- `web/src/app/knowledge-base/cases/page.tsx:161` search caller 從 v1 改打 v2 :search + kbDocumentToCaseEntry adapter
- `web/src/lib/kb-adapter.ts` 新增 — `KBDocument` interface + `kbDocumentToCaseEntry` adapter（CR-0005 step 3/3 解 meta-wrap shape 與 UI flat shape 不一致）
- `web/src/app/knowledge-base/cases/[id]/page.tsx` GET → v2 + adapter
- `web/src/app/knowledge-base/cases/[id]/edit/page.tsx` GET + PUT → v2 + adapter
- `web/src/app/knowledge-base/cases/new/page.tsx` POST → v2 + doc.id 導頁
- `api/routers/kb_v2.py:360+` 新增 CR-0005 step 2/3：
  * `_write_kb_audit_log` helper（best-effort 寫 saas.kb_audit_log，失敗 log warn 不阻擋）
  * `PUT /kb/documents/{docId}`（doc_type 自動 fallback / case 完整支援 / manual 暫 501 待 update_manual impl）
  * `DELETE /kb/documents/{docId}`（軟刪 case_entries SET is_active=FALSE + deleted_at=NOW；manuals SET deleted_at=NOW；before-snapshot 寫 audit；204）
- `api/services/manual_service.py:list_manuals` 加 `deleted_at IS NULL` 過濾（CR-0005 HD-02 軟刪兼容）
- `api/routers/kb_v2.py:get manual` 加 `deleted_at IS NULL` 過濾
- `SQL/migrations/015-kb-v2-expand.sql` — CR-0005 step 1/3：case_entries.deleted_at + manuals.deleted_at + saas.kb_audit_log（pending psql apply）
- `docs/architecture/adr/ADR-0103-kb-v2-expand-design.md` — CR-0005 §8 4/6 HD 決策 ADR
- `api/services/work_order_service.py` 新增：
  * `submit_door_check_v2`（HD-01 強制 arrival 前置：查 work_order_events arrival → 409）
  * `propose_reschedule_v2`（INSERT saas.reschedule_proposal；HD-02 slots 1-3 驗證 + send_via line/sms/email）
- `api/routers/work_orders_v2.py:560+` 新增 `POST /tenants/{tid}/work-orders/{id}/door-check`（CR-0007 / `_DoorCheckSubmitRequest`）
- `api/routers/work_orders_ops_v2.py:330+` 新增 `POST /tenants/{tid}/work-orders/{id}/reschedule:propose`（CR-0007 / `_ProposeRescheduleV2Body` + `_ProposedSlot`）
- `web/src/app/my-orders/[id]/door-check/page.tsx:161` v1 → v2 `tenantPath(/work-orders/{id}/door-check)`
- `web/src/app/work-orders/[id]/page.tsx:1076` v1 → v2 `tenantPath(/work-orders/{id}/reschedule:propose)`；移除 setOrder（v2 propose 不變更 scheduled_at，待 RSVP 後 confirm）
- `SQL/migrations/014-reschedule-proposals.sql` — saas.reschedule_proposal 表（CR-0007 落地步驟 1/3；pending psql apply）
- `docs/architecture/adr/ADR-0105-reschedule-doorcheck-v2-design.md` — CR-0007 §8 5 HD 決策正式落地 ADR
- `SQL/migrations/MIGRATION_REGISTRY.md`：014 row 加入（pending-apply 狀態標記）
- `api/routers/settlements_v2.py:75-130` 新增 `GET /tenants/{tid}/settlements` v2 endpoint（CR-0008 落地；預設 last_3_months + period_end desc）
- `api/services/settlement_service.py` `list_settlements` 擴 `period_filter` / `sort_by` 參數（v1 預設不變，向後相容）
- `docs/_audit/CR-0007-door-check-reschedule-contract.md` — door-check + reschedule contract 重設計 CIA（5 HD，HD-01 已裁）
- `docs/_audit/CR-0008-settlements-get-list-v2.md` — 最小 CIA（2 HD，本 session 全裁完並實作）
- `docs/_audit/CR-0009-agent-caller-migration-p4-t1.md` — agent caller P4-T1 CIA（5 HD，HD-02 已裁）
- `docs/_audit/CR-0010-fr-0019-promote-to-active.md` — FR-0019 promotion CIA（3 HD，HD-01/02/03 已裁並實作）
- `docs/_audit/CR-0005-kb-v2-expand-and-shape.md` — KB v2 expand CIA（PUT/DELETE/search/export/upload + 響應 shape 6 HD），解 9 個 v1 caller 遷移路徑
- `docs/_audit/CR-0006-sop-v2-list-expand.md` — SOP v2 list expand CIA（sop-drafts + family-reviews list/CRUD 5 HD），解 5 個 v1 caller
- `web/src/components/layout/AuthGuard.tsx`：mount RbacChangedBanner（之前定義未掛載），補齊 RBAC realtime 全鏈路
- `web/src/app/accounting/page.tsx:124` settlement list 從 `/api/v1/accounting/settlements?limit=50` 遷至 v2 `tenantPath("/settlements?limit=50")`（CR-0008 落地）
- `docs/4-exploration/WBS-0004-phase-5-flow-index-backlog-2026-q2.md` — Phase 5' Flow INDEX defer 紀錄 + T1-T4 啟動條件 / R1-R2 移除條件
- `docs/4-exploration/WBS-0003-phase-3.3-backlog-2026-q2.md` — Phase 3.3 backlog 推進紀錄（最終 16/18 page + hook 演化 5→8 features）
- `docs/1-decisions/ADR-0025-harness-branching-pipeline.md` — Phase 4' 修正版 ADR
- `docs/1-decisions/ADR-0024-tier1-refactor-revised.md` — 修正版 ADR（含 Phase 4' §9 修正紀錄）
- `docs/1-decisions/ADR-0023-tactical-refactor-2026-q2.md` — 初版 ADR（已 superseded）
- `docs/4-exploration/WBS-0002-2026-q2-tactical-refactor.md` — 對應 WBS v2.0（覆寫 v1.0）
- `CHANGELOG.md`（本檔）
- `agent/harness/__init__.py`：由空檔變為 PIPELINE 結構化常數 + PipelineEntry NamedTuple + module docstring（per ADR-0025）
- `agent/harness/{safety_gate,data_correction,quick_reply,intent_handler,pc_creator,profile_updater,validator_pipeline,memory_manager,agent_audit}.py`：各加 `PHASE: str` 模組層級常數（per ADR-0025）
- `web/src/hooks/`：新增目錄，含 5 個 hook（useRealtimeChannel, useSSEChannel, useBroadcast 自 lib/ 遷移；usePaginatedFetch 新增）+ README
- `docs/1-decisions/releases/`：83 個 v1.x.x.md release notes（自 root `report/` 遷移）
- `docs/_archive/legacy/web_design_spec_prompt_pipeline/`：legacy 設計系統 pipeline（自 root 遷移）

### Changed

- `web/src/app/admin/schedule-requests/page.tsx`：reject 從 `POST /api/v1/admin/schedule-requests/{id}/reject` 遷至 v2 `POST /tenants/{tid}/exceptions/{id}:approve`（body.decision="reject" 區分）；移除 P3-KEEP flat 註解（原註解「reject 無對應 v2 端點」與 exceptions_v2.py:33 不符，實為文件 stale）
- `web/src/components/admin/CustomerForm.tsx`、`web/src/app/admin/customers/[id]/edit/page.tsx`：docstring 同步至 v2 路徑（實際 caller 早已 v2，註解 stale）
- `web/docs/system-completion-status.md`：P3.5 Track-B caller 補遺改標 ✅ 100%（2026-06-04 取證收尾，原表述 stale）；總體 88% → 89%；架構遷移 85% → 88%；Caller 遷移 v1→v2 80% → 92%；§8 P0 移除已完成的 P3.5 條目、新增 P1「Reconciliation dual-sign UX rework」backlog
- `docs/_audit/CR-0003-full-cutover-wbs.md`：新增 §5 進度區（append-only），標記 P0/P1/P2/P3/P3.5 ✅、P4 ⏳
- `MISSION.md`：P3.5 驗收條件改標 ✅ 已收尾；迭代優先順序更新為「P4 Cutover 目前在這」+ 並行 backlog 區段
- `docs/1-decisions/ADR-0023-tactical-refactor-2026-q2.md`：frontmatter `status: superseded`、`superseded_by: [ADR-0024]`；補 §8 變更紀錄
- `docs/4-exploration/WBS-0002-2026-q2-tactical-refactor.md`：v1 → v2（5 Phase 範圍縮減 50%+；S5 改為 BACKLOG）
- `.gitignore`：新增 `api/data/`、`web/test-results/` 兩條（runtime 產物，含個資不入版控）
- `web/src/lib/api.ts`：檔頭註解路徑指向新位置 `web/types/api.generated.ts`（取代舊路徑 `docs/02-design/specs/generated/...`）

### Fixed

- **2026-06-11 E2E 揪出的 5 個產品 bug**（branch `test/ui-interaction-sweep`）：
  - `web/src/app/admin/refunds/page.tsx`：退款決策送出 `api.post(path, { body })` 多包一層 → decision/reason 不在頂層、後端 422，approve/reject/escalate 全失敗。改直傳 body。
  - `web/src/components/layout/AuthGuard.tsx`：`PUBLIC_PATHS` 漏 `/tech-login`（連 `/track`、`/scope-change` 客戶公開頁一起被踢去 /login）→ 技師永遠到不了登入頁。補公開頁清單 + 動態 token 頁 prefix。
  - `web/src/lib/api.ts`：`loginTechnician` WIP stub 打 admin `/api/v1/auth/login` 必 401；改打 `/api/v1/technicians/login`（後端早 ready）。
  - `web/src/app/admin/disputes/page.tsx`：`:co-sign` / `:review` 漏帶 v2 強制的 `X-Initiator` header → 422，co-sign UI 永遠失敗。補 X-Initiator（取 session.userId）。
  - `web/src/hooks/usePaginatedFetch.ts`：`onSuccess` 在 fetchPage deps 且 caller 傳 inline arrow → 無限 render 迴圈（Maximum update depth），凍結 /notifications。改 onSuccessRef 穩定身份。
- `scripts/seed/realistic_demo_seed.py`：
  - 發票號 `INV-YYYY-NNNNN` → 台灣電子發票格式 `^[A-Z]{2}\d{8}$`，修 `GET /accounting/invoices` 反序列化 500。
  - 爭議改寫入 `saas.dispute`（v2 前端實際讀的表）而非 legacy `public.disputes`，修 v2 清單永遠空；demo-tech 加跨狀態工單配額，技師端 my-orders 三 tab 不再 empty。
- `web/src/app/work-orders/page.tsx`、`web/src/app/admin/dispatch-queue/page.tsx`：捲動容器補 `min-h-0`，修 flex column 下 overflow-auto 失效（滾輪失效）。

### Added

- **5 角色帳號 + 前端 route 權限隔離（CR-0021 / ADR-0111）** — 2026-06-10 會議決議 #9 上線前必做。
  - 後端 `/auth/login` allowed_roles 放寬收後台角色（admin/reviewer/operations_manager/dispatcher/customer_service）;technician 維持 tech-login。
  - seed `SQL/seeds/rbac_role_users.sql` 補 ops/cs/reviewer 登入帳號（changeme123）。
  - 前端 `web/src/lib/rolePolicy.ts`（route→role 單一真相源）+ `AuthGuard` 認證後 route gate（無權限導 /dashboard）+ `Sidebar` 依角色過濾 nav。
  - 前端 gating 為 UX 層、以後端 `role_required` 為準;真正強制仍在 API。
  - 測試：pytest `test_login_roles`（6）+ Playwright `role-ui-isolation`（4 角色 route gate + sidebar 過濾）。
  - 修：A4 `test_admin_reset_password` 加密碼自我還原（原會污染 dispatcher 共用種子帳號）。
- **公單號改地區前綴 + per-region 流水（A8 / CR-0020 / ADR-0110）** — 2026-06-10 會議 Action #8 + Sunny 裁決。
  - 格式 `{2碼地區}-{6碼流水}`（如 `TP-000001`），工單建立時依 `customer_address` 縣市發號。
  - `SQL/migrations/031`：`wo_region_counter` 表 + `wo_region_code(addr)` + `generate_wo_number(addr)`（per-region 原子遞增）。
  - 接進 `work_order_service` 建立 INSERT + realistic_demo_seed;前端 work-orders 列表/詳情顯示公單號（並順手移除列表 `title={order.id}` 內部 UUID 洩漏，同 A6）。
  - 地區前綴僅用工單;ST/RM/WC/SOP 維持類型前綴 `generate_doc_number`。pytest 3 條（格式/per-region 遞增/fallback ZZ）。
- **忘記密碼（管理員代為重設，A4）** — 2026-06-10 會議 Action #7 / 決議 #9 上線前必做。
  - 後端新增 `POST /api/v1/auth/admin-reset-password`（`adminResetPassword`，admin 限定、限同租戶）→ 產隨機臨時密碼回傳明文;免 email 基礎設施（業主裁決）。已登錄 `docs/architecture/api/openapi.yaml`。
  - 前端 `AdminResetPasswordModal`（自包含）掛在 `/admin/roles`,按鈕僅 RBAC admin 角色可見。
  - 機制備註：使用者拿臨時密碼登入後沿用既有 `change-password` 改回（未做強制改密,users 表不加欄,避免 migration）；email 自助式留待 email 服務就緒再補。

### Added (tests)

- `web/tests/e2e/admin/ui-sweep.spec.ts`：45 admin-shell 路由互動健檢（render + 通用捲軸 bug 偵測 + 按鈕/篩選清點）。
- 5 條 P0 user-flow E2E：`refund-sod`、`dispute-cosign`、`gdpr-and-config`、`wo-cancel-cascade`（admin）、`tech/tech-flow`（tech project, Pixel 7，技師端從 0 → 有覆蓋）。
- A4 測試：`api/tests/test_admin_reset_password.py`（pytest 3 條）+ `web/tests/e2e/admin/admin-reset-password.spec.ts`（Playwright 1 條）。
- **A3 5 角色 RBAC 權限隔離矩陣**：`api/tests/test_rbac_role_isolation.py`（5 操作角色 × 4 守衛端點 = 20 條授權斷言）。取代原 `rbac.spec.ts`（@wip + mock 假 JWT）。發現前端 AuthGuard 無 route-level role gating（授權實際在 API 層強制）。

### Notes

本章節為 Q2 戰術級重構期間累積，待全部 Phase 1'-4' 完成後另開 release tag。

**Hands-on 修正歷程**：ADR-0023 於 2026-05-11 PR #62 merge 後 30 分鐘，進入 Phase 1.1 hands-on 階段。先發現 `report/` 是 95 個 v1.x.x release notes（非垃圾）、`api/data/` 命名類比錯誤，繼派 3 個 Explore agent 對全 5 訊號做深度驗證，揭露 4/5 處方不合理 + 3 處事實錯誤。Supersede 為 ADR-0024（hands-on 修正版），整體工期估計從 2-3 週縮減為 1 週內。決策軌跡保留作為「假設驅動 → hands-on 驗證」學習案例。

---

## [Historical] — Pre-2026-Q2

> 2026-Q2 之前的變更未維護於本檔。歷史紀錄請參考：
>
> - Git commit history（`git log --oneline`）
> - 各模組 module-boundary 文件（`docs/1-decisions/module-boundary/*.md`）
> - WBS Q1 進度（`docs/4-exploration/WBS-0001-2026-q1.md`）
