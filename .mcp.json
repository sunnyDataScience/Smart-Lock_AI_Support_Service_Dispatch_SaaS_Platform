{
  "mcpServers": {
    "codegraph": {
      "type": "stdio",
      "command": "codegraph",
      "args": [
        "serve",
        "--mcp"
      ]
    },
    "plane": {
      "type": "stdio",
      "command": "uvx",
      "args": [
        "plane-mcp-server",
        "stdio"
      ],
      "env": {
        "PLANE_API_KEY": "${PLANE_API_KEY}",
        "PLANE_WORKSPACE_SLUG": "${PLANE_WORKSPACE_SLUG}",
        "PLANE_BASE_URL": "${PLANE_URL}"
      }
    },
    "plane-qa": {
      "type": "stdio",
      "command": "node",
      "args": [
        "/home/os-sunnie.gd.weng/python_workstation/github_sunny/plane-QA-management/apps/plane-qa-mcp/dist/server.mjs"
      ],
      "env": {
        "PLANE_URL": "${PLANE_URL}",
        "PLANE_API_KEY": "${PLANE_API_KEY}",
        "PLANE_WORKSPACE": "${PLANE_WORKSPACE_SLUG}",
        "PLANE_PROJECT": "${PLANE_PROJECT}"
      }
    }
  }
}
