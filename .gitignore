# Environment
# 規則：所有實際 .env 都不入庫；只允許 *.example 範本入庫。
.env
.env.*
!.env.example
!.env.local.example
!.env.gcp.example
credentials.json
admin-credentials-*.md

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/
# 巢狀 workspace member 目錄內誤跑 uv run 會自建獨立 lock（正典=root uv.lock；CR-0157）
agent/rag/uv.lock
knowledge-pipeline/refinery/uv.lock

# 生成的文件檢視快取（tier-5 view；正典=smartlock-docs/*.md 與 docs/ 報告素材）
# 重生：scripts/dev/gen_enterprise_view.py（企業文件）/ gen_docs_html.py（報告素材）
docs_html/

# Node
node_modules/
.next/
npm-debug.log*
*.tsbuildinfo

# IDE
.vscode/
.idea/
*.swp
*.swo

# Git worktrees (parallel development workspaces)
.worktrees/

# OS
.DS_Store
Thumbs.db

# Docker
docker-compose.override.yml

# Misc
*.log
tmp/
agent/temp/
agent/result.txt

# AI assistant context (local only)
CLAUDE.md
GEMINI.md

# 執行期本地資料（用戶輪廓、媒體、資料庫等，含個資不進版控）
agent/data/
agent/memory.db
api/data/
data/media/
data/storage/skill_drafts/

# Playwright E2E 測試輸出（本地產物，不進版控）
web/*/test-results/
web/*/playwright-report/
web/*/.next/
web/*/node_modules/
web/*/tsconfig.tsbuildinfo

# 鎖匠方資料（大型二進位 / 含個資，僅保留 INDEX.md）
docs/Locksmith_Preparation_Checklist/01_各品牌電子鎖維修手冊/*.pdf
docs/Locksmith_Preparation_Checklist/04_歷史客服對話紀錄/*.csv
cloud-sql-proxy

# Web Design Spec Pipeline（本地參考用，不進版控）
# web_design_spec_prompt_pipeline/

# Claude Code 本機狀態與二進位（個人授權紀錄、執行期 session、跨平台 binary 不進版控）
.claude/settings.local.json
.claude/taskmaster-data/
.claude/statusline-go.exe
.claude/skills/
.dev-logs/

# Playwright MCP local cache（瀏覽器自動化工具的本地暫存）
.playwright-mcp/

# UAT 驗證指南（本地教學產物，含測試帳密，不進版控）
uat-guides/

# Python 測試與 property-based 測試暫存（本地產物）
.hypothesis/
.pytest_cache/

# Eval 跑分產物（run 輸出，可由 agent/scripts/*eval*.py 重新產生，不進版控；
# 沿用 2026-07-08 evals/README 清理慣例。保留輸入語料 *_corpus.json 與 README.md）
agent/evals/forbidden_run_*.json
agent/evals/sentiment_eval_result.json
agent/evals/reply_quality*.csv
agent/evals/sim_transcripts.json

# Debug screenshots（根目錄除錯截圖，不入庫）
/dashboard-*.png
/work-orders-*.png

# 會議資料歸檔（本機留存，不進版控；0707 決議根目錄雜物清理）
/meetings/

# Casdoor 本機實例匯出的驗簽公鑰(每套安裝不同,由 bootstrap 腳本產生)
infra/casdoor/casdoor_cert.pem
