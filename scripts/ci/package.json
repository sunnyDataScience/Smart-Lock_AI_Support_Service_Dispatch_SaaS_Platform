{
  "name": "smart-lock-ci-tools",
  "version": "0.0.1",
  "private": true,
  "description": "CI 用 Node.js 工具（asyncapi-validate / mock-server / 其他）— 與 web/ 隔離以避免污染前端依賴",
  "type": "module",
  "scripts": {
    "asyncapi:validate": "node asyncapi-validate.mjs"
  },
  "dependencies": {
    "@asyncapi/parser": "^3.4.0"
  }
}
