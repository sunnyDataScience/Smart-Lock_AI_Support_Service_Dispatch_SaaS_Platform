# 進度報告 v1.31.0：EventTimeline UI（admin 工單詳情）

- **版本**: v1.31.0（前版：v1.30.0 → 本版升級類型：MINOR）
- **Commit**: `8e54b2f` (2026-05-06 12:18)
- **分支**: feat/api-media-upload
- **作者**: Imding1211
- **影響模組**: `web/src/components/work-orders/EventTimeline.tsx`、`web/src/app/work-orders/[id]/page.tsx`、`web/docs/system-completion-status.md`

## 變更摘要 (WHAT)

- 新增 `EventTimeline` 元件：
  - 載入 `GET /work-orders/{id}/events`
  - 7 種 event_type 各對應圖示 + 顏色
  - `PayloadPreview` switch 元件依 type 顯示專屬欄位
  - 垂直時間軸樣式 + 過濾 + 重新整理
- Admin 工單詳情頁掛載 `EventTimeline`

## 背景與動機 (WHY)

v1.30.0 後端寫好 events 表 + GET endpoint，但前端不消費，subflow 資料只能用 SQL 查。EventTimeline 補完後 admin 端可視化追溯整個 subflow 歷史。

## 影響評估 (IMPACT)

- 後端：無變更
- 前端：admin 工單詳情多一個區塊，有 subflow event 時自動顯示時間軸
- 完成 v1.30.0 技術債清償的最後一塊

## 驗證方式

### 自動化
- ✅ `npx tsc --noEmit` exit 0
- ✅ `npx next build` 成功，`/work-orders/[id]` 18.4 → 20.3 kB

### 手動驗證

```bash
cd api && uvicorn main:app --reload --port 8001
cd web && npm run dev
# 技師端走 subflow（範圍變更/缺料/延遲/門面）→ admin /work-orders/[id]
```

| 步驟 | 預期 |
|:---|:---|
| 進 admin 工單詳情 | LineMediaGallery 之後出現「事件時間軸」區塊 |
| 上方下拉「全部類型」 | 過濾 event_type |
| 每筆事件 | 圖示 + 顏色 + 標題 + 時間 + payload 預覽 |
| scope_change | 顯示理由 + 工項清單 + 預估追加金額 |
| delay | 顯示分鐘數 + 原因 + 通知對象 |
| door_check | 顯示檢核完成度 + 照片數 |
| 點重新整理 | 重抓最新事件 |

## 下一步 (NEXT)

1. **SLA 引擎**（quote_expiring / dispatch_delay / response_overdue）
2. **整合測試 / E2E**
3. **開 PR `feat/api-media-upload → dev`**
