# 進度報告 v1.4.0：新增 /notifications 通知中心頁面 (G1)

- **版本**: v1.4.0（前版：v1.3.14 → 本版升級類型：MINOR）
- **Commit**: `73552fe` (2026-05-05 11:07)
- **分支**: feat/web-notifications-page
- **作者**: Imding1211
- **影響模組**: `web/src/app/notifications/`、`web/src/components/layout/NotificationDrawer.tsx`

> 附註：前一個 commit `40d9a06` 為純機械性 raw 資料清理（76 個 youtube transcript JSON + 1 個過期 xlsx），不消耗版本號。

## 變更摘要 (WHAT)

- 新增 `web/src/app/notifications/page.tsx`（約 510 行）落實 spec G1 規格：
  - header_bar、tab_group（4 Tab：未讀/全部/已讀/已存檔）、filter_chips（8 類型）
  - notification_list（含 checkbox 多選）、detail_preview（桌面右側 400px 側欄）
  - bulk_actions_toolbar（標已讀 / 存檔）、URL query 同步（tab / type / id）
- 串接四個 OpenAPI endpoints：`listNotifications`、`updateNotification`、`bulkUpdateNotifications`、`markAllNotificationsRead`
- `NotificationDrawer` footer 新增「查看全部通知」連結指向 `/notifications`

## 背景與動機 (WHY)

依 web 端 spec 盤點報告，使用者 workflow 覆蓋度約 45%，全域通知中心（G1）在 spec 21_global_notifications.md（328 行）有完整定義但實作端只有 420px 寬的 header drawer，缺獨立全頁版本。使用者面對大量通知時無法集中管理、無類型過濾、無批量操作、無深連結。本次補齊 G1 為「最快完成且元件複用率最高」的 gap，是 web workflow 完整化的第一步。

## 影響評估 (IMPACT)

- 破壞性變更：無。原 NotificationBell + Drawer 行為維持。
- 後續動作：
  - 尚未實作 WebSocket 即時推送（`subscribeUserNotifications`）與 BroadcastChannel 跨 tab 同步
  - 尚未實作 spec [EXCEPTION] 中「Critical 通知不可全部已讀」強制規則
  - `/settings?tab=notifications` 偏好設定頁需另案處理
  - 規格中「右滑手勢標已讀（手機）」、「raw event JSON 開發者模式」屬 P2，留待後續

## 驗證方式

- `npx tsc --noEmit` 通過（exit 0）
- `npx next build` 成功，`/notifications` 4.12 kB 已出現在 build output 路由表
- 程式碼 review：型別取自 `@/types/api.generated.ts`（OpenAPI SSOT）、複用 `formatRelative` 與 `api` client 共用元件
- 未跑 e2e UI 測試（無 dev server 環境）；建議部署 PR preview 後驗證

## 下一步 (NEXT)

依使用者 workflow 盤點優先級：
1. **派工人工介入頁 `/admin/dispatch-manual` (A37)** — 解鎖 Flow 2/9/14 三個 Admin 流程
2. **技師端 T1 案件池 + T3 工單詳情** — 0→2 解鎖 Flow 1 Happy Path
3. **WebSocket 訂閱層** — 把 polling 升級為即時推送（影響本頁 + 派工佇列等）
