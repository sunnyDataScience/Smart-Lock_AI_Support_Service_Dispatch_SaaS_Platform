# 進度報告 v1.26.0：完工 photos UI + admin 工單媒體縮圖瀏覽

- **版本**: v1.26.0（前版：v1.25.0 → 本版升級類型：MINOR）
- **Commit**: `0150e3d` (2026-05-06 11:25)
- **分支**: feat/api-media-upload
- **作者**: Imding1211
- **影響模組**: `web/src/app/my-orders/[id]/page.tsx`、`web/src/components/work-orders/MediaGallery.tsx`、`web/src/app/work-orders/[id]/page.tsx`、`web/docs/system-completion-status.md`

## 變更摘要 (WHAT)

- 技師完工 form 加 photos 上傳 UI（completion_before/after × 2 格 file input + camera capture）
- 新增 `MediaGallery` 元件（fetch+Bearer→blob URL 解決 auth 縮圖、7 種 purpose 分色 + lightbox）
- Admin 工單詳情頁掛載 `MediaGallery` — 看得到技師上傳的所有媒體
- 完成度文件：總體 90→**91%**、Flow 10 95→**100%**

## 背景與動機 (WHY)

v1.25.0 完成媒體上傳 endpoint + T8 串接後，剩兩個前端缺口（技師完工 form / admin 縮圖）。本次補完讓 Flow 10 端到端打通，媒體上傳的價值最大化。

## 影響評估 (IMPACT)

- 後端：無變更（複用 v1.25.0 endpoints）
- 前端：完工 photos 真實寫入 + admin 可瀏覽全部媒體（含門面 / 完工 / 證據 7 種類型分組）
- 解鎖：Flow 10 95% → 100% 完整端到端視覺呈現
- 限制：Lightbox 只放大縮圖（無原圖讀取，可後續加 size=full 參數）

## 驗證方式

### 自動化
- ✅ `npx tsc --noEmit` exit 0
- ✅ `npx next build` 成功，`/my-orders/[id]` 4.42 kB、`/work-orders/[id]` 18.4 kB

### 手動端到端

```bash
cd api && uvicorn main:app --reload --port 8001
cd web && npm run dev
```

| 步驟 | 預期 |
|:---|:---|
| 技師接單 → 進詳情 → 點「完工回報」 | 表單含 2 格 file input |
| 點「+ 完工前」/「+ 完工後」拍照 | 上傳中 → 顯示 [section] filename |
| 填 summary 提交 | photos_before/after 帶 URL 真實上傳 |
| Admin 進 `/work-orders/{id}` | LineMediaGallery 之後出現「相關媒體」區塊 |
| 區塊依 purpose 分組（門面前後 / 完工前後 / 證據雙方）| 正確顯示縮圖 |
| 縮圖左上角 purpose 標籤 + 底部 size + 時間 | 樣式 |
| 點縮圖 | lightbox 放大 + 點背景關閉 |

## 下一步 (NEXT)

1. **Dispute evidence 上傳 UI**（admin disputes 模態加 file input）
2. **Inventory low-stock 背景偵測 job**
3. **Refund 雙簽流程**
4. **work_order_events 表**（取代 service_report append）
5. **整合測試 / E2E**
6. **開 PR `feat/api-media-upload → dev`**
