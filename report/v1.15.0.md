# 進度報告 v1.15.0：整合剩餘 4 個 WS 頻道（10/10 完成）

- **版本**: v1.15.0（前版：v1.14.0 → 本版升級類型：MINOR）
- **Commit**: `faa796d` (2026-05-05 22:30)
- **分支**: feat/web-tech-app
- **作者**: Imding1211
- **影響模組**: `web/src/app/admin/{refunds,disputes,inventory}/page.tsx`、`web/src/components/realtime/RbacChangedBanner.tsx`、`web/src/components/layout/AuthGuard.tsx`

## 變更摘要 (WHAT)

- `/admin/refunds` 訂閱 `/realtime/refunds`，事件觸發重抓 + header indicator
- `/admin/disputes` 訂閱 `/realtime/disputes`，事件觸發重抓當前 tab
- `/admin/inventory` 訂閱 `/realtime/inventory/low-stock`，告警顯示 4 秒 toast + 重抓
- `RbacChangedBanner` 訂閱 `/realtime/rbac`，頂部 fixed banner 提示「重新整理」
- AuthGuard 在已登入狀態下渲染 RbacChangedBanner

## 背景與動機 (WHY)

完成 spec 10 個 realtime 頻道整合（最後 4 個）。RBAC 權限變更後若無提示，使用者可能在舊權限下繼續操作；inventory/refunds/disputes 之前都靠手動刷新才能看到變化，UX 不佳。

## 影響評估 (IMPACT)

- 破壞性變更：無
- Realtime 頻道整合進度：**10/10 完成**
  - notifications / pool / dispatch-queue / work-orders / diagnostics(SSE) / sla-alerts / refunds / disputes / inventory / rbac
- 後端要求：`access_token` + `tenant_id` query auth；spec 已定義所有事件 payload

## 驗證方式

### 自動化
- ✅ `npx tsc --noEmit` exit 0
- ✅ `npx next build` 成功
  - `/admin/refunds` 7.41 kB
  - `/admin/disputes` 6.68 kB
  - `/admin/inventory` 5.87 kB

### 手動驗證

```bash
cd web && npm run dev
# 設 NEXT_PUBLIC_REALTIME_BASE_URL 啟用即時功能
```

| 頁面 | 推送事件 | 預期 |
|:---|:---|:---|
| `/admin/refunds` | RefundDecisionMade | header 綠脈衝、列表重抓 |
| `/admin/disputes` | DisputeCreated | header 綠脈衝、tab 重抓 |
| `/admin/inventory` | LowStockAlert | 4 秒 amber toast + 列表重抓 |
| 任意 admin 頁 | RbacPermissionChanged | 頂部 fixed banner，含「重新整理」按鈕 |

### Echo 測試

```bash
npx wscat -l 8000
> {"type":"low_stock","payload":{"part_id":"abc12345-def6-7890-abcd-ef0123456789","current_stock":2,"threshold":10}}
> {"type":"rbac.changed","payload":{"role_id":"ops_mgr","changed_codes":["dispatch.assign.tenant"]}}
```

## Realtime 整合總結

| # | 頻道 | 整合處 |
|:---|:---|:---|
| 1 | notifications | `/notifications`、Bell、Drawer |
| 2 | pool | `/pool` |
| 3 | dispatch-queue | `/admin/dispatch-queue` |
| 4 | work-orders/{id} | `/my-orders/[id]/reschedule` |
| 5 | diagnostics (SSE) | `/conversations/[id]` |
| 6 | sla-alerts | `/dashboard` |
| 7 | refunds | `/admin/refunds` |
| 8 | disputes | `/admin/disputes` |
| 9 | inventory/low-stock | `/admin/inventory` |
| 10 | rbac | 全域（AuthGuard 內） |

## 下一步 (NEXT)

1. **開 PR `feat/web-tech-app → dev`** 同步進度
2. **後端 SLA 引擎 + 排班 API + subflow API**
3. **dispatch-queue patch state** 減少 fetchAll 頻率
4. **媒體上傳 endpoint** 對接 T8 photos / CompletionReport photos
