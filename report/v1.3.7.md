# 進度報告 v1.3.7：文件對齊新架構（CLAUDE.md / GEMINI.md / usage_guide / evals / harness_comparison）

- **版本**: v1.3.7（前版：v1.3.6 → 本版升級類型：PATCH）
- **Commit**: 即將提交
- **分支**: agent-improve
- **作者**: Imding1211
- **影響模組**: CLAUDE.md、agent/GEMINI.md、agent/docs/manuals/usage_guide.md、agent/docs/manuals/harness_comparison.md、agent/evals/README.md

## 變更摘要 (WHAT)

### A. CLAUDE.md（專案總覽）
- Project Overview 由「skill-based ReAct agent + SKILL.md」改為「ReAct agent + product_info/ mega-doc」並強調 bronze-only 規則
- Request Processing Flow 中 `load_skill, update_user_info, transfer_to_human` → `load_product_info, ...`
- Agent construction 段：3 tools 名單修正、prefix 描述 `[可用技能]` → `[可用產品資料]`
- Module map 加入 `agent_tools/` 與 `product_info/` 兩節，刪除舊 `skills/` 條目
- 整段「Skills System (`agent/skills/`)」重寫為「Product Info Knowledge Base (`agent/product_info/`)」：兩階段載入、新目錄佈局、bronze-only critical rule、loader API
- 「Tools (`agent/skills/tools.py`)」重寫為「Agent Tools (`agent/agent_tools/tools.py`)」並更新 3 tool 行為描述
- Data Pipeline 第 4 步加註 silver_to_skill 為 legacy artifact
- Post-Commit Progress Report 範例的「影響模組」例子改為新模組路徑

### B. agent/GEMINI.md（agent 模組視角總覽 — local-only，gitignore 中）
整份重寫（本檔在 .gitignore 內，本 commit 不包含此檔案，更新為本機開發 cache）：
- 標題敘述去除 skill-based 字眼
- Project Overview 改為 product_info mega-doc 架構說明
- Features 段：「Skill System: 63+ skills」改為「Product Info System: 36 mega-docs」
- Local CLI Testing 段更新（main.py 已是 smoke test）
- Project Structure：`skills/` → `agent_tools/` + 新增 `product_info/`
- Development Conventions 新增「Bronze-only knowledge sourcing (CRITICAL)」與「Strict profile gating」兩條鐵律
- Tool Call Privacy 範例字串 `load_skill` → `load_product_info`

### C. agent/docs/manuals/usage_guide.md（agent 操作手冊）
- Section 3「請求處理流程」flow chart：`filter_skills` / `load_skill` → `filter_loadable` / `load_product_info`
- Section 4「品牌感知目錄結構」：刪掉 67 SKILL.md 階層樹，改為 36 mega-doc 平鋪結構
- Section 4「技能總數」表改為「Mega-doc 總數：36」分布表（_common 4 / Chatlock 3 / Dormakaba 16 / 其他 13）
- SKILL.md 格式範例 → mega-doc 格式範例（含 brand/model/description frontmatter + bronze-only 鐵律警示）
- 工具表 3 行全部更新（load_product_info / update_user_info / transfer_to_human）
- Section 5「新增技能」整段重寫為「新增 Mega-Doc」：建檔流程、bronze-only 鐵律（✅/❌ 來源清單）、撰寫要點

### D. agent/docs/manuals/harness_comparison.md
本文件比較兩個 harness 框架的歷史對照，文中「agent_skills/」指本專案。在標題下加註：v1.3.6 後本專案內 `agent/skills/` 已重命名為 `agent/agent_tools/`、SKILL.md 架構退場、knowledge base 改 mega-doc，並修正「2 個工具」現為「3 個工具」。

採用加註而非全文重寫的理由：本文是歷史比較研究，重寫會破壞時間切片價值；註腳提供明確時間軸與重命名對照即可。

### E. agent/evals/README.md
迭代節奏第 3 步「改 `agent/skills/data/**/SKILL.md`」→「改 `agent/product_info/{Brand}/{Model}.md` 或 `_common/*.md`」

## 背景與動機 (WHY)

v1.3.4 / v1.3.5 / v1.3.6 三個 commit 重構完代碼層後，文件層仍引用舊架構（67 SKILL.md / load_skill / skills/ 路徑），造成：

1. 新人按 CLAUDE.md / GEMINI.md 找 `agent/skills/data/` 會撲空
2. usage_guide.md 教使用者新增 SKILL.md，但實際入口已是 product_info mega-doc
3. evals/README.md 的迭代步驟指向錯誤檔案
4. CLAUDE.md 的 Module map 缺少 `product_info/` 與 `agent_tools/`

文件層落後實作層三個 commit，必須補上。

## 影響評估 (IMPACT)

- **破壞性變更**：無（純文件）
- **未動但會殘留舊名的地方**：
  - `harness_comparison.md` 內文表格仍用「agent_skills/」對照名（已加標題註腳指明本專案實際路徑）
  - `data/pipeline/silver_to_skill/` 名稱保留（pipeline 階段命名歷史，不在本期範圍）

## 驗證方式

```bash
grep -rn "load_skill\b" CLAUDE.md agent/GEMINI.md agent/evals/README.md agent/docs/manuals/usage_guide.md
```

預期：無任何結果（全部主要文件已脫離舊 tool 名稱）。`harness_comparison.md` 因歷史對照保留，已加註腳。

`python main.py` 仍 6/6 全綠。

## 下一步 (NEXT)

文件全面對齊新架構，可進入：
1. 跑 LLM judge 對齊 v11 baseline（56 pass / 10 partial / 0 fail / 1 error），確認 v1.3.4–v1.3.7 四階段重構未打到 prod 行為
2. 若有 regression：定位是 v1.3.5 函式拔除（最高風險）或 v1.3.6 import 路徑改動（低風險）
3. 處理 studio_results Case 7 PDF 雙 App 問題
