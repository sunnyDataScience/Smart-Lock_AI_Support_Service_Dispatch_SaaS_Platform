# 進度報告 v1.37.3：批量修 9 個 table 的 shrink-0 + min-w-0

- **版本**: v1.37.3（前版：v1.37.2 → 本版升級類型：PATCH）
- **Commit**: `b0d2e42` (2026-05-06 20:32)
- **分支**: feat/api-media-upload
- **作者**: Imding1211
- **影響模組**: web/src/components/{accounting,work-orders,technicians,admin,problem-cards}/*Table.tsx（9 個元件）

## 變更摘要 (WHAT)

- 9 個 table 元件批量修：`columns` 全部 `w-[Npx]` → 加 `shrink-0`；`flex-1` → 加 `min-w-0`
- Row JSX 同步修，兩種 row 模式（含 / 不含 `flex` 前綴）都覆蓋
- 受影響元件：InvoicesTable / ReconciliationsTable / SettlementTable / WorkOrdersTable / TechniciansTable / ProblemCardsTable / InventoryTable / WarrantyClaimsTable / RefundReviewTable
- 148 insertions / 148 deletions（純 class 名修改）

## 背景與動機 (WHY)

v1.37.2 修 DisputesTable 後盤點，全 web 共 10 個 table 用同模式，同 bug 風險。一次掃完避免使用者一個個踩。

## 影響評估 (IMPACT)

- 無資料邏輯變更
- TypeScript check 通過
- 視窗較窄或 row 內容較長場景下，header / row 對齊正確

## 驗證方式

- `npx tsc --noEmit` ✓
- 兩個 sample（InvoicesTable / WorkOrdersTable）spot check class 增補正確

## 下一步 (NEXT)

- 使用者可在瀏覽器逐頁驗證跑版修復
- 之後若新增 table 元件，建議在 lint rule 或元件 review 加上「`w-[Npx]` 必配 `shrink-0`、`flex-1` 必配 `min-w-0`」規範
