---
title: File Dependencies (AI-AUTO)
tier: 5
status: active
last_regenerated: 2026-05-10
generator: manual (Phase 8 後改 pyan + madge); from grep over agent/, api/, web/
DO_NOT_EDIT: |
  Tier 5 = AI-AUTO 視圖。手動編輯會在下次 regen 被覆寫。
related:
  - "../1-decisions/ARCH-0001-architecture-overview.md"
  - "../1-decisions/module-boundary/"
---

# File Dependencies

> Cross-module import overview. 完整依賴圖待 Phase 8 跑 pyan/madge。

## 模組邊界（cross-module imports）

依 `code-architecture-review-2026-05-06` audit + 實際 grep:

```
agent/ ─────────────┐
                    │ (no direct import；經 PostgreSQL + HTTP 解耦)
api/   ─────────────┤
                    │
data/  ─────────────┤  (data/pipeline 產 SKILL.md → agent/skills/data/)
                    │
web/   ─────────────┘  (web → api 經 HTTP REST + WS only)

⚠️ 已知反向 import (待 P1-1 重構修復):
  agent/harness/debounce.py:24       from agent import get_system_prompt
  agent/skills/tools.py:10           from harness.line_ui_factory import ...
```

## agent/ 內部分層

```
agent/app.py                    # entry: FastAPI webhook
  ├── agent/agent.py            # ReAct Agent + LangGraph
  │     └── agent/skills/        # tools (load_skill, update_user_info, transfer_to_human)
  │           ├── agent/skills/data/{Brand}/{_all-models|Model}/SKILL.md
  │           └── agent/profiles/manager.py (DB SCD2)
  ├── agent/harness/             # 12-layer middleware
  │     ├── debounce.py (核心 orchestrator)
  │     ├── multimodal.py / safety_gate.py / output_validator.py
  │     ├── data_correction.py / line_ui_factory.py / quick_reply.py
  │     └── memory_manager.py / profile_updater.py / sop_extractor.py
  ├── agent/memory/              # checkpointer registry (in-process / SQLite / PostgreSQL)
  ├── agent/storage/             # audit log storage registry
  ├── agent/llms/                # LiteLLM unified (vertex_ai/gemini-...)
  ├── agent/notifications/       # channel adapters (LINE / FCM / Email / SMS)
  ├── agent/integrations/        # admin_api.py (HTTP client to api/)
  └── agent/core/                # config, line_bot wrapper
```

## api/ 內部分層

```
api/main.py                     # FastAPI app
  ├── api/middleware/            # JWT, RBAC, tenant context, error envelope
  ├── api/routers/               # endpoint definitions (per resource)
  ├── api/services/              # business logic
  │     ├── dispatch_engine.py
  │     ├── work_order_service.py
  │     ├── refund_service.py / scope_change_service.py
  │     ├── pricing_engine.py / sla_monitor.py
  │     ├── rbac_service.py / audit_logger.py
  │     ├── warranty_service.py / problem_card_service.py
  │     ├── conversation_service.py / line_push_service.py
  │     └── admin_api 系列
  ├── api/realtime/              # WebSocket handlers
  ├── api/agent/integrations/    # agent ↔ api bridge
  └── api/core/{db, config, auth, exceptions}.py
```

## data/pipeline/ 4 layer

```
source_to_raw/   →   raw_to_bronze/   →   bronze_to_silver/   →   silver_to_skill/
   (yt-dlp,            (Whisper ASR,         (LLM chunking,         (classify + draft +
    Playwright,          Vision LLM,            embedding store)       approve → SKILL.md
    GDrive)              PDF parse)                                    output to
                                                                       agent/skills/data/)
```

## web/ → api/

```
web/src/lib/api.ts                          # 統一 HTTP client
  └── web/types/api.generated.ts            # auto-generated from openapi.yaml
        └── (regenerated by scripts/ci/generate-api-types.sh)
```

所有 web/src/components/<domain>/ 與 web/src/hooks/ 透過 lib/api.ts 訪問後端。

## SQL/

```
SQL/Schema.sql                       # core tables (users, conversations, messages,
                                     #    problem_cards, manual_chunks, case_entries)
SQL/Schema_harness_migration.sql     # user_facts SCD2, audit_logs, data_corrections
SQL/Schema_v2_extensions.sql         # additional V2.0+ extensions
SQL/seed/                            # 待 CR-0006: technicians.example.sql
```

## 共用 helpers

| Helper | 位置 | 用途 |
| :-- | :-- | :-- |
| `agent/core/workday.py` | agent/core/ | Q4=C 工作日計算（holidays 套件）|
| `agent/core/config.py` | agent/core/ | TOML config loader |
| `agent/core/line_bot.py` | agent/core/ | LINE SDK wrapper |
| `agent/core/pg_pool.py` | (P1-1 重構) | PostgreSQL async pool（DRY 修復目標）|
| `api/core/db.py` | api/core/ | SQLAlchemy session factory |
| `api/core/auth.py` | api/core/ | JWT + RBAC |

## 變更紀錄

| 日期 | 變更 |
| :--- | :--- |
| 2026-05-10 | 初版 — 從 grep 與 audit report 整理，待 Phase 8 自動工具補完 |
