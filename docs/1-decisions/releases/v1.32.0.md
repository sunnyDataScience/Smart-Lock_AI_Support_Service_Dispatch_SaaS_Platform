# 進度報告 v1.32.0：pytest 整合測試套件（16 項通過）

- **版本**: v1.32.0（前版：v1.31.0 → 本版升級類型：MINOR）
- **Commit**: `6304ecb` (2026-05-06 12:30)
- **分支**: feat/api-media-upload
- **作者**: Imding1211
- **影響模組**: `api/tests/`、`api/pytest.ini`、`api/models/generated.py`、`api/services/refund_service.py`、`web/docs/system-completion-status.md`

## 變更摘要 (WHAT)

- `api/tests/` 新整合測試 4 個檔案 + conftest（**16 項全綠**）：
  - test_health（1 項）
  - test_auth_guards（4 項：未帶/假 token/admin 通行/tenant_id 403）
  - test_media_upload（5 項：upload→get→404→invalid purpose→list-by-dispute）
  - test_refund_dual_sign（6 項：v1.29.0 雙簽狀態機完整覆蓋）
- `pytest.ini`（asyncio_mode=auto + session loop scope）
- `models/generated.py` 重新 codegen（含 `csm_approved` enum）
- `refund_service._VALID_API_STATUS` 加 `csm_approved`（修復 coerce_status bug）

## 背景與動機 (WHY)

合約 1.2.7.3 整合測試 / E2E 未啟動。後端 30+ endpoints + 雙簽 + 媒體 + 排班等狀態機急需自動化守護，避免回歸。v1.29.0 雙簽流程是最複雜邏輯，必須先有測試覆蓋。

## 影響評估 (IMPACT)

- 後端品質防線啟動：refund 雙簽 / 認證 / 媒體三大關鍵路徑自動化保護
- 順手修 1 個 bug：`_VALID_API_STATUS` 漏 `csm_approved` 導致 status coerce 回 `pending`
- 後續可加 work_order state / subflow events / schedule 相關測試
- E2E Playwright 待 web/ 端設置（基礎設施已備）

## 驗證方式

### 自動化

```bash
cd api && python -m pytest tests/ -v
```

預期：
```
tests/test_auth_guards.py::test_missing_authorization_returns_401 PASSED
tests/test_auth_guards.py::test_invalid_token_returns_401 PASSED
tests/test_auth_guards.py::test_admin_can_list_work_orders PASSED
tests/test_auth_guards.py::test_tenant_mismatch_returns_403 PASSED
tests/test_health.py::test_health PASSED
tests/test_media_upload.py::test_upload_and_get_image PASSED
tests/test_media_upload.py::test_upload_invalid_purpose PASSED
tests/test_media_upload.py::test_upload_unsupported_content_type PASSED
tests/test_media_upload.py::test_get_media_404 PASSED
tests/test_media_upload.py::test_list_media_for_dispute_returns_items_array PASSED
tests/test_refund_dual_sign.py::test_single_sign_approve PASSED
tests/test_refund_dual_sign.py::test_dual_sign_first_step PASSED
tests/test_refund_dual_sign.py::test_dual_sign_same_user_blocked PASSED
tests/test_refund_dual_sign.py::test_dual_sign_completes_with_different_user PASSED
tests/test_refund_dual_sign.py::test_reject_from_pending PASSED
tests/test_refund_dual_sign.py::test_state_conflict_when_already_approved PASSED
========================= 16 passed in 0.38s =========================
```

### 部署 CI 建議
GitHub Actions workflow 可 hook：
```yaml
- run: |
    cd api && python -m pytest tests/ --tb=short
```

## 下一步 (NEXT)

1. **更多測試覆蓋**：work_order state machine / subflow events / schedule（每樣 0.5 天）
2. **Playwright E2E**（web 端 Flow 1 Happy Path 自動化）— 1-2 天
3. **SLA 引擎** — 1 週
4. **CI 整合**（GitHub Actions 加 pytest gate）— 半天
5. **開 PR `feat/api-media-upload → dev`** — 5 分鐘
