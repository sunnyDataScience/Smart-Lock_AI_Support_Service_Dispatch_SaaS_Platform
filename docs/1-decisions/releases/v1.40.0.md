# 進度報告 v1.40.0：refactor/agent-port 文件對齊（Stage A + Stage B 補手冊與 ADR）

- **版本**: v1.40.0（前版：v1.39.0 → 本版升級類型：MINOR — 新增 3 份手冊 + 1 份 ADR + CLAUDE.md 對齊 + ADR-0008 reinstate 附註）
- **branch**: `refactor/agent-port`
- **作者**: Imding1211 + Claude Opus 4.7
- **影響模組**:
  - `CLAUDE.md`（修改 — Architecture Lock + Experimental Lock + Module map + 移除 skills 描述）
  - `agent/docs/manuals/`（新增 — 3 個手冊）
  - `docs/1-decisions/`（新增 ADR-0010 + 修改 ADR-0008 加 reinstate 附註）
  - `report/v1.40.0.md`（本檔）

## 變更摘要 (WHAT)

本批 6 個 commit 為 `refactor/agent-port` 的「文件對齊」工作 — 把 2026-05-11 一日內 19 個 commit（Stage 0 + A-1～A-3b + Stage B 13 個）的設計脈絡寫成可追溯的手冊與 ADR：

| Commit | 內容 |
| --- | --- |
| `923ab27` | CLAUDE.md 對齊 — product_info canonical + Belief-Augmented ReAct 兩個 Lock + Module map 全面改寫 |
| `b945abc` | 新增 `agent/docs/manuals/turn_cycle_belief_augmented_react.md` — 7 檔結構 + Schema + Policy 規則樹 + Belief Hint + A/B 數據 + 已知限制 |
| `b30295a` | 新增 `agent/docs/manuals/product_info_cutover_2026-05-11.md` — 5 commit 時間線 + 各階段細節 + 跨 branch 對照表 + LLM 行為差異 + Rollback SOP |
| `f332aa7` | 新增 `agent/docs/manuals/action_policy_thresholds.md` — HIGH 0.70→0.55 / GAP 0.30→0.15 調整理由 + 對打驗證表 + 6 步 SOP + 4 條紅線 |
| `43423af` | 新增 `docs/1-decisions/ADR-0010-belief-augmented-react.md` — Experimental / On Hold 狀態 + 設計論證 + Pros/Cons + 替代方案 + Lock 4 條 |
| `afc9e23` | 修改 `docs/1-decisions/ADR-0008-product-info-architecture-canonical.md` — 加 §0a REINSTATE 附註（僅適用本 branch）|

## 背景與動機 (WHY)

`refactor/agent-port` 分支於 2026-05-11 一日內衝完整 Stage A（product_info cutover）+ Stage B（Belief-Augmented ReAct），程式碼齊備、quality_check 拿 89.6% strict pass / 0 fail。但所有設計脈絡只散在 commit messages 內，CLAUDE.md 還在描述舊架構（skill-based ReAct + load_skill + skills/data），agent/docs/manuals/ 沒有對應手冊，docs/1-decisions/ 沒有 ADR。

本批為 2026-05-13 對 refactor/agent-port 進行的文件對齊工作。

範圍上選擇「完整對齊」（非 archive 化），因為：

1. 目前 roadmap 走向 hermes-cs（hermes-cs 對應 ADR 已於另一 branch 寫成）但 agent-port 仍可能被 backport 部分模組
2. 對打數字明確（89.6% strict / 0 fail）值得保留為 reference 實作
3. 未來若覺得 hermes-cs 太薄想找補強想法時，可參考本 branch 的 Belief 模式

## 影響評估 (IMPACT)

- 破壞性變更：無（純文件）
- 跨 branch 影響：ADR-0008 在本 branch 加 §0a 附註但**未撤回 main/dev 上的 SUPERSEDED**，附註明確標記「僅適用 refactor/agent-port」
- 後續動作：
  - 若後續決議 archive 本 branch → 寫一張一行的 docs/\_audit 紀錄即可
  - 若決議 backport Belief 模式進 hermes-cs → 從 ADR-0010 §3.3「何時 revisit」開始評估

## 驗證方式

- `cat CLAUDE.md` — 開頭三段（Project Overview / Architecture Lock / Experimental Lock）已對齊
- `grep -n "load_skill\|SKILL.md" CLAUDE.md` — 剩餘提及全在 Lock 區段描述「已退場」的脈絡，正確
- 三個 manual 文件互相 cross-link，連回 ADR-0008 / ADR-0010 / 各自手冊均正確
- `git log refactor/agent-port --oneline | head -10` 顯示 6 個 commit 連續

## 下一步 (NEXT)

- 切回 `feat/hermes-customer-service` 繼續 hermes-cs 部署 work
- 等使用者 push refactor/agent-port → origin（依鐵律 Claude 不 push）
- 若後續決議 archive，本 branch 可標 `[archived]` tag 並停止維護
