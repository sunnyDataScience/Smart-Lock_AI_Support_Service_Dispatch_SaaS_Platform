# 進度報告 v1.16.0：PWA manifest + 桌面 guard + 文件同步收尾

- **版本**: v1.16.0（前版：v1.15.2 → 本版升級類型：MINOR）
- **Commit**: `5d6f33a` (2026-05-05 22:55)
- **分支**: feat/web-tech-app
- **作者**: Imding1211
- **影響模組**: `web/public/`、`web/src/app/layout.tsx`、`web/src/components/tech/DesktopMobileGuard.tsx`、`web/src/components/tech/TechShell.tsx`、`web/src/app/tech-login/page.tsx`、`web/docs/page-status.md`、`web/docs/wbs-completion-report.md`

## 變更摘要 (WHAT)

- **PWA 資產**：manifest.webmanifest（name/start_url=/tech-login/standalone + 3 shortcuts）+ 4 個 SVG icon（192/512/apple/favicon）
- **DesktopMobileGuard**：≥ 769px 顯示「請使用手機」提示頁 + QR Code，含「強制桌面」測試後門
- **page-status.md**：新增 1B / 18 / 19 / 20 / 21 共 5 個 section（A37、通知中心、技師端 12 頁、realtime 10 頻道、BroadcastChannel）
- **wbs-completion-report.md**：完成度總覽 Phase 5 85→95%、Phase 6 50→85%、Phase 7 70→80%
- **layout.tsx**：metadata 加 manifest / icons / appleWebApp，抽 viewport export

## 背景與動機 (WHY)

`feat/web-tech-app` branch 收尾前最後三項補強：(1) 文件還是 04-29 版、(2) 桌面訪問技師端違反 spec、(3) 無 manifest 無法加到主畫面。

## 影響評估 (IMPACT)

- 無破壞性
- 桌面訪問 `/pool` `/my-orders` `/account` `/tech-login` 顯示提示頁 + QR Code
- 手機從 Chrome/Safari「加到主畫面」可裝成 standalone app
- 業主可從文件看到最新狀態（含 12 頁師傅端 + 10 realtime 頻道）

## 驗證方式

### 自動化
- ✅ `npx tsc --noEmit` exit 0
- ✅ `npx next build` 成功

### 手動驗證

```bash
cd web && npm run dev
```

| 場景 | 預期 |
|:---|:---|
| 桌面（>768px）開 `/pool` | 顯示「請使用手機」+ QR Code |
| 點 QR Code（手機掃描） | 在手機開啟 `/pool` |
| 點「強制顯示桌面版」 | sessionStorage 持久，本 session 內桌面也可看 |
| 縮窄到 ≤ 768px | 自動回到正常 mobile 介面 |
| Chrome 開 `/tech-login` 點 ⋮ → 安裝應用程式 | 可裝為 standalone app |
| `<head>` 內含 manifest / favicon / apple-touch-icon | 已正確注入 |

### Lighthouse PWA 分數
建議跑 Lighthouse PWA audit 確認：installable / themed / icons / start_url。

## 下一步 (NEXT)

1. **開 PR `feat/web-tech-app → dev`** 同步進度
2. **後端 API 補齊**（subflow + 排班 + WS server）
3. **PWA Service Worker + 離線快取**（Workbox 整合，後續迭代）
