# 進度報告 v1.34.0：客戶歷史聚合面板（F3）

- **版本**: v1.34.0（前版：v1.33.0 → 本版升級類型：MINOR）
- **Commit**: `1a24f7d` (2026-05-06 13:15)
- **分支**: feat/api-media-upload
- **作者**: Imding1211
- **影響模組**: `api/services/customer_service.py`、`api/routers/customers.py`、`docs/02-design/specs/openapi.yaml`、`web/types/api.generated.ts`、`web/src/app/admin/customers/[id]/page.tsx`、`web/src/app/admin/customers/page.tsx`、`web/docs/system-completion-status.md`

## 變更摘要 (WHAT)

- 後端 `getCustomer` 服務：基本資料 + 工單統計 + 平均評分/時長 + 投訴/退款 + 最近工單/對話
- `GET /api/v1/customers/{id}` endpoint
- OpenAPI `CustomerDetail` schema（extends Customer with history）
- 前端 `/admin/customers/[id]` 完整詳情頁（profile + 8 KPI 卡 + 狀態分布 + 最近工單表 + 最近對話列）
- list 頁每 row 改 Link 跳 detail
- 完成度文件：admin 後台 95% → 97%

## 背景與動機 (WHY)

`/admin/customers` 列表頁從 v1.x 起就存在但點 row 無法進入詳情，使派工/客服無法快速判斷客戶風險。本次補完後客服 1 秒內可掌握全貌。

## 影響評估 (IMPACT)

- 後端：108 → **109 endpoints**
- 前端：customers list ↔ detail 雙向打通
- 解鎖：完整客戶 360 視圖 — 配合 v1.21.0 work-order 事件、v1.27.0 dispute 證據、v1.29.0 退款雙簽形成統一查詢入口

## 驗證方式

### 自動化
- ✅ 後端載入 125 routes
- ✅ 前端 `tsc --noEmit` exit 0
- ✅ `next build` 成功，`/admin/customers/[id]` 5.23 kB

### 手動端到端

```bash
cd api && uvicorn main:app --reload --port 8001
cd web && npm run dev
```

| 步驟 | 預期 |
|:---|:---|
| 進 `/admin/customers` 點任一客戶 | 跳 `/admin/customers/<id>` |
| profile 顯示 | display_name + 大頭像 + 電話 + LINE ID + 建檔時間 + 最近活動 |
| 8 KPI 卡 | 總工單 / 對話 / 平均評分 / 投訴 / 平均處理時長 / 退款 ×3 |
| 工單狀態分布 chip | 顯示各狀態工單數 |
| 最近工單表 | 點工單號跳 `/work-orders/[id]` |
| 最近對話列 | 點 ID 跳 `/conversations/[id]` |

### Curl

```bash
curl http://localhost:8001/api/v1/customers/<customer_id> \
  -H "Authorization: Bearer $TOKEN" -H "X-Tenant-ID: $TENANT" | jq .
```

## 下一步 (NEXT)

1. **F4 AI 派工推薦解釋化**（A37 候選表加 score breakdown tooltip）
2. **F2 LINE Flex RSVP**（Flow 11 客戶端改期確認）
3. **整合測試擴充**（work_order state machine / subflow events 覆蓋）
4. **開 PR `feat/api-media-upload → dev`**（已累積 20 commits）
