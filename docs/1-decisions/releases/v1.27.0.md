# 進度報告 v1.27.0：Dispute evidence 上傳 UI（admin 雙方證據）

- **版本**: v1.27.0（前版：v1.26.0 → 本版升級類型：MINOR）
- **Commit**: `550de07` (2026-05-06 11:35)
- **分支**: feat/api-media-upload
- **作者**: Imding1211
- **影響模組**: `api/services/media_service.py`、`api/routers/media.py`、`docs/02-design/specs/openapi.yaml`、`web/types/api.generated.ts`、`web/src/components/work-orders/MediaGallery.tsx`、`web/src/app/admin/disputes/page.tsx`、`web/docs/system-completion-status.md`

## 變更摘要 (WHAT)

- **後端**：`list_media_for_dispute` service + `GET /disputes/{id}/media` router
- **OpenAPI** + **TS types** 同步
- **MediaGallery 元件擴充**：支援 `disputeId` mode + `refreshKey` 外部觸發刷新 + `title` 自訂
- **Admin disputes 頁**：DisputeEvidencePanel（雙方上傳 + 縮圖瀏覽），移除 placeholder
- **完成度文件**：總體 91→92%，Flow 7 95→100%

## 背景與動機 (WHY)

v1.26.0 媒體流端到端後，admin disputes 頁面的證據面板仍是 placeholder。補完後 Flow 7 完整封閉：雙方證據上傳 → admin 縮圖瀏覽 → 仲裁決定（含金額調整）。

## 影響評估 (IMPACT)

- 後端：107 → **108 endpoints**
- 前端：admin 雙方證據實際可上傳，圖片 + PDF 皆允許
- 解鎖：Flow 7 爭議 95% → **100%** 完整端到端

## 驗證方式

### 自動化
- ✅ 後端 123 routes（含 9 WS）
- ✅ 前端 `tsc --noEmit` exit 0
- ✅ `next build` 成功，`/admin/disputes` 7.69 → 9.45 kB

### 手動端到端

```bash
cd api && uvicorn main:app --reload --port 8001
cd web && npm run dev
```

| 步驟 | 預期 |
|:---|:---|
| 進 `/admin/disputes` 點任一案件 | 描述塊下方出現「爭議證據」區塊（取代舊 placeholder） |
| 點「+ 客戶證據」/「+ 技師證據」 | 開啟 file picker（圖片或 PDF）|
| 選檔 | 上傳中 → MediaGallery 自動 refreshKey++ → 列表新增 |
| 圖片縮圖 | 含 purpose 標籤色塊（客戶證據紅 / 技師證據藍）|
| 點縮圖 | lightbox 放大 |
| 仲裁決定 modal 提交後 | 列表 + 證據持續可見 |

## 下一步 (NEXT)

1. **Inventory low-stock 背景偵測 job**
2. **Refund 雙簽流程**
3. **work_order_events 表**（取代 service_report append）
4. **整合測試 / E2E**
5. **開 PR `feat/api-media-upload → dev`**
