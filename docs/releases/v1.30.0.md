# 進度報告 v1.30.0：work_order_events 表（取代 service_report append）

- **版本**: v1.30.0（前版：v1.29.0 → 本版升級類型：MINOR）
- **Commit**: `aa84c5c` (2026-05-06 12:05)
- **分支**: feat/api-media-upload
- **作者**: Imding1211
- **影響模組**: `SQL/Schema_work_order_events.sql`、`api/services/work_order_service.py`、`api/routers/work_orders.py`、`docs/02-design/specs/openapi.yaml`、`web/types/api.generated.ts`、`web/docs/system-completion-status.md`

## 變更摘要 (WHAT)

- 新表 `work_order_events`（7 種 event_type enum + 2 索引 + JSONB payload）
- `_append_subflow_event` 重構：INSERT 結構化事件，不再 append service_report
- 新增 `list_work_order_events` service + `GET /work-orders/{id}/events` endpoint
- OpenAPI + TS types 同步
- 完成度文件：總體 94 → **95%**

## 背景與動機 (WHY)

v1.17.0 commit body 已註記「service_report append 為技術債，後續可遷移至 work_order_events 表」。文字 append 不支援查詢 / 統計 / UI timeline / 稽核，且 service_report 本應是「給人看的完工摘要」。本次清技術債後，subflow 事件結構化可索引可過濾。

## 影響評估 (IMPACT)

- 後端：107 → **108 endpoints**
- 軟性不向下相容：舊 service_report 紀錄保留不動，新事件改寫入新表
- 部署需求：執行 `SQL/Schema_work_order_events.sql` migration 一次
- 後續可做：前端 admin / 技師端工單詳情加 EventTimeline 元件、signature_submitted 事件接入

## 驗證方式

### 自動化
- ✅ migration 已套用（work_order_events 表存在）
- ✅ 後端載入成功（124 routes）
- ✅ 前端 `tsc --noEmit` exit 0
- ✅ `next build` 成功

### 手動驗證

```bash
cd api && uvicorn main:app --reload --port 8001
cd web && npm run dev
# 技師端進工單 → 點任一 subflow CTA → 填表送出
```

| 步驟 | 預期 |
|:---|:---|
| 提交 scope_change | INSERT 一筆 work_order_events（event_type=scope_change） |
| service_report 欄位 | 不再被 subflow 修改（保留純粹的完工摘要）|
| GET `/api/v1/work-orders/{id}/events` | 回傳該工單所有 event 倒序 |
| 加 `?event_type=delay` | 只回 delay 事件 |
| WS 推送 | 不變（仍走 work_order.subflow.{type}）|

### Curl

```bash
curl http://localhost:8001/api/v1/work-orders/<wo_id>/events \
  -H "Authorization: Bearer $TOKEN" -H "X-Tenant-ID: $TENANT"

curl 'http://localhost:8001/api/v1/work-orders/<wo_id>/events?event_type=delay&limit=20' \
  -H "Authorization: Bearer $TOKEN" -H "X-Tenant-ID: $TENANT"
```

## 下一步 (NEXT)

1. **前端 EventTimeline UI**（admin 工單詳情顯示 subflow 時間軸）— 半天
2. **SLA 引擎**（quote_expiring / dispatch_delay / response_overdue）— 1 週
3. **整合測試 / E2E**（合約 1.2.7.3）— 1-2 週
4. **開 PR `feat/api-media-upload → dev`** — 5 分鐘
