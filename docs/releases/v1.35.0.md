# 進度報告 v1.35.0：A37 派工推薦解釋化（F4）

- **版本**: v1.35.0（前版：v1.34.0 → 本版升級類型：MINOR）
- **Commit**: `030a431` (2026-05-06 19:05)
- **分支**: feat/api-media-upload
- **作者**: Imding1211
- **影響模組**: `api/services/dispatch_service.py`、`docs/02-design/specs/openapi.yaml`、`web/types/api.generated.ts`、`web/src/app/admin/dispatch-manual/page.tsx`、`web/docs/system-completion-status.md`

## 變更摘要 (WHAT)

- 後端 `_score_rows` 回傳新增 `score_breakdown`（三維度 factor/weight/contribution/rationale）
- 三個 helper 產生人類可讀理由（_explain_skill/_explain_distance/_explain_rating）
- OpenAPI 加 `ScoreDimension` 共用 schema
- 前端新增 `ScoreCell` 元件（hover tooltip 顯示三條進度 bar + 理由）
- A37 候選表 score 欄改用 ScoreCell

## 背景與動機 (WHY)

A37 候選技師表只顯示綜合分但沒解釋演算法，admin 對推薦缺乏信任。派工是高責任決策（影響客戶滿意度 + 技師負荷），需要透明依據。

## 影響評估 (IMPACT)

- 後端 routes 不變
- 前端：admin hover score 欄即顯示拆解
- 派工決策透明化、可稽核（rationale 可寫入 audit_event）

## 驗證方式

### 自動化
- ✅ 後端載入 125 routes
- ✅ 前端 `tsc --noEmit` exit 0
- ✅ `next build` 成功，`/admin/dispatch-manual` 8.36 → 9.05 kB

### 手動

```bash
cd api && uvicorn main:app --reload --port 8001
cd web && npm run dev
# 進 /admin/dispatch-queue → 點任一工單「人工介入」
```

| 步驟 | 預期 |
|:---|:---|
| 進 A37 候選表 | score 欄旁有 Info icon |
| Hover score 欄 | tooltip 320px 寬，含 3 條進度 bar + 理由 |
| 技能匹配條 | 「技師認證品牌包含 Yale ✓」（如品牌命中） |
| 距離條 | 「服務區包含板橋區，距離極近 ~5km」 |
| 評分條 | 「高評分技師 4.8/5」 |
| 公式提示 | 「技能 ×0.4 + 距離 ×0.3 + 評分 ×0.3 = 0~100 分」 |

## 下一步 (NEXT)

1. **F2 LINE Flex RSVP**（Flow 11 客戶端改期確認）— 1-2 天
2. **整合測試擴充**（work_order state machine / subflow events）
3. **開 PR `feat/api-media-upload → dev`**（已累積 22 commits）
