# 進度報告 v1.3.4：清空已停用的舊 skills/data 與 skills/docs，main.py 改為 product_info 自檢腳本

- **版本**: v1.3.4（前版：v1.3.3 → 本版升級類型：PATCH）
- **Commit**: 即將提交
- **分支**: agent-improve
- **作者**: Imding1211
- **影響模組**: agent/skills/、agent/main.py

## 變更摘要 (WHAT)

### A. 刪除已停用的舊 skill 資料
- `agent/skills/data/`（67 份 SKILL.md，涵蓋 8 品牌：3E / AiLock / Chatlock / Dormakaba / Kaadas / Milre / Philips / Waferlock + `_common`）整個目錄移除
- `agent/skills/docs/`（只剩 `.DS_Store` 的空目錄）移除
- `agent/skills/` 下保留 `__init__.py` 與 `tools.py`（仍然提供 `load_product_info`、`update_user_info`、`transfer_to_human` 三個現役 tool 與 ContextVar helper）

### B. main.py 改寫為 product_info 架構自檢腳本
原本只測 LLM 連線，改為 < 1 秒可跑完的 smoke test：
1. loader 自檢（36 份 mega-doc 全載入）
2. 品牌覆蓋對齊 `docs/brand_model_list.md`（7 個現役品牌都有 product_info）
3. `filter_loadable()` 在 4 種 profile 組合（D/C/A 路徑）下回傳正確
4. studio_results 真實 6 個案例的關鍵字（掌靜脈、卡片設定、WiFi 連結、安裝預約等）落在對應 mega-doc
5. 確認 `agent/skills/data` 已不存在
6. `build_agent()` 仍可 import

## 背景與動機 (WHY)

舊 skills/data 自 v1.2.0 起已被 `set_skills([])` 停用，agent.py:72 也只註冊 `[load_product_info, update_user_info, transfer_to_human]`，完全不再讀取 SKILL.md。但 67 份 .md 仍躺在 disk 上造成：

1. **新人 onboarding 困惑**——以為 skills/data 還在用
2. **路徑誤導**——CLAUDE.md 與部分 doc 仍引用 skills/data 結構
3. **disk 占用 312K（雖然不多）**

studio_results_20260504_1507.json 的 6 個案例分析顯示，**新 product_info 架構已 100% 覆蓋這些情境的內容需求**（命中所有期望關鍵字），舊 skills/data 已無功能需求。

`brand_model_list.md` 確認 Waferlock 為「（無特定型號）」，非現役品牌，可隨同清理。

## 影響評估 (IMPACT)

- **破壞性變更**：無。`load_skill` tool 已停用、`set_skills([])` 已生效，刪資料對線上行為零影響。
- **回滾路徑**：若需復活 → `git revert` 即可拿回 67 份 SKILL.md
- **後續 step**（不在本次 commit）：
  - Step 2：移除 `skills/tools.py` 中 `load_skill` 函式 + `skills/__init__.py` 的 `load_skills`/`filter_skills`，並清理 `agent.py`、`debounce.py`、`quality_check.py` 中相關 import 與 cleanup 分支
  - Step 3（可選）：將 `agent/skills/` 重命名為 `agent/agent_tools/` 以對齊語意（目錄裡只剩 product_info tool 與其他通用 agent tool）

## 驗證方式

```bash
cd agent && python main.py
```

預期輸出：6 個 section 全部 `✓ 通過`，最終結果 `全部通過`。

實測結果：
- 載入 36 份 mega-doc ✓
- 7 個現役品牌全部有 product_info ✓
- A/B/D 路徑 gating 全部正確 ✓
- studio_results 6 個案例關鍵字全命中 ✓
- `build_agent()` import OK ✓

## 下一步 (NEXT)

1. （建議）跑一輪 `quality_check --no-judge` 對齊 LLM judge baseline，確認沒 regression
2. 進入 Step 2：拔 `load_skill` 函式 + 重構 skills/tools.py 中尚與舊架構耦合的引用
3. 處理 studio_results Case 7 的 PDF 連結問題：16 份 Dormakaba mega-doc 都連到同一個 GDrive PDF（DDL App 版），但部分型號實際使用 SmartLocky App，需業主提供 SmartLocky 版 PDF URL 後逐型號替換
