# 進度報告 v1.11.0：技師端 T10 我的排班

- **版本**: v1.11.0（前版：v1.10.0 → 本版升級類型：MINOR）
- **Commit**: `a7582df` (2026-05-05 21:05)
- **分支**: feat/web-tech-app
- **作者**: Imding1211
- **影響模組**: `web/src/app/account/schedule/`、`web/src/app/account/page.tsx`

## 變更摘要 (WHAT)

- 新增 `/account/schedule` (T10) 我的排班頁：
  - 月份切換 + 配額摘要（休假 / 備勤 / 工單統計）
  - 自製月曆 grid（7 欄純 CSS，今日粗框、休假/備勤背景色、工單數紅點 badge）
  - 「關閉本日接單」toggle
  - 「申請休假」/「申請備勤」BottomSheet modal（日期區間 + 事由）
  - 待審核申請列表 + 取消按鈕
- `/account` 設定列表新增「我的排班」首項

## 背景與動機 (WHY)

T10 是技師端 6 個 subflow 中最後一個未實作的，缺此頁技師無法自主管理休假/備勤/排班。本次補齊讓技師端 spec 覆蓋達 100%（T0/T1/T2/T3/T4/T5/T6/T7/T8/T9/T10/T11 全部）。

## 影響評估 (IMPACT)

- 無破壞性：純新增，AuthGuard `/account` prefix 已涵蓋 `/account/schedule`
- 後端 API 待補（spec 定義但 OpenAPI 未含）：
  - GET `/api/v1/technicians/me/schedule?month=YYYY-MM`
  - POST `/api/v1/technicians/me/schedule/leave-request`
  - POST `/api/v1/technicians/me/schedule/standby-request`
  - PATCH `/api/v1/technicians/me/availability`
  - DELETE `/api/v1/technicians/me/schedule/request/{id}`
- 前端走 mock，amber banner 提示「API 待補」
- 完成技師端 spec 全 12 頁覆蓋

## 驗證方式

### 自動化
- ✅ `npx tsc --noEmit` exit 0
- ✅ `npx next build` 成功
  - `/account/schedule` 4.89 kB Static
  - `/account` 6.15 kB（CTA 已加）

### 手動驗證

```bash
cd web && npm run dev
# 登入技師 → /account → 點「我的排班」
```

| 項目 | 預期 |
|:---|:---|
| 月曆 | 7 欄 grid，今日粗框，工單數紅點 badge |
| 月份切換 | 上下月按鈕、年月顯示正確 |
| 配額摘要 | 3 卡（休假申請/備勤申請/本月工單） |
| 「關閉本日接單」toggle | 切換顯示綠色 toast |
| 「申請休假」按鈕 | 開 BottomSheet → 填表 → 送出 → 加入待審核列表 |
| 「申請備勤」按鈕 | 同上但藍色標籤 |
| 待審核列表 | 顯示申請項，點 trash icon 取消 |
| 返回鈕 | 回 `/account`（不是 `/my-orders`） |

## 技師端覆蓋度 100%

| ID | 頁面 | 路由 | 狀態 |
|:---|:---|:---|:---|
| T0 | 登入 | `/tech-login` | ✅ |
| T1 | 案件池 | `/pool` | ✅ |
| T2 | 我的工單 | `/my-orders` | ✅ |
| T3 | 工單詳情 | `/my-orders/[id]` | ✅ |
| T4 | 帳戶中心 | `/account` | ✅ |
| T5 | 範圍變更 | `/my-orders/[id]/scope-change` | ✅ |
| T6 | 缺料回報 | `/my-orders/[id]/material-request` | ✅ |
| T7 | 延遲通知 | `/my-orders/[id]/delay` | ✅ |
| T8 | 門面檢核 | `/my-orders/[id]/door-check` | ✅ |
| T9 | 電子簽章 | `/my-orders/[id]/signature` | ✅ |
| T10 | 我的排班 | `/account/schedule` | ✅ |
| T11 | 改期日曆 | `/my-orders/[id]/reschedule` | ✅ |

## 下一步 (NEXT)

1. **後端 API 補齊** — 排班 / scope-change / material-request / delay / door-check（5 組 endpoint）
2. **媒體上傳 endpoint** — T8 photos、CompletionReport photos_before/after
3. **SSE 支援** — `/realtime/diagnostics/{conv_id}` 對話頁串流
4. **BroadcastChannel** — 跨 tab 同步未讀數、改期狀態
5. **A37 candidate detail drawer** — 排班熱力圖 + 30 日表現
6. **Dashboard SLA 告警 banner** — 對齊 Flow 2/9
